# PM Progress

## Current Goal

设计并推进 `codex-core` 仓库级拆分：先清理公共类型层和 heavy runtime 的间接依赖，再拆出轻量 session/runtime service 边界，避免把直接依赖移除后又通过 `config`、`protocol`、`tools` 或 `app-server-protocol` 间接拉回。

## Active Work

- id: core-crate-topology-refactor-plan
  execution_mode: performance_refactor_exclusive
  workdir: /Users/bytedance/Projects/my-codex
  owner: root PM direct planning; implementation should use owner_main only while no active dev work exists
  status: active
  started_at: 2026-06-18 16:18:00 CST
  context:
    - 最新冷编译测量：`rtk cargo clean` 后执行 `rtk cargo build --timings -p codex-app-server --bin codex-app-server` 通过；Cargo timing total 为 359.2s，`codex-core` 单 unit 为 197.7s，是最大本地 crate 编译瓶颈。
    - `codex-rs/core/src` 当前有 298 个 Rust 文件、136993 行；顶层目录粗略规模仍以 root/tools/session/config/agent/guardian/unified_exec 为主要收缩对象。`codex-context-manager` 已承接迁出的历史管理主体，当前 3353 行；`codex-context-usage` 已承接 context usage 分类主体 592 行；`codex-rollout-api` 已承接 rollout truncation 纯规则，当前 590 行；`codex-model-client`、`codex-agent-roles` 和 `codex-permissions-runtime` 已分别承接 model client、agent role declaration/discovery 和 permissions runtime / exec-policy approval evaluation 边界。
    - `rtk cargo tree -p codex-app-server --invert codex-core --edges normal` 当前只剩 `codex-app-server -> codex-core`，说明前一轮已清掉 file-subscription、memories-write 等 app-server 旁路 core 依赖。
    - 这次目标不只是减少 app-server 直接依赖，而是重排仓库编译拓扑：公共类型层不能依赖 Starlark/V8/Rama/app-server v2 envelope；运行时编排层只能依赖轻量 API/trait；heavy implementation 由组合根注入。
  findings:
    - `codex-core` direct normal dependency 仍包括 `codex-app-server-protocol`、`codex-code-mode`、`codex-config`、`codex-execpolicy`、`codex-network-proxy`、`codex-tools`、`codex-workflow` 等大量 crate，因此 core 本身仍是聚合根。
    - `codex-app-server-protocol` 被 `config`、`tools`、`analytics`、`login`、`otel`、`core` 等共同依赖；core/config/tools 使用的 `ConfigLayerSource`、`ConfigLayerMetadata`、`AppInfo`、`AuthMode`、MCP elicitation schema、`ThreadHistoryBuilder`/`TurnStatus` 等不是 app-server transport 专属类型。若不先下沉这些 shared types，后续拆分会继续把 app-server v2 协议层拉进 runtime。
    - 初始 `codex-protocol` 也不是纯基础类型层：它曾直接依赖 `codex-execpolicy` 和 `codex-network-proxy`，还带入 `reqwest`、ICU、Linux sandbox 相关依赖。当前 execpolicy/network-proxy normal edge 已移除，但 `codex-protocol` 仍有 reqwest、ICU、image、quick-xml 等偏功能依赖；把 shared app-server types 简单搬进 `codex-protocol` 仍可能形成新的间接重依赖。
    - `codex-config` 当前 direct 依赖 `codex-app-server-protocol`、`codex-execpolicy-api`、`codex-network-proxy-api`、`codex-file-system`、`codex-git-utils`、`codex-model-provider-info`、`codex-features`、`codex-protocol`，不是纯 config types crate。配置类型、加载/IO、runtime validation/evaluator 需要分层。
    - `codex-tools` 当前 direct 依赖 `codex-app-server-protocol`、`codex-code-mode`、`rmcp` 和 `codex-protocol`；它只要 code-mode tool description/spec 时也会拉整个 V8 runtime crate。工具 spec/plan 和 code-mode runtime 必须拆开。
    - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal` 显示 code-mode 通过 `codex-core`、`codex-rollout-trace`、`codex-tools`、`codex-extension-api` 多条路径回到 app-server；不能只删除 core 到 code-mode 的 direct edge。
    - `rtk cargo tree -p codex-app-server --invert starlark --edges normal` 初始显示 Starlark 通过 `codex-execpolicy -> codex-config` 和 `codex-execpolicy -> codex-protocol` 多条公共路径回到 app-server；当前 protocol/config 公共路径均已清理，剩余路径是 `codex-core -> codex-execpolicy`，属于后续 runtime evaluator 边界。
    - `rtk cargo tree -p codex-app-server --invert rama-http --edges normal` 初始显示 Rama 通过 `codex-network-proxy -> codex-config`、`codex-network-proxy -> codex-protocol`、`codex-network-proxy -> codex-sandboxing` 等路径回到 app-server；当前 config/protocol 路径已清理，剩余是 core/sandboxing/exec-server 等真实运行时路径，network proxy backend 后续需要继续收缩为实现 crate。
    - service registry/constructor injection 是后续 runtime 边界的正确注入方式，但它不能替代类型层拆分；如果 service trait crate 继续依赖 heavy implementation，IoC 只会把 direct edge 伪装成 indirect edge。
    - 已落地 step 1 的第一条垂直切片：新增 `codex-network-proxy-api`，先迁移 protocol 需要的 `NetworkPolicyDecision` 和 `NetworkDecisionSource`；`codex-network-proxy` 继续 re-export 这两个类型以保持现有 callsite 兼容。
    - `codex-protocol` 的 normal dependency 已从 `codex-network-proxy` 改为 `codex-network-proxy-api`，并把仅测试使用的 `codex-execpolicy` 降到 dev-dependency；这移除了 `protocol -> network-proxy -> Rama` 和 `protocol -> execpolicy -> Starlark` 的 normal 路径。
    - 已继续扩大 `codex-network-proxy-api`：迁移 `NetworkProxyConfig`、`NetworkProxySettings`、`NetworkMode`、domain/unix socket permission DTO 和 `normalize_host`；`codex-network-proxy` 仍 re-export 这些类型，backend/runtime 解析和 state builder 留在旧实现 crate。
    - `codex-config` 的 normal dependency 已从 Rama-backed `codex-network-proxy` 改为 `codex-network-proxy-api`；config 侧只处理 TOML 到 proxy DTO 的转换，不再为了 DTO 拉入 Rama backend。
    - step 1 曾剩余 `codex-config -> codex-execpolicy -> starlark` 路径；当前已通过 `codex-execpolicy-api` 清理，`codex-config` normal graph 不再拉 `codex-execpolicy` 或 Starlark。
    - 2026-06-18 17:02 CST 复核：`codex-core --depth 1` 的 direct graph 比当前计划列举的更重，除 `config/tools/code-mode/proxy/policy` 外还直接依赖 login、mcp、rollout、sandboxing、model-provider、workflow 等运行时实现；后续 step 5/6 不能只写 session-runtime，需要按 model client、MCP runtime、tool runtime、rollout/thread-store、sandbox/exec、workflow manager 等功能域继续收缩。
    - 2026-06-18 17:02 CST 复核：`codex-protocol --depth 1` 已不再拉 Starlark/Rama，但它自身仍有 reqwest、ICU、image、quick-xml 等偏功能依赖；app-server shared types 不能默认下沉到 `codex-protocol`，否则只是把 `app-server-protocol` 的公共依赖问题换成新的 protocol 大依赖。
    - 2026-06-18 17:02 CST 复核：`codex-app-server-protocol` 实际混有多类用途：app-server v2 JSON-RPC/control/event envelope、`ThreadItem` display payload、config layer DTO、auth/app connector DTO、MCP elicitation schema、thread history/status builder、workflow notification 等。step 2 应先按类型族确定 owner crate，再迁移非 transport 共享 DTO；不能一次搬到单个 shared catch-all crate。
    - 2026-06-18 17:02 CST 复核：`codex-execpolicy-api` 切片应迁移纯策略模型和无 Starlark error；最初把 `PolicyParser`、Starlark span/location、append/amend 文件写入和 `ExecPolicyCheckCommand` 都留在 `codex-execpolicy` 实现 crate。后续 step 5 复核确认 append/amend writer 不依赖 Starlark，已下沉到 API crate；`codex-config` 只需要构造/持有 `Policy`，无需 parser。
    - 已落地 step 1 的第二条垂直切片：新增 `codex-execpolicy-api`，迁移 `Decision`、`Evaluation`、`RuleMatch`、`Policy`、`MatchOptions`、`PatternToken`、`PrefixPattern`、`PrefixRule`、`RuleRef`、`NetworkRuleProtocol`、network rule DTO、host executable lookup helper、append/amend writer 和 no-Starlark API error；`codex-execpolicy` 保留 Starlark parser、CLI check command 和旧 error/location display，并 re-export API 类型保持现有 callsite 简洁。注意纯 `Policy` mutation/validation 方法返回 `codex_execpolicy_api::Error`，append/amend writer 返回 `codex_execpolicy_api::AmendError`，Starlark parser/display 路径返回 `codex_execpolicy::Error`。
    - `codex-config` 的 normal dependency 已从 Starlark-backed `codex-execpolicy` 改为 `codex-execpolicy-api`；requirements policy TOML 只构造 API `Policy`，core load 时 merge 的仍是同一套 API type（经 `codex-execpolicy` re-export）。
    - 2026-06-18 17:18 CST 复核 proxy 剩余项后，后续已把 `NetworkProxyAuditMetadata`、blocked request observer 和 proxy env key 常量下沉到 `codex-network-proxy-api`。仍留在 implementation 边界的是 `ConfigReloader`、`NetworkProxyState`、`NetworkProxyConstraints`、backend config/state builder、proxy handle、host policy evaluation、读取/写入环境变量的 process wiring 和 Rama runtime；这些不能为了删除 direct edge 伪装成轻量 API。
    - 2026-06-18 17:42 CST 全局复核：`codex-core --depth 1` 仍直接依赖 login、mcp、model-provider、workflow、sandboxing、rollout、tools、code-mode、execpolicy、network-proxy 等运行时 crate，说明 core 目前还是组合根；runtime service 抽取必须等公共类型层和 tool/config 边界先变轻，否则会把旧聚合根搬到新 crate。
    - 2026-06-18 17:42 CST 全局复核：`codex-app-server-protocol --depth 1` 自身不算最大 heavy implementation，但它携带 v2 envelope、schema/TS/proc-macro、rmcp、codex-protocol，且反向依赖覆盖 config/tools/connectors/login/model-provider-info/otel/core/TUI/CLI；这使它不适合作为非 transport shared DTO 所有权层。
    - 2026-06-18 17:42 CST 全局复核：`AuthMode` 是最小有效垂直切片之一。`model-provider-info` 和 `otel` 当前直接为了 `AuthMode` 依赖 app-server-protocol；`login` 也直接使用 `AuthMode`，但仍会经 config/model-provider-info/otel 间接触达 app-server-protocol，因此 AuthMode 切片的验收应先消除 direct edge 和纯 AuthMode 消费者路径，不能夸大为 login 完全脱离。
    - 2026-06-18 17:42 CST 全局复核：`codex-config` 依赖 app-server-protocol 不只因为 `ConfigLayerSource`/`ConfigLayerMetadata`/`ConfigLayer`，还因为 config crate 内部直接实现了到 v1 `UserSavedConfig`、`Profile`、`Tools`、`SandboxSettings`、`ForcedChatgptWorkspaceIds` 的 protocol payload 转换。正确方向是把 transport DTO conversion 移到 app-server/protocol 边界；只有 config domain 也需要持有的 layer source/metadata 才进入 `codex-config-types`。
    - 2026-06-18 17:42 CST 全局复核：`AppInfo`/`AppBranding`/`AppMetadata`/`AppSummary` 被 connectors/chatgpt/tools/core/TUI/app-server 多处作为 connector domain model 使用，适合拆到 `codex-connectors` owner 下的轻量 app metadata API/types crate；app list request/response/notification 仍属于 app-server-protocol transport。
    - 2026-06-18 17:42 CST 全局复核：MCP elicitation 不能简单整体搬出 app-server-protocol。`McpServerElicitationRequestParams`/response 是 app-server server-request payload，TUI/exec/app-server 合理使用；tools/core 为了构造 request payload 依赖 app-server-protocol 的点，应优先改成返回 tool/domain-level elicitation plan，再由 core/app-server 边界投影成 protocol request。纯 schema 子类型是否拆到 `codex-mcp-api` 需要在该垂直切片中单独证明。
    - 2026-06-18 17:42 CST 全局复核：`ThreadHistoryBuilder` 和 `TurnStatus` 当前紧耦合 v2 `ThreadItem`/`Turn` display 投影，暂不应作为 shared DTO 拆出；移动它们会把 v2 display model 一起带出去，反而扩大依赖面。
    - 已完成 step 2a：新增 `codex-auth-types` 并迁移 `AuthMode`；`codex-app-server-protocol` 改为 re-export 以保持 wire 兼容，login/model-provider-info/otel/models-manager/core/app-server/CLI/TUI 等直接使用 `codex_auth_types::AuthMode`。`model-provider-info`、`otel` 和 `models-manager` 已移除 `codex-app-server-protocol` direct dependency；`login` 也已移除 direct dependency，剩余 app-server-protocol 路径只经 `codex-config`，属于 step 2b 的 config/protocol boundary 问题。
    - 2026-06-18 18:20 CST 再复核：`codex-core --depth 1` 仍直接依赖 `codex-app-server-protocol`、`codex-code-mode`、`codex-config`、`codex-connectors`、`codex-execpolicy`、`codex-login`、`codex-mcp`、`codex-model-provider`、`codex-network-proxy`、`codex-rollout-trace`、`codex-sandboxing`、`codex-tools`、`codex-workflow` 等，说明 core 不是单一 session runtime，而是多个 runtime/domain 的组合根。后续拆 `codex-session-runtime` 前必须先让 config/tools/connectors 这些上游 crate 变轻，否则只是把 direct edge 换到新的 runtime crate。
    - 2026-06-18 18:20 CST 再复核：`codex-config --invert codex-app-server-protocol --edges normal` 当前是直接路径，且 config 源码中 protocol 使用点分为两类：domain layer source/metadata/layer，以及到 v1/v2 protocol payload 的 `From`/helper conversion。step 2b 必须先新增 `codex-config-types` 承载 layer source/metadata/layer，再删除 config crate 内到 `UserSavedConfig`、`Profile`、`Tools`、`SandboxSettings`、`ForcedChatgptWorkspaceIds` 的 app-server protocol conversion；这些 conversion 若仍需要，应改为 app-server 边界本地 adapter 函数。不要把 impl 搬到 app-server-protocol，因为那会让 protocol 反向依赖 config。
    - 2026-06-18 18:20 CST 再复核：`codex-chatgpt --invert codex-app-server-protocol --edges normal` 有 direct chatgpt、connectors、config/login/analytics/core-plugins 多条路径。step 2c 只能保证 app metadata 路径脱离，不能把 chatgpt 全量脱离 app-server-protocol 作为单个切片的验收；每条路径要在对应切片中分别消除或记录为真实 transport/runtime 边界。
    - 2026-06-18 18:20 CST 再复核：当前 code-mode implementation crate 名为 `codex-code-mode`，`codex-app-server --invert codex-code-mode --edges normal` 同时经 core、rollout-trace、tools、extension-api 回到 app-server。step 4 的当前门禁应检查 `codex-code-mode`；若未来再拆成 `codex-code-mode-v8` 实现 crate，才把验证目标切到新 crate。
    - 已完成 step 2b：新增 `codex-config-types` 并迁移 `ConfigLayerSource`、`ConfigLayerMetadata`、`ConfigLayer`；`codex-app-server-protocol` 改为 re-export 保持 wire/API 兼容，`codex-config` 改为直接依赖 config types。config crate 中到 v1/v2 `UserSavedConfig`、`Profile`、`Tools`、`SandboxSettings`、`ForcedChatgptWorkspaceIds` 的 app-server protocol conversion 已删除；仓内无生产调用，后续若需要这类 transport conversion 应放在 app-server 边界本地 adapter。
    - 已完成 step 2c：新增 `codex-connectors-types` 并迁移 `AppBranding`、`AppReview`、`AppScreenshot`、`AppMetadata`、`AppInfo`、`AppSummary`；`codex-app-server-protocol` 改为 re-export 保持 wire/API 兼容。`codex-connectors` 不再依赖 app-server-protocol；`codex-chatgpt` 不再 direct 依赖 app-server-protocol 或经 connectors 为 app metadata 拉入 protocol；`codex-tools` 和 `codex-core` 的 AppInfo 生产路径改为直接依赖 connectors types。
    - 已完成 step 2d：`codex-tools` 的 request-plugin-install 不再构造 app-server protocol 的 `McpServerElicitationRequestParams`，改为返回 `RequestPluginInstallElicitationRequest` domain plan；core handler 在实际发 MCP elicitation 的 runtime 边界把该 plan 投影成 app-server-protocol payload。`codex-tools` normal graph 已无 `codex-app-server-protocol`，MCP server request/response wrapper 继续留在 app-server-protocol。
    - 已完成 step 4 的第一条垂直切片：新增 `codex-code-mode-api`，承载 code-mode description/tool definition/tool namespace description/public tool names/exec prompt builder/pragma parser/schema renderer、runtime response DTO 和 code-mode content item/image detail 等纯 API；`codex-code-mode` 继续 re-export 这些 API 类型并保留 V8 runtime/service。`codex-tools` 改为依赖 `codex-code-mode-api`，normal graph 已不再拉 `codex-code-mode` 或 V8。
    - 已完成 step 4 的第二条垂直切片：`codex-rollout-trace` 改为依赖 `codex-code-mode-api`，只记录 code-mode public tool name 和 `RuntimeResponse` DTO；不再为了 trace serialization 依赖 V8 runtime crate。
    - 2026-06-18 step 4 复核：`codex-app-server --invert codex-code-mode --edges normal` 现在只剩 `codex-core -> codex-app-server` 一条路径；`codex-tools` 和 `codex-rollout-trace` 路径已消失。后续需要判断 core code-mode handlers 是否是真 runtime boundary，并把 parse/description-only callsites 尽量改到 `codex-code-mode-api`。
    - 已完成 step 4 的第三条垂直切片：`codex-core` 中的 `parse_exec_source`、`PUBLIC_TOOL_NAME`、`WAIT_TOOL_NAME`、`CodeModeToolKind`、`RuntimeResponse`、code-mode output content/image detail adapter 和 nested-tool hide 判断已改为直接依赖 `codex-code-mode-api`。`codex-core` 对 `codex-code-mode` 的剩余 direct 依赖集中在 `CodeModeService`、`ExecuteRequest`、`WaitRequest`、`WaitOutcome`、`CodeModeTurnHost`、`CodeModeTurnWorker`、`CodeModeNestedToolCall` 和 wait 默认值，属于当前真实 V8 runtime/service boundary。
    - 已完成 step 4 的第四条垂直切片：`CodeModeRuntimeService`、`CodeModeRuntimeFactory`、`CodeModeTurnHost`、`CodeModeTurnWorker`、execute/wait request/outcome、nested tool call 和 wait/exec 默认值已下沉到 `codex-code-mode-api`。`codex-core` 的 session services 只持有 trait object，`ThreadManager` 每个 session 通过 factory 创建独立 service，subagent spawn 通过 parent factory 创建 fresh service，避免 stored values 跨 thread 串用；V8-backed `codex-code-mode::V8CodeModeRuntimeFactory` 由 app-server 组合根注入。`codex-core` normal graph 已无 `codex-code-mode`。
    - 已开始 step 3 的 config types 扩展：`History`、`HistoryPersistence`、`AuthCredentialsStoreMode`、`OAuthCredentialsStoreMode` 和 `ResidencyRequirement` 已下沉到 `codex-config-types`；`codex-config` 继续 re-export 旧路径以保持兼容。
    - `codex-message-history` 已改为直接依赖 `codex-config-types`，不再为了 `History`/`HistoryPersistence` 拉入 heavy `codex-config`。
    - `codex-login` 已改为直接依赖 `codex-config-types`，不再为了 credential store mode 和 residency enum 拉入 heavy `codex-config`；`codex-login::AuthCredentialsStoreMode` 和 `codex_login::default_client::ResidencyRequirement` 的公开 re-export 仍保留。
    - `McpServerEnvVar` 已下沉到 `codex-config-types`，`codex-config` 继续通过 `mcp_types` re-export；`codex-rmcp-client` 已改为直接依赖 `codex-config-types`，不再为了 MCP env var 和 OAuth credential store mode 拉入 heavy `codex-config`。
    - `codex-app-server-transport` 的 test-only credential mode import 已改为 `codex-config-types`，dev-dependency 不再使用 `codex-config`。
    - MCP server config 纯 DTO 已继续下沉到 `codex-config-types`：`McpServerConfig`、`RawMcpServerConfig`、`McpServerTransportConfig`、tool approval/OAuth/disabled reason/env var、`RequirementSource`、`Constrained` 等只依赖轻量类型；`codex-config` 保持 re-export 和 loader/edit/validation 边界。
    - Hook TOML/JSON DTO 已下沉到 `codex-config-types`：`HooksFile`、`HooksToml`、`HookStateToml`、`HookEventsToml`、`MatcherGroup`、`HookHandlerConfig`、`ManagedHooksRequirementsToml`。依赖 `codex_protocol::HookEventName` 的事件投影保留为 `codex-config::hook_events_into_matcher_groups`，避免 `codex-config-types` 拉入 protocol。
    - `codex-mcp` 已改为直接依赖 `codex-config-types`，不再为了 MCP config/env/tool approval 拉入 heavy `codex-config`；此前剩余的 indirect path `codex-mcp -> codex-plugin -> codex-config` 已通过让 `codex-plugin` 直接依赖 hook DTO 消除。
    - Memories config 纯 DTO 已下沉到 `codex-config-types`：`MemoriesToml`、`MemoriesConfig` 和默认值常量。`codex-config` 继续 re-export，`codex-memories-write` 生产路径已改为直接依赖 `codex-config-types`，不再为了 memory startup settings 拉入 heavy `codex-config`。
    - OTEL config 纯 DTO 已下沉到 `codex-config-types`：`OtelHttpProtocol`、`OtelTlsConfig`、`OtelExporterKind`、`OtelConfigToml`、`OtelConfig` 和 `DEFAULT_OTEL_ENVIRONMENT`。运行时 span/tracestate validation 仍在 `codex-core/src/config/otel.rs`，不进入 types crate。
    - `codex-mcp-server` 生产 manifest 已移除 direct `codex-config`；测试改用 `codex-config-types::OtelExporterKind`。由于它是产品入口/组合根，同时显式依赖 `codex-code-mode` 并给 `ThreadManager::new` 注入 `V8CodeModeRuntimeFactory`，避免把 V8 runtime edge 放回 `codex-core`。
    - `codex-hooks` 已移除 normal `codex-config` 依赖。hook discovery 改为消费 `HookConfigLayerStack` / `HookConfigLayerEntry` 只读 view；core/app-server 作为组合根从完整 `codex_config::ConfigLayerStack` 投影过去。hook event 投影和 trust hash helper 留在 hooks 边界，避免把 `codex_protocol::HookEventName` 或 config fingerprint/loader 反向塞进 `codex-config-types`。
    - `codex-core-skills` 已移除 normal `codex-config` 依赖。skills config DTO（`SkillsConfig`、`SkillConfig`、`BundledSkillsConfig`）下沉到 `codex-config-types`，core/app-server/core-plugins 在组合根边界把完整 `ConfigLayerStack` 投影成 `SkillConfigLayerStack` 只读 view 后再调用 skills runtime。`codex-config-types` 因 `SkillsConfig: TryFrom<toml::Value>` 新增 normal `toml` 依赖，这是纯 DTO 解析依赖，不是 loader/evaluator 边界回流。
    - TUI/settings 纯 DTO 切片已完成：`TuiKeymap`/`KeybindingSpec` keymap schema、notification/session picker/tool-suggest/notices/model availability NUX/file opener DTO 下沉到 `codex-config-types`；`codex_config::types` 继续 re-export 保持兼容。`codex-core-api` 对这些纯 DTO 的 public facade re-export 已改为直接依赖 `codex-config-types`，并已移除 direct `codex-config` manifest edge；`ConfigLayerStack`、`ProjectConfig` 和 Realtime config 兼容 re-export 暂时经 `codex_core::config` 提供，因此仍通过 `codex-core -> codex-config` 间接依赖 full config，归入后续 core runtime/config loader 边界。
    - Requirements 切片已完成：新增 `codex-config-requirements`，迁移 requirements TOML、normalized requirements、requirements exec policy TOML/evaluator 和 cloud requirements loader；`codex-config` 只 re-export 保持兼容，`codex-cloud-requirements` 改为直接依赖 `codex-config-requirements` 和 `codex-config-types`，不再为了 requirements 加载拉入完整 `codex-config`。
    - `codex-core-plugins` 读取侧切片已完成：新增 `PluginConfigLayerStack` / `PluginConfigLayerEntry` plugin 专用只读 view，core/app-server 在组合根边界从完整 `codex_config::ConfigLayerStack` 投影后传入 plugin manager；loader、installed marketplaces、marketplace upgrade 和 app-server cwd-specific skills/hooks 路径不再以完整 config stack 作为 core-plugins public API。
    - Plugin/marketplace 纯 DTO 已下沉到 `codex-config-types`：`PluginConfig`、`PluginMcpServerConfig`、`MarketplaceConfig`、`MarketplaceSourceType` 进入轻量 crate，`codex_config::types` 继续 re-export。`codex-core-plugins` 读取侧直接依赖这些 DTO；测试和旧调用仍可用旧路径。
    - Config edit 切片已完成：新增 `codex-config-edit`，迁移 plugin enable/clear、marketplace add/remove/upgrade、`CONFIG_TOML_FILE` 和 `version_for_toml`；`codex-config` 继续 re-export 保持兼容。`codex-core-plugins` production path 已改为依赖 `codex-config-edit`，full `codex-config` 只保留为 core-plugins dev-dependency 供测试 fixture 使用完整 loader。
    - Config loader API 切片已完成：新增 `codex-config-loader`，迁移 `LoaderOverrides`、`ConfigLoadOptions`、thread config source/loader trait、Noop/Static loader；`codex-config` 只保留 thread source 到 `ConfigLayerEntry` 的投影和旧 API re-export。新增 `codex-config-loader-remote` 承载 tonic/prost remote thread config loader，app-server/app-server-client 组合根显式依赖 remote implementation，避免 `codex-config -> codex-config-loader -> tonic/prost` 的间接重依赖。
    - `codex-app-server-client` 已移除 direct `codex-config` manifest edge，启动参数改为直接依赖 `codex-config-loader`、`codex-config-loader-remote` 和 `codex-config-requirements`。它仍经 `codex-app-server`/`codex-core` 间接触达 full config，这是组合根/legacy facade 路径，归入后续 app-server/core runtime config 边界。
    - Features/OTEL 切片已完成：`codex-features` 不再依赖 `codex-otel`；唯一的 `Features::emit_metrics(&SessionTelemetry)` 行为已迁到 core session 本地 helper，features crate 继续拥有 feature registry、TOML config、legacy key 和 warning event。`codex-config` 为了 `FeaturesToml`/feature key validation 依赖 features 时，不再间接拉 `codex-otel`、tonic 或 prost。
    - Config diagnostics 切片已完成：新增 `codex-config-diagnostics` 承载 `ConfigError`、`ConfigLoadError`、`TextPosition`/`TextRange`、TOML path/span helper 和 config error formatting；`codex-config` 继续 re-export 旧路径，并只保留需要 `ConfigLayerStack`、filesystem 读取和 `AbsolutePathBufGuard` 的 first-layer error 定位集成。
    - `codex-exec` 已移除 direct `codex-config` manifest edge；它对启动 loader options 改为直接依赖 `codex-config-loader`，对 config load error downcast/formatting 改为直接依赖 `codex-config-diagnostics`。`codex-exec -> codex-config` 的剩余间接路径只经 `codex-core` 和 `app-server-client -> app-server`，属于后续 runtime/组合根边界。
    - `codex-cli` 已移除 direct `codex-config` manifest edge；MCP/doctor/login 的纯 DTO 改为直接依赖 `codex-config-types`，`LoaderOverrides` 改为 `codex-config-loader`，`CONFIG_TOML_FILE` 改为 `codex-config-edit`。`codex-cli -> codex-config` 的剩余路径来自 `codex-app-server`、`codex-tui`、`codex-core`、`app-server-test-client`、ChatGPT/OSS helper 等组合根/legacy facade 依赖。
    - `app_test_support` 已移除 direct `codex-config` manifest edge；auth fixtures 直接依赖 `codex-auth-types` 和 `codex-config-types`。它仍经 `codex-core` 间接触达 full config，这是 core test runtime helper 的边界。
    - Config state 切片已完成：新增 `codex-config-state` 承载 `ConfigLayerEntry`、`ConfigLayerStack`、`ConfigLayerStackOrdering`、`merge_toml_values`、key alias 和 origin helper；`codex-config` 继续 re-export 旧路径并保留完整 loader/validation 集成。`codex-config-state` 不依赖 `codex-config`、app-server-protocol、Starlark、Rama 或 code-mode runtime。
    - TUI direct config 清理已完成：`codex-tui` 对 config layer stack/debug display、loader options、diagnostics、requirements、edit 常量和纯 DTO 分别改为直接依赖 `codex-config-state`、`codex-config-loader`、`codex-config-diagnostics`、`codex-config-requirements`、`codex-config-edit` 和 `codex-config-types`；`ProjectConfig` test helper 暂时经 legacy core facade 使用，避免重新加回 direct `codex-config` manifest edge。
    - `RealtimeTransport` 和 Windows sandbox TOML DTO 已下沉到 `codex-config-types`，`codex_config::config_toml::RealtimeTransport` 和 `codex_config::types::{WindowsSandboxModeToml, WindowsToml}` 继续作为兼容 re-export。`ApprovalsReviewer` 仍属于 `codex_protocol::config_types`，不要为了 TUI import 伪装成 config-types。
    - `codex-utils-sandbox-summary` 已移除 normal `codex-core` 依赖。它只保留 sandbox/permission profile summary，删除 workspace 内无人调用的 `create_config_summary_entries(&codex_core::config::Config, ...)` public helper；TUI/exec 不再通过 sandbox-summary 间接拉 `codex-core -> codex-config`。
    - OSS helper 切片已完成：`codex-utils-oss`、`codex-lmstudio` 和 `codex-ollama` 的 readiness API 从接收 `codex_core::config::Config` 改为接收调用方已持有的 `model: Option<&str>`、`ModelProviderInfo` 或 provider map；normal graph 不再为了 OSS bootstrap 拉 `codex-core`。测试中仍以 dev-dependency 使用 core sandbox env 常量。`codex-ollama` 的 `wiremock` 也已从 normal dependency 降为 dev-dependency，避免测试 HTTP mock 污染 OSS helper normal graph。
    - `core_test_support` 已移除 direct `codex-config` manifest edge；测试支撑 crate 对 config file 常量、loader options、requirements/cloud loader、layer stack 和 TOML value 分别改用 `codex-config-edit`、`codex-config-loader`、`codex-config-requirements`、`codex-config-state` 和 `toml`。它仍经 `codex-core` 间接触达 full config，这是 core runtime 测试 helper 边界。
    - App connector 配置纯 DTO 已下沉到 `codex-config-types`：`AppsConfigToml`、`AppsDefaultConfig`、`AppConfig`、`AppToolsConfig`、`AppToolConfig` 进入轻量 crate，`codex_config::types` 继续 re-export 旧路径。
    - `codex-core` 中 `core/src/config` 之外的 config DTO/state 生产引用已收敛到轻量 crate：session、connectors、mcp tool call、exec policy、workflow、plugins discoverable、guardian review session、otel init、request-plugin-install、AGENTS.md loader、turn/environment context 等路径直接依赖 `codex-config-types` / `codex-config-state` / `codex-config-edit` / `codex-config-requirements`。`codex-core -> codex-config` 仍保留，因为 `core/src/config`、network proxy loader、agent role reload 等仍是 effective config/loader 边界。
    - `codex-app-server` 的 config control/display 生产路径已改为直接依赖轻量 config crate：main/lib/in-process/config manager service/config processor/request processors/plugins/external agent config/memories runtime 分别使用 `codex-config-loader`、`codex-config-diagnostics`、`codex-config-state`、`codex-config-requirements`、`codex-config-edit` 和 `codex-config-types`。`codex-app-server -> codex-config` 仍保留，因为 `ConfigManager` 仍调用 full local `load_config_layers_state`，并且 `ConfigToml` / `project_trust_key` / model option config 仍属于未拆完的 full loader/effective config 边界。
    - config 继续需要拆分的重点不是继续零散移动 enum，而是按消费面分层：filesystem/git/MDM/local config layer IO、model-provider validation 和 effective runtime `Config` 构造仍在 full config 边界；下一步应审计剩余 `codex-config` direct consumers，尤其是 `codex-core` 的 runtime config loader/validation 和 TUI/app-server 等组合根，区分哪些必须继续使用 full loader，哪些能转成 config-types/config-edit/config-requirements/config-loader/config-diagnostics API 或窄 view。
  target_topology:
    - base types: `codex-protocol` 保留模型/会话 wire 基础类型，但先移除对 execpolicy/network-proxy implementation 的依赖；新增或拆出 `codex-auth-types`、`codex-config-types`、`codex-connectors-api`/app metadata types、`codex-execpolicy-api`、`codex-network-proxy-api`、`codex-code-mode-api`、`codex-tool-spec`，这些 crate 不依赖 V8/Starlark/Rama/app-server v2 envelope。不要把所有 shared DTO 默认塞进 `codex-protocol`；若目标类型只服务 auth/app/config/MCP elicitation 等窄领域，应拆到对应 owner crate。
    - light planning/loading: `codex-config-loader` 只承载 loader options 和 thread config loader API/Noop/Static implementation；`codex-config-loader-remote` 承载 tonic/prost remote implementation，由组合根显式依赖，不得经 `codex-config` 间接拉入。后续若拆 full local loader，再新增明确 loader/state crate 承载 IO/layer stack/validation skeleton；`codex-tool-planning` 负责 hosted tool specs、agent tool filtering、namespace merge、code-mode exec prompt planning；这些 crate 只依赖 base types/API。
    - runtime orchestration: 新的 `codex-session-runtime` 或等价边界持有 `RuntimeServices`/service registry，只依赖 API traits、model/mcp/tool handles、thread-store/rollout abstractions，不依赖 `codex-code-mode-v8`、`codex-execpolicy-starlark`、`codex-network-proxy-rama`、`codex-app-server-protocol`。
    - heavy implementations: `codex-code-mode-v8`、`codex-execpolicy-starlark`、`codex-network-proxy-rama`、model client、MCP runtime、tool runtime 作为具体实现 crate，由 `codex-app-server`、`codex-cli`、`codex-tui` 等组合根 wiring。
    - compatibility: 过渡期 `codex-core` 可以作为 legacy facade/re-export，但每拆一块都必须证明新轻量 crate 没有通过 `config`、`protocol`、`tools`、`rollout-trace` 或 `app-server-protocol` 把 heavy crate 间接拉回。
  plan:
    - step: 1
      status: completed
      description: 先拆 policy/proxy 基础类型：新增 `codex-execpolicy-api` 和 `codex-network-proxy-api`，迁移 `Decision`、`Evaluation`、`RuleMatch`、`NetworkRuleProtocol`、`Policy`、prefix/network rule DTO、host executable lookup helper、append/amend writer、proxy config/decision DTO、normalizer、audit metadata、blocked request observer 和 proxy env key 常量；`codex-execpolicy` 保留 Starlark parser/evaluator/CLI check command，`codex-network-proxy` 保留 Rama backend、runtime state、config reloader、constraints、proxy handle、host policy evaluation 和 process wiring，后续在 runtime service 边界阶段继续收缩。
      verification:
        - `rtk cargo tree -p codex-protocol --invert starlark --edges normal` 应无路径。
        - `rtk cargo tree -p codex-protocol --invert rama-http --edges normal` 应无路径。
        - `rtk cargo tree -p codex-config --invert starlark --edges normal` 和 `--invert rama-http` 应只在显式 runtime feature 或实现 crate 阶段出现，不能在 config types 阶段出现。
    - step: 2
      status: completed
      description: 拆 app-server shared types，但按 owner 小切片串行推进，不建 catch-all shared crate。2a 已完成：新增 `codex-auth-types` 迁移 `AuthMode`，app-server-protocol re-export 保持 wire 兼容，login/model-provider-info/otel/models-manager/core/CLI/TUI 直接依赖 auth types。2b 已完成：新增 `codex-config-types` 承载 `ConfigLayerSource`/metadata/layer，`codex-config` 和 `codex-app-server-protocol` 都依赖该轻量 crate；config crate 中到 v1/v2 `UserSavedConfig`、`Profile`、`Tools`、`SandboxSettings`、`ForcedChatgptWorkspaceIds` 等 transport payload 的 conversion 已删除，不再依赖 app-server-protocol。2c 已完成：新增 `codex-connectors-types` 承载 connector app metadata，`codex-connectors`/`codex-chatgpt`/`codex-tools`/core 生产路径不再为了 app metadata 直接依赖 app-server-protocol，app list request/response/notification 仍留在 app-server-protocol 并 re-export metadata types。2d 已完成：request-plugin-install/MCP elicitation 在 `codex-tools` 内表达为 domain-level plan，core/app-server runtime 边界再投影成 `McpServerElicitationRequestParams`。`ThreadItem`、thread/turn request/response、workflow notifications、v2 control/event envelope、schema export、`ThreadHistoryBuilder`/display projection 暂继续属于 app-server-protocol。
      verification:
        - 2a 后 `rtk cargo tree -p codex-model-provider-info --invert codex-app-server-protocol --edges normal` 和 `rtk cargo tree -p codex-otel --invert codex-app-server-protocol --edges normal` 不应存在路径；`codex-login --depth 1` 不应再 direct 依赖 app-server-protocol 只为 AuthMode。
        - 2b 后 `rtk rg -n "codex_app_server_protocol" config/src -g '*.rs'` 应无生产路径；`rtk cargo tree -p codex-config --invert codex-app-server-protocol --edges normal` 不应存在 config 路径。
        - 2c 后 `rtk cargo tree -p codex-connectors --invert codex-app-server-protocol --edges normal` 和 `rtk cargo tree -p codex-chatgpt --invert codex-app-server-protocol --edges normal` 不应因 app metadata 存在路径；app-server-protocol 仍可包装 app/list wire payload。
        - 2d 后 `rtk cargo tree -p codex-tools --invert codex-app-server-protocol --edges normal` 不应因 request-plugin-install/MCP elicitation 构造存在路径；保留的路径必须能指向真实 app-server transport boundary。
        - 完整 step 2 后 `rtk cargo tree -p codex-core --invert codex-app-server-protocol --edges normal` 不应通过 config/tools/connectors/auth/model-provider-info/otel 的非 transport DTO 路径进入；若仍有路径，必须明确属于 app-server display/thread/turn transport。
    - step: 3
      status: completed
      description: 拆 config：形成 `codex-config-types`（纯 DTO）、`codex-config-toml`（schema-heavy `config.toml` 外层 shape/schema）、`codex-config-loader`（loader API/project trust/CLI override）、`codex-config-state`（layer stack/merge）、runtime validation services（依赖 policy/proxy/model provider implementation 的组合层）。已把 `History`、credential store mode、residency enum、MCP server config/env/tool approval、hook TOML/JSON DTO、memories config、otel config、skills config DTO、TUI keymap/notification/session picker/tool-suggest/notices、Realtime/Windows TOML 和 app connector config DTO 等纯类型下沉到 `codex-config-types`，并新增 `codex-config-toml` 承载 `ConfigToml`、`ConfigProfile`、`RealtimeConfig`、`ToolsToml` 和 schema helper。`codex-message-history`、`codex-login`、`codex-rmcp-client`、`codex-plugin`、`codex-mcp`、`codex-memories-write`、`codex-hooks`、`codex-core-skills` 等生产路径已脱离 heavy `codex-config`，并清掉 `codex-mcp-server`、TUI/CLI/exec/core test support 等对 `codex-config` 的 direct manifest edge。core runtime 文件中纯 DTO/state/TOML shape 调用已改为直接依赖 `codex-config-types` / `codex-config-toml` / `codex-config-state` / `codex-config-edit` / `codex-config-requirements`；普通 feature、cloud requirement、tool planning 不得为了读取配置类型拉 evaluator/proxy/app-server protocol；需要完整 config layer stack 的 runtime crate 应由组合根投影成窄 view。
      verification:
        - `rtk cargo tree -p codex-config-types --depth 1 --edges normal` 不应包含 `codex-app-server-protocol`、`codex-execpolicy`、`codex-network-proxy`、`codex-model-provider-info` implementation；`toml` 只允许作为纯 DTO 反序列化依赖。
        - `rtk cargo tree -p codex-config-toml --depth 1 --edges normal` 可包含 `codex-config-types`、`codex-config-loader`、`codex-config-permissions`、`codex-features`、`codex-model-provider-info` 和 `codex-protocol`，但不得包含 full `codex-config`、`codex-app-server-protocol`、`codex-code-mode`、`codex-execpolicy`、`codex-network-proxy`、Starlark 或 Rama。
        - `rtk cargo tree -p codex-mcp --invert codex-config --edges normal` 不应存在路径；如果 package 不在 graph 中，也可作为 `codex-mcp` 已脱离 heavy config 的证据。
        - `rtk cargo tree -p codex-memories-write --invert codex-config --edges normal` 不应存在路径；normal graph 只应为 memory settings 依赖 `codex-config-types`。
        - `rtk cargo tree -p codex-hooks --invert codex-config --edges normal` 不应存在路径；`codex-hooks --depth 1` 应显示 `codex-config-types`、`codex-protocol` 和 runtime dependencies，但不显示 full `codex-config`。
        - `rtk cargo tree -p codex-core-skills --invert codex-config --edges normal` 不应存在路径；`codex-core-skills --depth 1` 应显示 `codex-config-types`，但不显示 full `codex-config`。
        - TUI/settings DTO 切片后 `rtk cargo test -p codex-config-types` 应覆盖迁移后的 keymap schema/normalization tests；`rtk cargo check -p codex-core-api -p codex-tui -p codex-core` 应验证旧 re-export 和产品路径编译。
        - Requirements 切片后 `rtk cargo tree -p codex-cloud-requirements --invert codex-config --edges normal` 和 `rtk cargo tree -p codex-config-requirements --invert codex-config --edges normal` 不应存在路径；`codex-config-requirements --depth 1` 只允许依赖 `codex-config-types`、`codex-execpolicy-api`、`codex-protocol` 和解析/匹配工具库，不得拉 full config loader。
        - Core-plugins view/edit 切片后，生产路径中 `codex-core-plugins` 不应接受 `codex_config::ConfigLayerStack`，只能接受 `PluginConfigLayerStack`；`rtk cargo tree -p codex-core-plugins --invert codex-config --edges normal` 应无路径，`codex-config` 只允许作为 dev-dependency 出现在 tests/fixtures。
        - `rtk cargo tree -p codex-app-server --invert codex-config-types --edges normal` 应显示轻量共享路径；heavy implementation 只应通过组合根或 runtime implementation crate 出现。
    - step: 4
      status: completed
      description: 拆 tools/code-mode：4a 已完成，新增 `codex-code-mode-api` 并把 code-mode description/tool definition/public tool names/exec prompt builder/pragma parser/schema renderer 从 V8 runtime crate 中分离，`codex-tools` 改为依赖 API crate。4b 已完成，`RuntimeResponse` 和 code-mode content item/image detail DTO 进入 API crate，`codex-rollout-trace` 改为依赖 API crate。4c 已完成，`codex-core` 中 description/DTO/helper callsites 改为直接依赖 API crate。4d 已完成，code-mode runtime service/turn host/factory trait 和 execute/wait DTO 进入 API crate，`codex-core` 只依赖 trait，V8-backed `codex-code-mode` 由 app-server 组合根注入。若后续需要进一步命名拆分，`codex-code-mode-v8` 只保留 execute/wait runtime implementation。
      verification:
        - 当前 crate 拆分阶段先以 `rtk cargo tree -p codex-tools --invert codex-code-mode --edges normal` 和 `rtk cargo tree -p codex-tool-planning --invert codex-code-mode --edges normal` 为门禁；未来若拆出 `codex-code-mode-v8`，再把门禁目标换成新 implementation crate。
        - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal` 必须分别审计 core、rollout-trace、tools、extension-api 路径；保留路径只能指向组合根/runtime handler 或 rollout trace 需要的明确 implementation 依赖。
    - step: 5
      status: completed
      description: 抽 runtime service 边界：在 policy/proxy/config/tools 都轻量化后，再从 `codex-core` 拆 `codex-session-runtime` 或等价窄边界，把 session/thread/turn/goals/command scheduling 的编排代码迁移到只依赖 service traits 的 crate；具体 `ModelClient`、MCP manager、code-mode runtime、exec policy evaluator、network proxy backend 由 `RuntimeServices` 注入。当前正在做前置收缩：继续清理 core default normal graph 中剩余 direct heavy deps（auth/model client stack、exec-server environment/process runtime、MCP manager runtime、workflow manager 等），每个切片都用 `cargo tree --depth 1` 与 `--invert <heavy-crate>` 证明没有把 direct edge 变成 indirect edge。该阶段必须同时定义 model client、MCP runtime、tool runtime、rollout/thread-store、sandbox/exec、workflow manager 的 host-provided trait 边界，否则 core 会继续通过这些 direct deps 保持聚合根。
      verification:
        - `rtk cargo tree -p codex-session-runtime --invert codex-app-server-protocol --edges normal` 无路径。
        - `rtk cargo tree -p codex-session-runtime --invert codex-code-mode-v8 --edges normal` 无路径。
        - `rtk cargo tree -p codex-session-runtime --invert codex-execpolicy-starlark --edges normal` 无路径。
        - `rtk cargo tree -p codex-session-runtime --invert codex-network-proxy-rama --edges normal` 无路径。
    - step: 6
      status: in_progress
      description: 收缩 `codex-core`：把 remaining domains 按功能聚合为 agent runtime/control、tool runtime、guardian runtime、compact/context manager、model client、connectors 等 crate；`codex-core` 仅作为过渡 facade，直到 callsites 迁移完成。
      verification:
        - 每拆一块都执行对应 `cargo tree --invert <old-heavy-crate> --edges normal`，同时检查新 crate 的 `--depth 1` direct dependency。
        - 关键目标是“轻量 crate 不依赖 heavy implementation”，不是单纯减少某个 direct edge。
  validation:
    - `rtk cargo tree -p codex-app-server --invert codex-core --edges normal`：当前只剩 `codex-app-server -> codex-core`。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：确认 core 仍直接依赖 app-server-protocol、code-mode、config、execpolicy、network-proxy、tools 等聚合边界。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：确认 config 仍直接依赖 app-server-protocol、model-provider-info、file-system/git-utils 等 heavy/不合理边界，execpolicy/network-proxy 已替换为 API crate。
    - `rtk cargo tree -p codex-tools --depth 1 --edges normal`：初始确认 tools 直接依赖 app-server-protocol、code-mode、rmcp。
    - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal`：确认 code-mode 通过 core、rollout-trace、tools、extension-api 多路径回到 app-server。
    - `rtk cargo tree -p codex-app-server --invert codex-app-server-protocol --edges normal`：确认 app-server-protocol 是 config/tools/core/login/analytics/otel 等多 crate 公共依赖。
    - `rtk cargo tree -p codex-app-server --invert starlark --edges normal`：当前剩余路径只剩 core direct；config/protocol normal graph 已不再拉 Starlark。
    - `rtk cargo tree -p codex-app-server --invert rama-http --edges normal`：当前剩余路径是 core direct 和 sandboxing/exec-server runtime；config/protocol normal graph 已不再拉 Rama。
    - `rtk rg -n "codex_app_server_protocol|app_server_protocol" core config tools protocol app-server-protocol -g '*.rs'`：确认具体 shared type 使用点。
    - `rtk rg -n "codex_execpolicy|execpolicy::|codex_network_proxy|network_proxy::" config protocol core tools -g '*.rs'`：确认 policy/proxy 纯类型与 implementation 混用点。
    - `rtk rg -n "codex_code_mode|code_mode::|ToolDefinition|CODE_MODE" tools core rollout-trace code-mode -g '*.rs'`：确认 code-mode spec 与 runtime 混用点。
    - `rtk cargo check -p codex-network-proxy-api -p codex-protocol -p codex-network-proxy`：通过，验证新 API crate、protocol import 和旧 proxy re-export 兼容。
    - `rtk cargo tree -p codex-protocol --invert starlark --edges normal`：无路径，protocol normal graph 不再拉 Starlark。
    - `rtk cargo tree -p codex-protocol --invert codex-execpolicy --edges normal`：无路径，execpolicy 仅保留为 protocol dev-dependency。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 显示 `codex-network-proxy-api`，不再显示 `codex-network-proxy`。
    - `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：仅依赖 `serde`。
    - `rtk rustfmt --check network-proxy-api/src/lib.rs network-proxy/src/network_policy.rs protocol/src/network_policy.rs`：通过；仍打印 workspace rustfmt nightly-only 配置 warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，退出码 0，验证组合根仍能编译。
    - `rtk cargo check -p codex-network-proxy-api -p codex-config -p codex-protocol -p codex-network-proxy`：通过，验证 config/protocol/API/backend crate 编译。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct normal deps 显示 `codex-network-proxy-api`，不再显示 `codex-network-proxy`。
    - `rtk cargo tree -p codex-config --invert codex-network-proxy --edges normal`：package 不在 config normal graph 中。
    - `rtk cargo tree -p codex-config --invert rama-http --edges normal`：package 不在 config normal graph 中。
    - `rtk cargo check -p codex-execpolicy-api -p codex-config -p codex-execpolicy`：通过，验证 no-Starlark API crate、config import 和旧 execpolicy re-export/parser 兼容。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct normal deps 显示 `codex-execpolicy-api` 和 `codex-network-proxy-api`，不再显示 `codex-execpolicy` 或 `codex-network-proxy`。
    - `rtk cargo tree -p codex-config --invert codex-execpolicy --edges normal`：package 不在 config normal graph 中。
    - `rtk cargo tree -p codex-config --invert starlark --edges normal`：package 不在 config normal graph 中。
    - `rtk cargo tree -p codex-execpolicy-api --depth 1 --edges normal`：仅依赖 `codex-utils-absolute-path`、`multimap`、`serde`、`shlex`、`thiserror`，不拉 Starlark。
    - `rtk cargo tree -p codex-protocol --invert starlark --edges normal`：无路径，protocol normal graph 仍不拉 Starlark。
    - `rtk cargo tree -p codex-app-server --invert starlark --edges normal`：剩余路径为 `starlark -> codex-execpolicy -> codex-core -> codex-app-server`，不再通过 config/protocol 公共类型层。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：再次通过，退出码 0；仅打印既有 file-subscription unused/dead_code warnings。
    - 早期 `rtk cargo test -p codex-execpolicy`：通过，33 个测试通过，当时覆盖 parser、append/amend 和 re-exported policy model 行为；后续 append/amend writer 已迁移到 `codex-execpolicy-api`，当前测试数量和覆盖记录见 step 5 更新。
    - `rtk cargo test -p codex-config requirements_exec_policy`：通过，覆盖 requirements TOML 到 API `Policy` 的转换和约束。
    - `rtk rustfmt --check --config skip_children=true ...`：通过本次 AuthMode 触碰文件；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-auth-types -p codex-app-server-protocol -p codex-model-provider-info -p codex-otel -p codex-login -p codex-models-manager`：通过，验证 auth types、protocol re-export 和目标消费者编译。
    - `rtk cargo check -p codex-app-server --bin codex-app-server -p codex-cli -p codex-tui`：通过，验证主要入口和直接导入 AuthMode 的 core/app-server/CLI/TUI 路径。
    - `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 仅为 `schemars`、`serde`、`strum_macros`、`ts-rs`，不拉 app-server-protocol 或 runtime implementation。
    - `rtk cargo tree -p codex-model-provider-info --invert codex-app-server-protocol --edges normal`：Cargo 返回 package 不在 graph；`--depth 1` 显示 `codex-auth-types` 且无 `codex-app-server-protocol`。
    - `rtk cargo tree -p codex-otel --invert codex-app-server-protocol --edges normal`：Cargo 返回 package 不在 graph；`--depth 1` 显示 `codex-auth-types` 且无 `codex-app-server-protocol`。
    - `rtk cargo tree -p codex-login --depth 1 --edges normal`：direct deps 显示 `codex-auth-types`，不再 direct 依赖 `codex-app-server-protocol`；`rtk cargo tree -p codex-login --invert codex-app-server-protocol --edges normal` 的剩余路径为 `codex-login -> codex-config -> codex-app-server-protocol`，归入 step 2b。
    - `rtk cargo tree -p codex-models-manager --depth 1 --edges normal`：direct deps 显示 `codex-auth-types`，无 `codex-app-server-protocol` direct edge。
    - `rtk rg -n "codex_app_server_protocol::AuthMode|use codex_app_server_protocol::AuthMode" codex-rs --glob '*.rs'`：无结果，AuthMode callsites 不再从 app-server-protocol 取类型。
    - `rtk git diff --check`：通过。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-app-server-protocol`：通过，验证新 config types crate、config import 和 protocol re-export 编译。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，退出码 0；仅打印既有 file-subscription/core unused warnings。
    - `rtk rg -n "codex_app_server_protocol|app_server_protocol" codex-rs/config/src -g '*.rs'`：无结果，config 生产源码不再引用 app-server-protocol。
    - `rtk cargo tree -p codex-config --invert codex-app-server-protocol --edges normal`：Cargo 返回 package 不在 graph，config normal graph 已无 app-server-protocol。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct normal deps 显示 `codex-config-types`，不再显示 `codex-app-server-protocol`。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仅为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`ts-rs`。
    - `rtk cargo tree -p codex-config-types --invert codex-app-server-protocol --edges normal`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package 不在 graph，新轻量 crate 未间接拉回 app-server protocol、Starlark、Rama 或 code-mode runtime。
    - `rtk cargo tree -p codex-login --invert codex-app-server-protocol --edges normal`：warning nothing to print，AuthMode 后残余的 login -> config -> app-server-protocol 路径已消失。
    - `rtk cargo check -p codex-cli -p codex-tui`：通过，退出码 0；验证 CLI/TUI 仍能经 app-server-protocol re-export 使用 config layer types。
    - `rtk rustfmt --check --config skip_children=true ...`：通过本次 config-types/config/protocol 触碰文件；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk git diff --check`：通过。
    - `rtk cargo check -p codex-connectors-types -p codex-connectors -p codex-chatgpt -p codex-tools -p codex-app-server-protocol`：通过，验证 connectors metadata types、connectors/chatgpt/tools consumer 和 protocol re-export 编译。
    - `rtk cargo test -p codex-connectors`：通过，14 个测试通过；为测试模块补充 `tokio` dev-dependency `sync` feature，生产 dependency graph 不受影响。
    - `rtk cargo test -p codex-tools`：通过，验证 `DiscoverableTool::Connector(AppInfo)` 等 tool discovery/request-plugin-install 逻辑仍编译并测试通过。
    - `rtk cargo check -p codex-chatgpt`：通过，验证 ChatGPT connector listing 使用 `codex-connectors-types::AppInfo`。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，退出码 0；仅打印既有 warnings。
    - `rtk cargo check -p codex-cli -p codex-tui`：通过，退出码 0；验证 CLI/TUI 仍能经 app-server-protocol re-export 使用 app metadata types。
    - `rtk cargo tree -p codex-connectors --invert codex-app-server-protocol --edges normal`：Cargo 返回 package 不在 graph，connectors normal graph 已无 app-server-protocol。
    - `rtk cargo tree -p codex-chatgpt --depth 1 --edges normal`：direct deps 显示 `codex-connectors` 和 `codex-connectors-types`，不再显示 `codex-app-server-protocol`；`--invert codex-app-server-protocol` 剩余路径为 analytics/core-plugins/exec-server/plugin 等非 app metadata 路径。
    - `rtk rg -n "codex_app_server_protocol" codex-rs/tools/src -g '*.rs'`：仅剩 `request_plugin_install.rs` 中 MCP elicitation request/schema wrapper，归入 step 2d。
    - `rtk cargo tree -p codex-connectors-types --depth 1 --edges normal`：direct deps 仅为 `schemars`、`serde`、`ts-rs`。
    - `rtk cargo tree -p codex-connectors-types --invert codex-app-server-protocol --edges normal`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package 不在 graph，新轻量 crate 未间接拉回 app-server protocol、Starlark、Rama 或 code-mode runtime。
    - `rtk cargo tree -p codex-app-server-protocol --depth 1 --edges normal`：protocol 现在 direct 依赖 `codex-auth-types`、`codex-config-types`、`codex-connectors-types` 这些 owner types crate。
    - `rtk rustfmt --check --config skip_children=true ...`：通过本次 connectors-types/connectors/chatgpt/tools/core/protocol 触碰文件；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk git diff --check`：通过。
    - `rtk cargo check -p codex-tools -p codex-core -p codex-app-server-protocol`：通过，验证 request-plugin-install domain plan、core projector 和 protocol payload 仍能编译。
    - `rtk cargo test -p codex-tools`：通过，97 个测试通过；覆盖 request-plugin-install domain plan 和 tool discovery。
    - `rtk cargo test -p codex-core request_plugin_install`：通过，覆盖 core handler 中 tools plan 到 `McpServerElicitationRequestParams` 的投影，以及持久化 disabled install request 等既有逻辑。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，退出码 0；仅打印既有 file-subscription/core unused/dead_code warnings。
    - `rtk cargo tree -p codex-tools --depth 1 --edges normal`：direct deps 显示 `codex-connectors-types`、`codex-code-mode`、`codex-protocol`、`rmcp` 等，不再显示 `codex-app-server-protocol`。
    - `rtk cargo tree -p codex-tools --invert codex-app-server-protocol --edges normal`：Cargo 返回 package 不在 graph，`codex-tools` normal graph 已无 app-server-protocol。
    - `rtk rg -n "codex_app_server_protocol|McpServerElicitation|McpElicitation" codex-rs/tools/src codex-rs/tools/Cargo.toml`：无结果，tools 源码和 manifest 无 app-server-protocol/MCP protocol type 残留。
    - `rtk rustfmt --config skip_children=true ...`：通过本次 request-plugin-install 触碰文件；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk git diff --check`：通过。
    - `rtk cargo check -p codex-code-mode-api -p codex-code-mode -p codex-tools`：通过，验证新 API crate、runtime re-export 和 tools consumer 编译。
    - `rtk cargo test -p codex-code-mode-api`：通过，11 个 description/pragma/schema 渲染测试通过。
    - `rtk cargo test -p codex-tools`：通过，97 个测试通过，覆盖 tools 改依赖后的 code-mode planning/spec 逻辑。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，退出码 0；仅打印既有 file-subscription/core unused/dead_code warnings。
    - `rtk cargo tree -p codex-tools --depth 1 --edges normal`：direct deps 显示 `codex-code-mode-api`，不再显示 `codex-code-mode`。
    - `rtk cargo tree -p codex-tools --invert codex-code-mode --edges normal`：Cargo 返回 package 不在 graph，`codex-tools` normal graph 已无 code-mode runtime。
    - `rtk cargo tree -p codex-code-mode-api --depth 1 --edges normal`：direct deps 仅为 `codex-protocol`、`serde`、`serde_json`。
    - `rtk cargo tree -p codex-code-mode-api --invert v8 --edges normal`：Cargo 返回 package 不在 graph，新 API crate 不拉 V8 runtime。
    - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal`：剩余路径只剩 `codex-core` 和 `codex-rollout-trace`，`codex-tools` 路径已移除。
    - `rtk cargo check -p codex-code-mode-api -p codex-code-mode -p codex-rollout-trace`：通过，验证 response DTO 移入 API crate 后 runtime 和 trace 编译。
    - `rtk cargo test -p codex-code-mode-api`：通过，11 个测试通过。
    - `rtk cargo test -p codex-code-mode`：通过，19 个 runtime/service 测试通过。
    - `rtk cargo test -p codex-rollout-trace code_cell`：通过，4 个 code-cell trace 相关测试通过。
    - `rtk cargo test -p codex-rollout-trace code_mode`：通过，1 个 code-mode response projection 相关测试通过。
    - `rtk cargo test -p codex-rollout-trace`：失败，44 passed / 4 failed；失败均在 `reducer::tool::agents` 测试，报 `missing field sender_agent_path`，和本次 code-mode API/trace DTO 切片不在同一路径，需另行处理。
    - `rtk cargo tree -p codex-rollout-trace --depth 1 --edges normal`：direct deps 显示 `codex-code-mode-api`，不再显示 `codex-code-mode`。
    - `rtk cargo tree -p codex-rollout-trace --invert codex-code-mode --edges normal`：Cargo 返回 package 不在 graph，rollout-trace normal graph 已无 code-mode runtime。
    - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal`：剩余路径只剩 `codex-code-mode -> codex-core -> codex-app-server`。
    - `rtk cargo check -p codex-core -p codex-code-mode-api -p codex-code-mode`：通过，验证 core 直接依赖 API crate 后仍能和 runtime crate 编译。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅出现既有 file-subscription dead_code 和 core unused import warning。
    - `rtk cargo test -p codex-core code_mode --lib`：通过，验证 core code-mode handler/registry 相关 filtered tests。
    - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal`：剩余路径仍只剩 `codex-code-mode -> codex-core -> codex-app-server`，说明本切片没有重新引入 tools/rollout-trace 路径。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：core 现在同时 direct 依赖 `codex-code-mode-api` 和 `codex-code-mode`，过渡边界显式可见。
    - `rtk cargo tree -p codex-code-mode-api --depth 1 --edges normal`：direct deps 仅为 `codex-protocol`、`serde`、`serde_json`；`rtk cargo tree -p codex-code-mode-api --invert v8 --edges normal` 返回 package not in graph。
    - `rtk rg -n "codex_code_mode::(PUBLIC_TOOL_NAME|WAIT_TOOL_NAME|CodeModeToolKind|RuntimeResponse|FunctionCallOutputContentItem|ImageDetail|parse_exec_source|is_code_mode_nested_tool)|use codex_code_mode::(CodeModeToolKind|RuntimeResponse|FunctionCallOutputContentItem|ImageDetail)" codex-rs/core/src`：无结果，core 中旧 runtime crate API/DTO/helper 引用已清空。
    - `rtk git diff --check`：通过。
    - `rtk cargo check -p codex-code-mode-api -p codex-code-mode -p codex-core`：通过，验证 API trait/DTO、V8 implementation 和 core trait-object consumer 编译。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 组合根显式依赖并注入 `codex-code-mode::V8CodeModeRuntimeFactory` 后仍能编译；仅出现既有 file-subscription dead_code 和 core unused import warning。
    - `rtk cargo test -p codex-code-mode`：通过，19 个 runtime/service 测试通过。
    - `rtk cargo test -p codex-code-mode-api`：通过，11 个 API/description/schema 测试通过。
    - `rtk cargo test -p codex-core code_mode --lib`：通过，验证 core code-mode handler/registry filtered tests 通过 trait-object runtime boundary。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct deps 显示 `codex-code-mode-api`，不再显示 `codex-code-mode`。
    - `rtk cargo tree -p codex-core --invert codex-code-mode --edges normal`：Cargo 返回 package 不在 graph，core normal graph 已无 V8 implementation。
    - `rtk cargo tree -p codex-app-server --invert codex-code-mode --edges normal`：剩余路径为 `codex-code-mode -> codex-app-server`，implementation 只由组合根直连。
    - `rtk git diff --check`：通过。
    - `rtk cargo check -p codex-config-types -p codex-message-history`：通过，验证 `History`/`HistoryPersistence` 下沉后轻量 consumer 编译。
    - `rtk cargo test -p codex-message-history`：通过，4 个测试通过。
    - `rtk cargo tree -p codex-message-history --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-message-history --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，message-history normal graph 已无 heavy config。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-login`：通过，验证 credential store mode 和 residency enum 下沉后 config re-export 与 login 编译。
    - `rtk cargo tree -p codex-login --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-login --invert codex-config --edges normal`：无路径，login normal graph 已无 heavy config。
    - `rtk cargo test -p codex-login storage --lib`：通过，覆盖 credential store mode 相关 storage 路径。
    - `rtk rustfmt --check --config skip_children=true ...`：通过本次 config-types/message-history/login 触碰文件；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-login -p codex-rmcp-client -p codex-app-server-transport --tests`：通过，验证 config re-export、login/rmcp-client 和 app-server-transport test targets 编译。
    - `rtk cargo tree -p codex-rmcp-client --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-rmcp-client --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，rmcp-client normal graph 已无 heavy config。
    - `rtk rustfmt --check --config skip_children=true ...`：通过本次 `McpServerEnvVar`/rmcp-client/app-server-transport 触碰文件；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk git diff --check`：通过。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-plugin -p codex-hooks`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-plugin -p codex-mcp -p codex-hooks`：通过，验证 hook DTO 下沉、config re-export、plugin/mcp/hook caller 编译。
    - `rtk cargo test -p codex-config-types`：通过，14 个测试通过，覆盖新下沉的 hook/MCP/constraint 类型。
    - `rtk cargo test -p codex-config hooks --lib`：通过，8 个 hook 相关测试通过，覆盖 config re-export 和协议投影边界。
    - `rtk cargo test -p codex-plugin`：通过，退出码 0，验证 plugin 不再依赖 heavy config。
    - `rtk cargo test -p codex-mcp`：通过，退出码 0，验证 MCP manager/server/auth/tool 路径在轻量 config types 下仍可用。
    - `rtk cargo test -p codex-hooks`：通过，退出码 0，验证 hook discovery/declaration 调用新的 `hook_events_into_matcher_groups` 边界。
    - `rtk cargo tree -p codex-mcp --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，`codex-mcp` normal graph 已无 heavy `codex-config`，包括此前 `codex-plugin` indirect path。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仅为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`ts-rs`，没有 protocol/config/app-server/V8/Starlark/Rama。
    - `rtk cargo tree -p codex-plugin --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-memories-write`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-memories-write`：通过，验证 memories config 下沉、config re-export 和 memory writer consumer 编译。
    - `rtk cargo test -p codex-config-types`：通过，16 个测试通过，新增 memories config clamp 测试进入轻量 crate。
    - `rtk cargo test -p codex-config memories --lib`：通过，2 个 memories 相关过滤测试通过，验证 config 旧路径兼容。
    - `rtk cargo test -p codex-memories-write`：通过，退出码 0。
    - `rtk cargo tree -p codex-memories-write --invert codex-config --edges normal`：无路径，`codex-memories-write` normal graph 已无 heavy `codex-config`。
    - `rtk cargo tree -p codex-memories-write --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-mcp-server -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core -p codex-mcp-server --tests`：通过，验证 otel DTO 下沉、config re-export、core resolver、mcp-server test target，以及补齐的 `ThreadManager::new` code-mode factory 注入。
    - `rtk cargo test -p codex-config-types`：通过，16 个测试通过。
    - `rtk cargo test -p codex-mcp-server`：通过，退出码 0。
    - `rtk cargo tree -p codex-mcp-server --depth 1 --edges normal`：direct deps 不再显示 `codex-config`；显示 `codex-code-mode`，这是 mcp-server 作为组合根注入 V8 runtime 的显式 implementation edge。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仍仅为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`ts-rs`。
    - `rtk cargo check -p codex-hooks`：通过，验证 hooks 生产 path 使用 `codex-config-types` 和 `HookConfigLayerStack` 编译。
    - `rtk cargo check -p codex-hooks --tests`：通过，测试 fixture 通过 cfg(test) 从完整 config stack 转换成 hooks view。
    - `rtk cargo check -p codex-core -p codex-hooks --tests`：通过，验证 core session/test support 调用新的 hook config projection helper。
    - `rtk cargo fmt -p codex-hooks -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server catalog hook listing 通过 core 投影 helper 编译；仅出现既有 file-subscription/core unused warnings。
    - `rtk cargo test -p codex-hooks`：通过，110 个测试通过。
    - `rtk cargo test -p codex-core hooks --lib`：通过，验证 core hook filtered tests。
    - `rtk cargo tree -p codex-hooks --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-hooks --invert codex-config --edges normal`：warning nothing to print，`codex-hooks` normal graph 已无 heavy `codex-config`。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core-skills`：通过，验证 skills config DTO 下沉、config re-export 和 skills runtime consumer 编译。
    - `rtk cargo check -p codex-core-skills --tests`：通过，验证 skills 测试 fixture 经 `SkillConfigLayerStack` view 编译。
    - `rtk cargo check -p codex-core --tests`：通过，验证 core 投影 helper 和测试路径编译。
    - `rtk cargo test -p codex-core-skills`：通过，验证 skills loader/manager 逻辑。
    - `rtk cargo tree -p codex-core-skills --depth 1 --edges normal`：direct deps 显示 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-core-skills --invert codex-config --edges normal`：warning nothing to print，`codex-core-skills` normal graph 已无 heavy `codex-config`。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 显示 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml`、`ts-rs`；`toml` 只服务 `SkillsConfig` 纯 DTO 反序列化，不代表 full config loader 回流。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server skills watcher/catalog 通过 core 投影 helper 编译；仅出现既有 warnings。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：剩余 direct consumers 为 app-server/client/CLI/TUI/exec 等组合根、`codex-core`、`codex-core-api`、`codex-core-plugins`、`codex-cloud-requirements` 和 test support；说明后续已进入 loader/config-edit/core facade 边界。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core-api`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config`：通过，验证 UI/settings DTO 和 keymap schema 从 config 移到 config-types 后旧 config re-export 编译。
    - `rtk cargo test -p codex-config-types`：通过，21 个测试通过，覆盖迁移后的 keymap schema/normalization 以及既有 config-types tests。
    - `rtk cargo check -p codex-core-api -p codex-tui -p codex-core`：通过，验证 core-api pure DTO facade re-export、TUI 产品路径和 core 旧 `codex_config::types` 兼容路径。
    - `rtk cargo check -p codex-core-api -p codex-tui -p codex-core --tests`：失败，阻断为 TUI test-only `tui/src/app/app_server_event_targets.rs` 导入 `codex_app_server_protocol::v2::{WorkflowRun, WorkflowRunStatus, WorkflowRunUpdatedNotification, WorkflowSource, WorkflowSummary}`，当前 app-server-protocol 没有公开 `v2` 模块；这不是本次 config-types 迁移产生的类型错误，需另行修复。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仍为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml`、`ts-rs`，未引入 protocol/config/app-server/V8/Starlark/Rama。
    - `rtk cargo tree -p codex-core-api --depth 1 --edges normal`：direct deps 显示 `codex-config-types` 和仍保留的 `codex-config`；保留原因是 core-api 仍 re-export `ConfigLayerStack`、`ProjectConfig`/Realtime config 和 core facade。
    - `rtk cargo fmt -p codex-config-requirements -p codex-config -p codex-cloud-requirements`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-requirements -p codex-config -p codex-cloud-requirements`：通过，验证 requirements 新 crate、config re-export 和 cloud consumer 编译。
    - `rtk cargo test -p codex-config-requirements`：通过，50 个 tests 通过，覆盖 requirements TOML、constraints、policy evaluator 和 loader 逻辑。
    - `rtk cargo test -p codex-cloud-requirements`：通过，验证 cloud requirements 迁移到轻量 requirements crate 后仍可用。
    - `rtk cargo tree -p codex-cloud-requirements --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，`codex-cloud-requirements` normal graph 已无 heavy `codex-config`。
    - `rtk cargo tree -p codex-cloud-requirements --depth 1 --edges normal`：direct deps 显示 `codex-config-requirements` 和 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-config-requirements --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-execpolicy-api`、`codex-protocol`、`codex-utils-absolute-path`、`futures`、`multimap`、`serde`、`thiserror`、`wildmatch`，不拉 full config loader。
    - `rtk cargo tree -p codex-config-requirements --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，新 requirements crate normal graph 已无 heavy `codex-config`。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：剩余 direct consumers 中 `codex-cloud-requirements` 已消失；剩余主要是组合根、`codex-core`、`codex-core-api`、`codex-core-plugins`、exec/TUI/CLI/app-server client 和 test support。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core-plugins -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core-plugins -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 plugin view、DTO re-export、core projection 和 app-server cwd-specific catalog paths 编译。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo test -p codex-core-plugins`：通过，191 个 tests 通过，覆盖 plugin loader/manager/marketplace 行为在 plugin view 下仍正常。
    - `rtk cargo tree -p codex-core-plugins --depth 1 --edges normal`：direct deps 现在同时显示 `codex-config-types` 和 `codex-config`；保留 `codex-config` 的原因是 plugin/marketplace edit、`CONFIG_TOML_FILE` 和 fingerprint，下一步应继续收缩 edit/IO 边界。
    - `rtk rg -n "ConfigLayerStack|ConfigLayerStackOrdering" core-plugins/src -g '*.rs'`：生产路径只剩 `PluginConfigLayerStack`/ordering；完整 `codex_config::ConfigLayerStack` 仅在 test-only conversion/tests 中出现。
    - `rtk rg -n "codex_config::types::(Plugin|Marketplace)|use codex_config::types::(Plugin|Marketplace)|codex_config::(PluginConfig|McpServerConfig|HooksFile)" core-plugins/src -g '*.rs'`：生产路径不再通过 `codex_config::types` 获取 plugin/marketplace DTO；剩余 `codex_config::PluginConfigEdit` 是写配置边界。
    - `rtk cargo fmt -p codex-config-edit -p codex-config -p codex-core-plugins`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-edit -p codex-config -p codex-core-plugins -p codex-app-server --bin codex-app-server`：通过，验证新 edit crate、旧 config re-export、core-plugins 调用方和 app-server 入口编译。
    - `rtk cargo test -p codex-config-edit`：通过，9 个 tests 通过，覆盖 plugin edit、marketplace edit 和 config symlink 写入行为。
    - `rtk cargo test -p codex-core-plugins`：通过，191 个 tests 通过，验证 core-plugins 从 full config 切到 edit crate 后行为保持。
    - `rtk cargo tree -p codex-core-plugins --invert codex-config --edges normal`：warning nothing to print，`codex-core-plugins` normal graph 已无 full `codex-config`。
    - `rtk cargo tree -p codex-core-plugins --depth 1 --edges normal`：direct deps 显示 `codex-config-edit` 和 `codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-config-edit --depth 1 --edges normal`：direct deps 仅为 `codex-utils-path`、`serde_json`、`sha2`、`tokio`、`toml`、`toml_edit`，不拉 loader/model-provider/protocol/app-server。
    - `rtk cargo tree -p codex-config-edit --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，新 edit crate 未间接拉回 full config。
    - `rtk cargo tree -p codex-config-edit --invert codex-app-server-protocol --edges normal`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package 不在 graph，新 edit crate 未间接拉回 app-server protocol、Starlark、Rama 或 code-mode runtime。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：剩余 direct consumers 中 `codex-core-plugins` 已消失；剩余主要是 app-server/client/CLI/TUI/exec 组合根、`codex-core`、`codex-core-api` 和 test support。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct deps 显示 `codex-config-edit`、`codex-config-requirements`、`codex-config-types` 等；`sha2` 不再是 full config direct dependency。
    - `rtk git diff --check`：通过。
    - `rtk cargo tree -p codex-core-api --depth 1 --edges normal`：direct deps 已显示 `codex-config-types`，不再显示 `codex-config`；`codex-core-api` 的纯 DTO facade 不再直接拉 full config。
    - `rtk cargo tree -p codex-core-api --invert codex-config --edges normal`：剩余路径为 `codex-core-api -> codex-core -> codex-config`，说明当前未完成的是 core facade/runtime config loader 边界，不是 core-api direct manifest edge。
    - `rtk git diff --check`：通过。
    - `rtk cargo fmt -p codex-config-loader -p codex-config-loader-remote -p codex-config -p codex-app-server-client -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-loader -p codex-config-loader-remote -p codex-config -p codex-app-server-client -p codex-app-server --bin codex-app-server`：通过，验证 loader API/remote split、旧 config re-export、app-server-client 启动 wiring 和 app-server 入口编译。
    - `rtk cargo test -p codex-config-loader -p codex-config-loader-remote`：通过，4 个 tests/suites 通过，覆盖 Static/Noop loader API 和 remote loader proto conversion/request timeout。
    - `rtk cargo tree -p codex-config-loader --depth 1 --edges normal`：direct deps 仅为 `codex-model-provider-info`、`codex-protocol`、`codex-utils-absolute-path`、`thiserror`，不含 tonic/prost 或 full config。
    - `rtk cargo tree -p codex-config-loader-remote --depth 1 --edges normal`：remote implementation deps 为 `codex-config-loader`、model-provider/protocol/absolute-path、`prost`、`tonic`、`tonic-prost`。
    - `rtk cargo tree -p codex-config-loader-remote --invert codex-config --edges normal`：Cargo 返回 package 不在 graph，remote implementation 未回拉 full config。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct deps 显示 `codex-config-loader`，不显示 `codex-config-loader-remote`、`prost`、`tonic` 或 `tonic-prost`。
    - `rtk cargo tree -p codex-config --invert tonic --edges normal --depth 4` 和 `--invert prost`：剩余路径来自既有 `codex-features -> codex-otel`，不再来自 `codex-config-loader`。
    - `rtk cargo tree -p codex-app-server-client --depth 1 --edges normal`：direct deps 显示 `codex-config-loader`、`codex-config-loader-remote`、`codex-config-requirements`，不再显示 full `codex-config`。
    - `rtk cargo tree -p codex-app-server-client --invert codex-config --edges normal --depth 3`：剩余路径仅为 `codex-app-server-client -> codex-app-server -> codex-config` 和 `codex-app-server-client -> codex-core -> codex-config`。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：剩余 direct consumers 中 `codex-app-server-client` 已消失；剩余主要是 app-server/CLI/TUI/exec 组合根、`codex-core` 和 test support。
    - `rtk git diff --check`：通过。
    - `rtk cargo fmt -p codex-features -p codex-core -p codex-config -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-features -p codex-config -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 features 去 OTEL 依赖后 core session metrics helper、config TOML/schema 和 app-server 入口编译。
    - `rtk cargo test -p codex-features`：通过，47 个 tests 通过。
    - `rtk cargo tree -p codex-features --depth 1 --edges normal`：direct deps 只剩 `codex-protocol`、`schemars`、`serde`、`toml`、`tracing`，不再显示 `codex-otel`。
    - `rtk cargo tree -p codex-config --invert codex-otel --edges normal --depth 4`、`--invert tonic`、`--invert prost`：均返回 package 不在 graph，config normal graph 已不再通过 features/otel 拉 gRPC/OTEL 依赖。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct deps 仍显示 `codex-features`，但该 crate 已不再带 `codex-otel`、tonic 或 prost。
    - `rtk git diff --check`：通过。
    - `rtk cargo fmt -p codex-config-diagnostics -p codex-config -p codex-exec`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-diagnostics -p codex-config -p codex-exec`：通过，验证新 diagnostics crate、config re-export/first-layer integration 和 exec 入口编译。
    - `rtk cargo test -p codex-config-diagnostics`：通过，0 个测试；验证新 crate test target 编译。
    - `rtk cargo test -p codex-config strict_config --lib`：通过，覆盖 strict config span/error helper 经新 diagnostics crate 复用后的调用路径。
    - `rtk cargo tree -p codex-exec --depth 1 --edges normal`：direct deps 显示 `codex-config-diagnostics` 和 `codex-config-loader`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-exec --invert codex-config --edges normal --depth 3`：剩余路径为 `codex-exec -> codex-core -> codex-config` 以及 `codex-exec -> codex-app-server-client -> codex-app-server -> codex-config`，不是 exec 自身的 config diagnostics/loader direct edge。
    - `rtk cargo tree -p codex-config-diagnostics --depth 1 --edges normal`：direct deps 仅为 `serde`、`serde_path_to_error`、`toml`、`toml_edit`。
    - `rtk cargo tree -p codex-config-diagnostics --invert codex-config --edges normal --depth 3`：Cargo 返回 package 不在 graph，新 diagnostics crate 未间接拉回 full config。
    - `rtk cargo fmt -p codex-cli`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-cli`：通过，验证 CLI 从 full config 切到 config edit/loader/types 后编译。
    - `rtk cargo tree -p codex-cli --depth 1 --edges normal`：direct deps 显示 `codex-config-edit`、`codex-config-loader`、`codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-cli --invert codex-config --edges normal --depth 3`：剩余路径来自 `codex-app-server`、`codex-tui`、`codex-core`、`app-server-test-client`、ChatGPT/OSS helper 等组合根/legacy facade，不是 CLI 自身的 config DTO/loader direct edge。
    - `rtk cargo check -p app_test_support`：通过，验证 test support 从 full config 切到 auth/config types 后编译；仅打印既有 core warnings。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：`app_test_support` 已从 direct consumer 列表消失，剩余 direct consumers 为 `codex-app-server`、`codex-core`、`codex-tui` 和 `core_test_support`。
    - `rtk cargo fmt -p codex-config-state -p codex-config -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-state -p codex-config -p codex-app-server --bin codex-app-server`：通过，验证新 config-state crate、config re-export 和 app-server profile stack callsite 编译。
    - `rtk cargo test -p codex-config-state`：通过，6 个 tests 通过，覆盖 layer stack 和 TOML merge。
    - `rtk cargo tree -p codex-config-state --depth 1 --edges normal`：direct deps 仅为 `codex-config-edit`、`codex-config-requirements`、`codex-config-types`、`codex-utils-absolute-path`、`serde_json`、`toml`。
    - `rtk cargo tree -p codex-config-state --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package 不在 graph，新 state crate 未间接拉回 full config、app-server protocol 或 runtime implementation。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-tui`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-tui`：通过，验证 TUI 移除 direct `codex-config` 后产品路径编译；仅打印既有 unused/dead_code warnings。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo tree -p codex-tui --depth 1 --edges normal`：direct deps 显示 `codex-config-diagnostics`、`codex-config-edit`、`codex-config-loader`、`codex-config-requirements`、`codex-config-state`、`codex-config-types`，不再显示 `codex-config`。
    - `rtk cargo tree -p codex-tui --invert codex-config --edges normal --depth 3`：TUI direct config 清理后的首次反向图显示剩余路径来自 `codex-app-server-client -> codex-app-server -> codex-config`、`codex-core` 和 `codex-utils-sandbox-summary -> codex-core` 等组合根/legacy facade，不是 TUI 自身的 direct config edge；后续 sandbox-summary/OSS helper 清理已继续收缩这些间接路径。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：`codex-tui` 已从 direct consumer 列表消失；剩余 direct consumers 主要为 `codex-app-server`、`codex-core` 和 `core_test_support`。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml`、`ts-rs`，新增 Windows/Realtimes DTO 未引入 protocol/config/app-server/V8/Starlark/Rama。
    - `rtk cargo fmt -p codex-utils-sandbox-summary`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-utils-sandbox-summary`：通过，5 个 tests 通过。
    - `rtk cargo tree -p codex-utils-sandbox-summary --depth 1 --edges normal`：direct deps 仅为 `codex-protocol` 和 `codex-utils-absolute-path`，不再显示 `codex-core`。
    - `rtk cargo fmt -p codex-lmstudio -p codex-ollama -p codex-utils-oss -p codex-exec -p codex-tui`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-lmstudio -p codex-ollama -p codex-utils-oss -p codex-exec -p codex-tui`：通过，验证 OSS readiness API 改造和 exec/TUI 调用方编译；仅打印既有 unused/dead_code warnings。
    - `rtk cargo tree -p codex-lmstudio --depth 1 --edges normal`、`rtk cargo tree -p codex-ollama --depth 1 --edges normal`、`rtk cargo tree -p codex-utils-oss --depth 1 --edges normal`：direct deps 均不再显示 `codex-core`；`codex-utils-oss` 只依赖 lmstudio、ollama 和 model-provider-info。
    - `rtk cargo tree -p codex-tui --invert codex-config --edges normal --depth 4`：剩余路径只剩 `codex-tui -> codex-app-server-client -> codex-app-server -> codex-config` 和 `codex-tui -> codex-app-server-client -> codex-core -> codex-config`；`codex-utils-sandbox-summary`、`codex-utils-oss`、lmstudio/ollama 路径已消失。
    - `rtk cargo test -p codex-utils-oss`：通过，3 个 tests 通过。
    - `rtk cargo tree -p codex-ollama --depth 1 --edges normal` 和 `rtk cargo tree -p codex-utils-oss --depth 2 --edges normal`：`wiremock` 已不在 normal graph；`codex-utils-oss` 的 normal graph 只经 lmstudio/ollama/model-provider-info 和网络客户端依赖。
    - `rtk cargo check -p codex-ollama -p codex-utils-oss`：通过，验证 `wiremock` 降级到 dev-dependency 后 production targets 编译。
    - `rtk cargo fmt -p core_test_support`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p core_test_support`：通过，验证测试支撑 crate direct config import 替换后编译；仅打印既有 core warning。
    - `rtk cargo tree -p core_test_support --depth 1 --edges normal`：direct deps 显示 `codex-config-edit`、`codex-config-loader`、`codex-config-requirements`、`codex-config-state` 和 `toml`，不再显示 `codex-config`。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 已收敛到 `codex-app-server` 和 `codex-core`；其他路径均经这两个组合/runtime 根。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core`：通过，验证 apps config DTO 下沉、config re-export 和 core 轻量 import 替换编译；仅打印既有 core unused import warning。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo test -p codex-core connectors --lib`：通过，覆盖 apps config 解析和 connector policy 使用路径。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml`、`ts-rs`，apps DTO 未引入 full config、protocol 或 runtime implementation。
    - `rtk cargo tree -p codex-config-types --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，轻量 config-types 未间接拉回 heavy implementation。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：core direct deps 明确显示 `codex-config-edit`、`codex-config-requirements`、`codex-config-state`、`codex-config-types` 和仍保留的 full `codex-config`；保留 full config 是 effective config/loader 边界，后续需继续拆。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；本次 core import cleanup 未新增其他 direct consumer。
    - 精确搜索 `codex_config::|use codex_config` 确认本次处理的 core runtime 文件不再直接引用 full `codex_config`；剩余旧路径集中在 `core/src/config`、loader/agent-role/network-proxy 边界和测试兼容路径。
    - `rtk git diff --check`：通过。
    - `rtk cargo fmt -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server direct 轻量 config imports 编译；仅打印既有 file-subscription/core unused warnings。
    - `rtk cargo tree -p codex-app-server --depth 1 --edges normal`：direct deps 明确显示 `codex-config-diagnostics`、`codex-config-edit`、`codex-config-loader`、`codex-config-loader-remote`、`codex-config-requirements`、`codex-config-state`、`codex-config-types`，同时仍显示 full `codex-config` 作为 local loader/effective config 边界。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；app-server cleanup 未扩大 full config consumer 集合。
    - 搜索 `app-server/src` 的 `codex_config::|use codex_config`，剩余生产 full config 用点集中在 `ConfigManager` 的 `load_config_layers_state`、`ConfigManagerService` 的 `ConfigToml`、request processors 的 `project_trust_key`、models 的 `ModelOptionToml`，以及测试模块旧兼容路径。
    - app-server model catalog display helper 不再直接命名 `codex_config::config_toml::ModelOptionToml`；`app-server/src/models.rs` 改为本地 `ConfiguredModelOptionView`，只暴露列表展示需要的 provider/model/context window/auto-compact 字段。`ModelOptionToml` 本体仍留在 full config，因为它包含 inline provider synthesis 和 `WireApi -> ModelProviderInfo` runtime config 边界，不适合下沉到 `codex-config-types`。
    - `rtk cargo fmt -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过；app-server 旧的 `NoopThreadConfigLoader` production unused import warning 已清理，剩余为既有 core/file-subscription/app-server dead code warnings。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；这次 display helper 整理没有新增 indirect heavy config consumer。
    - `rtk git diff --check`：通过。
    - `project_trust_key`、`normalized_project_trust_keys` 和 trust lookup helper 已从 full `codex-config::loader` 下沉到 `codex-config-loader`；旧 `codex_config::loader::project_trust_key` 保持 re-export 兼容。app-server request processor 和 core effective config 生产路径已改为直接依赖 `codex_config_loader::project_trust_key`，避免为了 trust key helper 拉入 full config。
    - `rtk cargo fmt -p codex-config-loader -p codex-config -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-loader -p codex-config`：通过，验证新 loader helper 和旧 config re-export 编译。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server import、core 新 direct loader dependency 和组合根编译；仅打印既有 warnings。
    - `rtk cargo tree -p codex-config-loader --depth 1 --edges normal`：direct deps 为 `codex-model-provider-info`、`codex-protocol`、`codex-utils-absolute-path`、`dunce` 和 `thiserror`，不显示 full `codex-config`。
    - `rtk cargo tree -p codex-config-loader --invert codex-config --edges normal --depth 3`：返回 package not in graph，loader helper 未间接拉回 full config。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - 搜索 `core/src` 和 `app-server/src` 的 `codex_config::loader::project_trust_key`，生产路径已无旧 import；旧路径只留在 tests 兼容覆盖。
    - `AGENTS.md` 已记录 project trust key/lookup helper 属于 `codex-config-loader`，旧 `codex_config::loader` 只作为兼容 re-export。
    - `core/src/config/mod.rs` 内部纯 config type / requirements / layer stack / loader option import 已改为直接依赖 `codex-config-types`、`codex-config-requirements`、`codex-config-state` 和 `codex-config-loader`；`codex_config::types::*`、`codex_config::ConfigLayerStack`、`codex_config::ConfigLoadOptions` 等旧 re-export 不再作为 core effective config 模块的内部来源。`ConfigToml`、`ModelOptionToml`、`validate_model_providers`、`load_config_layers_state`、`ConfigProfile` 等仍保留 full `codex-config`，因为它们属于 effective config 构造/validation/loader 边界。
    - `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-core`：通过；仅打印既有 core unused/dead_code warnings。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 core config facade re-export 来源变化后组合根仍编译；仅打印既有 warnings。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：core direct deps 明确显示 `codex-config-loader`、`codex-config-requirements`、`codex-config-state`、`codex-config-types` 和仍保留的 full `codex-config`；这说明直接依赖没有被伪装成 full config 的 indirect re-export。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - `rtk rg -n "codex_config::types::|use codex_config::types|codex_config::loader::project_trust_key|use codex_config::(CloudRequirementsLoader|ConfigLayerSource|ConfigLayerStack|ConfigRequirements|ConstrainedWithSource|ThreadConfigLoader|LoaderOverrides|ConfigLoadOptions)" codex-rs/core/src/config/mod.rs codex-rs/app-server/src/request_processors.rs`：无结果，目标 production import 已清理。
    - `rtk git diff --check`：通过。
    - core 其它 production 文件的旧 full config re-export import 已继续收窄：`mcp.rs`、`session/session.rs`、`session/config_lock.rs`、`config/otel.rs`、`windows_sandbox.rs`、`config/resolved_permission_profile.rs`、`config/managed_features.rs`、`config/edit.rs`、`network_proxy_loader.rs`、`config/network_proxy_spec.rs`、`config_lock.rs`、`mcp_skill_dependencies.rs` 和 `config/agent_roles.rs` 中的 MCP/OTEL/memories/requirements/layer-stack/loader-option/network constraints 等已改为直接从 `codex-config-types`、`codex-config-requirements`、`codex-config-state`、`codex-config-edit` 或 `codex-config-loader` 获取。保留的 full config 引用集中在 `ConfigToml`、schema、permissions TOML、MCP edit builder、project root markers、local loader 和 feature/config validation 边界。
    - `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-core`：通过；仅打印既有 core unused/dead_code warnings。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 core 轻量 import cleanup 后上层组合根仍编译；仅打印既有 warnings。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - 目标扫描 `codex_config::types::|use codex_config::types|codex_config::NoopThreadConfigLoader|codex_config::merge_toml_values|codex_config::FilesystemConstraints|use codex_config::(McpServerConfig|RequirementSource|ConfigLayerStack|ConfigLayerStackOrdering|CloudRequirementsLoader|LoaderOverrides|Constrained|ConstraintResult|NetworkConstraints)`：production 仅剩 `core/src/config/permissions.rs` 的 `SandboxWorkspaceWrite`，该类型尚未下沉到 `codex-config-types`，暂归入后续 permissions TOML 边界。
    - `rtk git diff --check`：通过。
    - `SandboxWorkspaceWrite` 已从 `codex-config::types` 下沉到 `codex-config-types`，`codex_config::types::SandboxWorkspaceWrite` 保持 re-export 兼容。`core/src/config/permissions.rs` 生产路径已改为直接依赖 `codex_config_types::SandboxWorkspaceWrite`，清掉 core production 中最后一个 `codex_config::types::*` 旧 re-export import。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core`：通过，验证 DTO 下沉、旧 re-export 和 core permissions 使用路径编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仍为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml`、`ts-rs`，未新增 heavy dependency。
    - `rtk cargo tree -p codex-config-types --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，`SandboxWorkspaceWrite` 下沉未间接拉回 full config、app-server protocol 或 runtime implementation。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - `rtk rg -n "codex_config::types::|use codex_config::types" codex-rs/core/src -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：无结果，core production 不再通过 `codex_config::types` 取已下沉轻量 DTO。
    - `AGENTS.md` 已记录 `SandboxWorkspaceWrite` 属于 `codex-config-types`。
    - `rtk git diff --check`：通过。
    - `default_project_root_markers` 和 `project_root_markers_from_config` 已从 full `codex-config` 下沉到 `codex-config-loader`，旧 `codex_config::*` 路径保持 re-export 兼容。`config/src/loader/mod.rs` 和 `core/src/agents_md.rs` 生产路径已改为直接依赖 `codex_config_loader::*`，避免为了 merged TOML 根检测 helper 拉入 full config。
    - `rtk cargo fmt -p codex-config-loader -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-loader -p codex-config -p codex-core`：通过，验证新 loader helper、旧 config re-export 和 core AGENTS.md discovery 使用路径编译；仅打印既有 core warning。
    - `rtk cargo tree -p codex-config-loader --depth 1 --edges normal`：direct deps 为 `codex-model-provider-info`、`codex-protocol`、`codex-utils-absolute-path`、`dunce`、`thiserror` 和 `toml`；新增 `toml` 是解析 merged TOML helper 的轻量依赖，不拉 full config。
    - `rtk cargo tree -p codex-config-loader --invert codex-config --edges normal --depth 3`：返回 package not in graph，loader helper 未间接拉回 full config。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - 搜索 `core/src` 和 `app-server/src` 的 `codex_config::default_project_root_markers` / `codex_config::project_root_markers_from_config`：production 无结果。
    - `AGENTS.md` 已记录 project root marker helper 属于 `codex-config-loader`。
    - `rtk git diff --check`：通过。
    - `build_cli_overrides_layer` 和内部 dotted TOML override helper 已从 full `codex-config::overrides` 下沉到 `codex-config-loader`，旧 `codex_config::build_cli_overrides_layer` 保持 re-export 兼容。`config/src/loader/mod.rs` 和 `core/src/config/mod.rs` 生产路径已改为直接依赖 `codex_config_loader::build_cli_overrides_layer`，避免为了 runtime CLI override layer 构造 helper 继续经过 full config facade。
    - `rtk cargo fmt -p codex-config-loader -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-loader -p codex-config -p codex-core`：通过，验证 CLI override helper 下沉、旧 config re-export 和 core default config load 使用路径编译；仅打印既有 core unused/dead_code warnings。
    - `rtk cargo test -p codex-config-loader`：通过，1 个 test suite 通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core/app-server unused/dead_code warnings。
    - `rtk cargo tree -p codex-config-loader --depth 1 --edges normal`：direct deps 为 `codex-model-provider-info`、`codex-protocol`、`codex-utils-absolute-path`、`dunce`、`thiserror` 和 `toml`，不显示 full `codex-config`。
    - `rtk cargo tree -p codex-config-loader --invert codex-config --edges normal --depth 3`：返回 package not in graph，CLI override helper 下沉未间接拉回 full config。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；本切片未扩大 full config consumer 集合。
    - 搜索 `codex_config::build_cli_overrides_layer` / `crate::overrides::build_cli_overrides_layer`：production 无结果，旧路径只保留在 `codex-config` re-export 边界。
    - `AGENTS.md` 已记录 CLI override TOML layer builder 属于 `codex-config-loader`，旧 `codex_config::*` 只作为兼容 re-export。
    - app-server 测试代码中的旧 full config facade import 已收窄：`mcp_refresh`、`config_manager_service_tests`、`message_processor_tracing_tests`、`thread_processor_tests` 改为直接依赖 `codex-config-loader` / `codex-config-requirements`，`external_agent_config_tests` 和 `config_processor` tests 改为直接依赖 `codex-config-types` / `codex-config-requirements`。app-server 源码扫描中 full `codex_config` 只剩 `config_manager.rs` 的 `load_config_layers_state` 和 `config_manager_service.rs` 的 `ConfigToml`，均属于当前 local loader/effective config 重边界。
    - `rtk cargo fmt -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-app-server --lib --no-run`：通过，确认 app-server lib tests 全部编译。
    - `rtk cargo test -p codex-app-server --lib`：失败，242 passed / 5 failed；失败为既有 `request_processors::thread_summary::thread_summary_tests::extract_conversation_summary_prefers_plain_user_messages` 和 4 个 `thread_status::tests::subagent_wait_*` timeout/summary 断言，非本次 test import 收窄触碰路径。后续如果要恢复全量 app-server lib test，需要单独处理这些 thread_summary/thread_status failures。
    - 本次触碰模块窄测试通过：`rtk cargo test -p codex-app-server mcp_refresh --lib`、`rtk cargo test -p codex-app-server requirements_api_includes_allow_managed_hooks_only --lib`、`rtk cargo test -p codex-app-server external_agent_config --lib`、`rtk cargo test -p codex-app-server tracing --lib`、`rtk cargo test -p codex-app-server thread_processor --lib` 均通过。
    - 搜索 `use codex_config::|codex_config::` 于 `codex-rs/app-server/src`：仅剩 `ConfigToml` 和 `load_config_layers_state` 两处生产边界。
    - `rtk git diff --check`：通过。
    - `first_layer_config_error` 和 `first_layer_config_error_from_entries` 已从 full `codex-config::diagnostics` 下沉到 `codex-config-state`，旧 `codex_config::*` 继续 re-export 兼容。这个 helper 的真实 owner 是 `ConfigLayerStack` 到具体 config 文件路径的 diagnostic 映射；纯 TOML span/formatting 仍留在 `codex-config-diagnostics`。`core/src/config/mod.rs` 已改为直接依赖 `codex_config_state::first_layer_config_error` 和 `codex_config_diagnostics::io_error_from_config_error`，不再通过 full config root facade 取错误包装 helper。
    - `rtk cargo fmt -p codex-config-state -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-state -p codex-config -p codex-core`：通过，验证 state diagnostic helper 下沉、旧 config re-export 和 core effective config 错误定位路径编译；仅打印既有 core unused warning。
    - `rtk cargo test -p codex-config-state`：通过，6 个 tests 通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-config-state --depth 1 --edges normal`：direct deps 为 `codex-config-diagnostics`、`codex-config-edit`、`codex-config-requirements`、`codex-config-types`、`codex-utils-absolute-path`、`serde`、`serde_json`、`tokio`、`toml` 和 `tracing`；新增 tokio/tracing 只服务 first-layer 文件定位，不拉 full config loader/model-provider/app-server protocol/runtime implementation。
    - `rtk cargo tree -p codex-config-state --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，state diagnostic 下沉未间接拉回 full config、app-server protocol、Starlark、Rama 或 code-mode runtime。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - 搜索 `codex_config::first_layer_config_error` / `codex_config::io_error_from_config_error` / `codex_config::CONFIG_TOML_FILE` 于 core/config 生产源码：生产旧 diagnostics/root facade 调用已清理；剩余 `codex_config::CONFIG_TOML_FILE` 仅在 core tests。
    - `AGENTS.md` 已记录 first-layer diagnostic 定位 helper 属于 `codex-config-state`，纯 TOML diagnostic helper 属于 `codex-config-diagnostics`。
    - core tests 中的 `codex_config::CONFIG_TOML_FILE` 旧常量路径已收窄到 `codex-config-edit::CONFIG_TOML_FILE`；未顺手重构仍需要 full config fixture 类型的测试 import，避免扩大测试 fixture churn。
    - `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-core`：通过；仅打印既有 core unused/dead_code warnings。
    - 搜索 `codex_config::CONFIG_TOML_FILE` / `use codex_config::CONFIG_TOML_FILE` 于 `codex-rs/core/src`：无结果。
    - `rtk git diff --check`：通过。
    - `ConfigLockfileToml` 外层 DTO 已下沉到 `codex-config-types`，定义为 generic `ConfigLockfileToml<TConfig>`；`codex_config::config_toml::ConfigLockfileToml` 保留为绑定 `ConfigToml` 的旧路径 alias。core config lock 生产路径改为直接依赖 `codex_config_types::ConfigLockfileToml<ConfigToml>`，清理 `codex_config::config_toml::ConfigLockfileToml` 旧 import。注意 lock replay 仍真实依赖 full `ConfigToml`，本切片只迁移外层 lockfile metadata/shape owner，不伪装移除 full effective config 依赖。
    - 顺手修复 config lock materialization 的 feature state bug：`features.multi_agent_v2.enabled` 现在写入 `config.features.enabled(Feature::MultiAgentV2)`，不再无条件强制 `true`，避免 disabled feature 被 lockfile replay 错误打开。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core`：通过，验证 generic lockfile DTO、旧 config alias 和 core 使用路径编译；仅打印既有 core unused warning。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo test -p codex-core config_lock --lib`：通过，7 个 tests 通过，覆盖 generic lockfile DTO serde/round-trip 使用和 multi_agent_v2 feature state 修正。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仍为 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml` 和 `ts-rs`，未新增 heavy dependency。
    - `rtk cargo tree -p codex-config-types --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，lockfile DTO 下沉未间接拉回 full config、app-server protocol、Starlark、Rama 或 code-mode runtime。
    - 搜索 `codex_config::config_toml::ConfigLockfileToml` / `use codex_config::config_toml::ConfigLockfileToml` 于仓库 Rust 源码：无结果，旧路径只保留为兼容 alias。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - `AGENTS.md` 已记录 `ConfigLockfileToml<TConfig>` 属于 `codex-config-types`，`codex_config::config_toml` 只做旧路径兼容 alias。
    - `rtk git diff --check`：通过。
    - `ConfigEditsBuilder` 和 `load_global_mcp_servers` 的 full config 旧路径已下沉到 `codex-config-edit`，`codex-config` 继续 re-export 以保持兼容。`core/src/mcp_skill_dependencies.rs` 生产路径已改为直接依赖 `codex_config_edit::*`，不再为了全局 MCP server edit/load helper 经过 full `codex-config`。
    - `rtk cargo fmt -p codex-config-edit -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-edit -p codex-config -p codex-core`：通过，验证 MCP edit helper 下沉、旧 config re-export 和 core 调用路径编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-config-edit`：通过，11 个 tests 通过，覆盖 plugin/marketplace edit 以及迁移后的 MCP server 序列化/加载。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warning。
    - `rtk cargo tree -p codex-config-edit --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-utils-path`、`serde_json`、`sha2`、`tokio`、`toml`、`toml_edit`，不拉 full config loader/model-provider/protocol/app-server。
    - `rtk cargo tree -p codex-config-edit --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，MCP edit helper 下沉未间接拉回 full config、app-server protocol 或 runtime implementation。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - `AGENTS.md` 已记录 MCP server edit/load helper 属于 `codex-config-edit`，旧 `codex_config::*` 只作为兼容 re-export。
    - `RealtimeWsMode` 已从 full `codex-config::config_toml` 下沉到 `codex-config-types`，旧路径继续 re-export 以保持兼容。`core/src/realtime_conversation.rs` 生产路径已改为直接依赖 `codex_config_types::RealtimeWsMode`，`RealtimeWsVersion` 直接使用 `codex_protocol::protocol::RealtimeConversationVersion`，避免为了 realtime websocket mode 拉 full config。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core`：通过，验证 realtime mode 下沉和旧 config re-export 编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warning。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 未变化，仍只有 absolute-path、schemars、serde、serde_json、thiserror、toml、ts-rs；未为 realtime mode 引入 protocol 或 runtime implementation。
    - `rtk cargo tree -p codex-config-types --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，realtime mode 下沉未间接拉回 heavy implementation。
    - `rtk rg -n "use codex_config::config_toml::RealtimeWsMode|codex_config::config_toml::RealtimeWsMode" codex-rs/core/src -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：无结果，core production 不再通过 full config 取该 enum。
    - `AGENTS.md` 已记录 Realtime transport/mode 属于 `codex-config-types`。
    - `rtk git diff --check`：通过。
    - `RealtimeAudioConfig` 已从 full `codex-config::config_toml` 下沉到 `codex-config-types`，旧路径继续 re-export 以保持兼容。`codex_core::config` 继续公开该类型，但来源改为 `codex_config_types::RealtimeAudioConfig`；core tests 中最后一个旧 full config import 也改为直接依赖 config-types。
    - `ProjectConfig` 已从 full `codex-config::config_toml` 下沉到 `codex-config-loader` 的 project trust 边界，旧 `codex_config::config_toml::ProjectConfig` 路径继续 re-export。它依赖 `codex_protocol::config_types::TrustLevel`，因此不进入无 protocol 依赖的 `codex-config-types`；core config facade 和 core tests 改为直接使用 `codex_config_loader::ProjectConfig`。`RealtimeConfig` / `RealtimeToml` 仍依赖 realtime protocol voice/version，暂不作为本轮小切片。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core -p codex-core-api -p codex-tui`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core -p codex-core-api -p codex-tui`：通过，验证 resolved realtime audio config 下沉、旧 config re-export、core/core-api facade 和 TUI 使用路径编译。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo check -p codex-core`：通过；仅打印既有 core unused/dead_code warnings。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 未变化，仍只有 absolute-path、schemars、serde、serde_json、thiserror、toml、ts-rs。
    - `rtk cargo tree -p codex-config-types --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，RealtimeAudioConfig 下沉未间接拉回 full config、app-server protocol 或 runtime implementation。
    - `rtk cargo fmt -p codex-config-loader -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-loader -p codex-config -p codex-core`：通过，验证 `ProjectConfig` 下沉、旧 config re-export 和 core 使用路径编译；仅打印既有 core unused import warning。
    - `rtk cargo test -p codex-config-loader`：通过，1 个 test suite 通过。
    - `rtk cargo tree -p codex-config-loader --depth 1 --edges normal`：direct deps 为 `codex-model-provider-info`、`codex-protocol`、`codex-utils-absolute-path`、`dunce`、`schemars`、`serde`、`thiserror` 和 `toml`；新增 `schemars`/`serde` 只服务 project trust TOML shape。
    - `rtk cargo tree -p codex-config-loader --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，project trust shape 下沉未间接拉回 full config、app-server protocol、Starlark、Rama 或 code-mode runtime。
    - `rtk rg -n "codex_config::config_toml::ProjectConfig|use codex_config::config_toml::ProjectConfig|crate::config_toml::ProjectConfig" codex-rs -g '*.rs'`：无结果，仓内 Rust 源码不再通过旧 full config 路径获取 `ProjectConfig`。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；本切片没有扩大 full config consumer 集合。
    - `rtk git diff --check`：通过。
    - 新增 `codex-config-toml`，迁移 `ConfigToml`、`ConfigProfile`、`ProfileTui`、`ToolsToml`、`RealtimeConfig` / `RealtimeToml` / `RealtimeAudioToml`、`DebugToml`、`AutoReviewToml`、`GhostSnapshotToml`、config schema helper，以及外层 TOML shape 专用的 `AnalyticsConfigToml`、`FeedbackConfigToml`、`Tui`、`ShellEnvironmentPolicyToml`。`codex-config` 的 `config_toml`、`profile_toml`、`schema` 和相关 `types` 只保留兼容 re-export。
    - `core` 和 `app-server` 生产路径已改为直接依赖 `codex-config-toml` 获取 `ConfigToml` / `ConfigProfile` / schema helper；测试保留部分 `codex_config::config_toml` 旧路径以覆盖 facade 兼容。full `codex-config` 仍只剩 core/app-server 两个组合/runtime 根依赖，原因是 local `load_config_layers_state`、diagnostics/edit 和 effective `Config` 构造尚未拆完。
    - `rtk cargo fmt -p codex-config-toml -p codex-config -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-toml -p codex-config -p codex-core`：通过，验证新 TOML shape crate、旧 config facade 和 core 生产 imports 编译；仅打印既有 core unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 组合根直接依赖 `codex-config-toml` 后仍能编译；仅打印既有 file-subscription/core warnings。
    - `rtk cargo test -p codex-config-toml`：通过，3 个 tests 通过。
    - `rtk cargo test -p codex-config`：通过，46 个 tests 通过，覆盖旧 facade/re-export 兼容。
    - `rtk cargo tree -p codex-config-toml --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-auth-types`、`codex-config-loader`、`codex-config-permissions`、`codex-config-types`、`codex-features`、`codex-model-provider-info`、`codex-protocol`、absolute/path utils、`schemars`、`serde`、`serde_json`、`toml`、`tracing`；不依赖 full `codex-config`。
    - `rtk cargo tree -p codex-config-toml --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert codex-execpolicy`、`--invert codex-network-proxy`：均返回 package not in graph，`ConfigToml` shape 下沉未间接拉回 full config、app-server protocol、code-mode runtime、Starlark、Rama、execpolicy implementation 或 network-proxy implementation。
    - `rtk rg -n "codex_config::(config_toml|profile_toml|schema)::" codex-rs/core/src codex-rs/app-server/src -g '*.rs'`：剩余命中均为 core 测试/旧路径兼容；app-server 生产路径无旧 full config TOML/schema import。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；TOML shape 下沉没有扩大 heavy config consumer 集合。
    - `AGENTS.md` 已记录 `codex-config-toml` 的 owner 边界、允许依赖和禁止回拉 full config/app-server-protocol/runtime implementation 的约束。
    - 已为 `codex-config-toml` 补充 `BUILD.bazel`。本切片没有新增第三方 Rust dependency，`MODULE.bazel.lock` 暂无需要更新的外部依赖内容。
    - `rtk git diff --check`：通过。
    - 新增并接入 `codex-config-local-loader`，迁移 local filesystem config layer loader：system/user/profile/project/repo/runtime layer IO、strict TOML validation、legacy managed config 到 requirements 的映射、thread config source 投影、project trust/root/git checkout 处理、relative path resolution、system config/requirements path helper。`codex_config::loader::*` 现在只保留兼容 re-export；旧 `codex-rs/config/src/loader/*`、`host_name.rs`、`strict_config.rs`、`thread_config.rs` 重复实现已删除。
    - `app-server`、`core` 和 `core-plugins` test support 的 `load_config_layers_state` / relative path resolution 生产或测试 helper 已改为直接依赖 `codex-config-local-loader`。`codex-app-server` 的 full `codex-config` 已从 normal dependency 降为 dev-dependency；`codex-core` 生产源码精确搜索无 `codex_config::` / `use codex_config` 后，也把 full `codex-config` 降为 dev-dependency。
    - `rtk cargo fmt -p codex-config-local-loader -p codex-config -p codex-core -p codex-app-server -p codex-core-plugins`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-local-loader -p codex-config -p codex-core`：通过；清理旧 loader 后 `codex-config` 不再新增 diagnostics unused warning，剩余为既有 core warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 只经 new local-loader/config 子 crate 和 core 编译；仅打印既有 file-subscription/core/app-server warning。
    - `rtk cargo test -p codex-config-local-loader`：通过，13 个 tests 通过；迁移 strict config tests 到新 crate 后修正 test-only import，并补 `anyhow` dev-dependency。
    - `rtk cargo test -p codex-config`：通过，33 个 tests 通过，验证旧 facade/re-export 兼容。
    - `rtk cargo test -p codex-core-plugins`：通过，验证 test support 改用 local-loader 后插件测试仍编译运行。
    - `rtk cargo test -p codex-core config_loader --lib`：通过，验证 core 把 full `codex-config` 降到 dev-dependency 后 config loader 相关测试仍能编译运行。
    - `rtk cargo tree -p codex-config-local-loader --depth 1 --edges normal`：direct deps 为 config diagnostics/edit/loader/requirements/state/toml/types、features、file-system、git-utils、model-provider-info、protocol、absolute-path、base64、dunce、gethostname、tokio、toml、tracing 和平台依赖；不直接依赖 full `codex-config`。
    - `rtk sh -lc 'cargo tree -p codex-config-local-loader --edges normal | rg "codex-config v|codex-app-server|codex-code-mode|codex-execpolicy v|codex-network-proxy v|codex-core v|starlark|rama"'`：无匹配（rg exit 1），local-loader normal graph 未间接拉回 full config、app-server、code-mode、execpolicy/network-proxy implementation、core、Starlark 或 Rama。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：只显示 `codex-config` 根节点，workspace normal graph 已无 full `codex-config` dependent；`codex-config` 目前只作为旧路径兼容 facade 和测试/dev dependency 使用。
    - `rtk sh -lc 'cargo tree -p codex-app-server --edges normal --depth 1 | rg "codex-config( |$|-local-loader)"'`：app-server 一层 normal deps 只显示 `codex-config-local-loader`，不再显示 full `codex-config`。
    - `rtk sh -lc 'cargo tree -p codex-core --edges normal --depth 1 | rg "codex-config( |$|-local-loader|-types|-toml|-state|-loader|-requirements|-permissions|-edit|-diagnostics)"'`：core 一层 normal deps 显示拆分后的 config 子 crate，不再显示 full `codex-config`。
    - `codex-config` facade manifest 已收缩：normal deps 仅保留 diagnostics/edit/loader/local-loader/permissions/requirements/state/toml/types、protocol、absolute-path 和 toml；旧 loader 平台依赖、file-system/git-utils/model-provider-info/features、network-proxy-api、serde/serde_ignored/toml_edit 等不再作为 facade 直接依赖。`rtk cargo check -p codex-config`：通过；`rtk cargo test -p codex-config`：通过，33 个 tests 通过。
    - `rtk cargo tree -p codex-config --depth 1 --edges normal`：direct deps 显示 facade owner crates 和 `toml`，不再显示旧 loader implementation 依赖。
    - `AGENTS.md` 已记录 `codex-config-local-loader` 的 owner 边界、允许依赖和禁止回拉 full config/app-server-protocol/runtime implementation 的约束，并明确 effective runtime `Config` 构造不得移入 local-loader。
    - `rtk git diff --check`：通过。
    - 搜索 `codex_config::config_toml::RealtimeAudioConfig` / `use codex_config::config_toml::RealtimeAudioConfig` 于仓库 Rust 源码：无结果，旧路径只保留为兼容 re-export。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - `AGENTS.md` 已记录 `RealtimeAudioConfig` 属于 `codex-config-types`。
    - `rtk git diff --check`：通过。
    - 新增 `codex-config-permissions`，迁移 permissions profile TOML、filesystem/network permissions TOML、profile network 到 `NetworkProxyConfig` 的 projection helper 和 network overlay helper；`codex-config` 的 `permissions_toml` 只保留 re-export。该 crate 明确依赖 `codex-protocol` 和 `codex-network-proxy-api`，避免把这两个依赖塞进通用 `codex-config-types`。
    - `core/src/config/permissions.rs` 和 `core/src/network_proxy_loader.rs` 生产路径已改为直接依赖 `codex_config_permissions::*`，不再为了 permissions TOML/profile 从 full `codex-config::permissions_toml` 取类型或 helper。
    - `rtk cargo fmt -p codex-config-permissions -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-permissions -p codex-config -p codex-core`：通过，验证新 permissions crate、旧 config re-export 和 core consumer 编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-core permissions --lib`：通过，覆盖 permissions profile 编译/解析/应用相关单测。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warning。
    - `rtk cargo tree -p codex-config-permissions --depth 1 --edges normal`：direct deps 为 `codex-network-proxy-api`、`codex-protocol`、`schemars`、`serde`，不依赖 full config 或 runtime implementation。
    - `rtk cargo tree -p codex-config-permissions --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert codex-network-proxy`、`--invert starlark`、`--invert rama-http`：均返回 package not in graph，permissions 切片未间接拉回 full config、app-server protocol、code-mode runtime、network-proxy implementation、Starlark 或 Rama。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`。
    - `rtk rg -n "codex_config::permissions_toml|use codex_config::permissions_toml" codex-rs/core/src codex-rs/app-server/src -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：无结果，core/app-server production 不再通过 full config 取 permissions TOML。
    - `AGENTS.md` 已记录 permissions profile/TOML/overlay helper 属于 `codex-config-permissions`，旧 `codex_config::permissions_toml` 只作为兼容 re-export。
    - 已为 `codex-config-permissions` 补充 `BUILD.bazel`。本切片没有新增第三方 Rust dependency，`MODULE.bazel.lock` 暂无需要更新的外部依赖内容。
    - `rtk git diff --check`：通过。
    - `AgentsToml`、`AgentRoleToml` 和 `ThreadStoreToml` 已从 full `codex-config::config_toml` 下沉到 `codex-config-types`，旧路径继续 re-export。`core/src/config/agent_roles.rs` 和 `core/src/config/mod.rs` 生产路径已改为直接依赖 `codex_config_types::*`，不再为了 agent role declaration/thread store DTO 经过 full config。
    - 重跑 `rtk cargo test -p codex-core agent_role --lib` 时暴露两个既有 agent role 红测试：markdown frontmatter description 没有应用长度上限、plugin role merge 会把缺 description 的 hidden role 插入。已修复为 markdown parser 使用长度校验，plugin merge 在不会再发生 layer inheritance 的边界过滤缺 description 的 role；保留普通 discovery 允许高优先级 role 从低优先级 layer 继承 description 的时序。
    - `rtk cargo fmt -p codex-config-types -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-config-types -p codex-config -p codex-core`：通过，验证 agent/thread-store DTO 下沉和旧 config re-export 编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-config-types`：通过，21 个 tests 通过。
    - `rtk cargo test -p codex-core agent_role --lib`：通过，25 个 filtered tests 通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warning。
    - `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 未变化，仍只有 absolute-path、schemars、serde、serde_json、thiserror、toml、ts-rs。
    - `rtk cargo tree -p codex-config-types --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert starlark`、`--invert rama-http`、`--invert codex-code-mode`：均返回 package not in graph，agent/thread-store DTO 下沉未间接拉回 heavy implementation。
    - `rtk rg -n "use codex_config::config_toml::(AgentRoleToml|AgentsToml|ThreadStoreToml)|codex_config::config_toml::(AgentRoleToml|AgentsToml|ThreadStoreToml)" codex-rs/core/src -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：无结果，core production 不再通过 full config 取这些 DTO。
    - `AGENTS.md` 已记录 agent role declaration DTO 和 `ThreadStoreToml` 属于 `codex-config-types`，并记录 agent role description 校验不能放在通用 discovery 阶段的继承时序约束。
    - `ModelOptionToml`、`validate_model_providers`、`validate_reserved_model_provider_ids`、`deserialize_model_providers` 和 `validate_oss_provider` 已从 full `codex-config::config_toml` 下沉到 `codex-model-provider-info`，旧 `codex_config::config_toml::*` 路径继续 re-export 以保持兼容。`core/src/config/mod.rs` 生产路径已改为直接依赖 `codex_model_provider_info::*`，不再为了 model option/provider validation 通过 full config 旧 re-export。
    - 本切片顺带修复 `codex-config` 测试模块缺失 `AbsolutePathBuf` 显式 import；这是此前 DTO 下沉后 `super::*` 不再提供该类型暴露出的 test-only 编译缺口。
    - `rtk cargo fmt -p codex-model-provider-info -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-model-provider-info -p codex-config -p codex-core`：通过，验证 model-provider owner 迁移、旧 config re-export 和 core 使用路径编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-model-provider-info`：通过，22 个 tests 通过。
    - `rtk cargo test -p codex-config forced_chatgpt_workspace_id --lib`：通过，3 个 tests 通过，46 个 filtered。
    - `rtk cargo test -p codex-core model_provider --lib`：通过，model provider 相关 filtered tests 通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warning。
    - `rtk cargo tree -p codex-model-provider-info --depth 1 --edges normal`：direct deps 为 `codex-api`、`codex-auth-types`、`codex-protocol`、`http`、`schemars`、`serde`、`serde_json`，未新增 full config、app-server protocol、code-mode、Starlark 或 Rama direct edge。
    - `rtk cargo tree -p codex-model-provider-info --invert codex-config --edges normal --depth 3`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`：Cargo 返回 package ID 不在该 package graph 中，说明 model-provider helper 下沉未间接拉回这些 heavy crates。
    - `rtk rg -n "use codex_config::config_toml::(ModelOptionToml|validate_model_providers|validate_oss_provider)|codex_config::config_toml::(validate_model_providers|validate_oss_provider)" codex-rs -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：无结果，生产代码不再通过 full config 旧路径获取 model-provider helper。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；本切片没有扩大 full config consumer 集合。
    - `AGENTS.md` 已记录 model provider domain TOML/helper 属于 `codex-model-provider-info`，旧 `codex_config::config_toml` 只作为兼容 re-export。
    - 当前继续清理 model-provider info 轻量边界：`ModelProviderInfo::to_api_provider()` 和 HTTP header map 构造已迁移到 runtime `codex-model-provider::model_provider_info_to_api_provider()`，`codex-model-provider-info` 只保留 DTO/catalog/validation、env API key helper 和 Azure remote-compaction 轻量判断。
    - `models_endpoint`、Amazon Bedrock provider 和 realtime conversation 的 API provider 构造已改为调用 runtime adapter；Bedrock provider header 测试改为验证 info DTO 的内置 header，而不是在 info crate 内构造 API client provider。
    - `rtk cargo fmt -p codex-model-provider-info -p codex-model-provider -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-model-provider-info -p codex-model-provider -p codex-config-loader -p codex-config-toml -p codex-config-local-loader -p codex-core`：通过；仅打印既有 core `MIN_YIELD_TIME_MS` unused warning。
    - `rtk cargo test -p codex-model-provider-info`：通过，22 个 tests 通过。
    - `rtk cargo test -p codex-model-provider`：通过，验证 runtime adapter 所在 crate 和 Bedrock runtime provider。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过；仅打印既有 file-subscription dead_code/unused 和 core `MIN_YIELD_TIME_MS` warning。
    - `rtk cargo tree -p codex-model-provider-info --depth 1 --edges normal`：direct deps 已收缩为 `codex-protocol`、`schemars`、`serde`、`serde_json`，不再包含 `codex-api`、`codex-auth-types` 或 `http`。
    - `rtk cargo tree -p codex-model-provider-info --invert codex-api --edges normal --depth 3`：Cargo 返回 package ID 不在该 package graph 中，说明 info crate normal graph 不再拉 API client crate。
    - `rtk cargo tree -p codex-config-loader --edges normal --depth 3`、`rtk cargo tree -p codex-config-toml --edges normal --depth 3`、`rtk cargo tree -p codex-config-local-loader --edges normal --depth 3`：均未显示 `codex-api`，config-facing crate 不再通过 provider info 拉入 API client 栈；但仍可见 `codex-protocol -> reqwest` 等现存 protocol 重依赖，需后续单独拆。
    - `AGENTS.md` 已补充 `codex-model-provider-info` 不得依赖 `codex-api`/`http`/client stack，API provider adapter 属于 `codex-model-provider` runtime 边界。
    - 继续清理 `codex-protocol` 偏功能依赖：`num_format` 已从 ICU/sys-locale locale formatter 改为固定逗号分隔实现，保留 `format_with_separators` / `format_si_suffix` public API 和既有 en-US 输出形状；新增负数和 `i64::MIN` separator 覆盖。
    - `rtk cargo fmt -p codex-protocol`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-protocol num_format --lib`：通过，覆盖 SI suffix 和 separator helper。
    - `rtk cargo check -p codex-protocol -p codex-config-loader -p codex-config-toml -p codex-config-local-loader -p codex-core`：通过，验证 protocol 和 config-facing/core 消费路径。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 组合根仍能编译。
    - `rtk cargo test -p codex-protocol`：通过，223 个 tests 通过。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `icu_decimal`、`icu_locale_core`、`icu_provider` 或 `sys-locale`；仍保留 chardetng/encoding、image utils、quick-xml、reqwest 等后续待拆偏功能依赖。
    - `AGENTS.md` 已记录 protocol 基础格式化不得重新引入 ICU/sys-locale；locale-aware UI 展示如有需要应放在 UI formatting 边界。
    - 继续清理 `codex-protocol -> reqwest`：`ConnectionFailedError` 和 `ResponseStreamFailed` 保留 public error 类型，但字段从 `reqwest::Error` 改为 `message: String` + `status: Option<http::StatusCode>`；`UnexpectedResponseError` / retry status 统一使用轻量 `http::StatusCode`。当前仓内没有生产构造点需要保存 reqwest source，API bridge 已经通过 `codex-client::TransportError` 映射成 protocol error。
    - `rtk cargo fmt -p codex-protocol`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-protocol error --lib`：通过，覆盖 error event/status 投影。
    - `rtk cargo check -p codex-protocol -p codex-api -p codex-config-loader -p codex-config-toml -p codex-config-local-loader -p codex-core`：通过，验证 protocol、API adapter、config-facing 和 core 消费路径。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过；仅打印既有 file-subscription warning。
    - `rtk cargo test -p codex-protocol`：通过，223 个 tests 通过。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `reqwest`，新增/保留轻量 `http`；protocol 剩余偏功能依赖为 chardetng/encoding、image utils、quick-xml 等后续候选。
    - `AGENTS.md` 已记录 protocol error DTO 不得重新保存 `reqwest::Error`，HTTP 语义使用 `http::StatusCode` + message/request id，由 API/runtime adapter 映射。
    - 继续清理 `codex-protocol -> quick-xml`：hook prompt marker 序列化/解析从 `quick_xml` serde 改为受控 `<hook_prompt hook_run_id="...">...</hook_prompt>` escape/unescape helper，保留 legacy marker parse 和 entity roundtrip 行为。
    - `rtk cargo fmt -p codex-protocol`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-protocol hook_prompt --lib`：通过，覆盖 hook prompt roundtrip 和 legacy parse。
    - `rtk cargo check -p codex-protocol -p codex-config-loader -p codex-config-toml -p codex-config-local-loader -p codex-core`：通过，验证 protocol 和 config-facing/core 消费路径。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过；仅打印既有 file-subscription warning。
    - `rtk cargo test -p codex-protocol`：通过，223 个 tests 通过。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `quick-xml`；protocol 剩余偏功能依赖主要为 chardetng/encoding、image utils、globset/wildmatch 等后续候选。
    - `AGENTS.md` 已记录 hook prompt marker 不得为了受控内部 XML 重新拉 quick-xml；需要通用 XML parser 时必须先证明新的协议边界。
    - `ForcedChatgptWorkspaceIds` 已从 full `codex-config::config_toml` 下沉到 `codex-auth-types`，旧 `codex_config::config_toml::ForcedChatgptWorkspaceIds` 路径继续 re-export。`core/src/config/mod.rs` 生产路径已改为直接依赖 `codex_auth_types::ForcedChatgptWorkspaceIds`，不再为了 ChatGPT workspace login restriction 形状通过 full config 旧 re-export。
    - `rtk cargo fmt -p codex-auth-types -p codex-config -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-auth-types -p codex-config -p codex-core`：通过，验证 auth DTO 下沉、旧 config re-export 和 core 使用路径编译；仅打印既有 core warning。
    - `rtk cargo test -p codex-auth-types`：通过，0 个 tests。
    - `rtk cargo test -p codex-config forced_chatgpt_workspace_id --lib`：通过，3 个 tests 通过，46 个 filtered。
    - `rtk cargo test -p codex-core forced_chatgpt_workspace_id --lib`：通过，覆盖 runtime config 对 workspace id 列表的 trim/empty filtering。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription/core warning。
    - `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 仍只有 `schemars`、`serde`、`strum_macros`、`ts-rs`，未拉 full config、app-server protocol 或 runtime implementation。
    - `rtk rg -n "use codex_config::config_toml::ForcedChatgptWorkspaceIds|codex_config::config_toml::ForcedChatgptWorkspaceIds" codex-rs -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：无结果，生产代码不再通过 full config 旧路径获取该 auth DTO。
    - `rtk cargo tree --workspace --invert codex-config --edges normal --depth 2`：full `codex-config` direct consumers 仍只有 `codex-app-server` 和 `codex-core`；本切片没有扩大 full config consumer 集合。
    - `AGENTS.md` 已记录 `ForcedChatgptWorkspaceIds` 属于 `codex-auth-types`，旧 `codex_config::config_toml` 只作为兼容 re-export。
    - 继续清理 `codex-protocol -> wildmatch`：`EnvironmentVariablePattern` 从 `WildMatchPattern<'*', '?'>` type alias 改为 protocol 内自有轻量 wildcard newtype，保留 `new()`、`new_case_insensitive()`、`matches()`、`Display/to_string()`、`pattern()`、`pattern_chars()` 和 `is_case_insensitive()` API；匹配语义仍为整串匹配，`*` 匹配任意长度、`?` 匹配单字符，连续 `*` 合并，大小写无关比较保留 Unicode lowercase 行为。
    - `rtk cargo fmt -p codex-protocol`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-protocol environment_variable_pattern --lib`：通过，5 个 tests 通过，覆盖 wildcard、`?`、连续 `*`、Unicode 大小写无关和空 pattern。
    - `rtk cargo test -p codex-protocol shell_environment --lib`：通过，1 个当前平台相关 test 通过，验证 shell env policy 调用路径。
    - `rtk cargo check -p codex-protocol -p codex-config-toml -p codex-exec-server -p codex-core`：通过，验证 protocol、TOML adapter、exec-server policy bridge 和 core 消费路径。
    - `rtk cargo test -p codex-core exec_env --lib`：通过，覆盖 core exec env include/exclude/default sensitive filtering 调用面。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `wildmatch`；workspace 级 `wildmatch` 仍由 `codex-config-requirements` 独立使用，未通过 protocol normal graph 传播。
    - `rtk cargo test -p codex-protocol`：通过，228 个 tests 通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过；仅打印既有 file-subscription dead_code/unused warning。
    - `AGENTS.md` 已记录 protocol shell env wildcard 不得为了 include/exclude pattern 重新拉 `wildmatch`/regex/glob runtime。
    - 继续清理 `codex-protocol -> chardetng/encoding_rs`：`StreamOutput` / `ExecToolCallOutput` 轻量 DTO 保留在 protocol，legacy encoding 智能解码 `bytes_to_string_smart` 和 CP1251/CP866/Windows-1252 回归测试迁移到 `codex-command-runtime::output_decoding`；core old exec path、app-server command_exec/process_exec streaming buffer 改为显式调用 command-runtime helper，避免 runtime 行为回退，也避免 protocol 通过 re-export 间接拉回编码检测依赖。
    - `rtk cargo fmt -p codex-command-runtime -p codex-protocol -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-command-runtime output_decoding --lib`：通过，8 个 tests 通过，覆盖 legacy codepage 和 smart punctuation 解码。
    - `rtk cargo check -p codex-command-runtime -p codex-protocol -p codex-core -p codex-app-server --bin codex-app-server`：通过；仅打印既有 file-subscription dead_code/unused warning。
    - `rtk cargo test -p codex-core exec --lib`：失败；过滤词过宽命中了无关 `tools::spec::*unified_exec_web_search` 快照顺序测试，diff 显示 workflow tools/`close_agent` 顺序与期望不一致，非本切片解码路径问题。
    - `rtk cargo test -p codex-core 'exec::tests::' --lib`：通过，69 个 tests 通过、2 个 ignored，验证旧 exec 收尾路径改用 command-runtime smart decoder 后仍通过。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `chardetng` 或 `encoding_rs`；当时剩余偏功能依赖主要为 `codex-utils-image` 和 `globset`。
    - `rtk cargo test -p codex-protocol`：通过，220 个 tests 通过；原 exec_output 编码测试已迁移到 `codex-command-runtime`。
    - `rtk cargo test -p codex-command-runtime`：通过，20 个 tests 通过。
    - `rtk cargo tree -p codex-protocol --invert chardetng --edges normal --depth 3` / `rtk cargo tree -p codex-protocol --invert encoding_rs --edges normal --depth 3`：均返回 package ID 不在 `codex-protocol` package graph 中，说明没有 direct-to-indirect 回流；这两个 tree 命令曾并行触发 Cargo package cache lock 等待，后续 Rust/Cargo 验证继续保持串行。
    - `AGENTS.md` 已记录 command runtime 拥有 legacy output decoding，protocol exec_output 只保留轻量 DTO。
    - 继续清理 `codex-protocol -> globset`：filesystem read-deny matcher runtime 从 protocol permissions DTO 中拆出到新 crate `codex-permissions-runtime`。`ReadDenyMatcher`、glob matcher 构造、normalized/canonical path candidates 和相关 matcher tests 由 runtime crate 拥有；`codex-protocol::permissions` 只保留 `AskForApproval`、`SandboxPolicy`、`ReadOnlyReason`、`WritableRoot`、`read_only_reasons` 等轻量 DTO/helper。
    - `codex-sandboxing` 和 `codex-windows-sandbox` 已改为依赖 `codex-permissions-runtime::ReadDenyMatcher`，避免 sandbox policy translation / Windows deny resolver 为 matcher 逻辑继续经 protocol 拉入 `globset`。
    - `rtk cargo fmt -p codex-permissions-runtime -p codex-protocol -p codex-sandboxing -p codex-windows-sandbox`：通过。
    - `rtk cargo test -p codex-permissions-runtime`：通过，7 个 matcher tests 通过。
    - `rtk cargo check -p codex-protocol -p codex-permissions-runtime -p codex-sandboxing -p codex-windows-sandbox`：通过，验证 protocol DTO、runtime matcher 和 sandbox consumers 编译。
    - `rtk cargo test -p codex-protocol`：通过，protocol tests 通过，确认 matcher tests 迁移后 protocol 仍覆盖自身 DTO/helper 行为。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证组合根仍能编译；仅打印既有 file-subscription dead_code/unused warning。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `globset`；`rtk cargo tree -p codex-protocol --invert globset --edges normal --depth 3` 返回 package ID 不在 `codex-protocol` package graph 中，说明没有 direct-to-indirect 回流。
    - `AGENTS.md` 已记录 filesystem read-deny matcher / globset 属于 `codex-permissions-runtime`，protocol permissions 只保留轻量 DTO。
    - 继续清理 `codex-protocol -> codex-utils-image`：新增窄 crate `codex-model-input`，把 `UserInput -> ResponseInputItem` 的 model input adapter、本地图片读取、resize/encode、LocalImage placeholder 和图片 label 序列从 protocol DTO 层迁出。`codex-protocol` 只保留 Responses API DTO、pre-encoded `UserInput::Image` wrapping、image tag helper 和 `UserInput::LocalImage` 的轻量误用 fallback，不再做文件 IO 或图片处理。
    - core/session 普通 turn、active turn pending input 注入、compact task 和 prompt debug 路径已改为显式调用 `codex_model_input::response_input_item_from_user_input`，保持产品路径中 LocalImage 读取、resize/encode 和错误 placeholder 行为不变。
    - `rtk cargo fmt -p codex-model-input -p codex-protocol -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-model-input`：通过，5 个 tests 通过，覆盖远端 image wrapping、远端/本地图片 label 序列、本地图片读取失败、非图片 MIME 和 unsupported image format placeholder。
    - `rtk cargo test -p codex-protocol`：通过，209 个 tests 通过；本地图片处理测试已随行为迁移到 `codex-model-input`。
    - `rtk cargo check -p codex-model-input -p codex-protocol -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证新 adapter、protocol、core 和 app-server 组合根编译；仅打印既有 file-subscription dead_code/unused warning。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 不再显示 `codex-utils-image`；`rtk cargo tree -p codex-protocol --invert codex-utils-image --edges normal --depth 3` 返回 package ID 不在 `codex-protocol` package graph 中，说明没有 direct-to-indirect 回流。
    - `rtk cargo tree -p codex-model-input --depth 1 --edges normal`：direct deps 仅为 `codex-protocol` 和 `codex-utils-image`，图片处理依赖被限制在 model input adapter 边界。
    - `AGENTS.md` 已记录 LocalImage 文件 IO/resize/placeholder 属于 `codex-model-input`，protocol 只保留 DTO/tag helper。
    - step 5/6 重新盘点 `codex-core --depth 1 --edges normal`：core 仍是组合根，direct deps 同时包含 `codex-app-server-protocol`、`codex-mcp`、`codex-model-provider`、`codex-network-proxy`、`codex-execpolicy`、`codex-rollout`、`codex-rollout-trace`、`codex-thread-store`、`codex-workflow`、`codex-tools` 等。后续必须按功能域抽 service facade，不能再只做单个 direct edge 删除。
    - 生产源码中 `core -> app-server-protocol` 使用点已收缩为两类：MCP elicitation request/schema DTO，以及 `thread_manager` 的 `ThreadHistoryBuilder`/`TurnStatus` display history builder。MCP elicitation 不能只搬 schema 子类型；`McpServerElicitationRequestParams` 带 thread/turn/server correlation，当前 core 直接构造 app-server server-request payload，正确方向是 core domain event/plan + app-server 边界投影。`ThreadHistoryBuilder`/`TurnStatus` 则紧耦合 v2 display projection，风险更高，不能作为第一个小切片硬拆。
    - core 内剩余图片依赖主要在 `view_image` tool 和 context manager original image token estimate；`view_image` 仍是真实 runtime handler 边界，但 original image token estimate 只需要从内存图片解尺寸，已改为调用 `codex-utils-image::dimensions_from_memory`，避免 core 生产代码直接依赖 `image` crate API。
    - `ImageProcessingError` 新增 `DecodeMemory` 以表达内存图片解码失败；`codex-model-input` 的 exhaustive error handling 已补齐该 variant，保持 invalid image placeholder 与普通 local image error 的既有分流。
    - `image` 已从 `codex-core` normal dependency 移到 dev-dependency；core 测试继续用 `image` 生成 fixture，生产路径通过 `codex-utils-image` 访问图片处理能力。
    - `rtk cargo fmt -p codex-utils-image -p codex-model-input -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-utils-image`：通过，7 个 tests 通过，覆盖新增 `dimensions_from_memory` helper。
    - `rtk cargo test -p codex-model-input`：通过，5 个 tests 通过，验证新增 error variant 后 model input placeholder 分流仍编译通过。
    - `rtk cargo check -p codex-core`：通过，验证 core 生产路径不再需要 direct `image` dependency；仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo test -p codex-core original_detail_images_scale_with_dimensions --lib`：通过，覆盖 original-detail image token estimate 仍按图片尺寸缩放。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 组合入口仍能编译；仅打印既有 file-subscription/core unused/dead_code warnings。
    - `rtk sh -lc 'rtk cargo tree -p codex-core --depth 1 --edges normal | rg -q "^[├└]── image v" && exit 1 || exit 0'`：通过，确认 `codex-core` depth-1 normal graph 不再直接包含 `image`。
    - 继续收缩 `core -> app-server-protocol`：`thread_manager::snapshot_turn_state` 不再使用 `ThreadHistoryBuilder`/`TurnStatus`。fork snapshot 只需要判断 rollout 是否截在显式 turn 中间，现改为直接扫描 typed `RolloutItem::EventMsg(EventMsg::TurnStarted | TurnComplete | TurnAborted)`，保留 legacy/synthetic history 的“最后一个 user message 后无 TurnComplete/TurnAborted 则视为 mid-turn”fallback。
    - `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-core thread_manager --lib`：失败；过滤词过宽命中既有 `resume_and_fork_do_not_restore_thread_environments_from_rollout` 栈溢出路径，非本切片断言失败。
    - `rtk cargo test -p codex-core out_of_range_truncation_drops_pre_user_active_turn_prefix --lib`：通过，覆盖显式 `TurnStarted` 的 active turn start index 和 out-of-range fork truncation。
    - `rtk cargo test -p codex-core interrupted_fork_snapshot_preserves_explicit_turn_id --lib`：通过，覆盖 interrupted fork 追加 `TurnAborted` 时保留显式 turn id。
    - `rtk rg -n "codex_app_server_protocol::|use codex_app_server_protocol|ThreadHistoryBuilder|TurnStatus" codex-rs/core/src -g '*.rs' -g '!**/*tests.rs' -g '!**/tests.rs' -g '!**/tests/**'`：生产源码中不再出现 `ThreadHistoryBuilder` 或 `TurnStatus`，剩余 app-server-protocol 使用只在 MCP elicitation request/schema DTO。
    - `rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 core/app-server 组合路径编译；仅打印既有 file-subscription/core dead_code/unused warning。
    - `AGENTS.md` 已记录 core fork/resume snapshot turn-state 判断不得依赖 app-server display history builder，必须扫描 typed turn lifecycle `EventMsg`。
    - `rtk git diff --check`：通过。
    - 继续收缩 `core -> app-server-protocol`：新增 `codex-mcp-types`，迁移 `McpServerElicitationRequestParams`、`McpServerElicitationRequest`、`McpElicitationSchema` 及 schema 子类型；`codex-app-server-protocol` 只 re-export request/schema DTO 并保留 response wrapper 与 `rmcp` conversion，core/session/tool handlers 改为直接依赖 `codex-mcp-types`。`rtk rg -n "codex_app_server_protocol" codex-rs/core -g '*.rs' -g 'Cargo.toml'` 已无结果。
    - `rtk cargo fmt -p codex-mcp-types -p codex-app-server-protocol -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-mcp-types -p codex-app-server-protocol -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 MCP types、protocol re-export、core consumer 和 app-server 入口编译。
    - `rtk cargo tree -p codex-core --invert codex-app-server-protocol --edges normal --depth 3`：MCP/direct core path 已消失；当时剩余 indirect path 为 analytics、core-plugins/core-skills、exec-server/apply-patch/codex-mcp/rmcp-client/utils-plugins。
    - 继续收缩 plugin/skill indirect 回流：新增 `codex-plugin-types`，迁移 `PluginInstallPolicy`、`PluginAuthPolicy`、`PluginAvailability`、`PluginInterface`、`SkillInterface`；`codex-app-server-protocol` 只 re-export 用于 wire/schema 兼容，`codex-core-plugins` remote/marketplace/share checkout 直接依赖 plugin types。remote plugin id 校验改为 core-plugins 领域错误，app-server processor 在 JSON-RPC 边界映射成 `JSONRPCErrorError`。
    - 新增 `codex_file_system::LOCAL_FS` unsandboxed local filesystem implementation；`codex-utils-plugins`、`codex-core-skills`、`codex-core-plugins` 改为直接依赖 `codex-file-system`，不再为了 `ExecutorFileSystem` trait 或 local loader FS 拉 `codex-exec-server`。
    - `codex-core-skills` 的 explicit skill injection 不再直接调用 `AnalyticsEventsClient`，改为返回领域 `SkillInvocation`；core turn 将其转换为 analytics invocation 并继续更新 thread skill state。`codex-core-plugins` 新增 `PluginAnalyticsEventSink`，app-server 组合根用 wrapper 转发到 `AnalyticsEventsClient`；plugin/skill core crate 不再直接依赖 `codex-analytics`。
    - `rtk cargo fmt -p codex-file-system -p codex-utils-plugins -p codex-core-skills -p codex-core-plugins -p codex-plugin-types -p codex-app-server-protocol -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-file-system -p codex-utils-plugins -p codex-core-skills -p codex-core-plugins -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 local FS、plugin/skill analytics boundary、core turn caller 和 app-server wrapper 编译；仅打印既有 warnings。
    - `rtk cargo tree -p codex-utils-plugins --invert codex-exec-server --edges normal --depth 3`、`rtk cargo tree -p codex-core-skills --invert codex-exec-server --edges normal --depth 3`、`rtk cargo tree -p codex-core-plugins --invert codex-exec-server --edges normal --depth 3`：均返回 package ID 不在 graph 中。
    - `rtk cargo tree -p codex-core-skills --invert codex-app-server-protocol --edges normal --depth 4` 和 `rtk cargo tree -p codex-core-plugins --invert codex-app-server-protocol --edges normal --depth 4`：均返回 package ID 不在 graph 中。
    - `rtk cargo tree -p codex-core-skills --invert codex-analytics --edges normal --depth 4` 和 `rtk cargo tree -p codex-core-plugins --invert codex-analytics --edges normal --depth 4`：均返回 package ID 不在 graph 中。
    - 最新 `rtk cargo tree -p codex-core --invert codex-app-server-protocol --edges normal --depth 4`：只剩两类回流路径：`codex-app-server-protocol -> codex-analytics -> codex-core`，以及 `codex-app-server-protocol -> codex-exec-server -> codex-core` / `codex-apply-patch` / `codex-mcp` / `codex-rmcp-client`。`core-plugins`、`core-skills`、`utils-plugins` 路径已消失。
    - 继续收缩 `exec-server -> app-server-protocol`：新增 `codex-jsonrpc-types`，迁移 `JSONRPC_VERSION`、`RequestId`、JSON-RPC request/notification/response/error message envelope 和 `JSONRPCErrorError`；`codex-app-server-protocol::jsonrpc_lite` 只做 re-export，`codex-exec-server` 改为直接依赖 `codex-jsonrpc-types`。
    - `rtk cargo fmt -p codex-jsonrpc-types -p codex-app-server-protocol -p codex-exec-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-jsonrpc-types -p codex-app-server-protocol -p codex-exec-server -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 JSON-RPC types、protocol re-export、exec-server consumer、core 和 app-server 入口编译。
    - `rtk sh -lc 'cargo tree -p codex-exec-server --edges normal | rg -q "codex-app-server-protocol" && { echo "unexpected: codex-app-server-protocol present in codex-exec-server graph"; exit 1; } || { echo "ok: codex-app-server-protocol absent from codex-exec-server graph"; exit 0; }'`：通过，确认 `codex-exec-server` normal graph 不含 `codex-app-server-protocol`。
    - 最新 `rtk cargo tree -p codex-core --invert codex-app-server-protocol --edges normal --depth 4`：只剩 `codex-app-server-protocol -> codex-analytics -> codex-core`，`exec-server` 回流路径已消失。
    - `rtk cargo tree -p codex-jsonrpc-types --depth 1 --edges normal`：direct normal deps 仅为 `codex-protocol`、`schemars`、`serde`、`serde_json`、`ts-rs`。
    - 继续收缩 `core -> analytics -> app-server-protocol`：新增 `codex-analytics-api`，承载 core/runtime 需要的 analytics DTO、guardian/compaction/turn facts 和 `AnalyticsEventsClient` facade；真实 `codex-analytics` 继续拥有事件队列、reducer、HTTP sender 和 app-server protocol adapter，并通过 `api_client()` 向 core 注入 facade。core/core-api 已改为依赖 `codex-analytics-api`，core 内缺省 analytics client 改为 disabled facade。
    - `rtk cargo fmt -p codex-analytics-api -p codex-analytics -p codex-core -p codex-core-api -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-analytics-api -p codex-analytics -p codex-core -p codex-core-api -p codex-app-server --bin codex-app-server`：通过，验证 API facade、真实 analytics sink adapter、core consumer 和 app-server 注入编译。
    - `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg -q "codex-app-server-protocol" && { echo "unexpected: codex-app-server-protocol present in codex-core graph"; exit 1; } || { echo "ok: codex-app-server-protocol absent from codex-core graph"; exit 0; }'`：通过，确认 `codex-core` normal graph 不含 `codex-app-server-protocol`。
    - `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg -q "codex-analytics v" && { echo "unexpected: codex-analytics present in codex-core graph"; exit 1; } || { echo "ok: codex-analytics absent from codex-core graph"; exit 0; }'`：通过，确认 `codex-core` normal graph 不含真实 `codex-analytics`。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal | rg "codex-analytics|codex-app-server-protocol"`：只显示 `codex-analytics-api`，不显示真实 `codex-analytics` 或 `codex-app-server-protocol`。
    - 2026-06-19 复核发现 `codex-analytics-api` 直接依赖 `codex-plugin` 会经 `codex-plugin -> codex-utils-plugins -> codex-login` 把 login/plugin runtime 作为间接依赖拉回，违反“不要把 direct edge 变成 indirect edge”的拓扑目标；已把 `PluginId`、`AppConnectorId`、`PluginCapabilitySummary`、`PluginTelemetryMetadata` 下沉到 `codex-plugin-types`，`codex-plugin` 仅 re-export 并继续承载 loader/runtime outcome。
    - 已补充 `codex-rs/analytics-api/BUILD.bazel` 与 `codex-rs/plugin-types/BUILD.bazel` 的最小 `codex_rust_crate` 入口，避免新 crate 只有 Cargo workspace 入口而缺 Bazel crate 定义。
    - `rtk cargo fmt -p codex-plugin-types -p codex-plugin -p codex-analytics-api -p codex-analytics -p codex-core-plugins -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-plugin-types -p codex-plugin -p codex-analytics-api -p codex-analytics -p codex-core-plugins -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 plugin type 下沉、plugin re-export、analytics facade/implementation、core consumer 和 app-server 入口编译。
    - `rtk sh -lc 'cargo tree -p codex-analytics-api --edges normal | rg -q "codex-login|codex-plugin v" && exit 1 || exit 0'`：通过，确认 `codex-analytics-api` normal graph 不含 `codex-login` 或 `codex-plugin`。
    - `rtk cargo tree -p codex-analytics-api --depth 1 --edges normal`：direct normal deps 仅为 `codex-plugin-types`、`codex-protocol`、`serde`。
    - `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg -q "codex-app-server-protocol|codex-analytics v" && exit 1 || exit 0'`：通过，确认 `codex-core` normal graph 仍不含 `codex-app-server-protocol` 或真实 `codex-analytics`。
    - `rtk sh -lc 'cargo tree -p codex-plugin-types --edges normal | rg -q "codex-login|codex-plugin v|codex-utils-plugins" && exit 1 || exit 0'`：通过，确认 `codex-plugin-types` 未把 plugin/login runtime 或 utils-plugins 间接拉回。
    - `rtk git diff --check`：通过。
    - 继续清理 `codex-core -> codex-plugin` direct type edge：core 的 `PluginCapabilitySummary` import 改为 `codex-plugin-types`，`PluginLoadOutcome` 改为使用 `codex-core-plugins` 的领域 alias，`codex-core` manifest 移除 `codex-plugin` direct dependency 并新增 `codex-plugin-types`。runtime `codex-plugin` 仍通过 `codex-core-plugins`、`codex-hooks`、`codex-mcp` 等真实 plugin/MCP runtime 路径间接存在，后续应按这些边界分别拆，不能把本切片解释为 plugin runtime 已从 core graph 移除。
    - `rtk cargo fmt -p codex-core -p codex-plugin-types -p codex-core-plugins -p codex-plugin`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-plugin-types -p codex-plugin -p codex-core-plugins -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 core 使用 plugin types 和 core-plugins alias 后仍能编译。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal | rg "codex-plugin"`：只显示 `codex-plugin-types`，不显示 direct runtime crate `codex-plugin`。
    - `rtk cargo tree -p codex-core --invert codex-plugin --edges normal --depth 3`：仍显示 residual indirect paths via `codex-core-plugins`、`codex-hooks`、`codex-mcp`，记录为真实 runtime 边界待后续切分。
    - `rtk sh -lc 'cargo tree -p codex-core --depth 1 --edges normal | rg -q "codex-plugin v" && exit 1 || exit 0'`：通过，确认 depth-1 direct graph 不含 runtime `codex-plugin`。
    - `rtk git diff --check`：通过。
    - 继续收缩 plugin runtime indirect paths：`PluginHookSource` 下沉到 `codex-plugin-types`，`codex-plugin` 继续 re-export；`codex-hooks` 改为直接依赖 `codex-plugin-types` 获取 `PluginHookSource`/`PluginId`，`codex-mcp` 改为直接依赖 `codex-plugin-types` 获取 `PluginCapabilitySummary`/`AppConnectorId`，不再为了 hook/capability DTO 拉入 plugin loader/runtime。
    - `rtk cargo fmt -p codex-plugin-types -p codex-plugin -p codex-hooks -p codex-mcp -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-plugin-types -p codex-plugin -p codex-hooks -p codex-mcp -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 hook source 下沉、hooks/mcp consumer 和 core/app-server 入口编译。
    - `rtk sh -lc 'cargo tree -p codex-hooks --edges normal | rg -q "codex-plugin v" && exit 1 || exit 0'`：通过，确认 `codex-hooks` normal graph 不含 runtime `codex-plugin`。
    - `rtk sh -lc 'cargo tree -p codex-mcp --edges normal | rg -q "codex-plugin v" && exit 1 || exit 0'`：通过，确认 `codex-mcp` normal graph 不含 runtime `codex-plugin`。
    - `rtk cargo tree -p codex-core --invert codex-plugin --edges normal --depth 4`：只剩 `codex-plugin -> codex-core-plugins -> codex-core`，此前经 `codex-hooks` 和 `codex-mcp` 的 residual paths 已消失。
    - `rtk sh -lc 'cargo tree -p codex-plugin-types --edges normal | rg -q "codex-login|codex-plugin v|codex-utils-plugins" && exit 1 || exit 0'`：通过，确认 `PluginHookSource` 需要的 `codex-config-types` 没有把 plugin/login runtime 间接拉回。
    - `rtk git diff --check`：通过。
    - 2026-06-19 复核剩余 core direct graph：`rtk cargo tree -p codex-core --depth 1 --edges normal` 当前仍直接包含 `codex-api`、`codex-login`、`codex-model-provider`、`codex-exec-server`、`codex-mcp`、`codex-network-proxy`、`codex-workflow`、`codex-rollout` 等 runtime implementation；app-server-protocol 已不是 core normal graph 的问题，下一阶段要按 model client/auth、exec/environment、MCP manager、workflow manager 等真实边界拆 facade/trait 或把 implementation 移到组合根。
    - 继续扩大 `codex-network-proxy-api`：`NetworkPolicyRequest`、`NetworkProtocol`、`NetworkDecision`、`NetworkPolicyDecider`、`NetworkProxyAuditMetadata`、`BlockedRequest`、`BlockedRequestObserver` 已从 Rama-backed `codex-network-proxy` 下沉到 API crate；`codex-network-proxy` 继续 re-export 旧路径，backend/runtime state/reloader/handle 保留在 implementation crate。
    - core 中纯 proxy DTO/trait/helper callsite 已改为直接依赖 `codex-network-proxy-api`，包括 network approval、policy decision、session tests、guardian live network config、network proxy loader 和 permissions/network proxy spec tests；`core -> codex-network-proxy` 剩余 direct edge 只对应 `NetworkProxy`、`NetworkProxyState`、`ConfigReloader`、`ConfigState`、constraints、process environment wiring 等真实 runtime 类型。
    - 继续扩大 `codex-network-proxy-api`：`PROXY_ACTIVE_ENV_KEY`、`ALLOW_LOCAL_BINDING_ENV_KEY`、`ELECTRON_GET_USE_PROXY_ENV_KEY`、`PROXY_ENV_KEYS`，以及 macOS 下的 `PROXY_GIT_SSH_COMMAND_ENV_KEY`、`CODEX_PROXY_GIT_SSH_COMMAND_MARKER` 已下沉到 API crate；`codex-network-proxy` 继续 re-export 旧路径，runtime 中真正读取/写入 env 和 process wiring 留在 implementation crate。
    - core 生产路径和测试中仅需要 proxy env key 常量的 callsite 已改为直接依赖 `codex-network-proxy-api`，包括 `core/src/tools/runtimes/mod.rs`、`core/src/tasks/user_shell.rs`、`core/src/tools/runtimes/mod_tests.rs` 和 `core/tests/suite/realtime_conversation.rs`。
    - `rtk cargo fmt -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过，验证 proxy env key constants API、backend re-export 和 core consumer 编译；仅打印既有 warnings。
    - `rtk cargo test -p codex-network-proxy`：通过，覆盖 proxy backend crate；本切片补充了 `network_policy.rs` test 模块缺失的 `async_trait` import。
    - `rtk cargo test -p codex-network-proxy-api`：通过，0 个 tests。
    - `rtk sh -lc 'rtk cargo tree -p codex-network-proxy-api --edges normal | rg -q "rama|codex-network-proxy v|tokio|async-trait|globset" && exit 1 || exit 0'`：通过，确认 env key constants 下沉未让 API crate 间接拉回 Rama/proxy runtime、tokio、async-trait 或 globset。
    - `rtk rg -n "codex_network_proxy::(CODEX_PROXY_GIT_SSH_COMMAND_MARKER|PROXY_ACTIVE_ENV_KEY|PROXY_ENV_KEYS|PROXY_GIT_SSH_COMMAND_ENV_KEY)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，core 不再通过 backend re-export 获取这些 env key 常量。
    - `rtk cargo fmt -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-network-proxy-api -p codex-network-proxy -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 proxy API、proxy backend re-export、core consumer 和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：direct normal deps 仍只有 `serde`。
    - `rtk sh -lc 'cargo tree -p codex-network-proxy-api --edges normal | rg -q "rama|codex-network-proxy v|tokio|async-trait|globset" && exit 1 || exit 0'`：通过，确认 API crate 未间接拉回 Rama/proxy runtime、tokio、async-trait 或 globset。
    - `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 4`：剩余路径为 `codex-network-proxy -> codex-core` direct runtime 边界，以及 `codex-network-proxy -> codex-sandboxing -> codex-core` / `codex-exec-server` 等 sandbox/process runtime 边界；不再是纯 DTO/trait/helper 通过 re-export 回流。
    - 继续扩大 `codex-network-proxy-api`：`NetworkHostPort`、`parse_network_host_port` 和 `host_and_port_from_network_addr` 已从 Rama-backed `codex-network-proxy` 下沉到 API crate，且未新增 `url` dependency；`codex-network-proxy` 继续 re-export 旧 helper 路径，runtime `resolve_addr` 也复用 API parser。
    - core `NetworkProxySpec::proxy_host_and_port` 已改为直接依赖 `codex-network-proxy-api::host_and_port_from_network_addr`，不再通过 full network proxy runtime re-export 获取纯 display helper；`core -> codex-network-proxy` 剩余 direct edge 仍对应 proxy runtime/state/reloader/handle/constraints 和 process wiring，后续继续拆 runtime service boundary。
    - `rtk cargo fmt -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-network-proxy-api`：通过，11 个 host/port parser、display helper 和既有 API tests 通过。
    - `rtk cargo test -p codex-network-proxy`：通过，134 个 tests 通过，验证 runtime crate 复用 API parser 后行为仍通过。
    - `rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：direct normal deps 仍只有 `serde`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-network-proxy-api --edges normal | rtk rg -q "codex-network-proxy v|rama-|tokio v|url v|globset v"; then exit 1; else exit 0; fi'`：通过，确认 host/port helper 下沉没有把 proxy runtime、Rama、tokio、url 或 globset 间接拉入 API crate。
    - `rtk sh -lc 'if rtk rg -n "codex_network_proxy::host_and_port_from_network_addr|use codex_network_proxy::host_and_port_from_network_addr" core/src core/tests; then exit 1; else exit 0; fi'`：通过，确认 core 不再通过 backend re-export 获取该 helper。
    - `rtk git diff --check`：通过。
    - 继续收缩 `core -> codex-mcp` 的纯类型/常量使用：`CODEX_APPS_MCP_SERVER_NAME`、`MCP_TOOL_CODEX_APPS_META_KEY`、`MCP_SANDBOX_STATE_META_CAPABILITY` 和 `SandboxState` 已下沉到 `codex-mcp-types`；`codex-mcp` 继续 re-export 保持旧路径兼容。当时 `ToolInfo` 因直接包含 `rmcp::model::Tool` 暂缓迁移，后续已在 `codex-mcp-tool-types` 切片中迁出并单独记录 rmcp-backed 边界。
    - core 中只消费 MCP 常量/sandbox payload 的 callsite 已改为直接依赖 `codex-mcp-types`，包括 app instructions、connectors、plugin injection、mcp tool call、request-plugin-install handler、mcp tool exposure 和相关测试。
    - `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-mcp-types -p codex-mcp -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 MCP types、runtime re-export、core consumer 和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct normal deps 为 `codex-protocol`、`schemars`、`serde`、`serde_json`、`ts-rs`，没有新增 `rmcp`。
    - `rtk sh -lc 'cargo tree -p codex-mcp-types --edges normal | rg -q "codex-mcp v|rmcp|codex-login|codex-model-provider|codex-exec-server" && exit 1 || exit 0'`：通过，确认 MCP types 未间接拉回 MCP runtime、rmcp、login、model-provider 或 exec-server。
    - `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 4`：剩余路径只为 `codex-mcp -> codex-core` direct runtime boundary；纯常量/sandbox payload 不再通过 runtime re-export 回流。
    - 继续收缩 `core -> codex-exec-server` 的纯 filesystem API 使用：`codex-core` 新增 direct `codex-file-system` 依赖，`ExecutorFileSystem`、`LOCAL_FS`、`FileSystemSandboxContext`、`CreateDirectoryOptions`、`CopyOptions`、`RemoveOptions` 等 callsite 改为直接依赖 `codex_file_system`；`codex-exec-server` 继续 re-export 兼容旧路径，但 core 不再为纯 FS trait/local FS/options/sandbox context 走 exec-server re-export。
    - `rtk rg -n "codex_exec_server::(LOCAL_FS|ExecutorFileSystem|FileSystemSandboxContext|CopyOptions|CreateDirectoryOptions|FileMetadata|FileSystemResult|ReadDirectoryEntry|RemoveOptions)|use codex_exec_server::(LOCAL_FS|ExecutorFileSystem|FileSystemSandboxContext|CopyOptions|CreateDirectoryOptions|FileMetadata|FileSystemResult|ReadDirectoryEntry|RemoveOptions)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，确认 core 中纯 filesystem API 来源已切干净。
    - `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-file-system -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 file-system direct dependency、core callsite 和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo tree -p codex-file-system --depth 1 --edges normal`：direct normal deps 为 `async-trait`、`codex-protocol`、`codex-utils-absolute-path`、`serde`。
    - `rtk sh -lc 'cargo tree -p codex-file-system --edges normal | rg -q "codex-exec-server|reqwest|codex-mcp|codex-model-provider|codex-login" && exit 1 || exit 0'`：通过，确认 file-system 轻量 crate 没有间接拉回 exec-server、reqwest、MCP、model-provider 或 login runtime。
    - `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 4`：剩余路径为 `codex-exec-server -> codex-core` direct environment/process runtime boundary，以及 apply-patch、MCP/RMCP runtime 路径；不再是纯 filesystem trait/local FS re-export 回流。
    - 继续收缩 `core/codex-mcp -> codex-rmcp-client` 的纯 elicitation DTO 使用：`ElicitationAction` / `ElicitationResponse` 已下沉到 `codex-mcp-types`；`codex-rmcp-client` 保留兼容 re-export，并在 rmcp protocol 边界显式转换 `codex_mcp_types::ElicitationAction` 与 `rmcp::model::ElicitationAction`。`core`、`codex-mcp` 和相关测试已改为直接依赖 `codex-mcp-types` 构造 response DTO。
    - `rtk cargo fmt -p codex-mcp -p codex-mcp-types -p codex-rmcp-client -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-mcp-types -p codex-rmcp-client -p codex-mcp -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 MCP types、rmcp-client conversion、codex-mcp runtime consumer、core consumer 和 app-server 入口编译；仅打印既有 file-subscription/core/app-server unused/dead_code warning。
    - `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct normal deps 为 `codex-protocol`、`schemars`、`serde`、`serde_json`、`ts-rs`，没有 `rmcp` 或 rmcp-client。
    - `rtk sh -lc 'cargo tree -p codex-mcp-types --edges normal | rg -q "codex-rmcp-client|rmcp|reqwest|oauth2|axum|codex-exec-server|codex-login|codex-model-provider" && exit 1 || exit 0'`：通过，确认 mcp-types 未间接拉回 rmcp-client、rmcp、HTTP/OAuth 或 runtime implementation。
    - `rtk cargo tree -p codex-core --invert codex-rmcp-client --edges normal --depth 4`：当时剩余路径为 `codex-rmcp-client -> codex-core` direct OAuth runtime helper，以及 `codex-rmcp-client -> codex-mcp -> codex-core` MCP runtime boundary；不再是 elicitation response DTO 通过 re-export 回流。后续已把 direct OAuth helper 也收敛到 `codex-mcp` runtime boundary。
    - `rtk rg -n "codex_rmcp_client::Elicitation(Action|Response)|use codex_rmcp_client::Elicitation(Action|Response)|use rmcp::model::ElicitationAction" codex-rs/core/src codex-rs/core/tests codex-rs/codex-mcp/src codex-rs/rmcp-client/src codex-rs/mcp-types/src -g '*.rs'`：无结果，确认旧 DTO import 已清空；后续 `codex_rmcp_client::perform_oauth_login` 也不再由 core 直接引用。
    - 继续瘦身 shared `codex-protocol`：移除 `codex-protocol -> codex-async-utils` 和 direct `tokio` normal dependency；`CodexErr` 不再持有 `tokio::task::JoinError` 或实现 `From<codex_async_utils::CancelErr>`。core 中依赖 `.or_cancel(...).await?` 的 3 个 callsite 已改为显式 `map_err(|_| CodexErr::TurnAborted)`，保持 cancellation 语义但不让 protocol crate 认识 runtime cancellation 类型。
    - `rtk cargo fmt -p codex-protocol -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-protocol`：通过，验证 protocol 自身可在无 async-utils/tokio direct edge 下编译。
    - `rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 core 显式 cancellation 映射和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct normal deps 已不包含 `codex-async-utils` 或 `tokio`。
    - `rtk sh -lc 'cargo tree -p codex-protocol --edges normal | rg -q "codex-async-utils|tokio v|tokio-util|async-trait" && exit 1 || exit 0'`：通过，确认 protocol normal graph 不再间接拉回这些 async runtime crates。
    - `rtk sh -lc 'cargo tree -p codex-mcp-types --edges normal | rg -q "codex-async-utils|tokio v|tokio-util|async-trait" && exit 1 || exit 0'`：通过，确认 mcp-types 也不会经 protocol 间接拉回 async runtime crates。
    - `rtk just bazel-lock-check`：首次失败并提示 `MODULE.bazel.lock` 过期；随后执行 `rtk just bazel-lock-update` 更新 lockfile，再次执行 `rtk just bazel-lock-check` 通过。
    - 继续收缩 `core -> codex-execpolicy` 的纯策略模型使用：`codex-core` 新增 direct `codex-execpolicy-api` 依赖，session、network proxy loader、permissions instructions、network policy decision、shell unix escalation runtime 和相关测试中的 `Policy`、`Decision`、`Evaluation`、`MatchOptions`、`RuleMatch`、`NetworkRuleProtocol` 改为直接来自 `codex-execpolicy-api`。后续又确认 append/amend writer 不依赖 Starlark，并下沉到 `codex-execpolicy-api`；`codex-execpolicy` 源码残留只剩 `PolicyParser`、parser error/location/display、`ExecPolicyCheckCommand` 和一个需要 `PolicyParser` 的 shell runtime 测试。
    - `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-execpolicy-api -p codex-execpolicy -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 core 使用 API policy model、execpolicy implementation 边界和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo tree -p codex-execpolicy-api --depth 1 --edges normal`：direct normal deps 为 `codex-utils-absolute-path`、`multimap`、`serde`、`serde_json`、`shlex`、`thiserror`，无 Starlark/runtime。
    - `rtk sh -lc 'cargo tree -p codex-execpolicy-api --edges normal | rg -q "codex-execpolicy v|starlark|tokio|async-trait" && exit 1 || exit 0'`：通过，确认 API crate 没有把 execpolicy implementation、Starlark 或 async runtime 间接拉回。
    - `rtk cargo tree -p codex-core --invert codex-execpolicy --edges normal --depth 4`：仍只显示 `codex-execpolicy -> codex-core` direct runtime boundary；纯策略模型不再通过 implementation re-export 回流。
    - `rtk rg -n "codex_execpolicy::|use codex_execpolicy::" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：仅剩 `exec_policy.rs` 的 `PolicyParser` 与 parser error display，以及 `unix_escalation_tests.rs` 的 `PolicyParser`；`AmendError` 和 append/amend writer 已改为 `codex_execpolicy_api`。
    - append/amend writer 下沉后执行 `rtk cargo fmt -p codex-execpolicy-api -p codex-execpolicy -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - append/amend writer 下沉后执行 `rtk cargo check -p codex-execpolicy-api -p codex-execpolicy -p codex-core`：通过，验证 API writer、旧 re-export 和 core parser boundary 编译。
    - append/amend writer 下沉后执行 `rtk cargo test -p codex-execpolicy-api`：通过，6 个测试通过；`rtk cargo test -p codex-execpolicy`：通过，27 个测试通过；`rtk cargo test -p codex-core exec_policy --lib`：通过，验证 core append/amend 调用改用 API crate 后仍正常。
    - append/amend writer 下沉后执行 `rtk cargo tree -p codex-execpolicy-api --invert starlark --edges normal` 和 `rtk cargo tree -p codex-execpolicy-api --invert codex-execpolicy --edges normal`：Cargo 均返回 package not in graph，确认 API crate 未间接拉回 Starlark 或 implementation crate。
    - append/amend writer 下沉后执行 `rtk cargo tree -p codex-core --invert codex-execpolicy --edges normal --depth 4`：仍只显示 `codex-execpolicy -> codex-core` direct parser/runtime boundary，没有新增 indirect path。
    - append/amend writer 下沉后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - `rtk just bazel-lock-check`：通过，新增 core manifest dependency 不需要更新 `MODULE.bazel.lock`。
    - 继续收缩 core direct utility edge：shell executable PATH lookup 新增 `codex_shell_command::resolve_executable_in_path`，`core/src/shell.rs` 不再直接调用 `which::which`；`which` 从 `codex-core` normal dependency 移到 dev-dependency，core tests 仍可直接探测本机可执行文件。
    - `rtk cargo fmt -p codex-shell-command -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-shell-command`：通过，139 个 tests 通过，覆盖新增缺失 executable lookup helper。
    - `rtk cargo check -p codex-core`：通过，验证 core 生产路径改用 shell-command utility；仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 组合入口仍能编译；仅打印既有 file-subscription/core unused/dead_code warnings。
    - `rtk sh -lc 'rtk cargo tree -p codex-core --depth 1 --edges normal | rg -q "^[├└]── which v" && exit 1 || exit 0'`：通过，确认 `codex-core` depth-1 normal graph 不再直接包含 `which`。
    - `rtk cargo tree -p codex-core --invert which --edges normal --depth 3`：当时仍显示 `which` 经 `codex-rmcp-client`、`codex-sandboxing` 和 `codex-shell-command` 的真实 utility/runtime 路径进入 core；本切片只声明 direct owner 修正，不声明 full graph 移除。后续 `codex-rmcp-client` direct edge 已清理，`which` 的 rmcp-client 路径只应经 `codex-mcp` runtime boundary。
    - 继续收缩 `core -> codex-rmcp-client` direct runtime edge：`codex-mcp` re-export `perform_oauth_login` 作为 MCP runtime/OAuth login entry，`core/src/mcp_skill_dependencies.rs` 改为从 `codex_mcp` 调用，`codex-core` manifest 移除 direct `codex-rmcp-client` dependency。skill MCP dependency 安装仍会通过 `codex-mcp -> codex-rmcp-client` 间接使用 OAuth browser callback flow，这是当前真实 MCP runtime boundary。
    - `rtk cargo fmt -p codex-mcp -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-mcp -p codex-core`：通过，验证 MCP runtime re-export 和 core callsite 编译；仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，验证 app-server 组合入口仍能编译；仅打印既有 file-subscription/core unused/dead_code warnings。
    - `rtk sh -lc 'rtk cargo tree -p codex-core --depth 1 --edges normal | rg -q "codex-rmcp-client" && exit 1 || exit 0'`：通过，确认 `codex-core` depth-1 normal graph 不再直接包含 `codex-rmcp-client`。
    - `rtk cargo tree -p codex-core --invert codex-rmcp-client --edges normal --depth 4`：剩余路径只为 `codex-rmcp-client -> codex-mcp -> codex-core`；direct `codex-rmcp-client -> codex-core` 路径已消失。
    - `rtk rg -n "codex_rmcp_client::|use codex_rmcp_client|codex-rmcp-client" codex-rs/core/src codex-rs/core/tests codex-rs/core/Cargo.toml -g '*.rs'`：无结果，确认 core 源码和 manifest 不再直接引用 rmcp-client。
    - 最新 `rtk cargo tree -p codex-core --invert which --edges normal --depth 4`：`which` 仍经 `codex-rmcp-client -> codex-mcp -> codex-core`、`codex-sandboxing` 和 `codex-shell-command` 进入 core；rmcp-client direct core path 已消失，shell lookup direct owner 修正仍不声明 full graph 移除。
    - 继续收缩 model-provider request adapter 使用：新增 `codex-model-provider-api`，迁移 `BearerAuthProvider`、`auth_provider_from_auth`、`unauthenticated_auth_provider` 和 `model_provider_info_to_api_provider`。`codex-model-provider` 保留兼容 re-export 和 `CodexResult` wrapper；当时真实 `ModelProvider` trait/factory、models manager、Bedrock runtime provider 和 provider-scoped auth manager 暂留在 implementation crate，后续 trait/types 已在下一条切片继续下沉。
    - `core-plugins`、`core-skills` 和 `codex-mcp` 已改为直接依赖 `codex-model-provider-api`，不再为了构造 request headers 依赖完整 `codex-model-provider`。`core` 中 arc monitor、MCP file upload、realtime API provider config 和相关测试也改为直接使用新 API crate；当时 `core` 对 full `codex-model-provider` 的 direct dependency 还存在于 session/client/thread manager runtime provider boundary，后续已通过 factory/组合根注入切片移除。
    - 继续收缩 runtime provider trait/type 使用：`ModelProvider`、`SharedModelProvider`、`ModelProviderFuture`、`ProviderCapabilities`、`ProviderAccountState`、`ProviderAccountError` 和 `ProviderAccountResult` 已下沉到 `codex-model-provider-api`；API trait 使用 boxed future，避免把新的 `async_trait` proc-macro 边界加入 API trait。`codex-model-provider` 继续 re-export 旧路径，concrete factory、configured provider、Bedrock provider、provider-scoped auth manager construction 和 model manager implementation selection 仍留在 implementation crate。
    - `core` 中 `SharedModelProvider` 的生产调用面已改为直接依赖 `codex-model-provider-api`；`app-server` 中只需要 account error enum 的路径也改为直接依赖 API crate。后续已通过 factory/组合根注入切片移除 `core` 对 full `codex-model-provider` 的 normal dependency；`app-server`、CLI、MCP server 等组合根继续依赖 full implementation 来构造 concrete provider factory。
    - `rtk cargo fmt -p codex-model-provider-api -p codex-model-provider -p codex-core -p codex-core-plugins -p codex-core-skills -p codex-mcp`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-model-provider-api -p codex-model-provider -p codex-core-plugins -p codex-core-skills -p codex-mcp -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证新 adapter crate、旧 runtime re-export、三个消费者、core 和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo test -p codex-model-provider-api`：通过，3 个测试通过；`rtk cargo test -p codex-model-provider`：通过，验证旧 runtime crate wrapper 和 provider 行为。
    - `rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal`：direct normal deps 为 `codex-agent-identity`、`codex-api`、`codex-auth-types`、`codex-login`、`codex-model-provider-info` 和 `http`。该 crate 因理解 `CodexAuth` 仍会经 `codex-login` 间接带入 login client stack，不应被当作纯基础类型层。
    - `rtk sh -lc 'cargo tree -p codex-model-provider-api --edges normal | rg -q "codex-model-provider v|codex-models-manager|codex-aws-auth" && exit 1 || exit 0'`：通过，确认 adapter crate 没有间接拉回 model-provider implementation、models manager 或 AWS provider implementation。
    - `rtk sh -lc 'cargo tree -p codex-core-plugins --edges normal | rg -q "codex-model-provider v" && exit 1 || exit 0'`、`rtk sh -lc 'cargo tree -p codex-core-skills --edges normal | rg -q "codex-model-provider v" && exit 1 || exit 0'`、`rtk sh -lc 'cargo tree -p codex-mcp --edges normal | rg -q "codex-model-provider v" && exit 1 || exit 0'`：均通过，确认三个消费者不再通过 normal graph 拉回 full `codex-model-provider`。
    - trait/type 下沉后执行 `rtk cargo fmt -p codex-model-provider-api -p codex-model-provider -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - trait/type 下沉后执行 `rtk cargo check -p codex-model-provider-api -p codex-model-provider -p codex-core -p codex-app-server --bin codex-app-server`：通过，验证 API trait、implementation re-export、core runtime provider use 和 app-server 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - trait/type 下沉后执行 `rtk cargo test -p codex-model-provider-api`：通过，3 个测试通过；`rtk cargo test -p codex-model-provider`：通过，29 个测试通过。
    - trait/type 下沉后执行 `rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal`：direct normal deps 为 `codex-agent-identity`、`codex-api`、`codex-auth-types`、`codex-login`、`codex-model-provider-info`、`codex-models-manager-api`、`codex-protocol` 和 `http`。新增 provider trait 因模型目录 trait/DTO 需要 `codex-models-manager-api`/`codex-protocol`；该 crate 仍因 `CodexAuth` 理解带入 login client stack。
    - trait/type 下沉后执行 `rtk sh -lc 'rtk cargo tree -p codex-model-provider-api --edges normal | rg -q "codex-model-provider v|codex-models-manager v|codex-aws-auth" && exit 1 || exit 0'`：通过，确认 API crate 没有间接拉回 full provider implementation、full models-manager 或 AWS provider implementation。
    - 已完成 model-provider factory/组合根注入切片：`ModelProviderFactory`、`SharedModelProviderFactory` 下沉到 `codex-model-provider-api`，完整 `codex-model-provider` 提供 `DefaultModelProviderFactory` 并保留 `create_model_provider` implementation。`ThreadManagerState`、`CodexSpawnArgs`、`SessionServices`、`ModelClientState` 和 `Session::new` 通过 constructor injection 持有 factory trait；`ModelClient::clone_with_provider`、provider capability lookup、model catalog manager 构造和 subagent spawn 都复用注入的 factory。app-server、CLI 和 MCP server 组合根注入 `DefaultModelProviderFactory`；core test support 提供 `model_provider_factory_for_tests()` / `create_model_provider_for_tests()`，避免 core test 依赖 full provider implementation。
    - `rtk cargo fmt -p codex-model-provider-api -p codex-model-provider -p codex-core -p codex-cli -p codex-app-server -p codex-mcp-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-model-provider-api -p codex-model-provider -p codex-core -p codex-app-server --bin codex-app-server -p codex-cli -p codex-mcp-server`：通过，验证 API factory、implementation factory、core runtime consumer 和组合根注入路径编译；仅打印既有 unused/dead_code warnings。
    - `rtk cargo test -p codex-model-provider-api`：通过，3 个测试通过；`rtk cargo test -p codex-model-provider`：通过，29 个测试通过；`rtk cargo test -p codex-core --no-run`：通过，验证 core test support 和 test-only constructor callsites 编译。
    - `rtk sh -lc 'rtk cargo tree -p codex-core --edges normal | rg -q "codex-model-provider v" && exit 1 || exit 0'` 和同命令加 `--depth 1`：均通过，确认 `codex-core` normal graph 不再拉 full `codex-model-provider`，不是只移除了 direct edge。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal | rg "codex-model-provider|codex-models-manager|codex-login|codex-api"`：显示 `codex-model-provider-api` / `codex-model-provider-info`，不显示 full `codex-model-provider`；仍显示 full `codex-models-manager`，作为后续独立 implementation boundary 处理。
    - `rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal`：direct normal deps 为 `codex-agent-identity`、`codex-api`、`codex-auth-types`、`codex-login`、`codex-model-provider-info`、`codex-models-manager-api`、`codex-protocol` 和 `http`；这是 request adapter/provider trait 需要理解 auth/API/model catalog DTO 的边界，不包含 full provider implementation。
    - `rtk sh -lc 'rtk cargo tree -p codex-model-provider-api --edges normal | rg -q "codex-model-provider v|codex-models-manager v|codex-aws-auth" && exit 1 || exit 0'`：通过，确认 API crate 没有间接拉回 full provider implementation、full models-manager 或 AWS provider implementation。
    - `rtk just bazel-lock-check`：通过；`rtk git diff --check`：通过。
    - 继续收缩 model catalog trait/config 使用：新增 `codex-models-manager-api`，迁移 `ModelsManager`、`SharedModelsManager`、`ModelsManagerFuture`、`RefreshStrategy`、`TryListModelsError`、`ModelsManagerConfig` 和 `ModelMetadataOverride`。`codex-models-manager` 保留兼容 re-export，并继续拥有 bundled catalog、cache、remote refresh、concrete manager、collaboration presets、model_info fallback/override 逻辑和测试。
    - `core-api`、`core`、`app-server`、`cli`、`model-provider` 等生产调用面已改为直接使用 `codex-models-manager-api` 的 trait/config/refresh strategy；`codex-core`、`codex-app-server`、`codex-cli` 和 `codex-model-provider` 仍各自保留 full `codex-models-manager` dependency，用于 test_support、bundled/model_info helper、collaboration presets 或 concrete manager construction 等真实 implementation 边界。
    - `rtk cargo fmt -p codex-models-manager-api -p codex-models-manager -p codex-model-provider -p codex-core -p codex-core-api -p codex-app-server -p codex-cli`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-models-manager-api -p codex-models-manager -p codex-model-provider -p codex-core -p codex-core-api -p codex-app-server --bin codex-app-server -p codex-cli`：通过，验证新 API crate、旧 manager implementation、model-provider、core facade、core 和 app-server/CLI 入口编译；仅打印既有 file-subscription/core unused/dead_code warning。
    - `rtk cargo test -p codex-models-manager`：通过，33 个测试通过，覆盖 manager behavior、serialization 和 re-exported model catalog 路径。
    - `rtk cargo tree -p codex-models-manager-api --depth 1 --edges normal`：direct normal deps 仅为 `codex-protocol`。该 API crate 当前复用 `codex_protocol::openai_models` 的 `ModelInfo`/`ModelPreset`/`ModelsResponse` DTO，因此仍会经 protocol 带入 `chrono` 等 shared protocol 依赖；这是真实 DTO owner 残留，不应被描述为完全无 protocol 依赖。
    - `rtk sh -lc 'rtk cargo tree -p codex-models-manager-api --edges normal | rg -q "codex-models-manager v|codex-login|codex-otel|tokio|async-trait|codex-collaboration-mode-templates" && exit 1 || exit 0'`：通过，确认 API crate 没有间接拉回 manager implementation、login/otel/runtime async crate 或 collaboration templates。
    - `rtk cargo tree -p codex-core-api --edges normal -i codex-models-manager`：显示剩余路径为 `codex-core-api -> codex-core -> codex-models-manager` 和 `codex-core-api -> codex-core -> codex-model-provider -> codex-models-manager`。这说明 `codex-core-api` 作为 core facade 仍因 re-export full core 而间接拉回 implementation；不是 model catalog API import 残留，后续要先拆 core facade/runtime boundary。
    - `rtk just bazel-lock-check`：通过；新增 crate/manifest 调整不需要更新 `MODULE.bazel.lock`。
    - 已完成 `codex-core` default graph 去除 full `codex-models-manager` 切片：`codex_core::test_support` 改为只在 `cfg(test)` 或 `codex-core/test-support` feature 下编译，`codex-models-manager` 在 core manifest 中改为 optional normal dependency 并仅由该 feature 启用；core dev-dependency 仍保留 full manager 供 unit/test support 编译。`core_test_support`、`app_test_support`、app-server/app-server-transport dev graph 和 TUI dev graph 显式启用相关 test-support feature；`app-server-client::legacy_core::test_support` 也改为只在 `codex-app-server-client/test-support` feature 或本 crate tests 下 re-export，避免生产客户端默认暴露 core test support。
    - `rtk cargo fmt -p codex-core -p codex-app-server-client -p codex-app-server -p codex-app-server-transport -p codex-tui`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-core`：通过，验证 default feature 下 core 生产构建仍编译。
    - `rtk cargo test -p codex-core --no-run`：通过，验证 `codex-core/test-support` feature wiring 对 core unit/integration tests 生效。
    - `rtk cargo check -p codex-app-server-client -p codex-app-server --bin codex-app-server`：通过；`rtk cargo check -p codex-app-server --tests`：通过；`rtk cargo check -p codex-app-server-transport --tests`：通过；`rtk cargo check -p codex-app-server-client --features test-support`：通过。
    - `rtk cargo check -p codex-tui --tests`：失败于既有 TUI test import `codex_app_server_protocol::v2::*` 不存在（`tui/src/app/app_server_event_targets.rs:185-189`），不是本切片的 test-support feature wiring 问题；生产 app-server-client/app-server check 和 app-server-client feature check 已覆盖本切片关键路径。
    - `rtk sh -lc 'rtk cargo tree -p codex-core --edges normal | rg -q "codex-models-manager v" && exit 1 || exit 0'`：通过；`rtk sh -lc 'rtk cargo tree -p codex-core --edges normal | rg -q "codex-model-provider v|codex-models-manager v" && exit 1 || exit 0'`：通过，确认 core default normal graph 不再拉 full provider 或 full model manager。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal | rg "codex-models-manager|codex-model-provider"`：只显示 `codex-model-provider-api`、`codex-model-provider-info` 和 `codex-models-manager-api`。
    - `rtk just bazel-lock-check`：通过；新增 optional dependency/feature 不需要更新 `MODULE.bazel.lock`。
    - 已完成 `codex-core` 去除 direct `whoami` 切片：新增 `codex-user-info` 承载 OS/user display-name lookup 和 first-name fallback；core realtime prompt 和相关测试改为调用 `codex_user_info::current_user_first_name()`，core manifest 移除 normal `whoami` direct dependency。
    - `rtk cargo fmt -p codex-user-info -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-user-info`：通过，2 个测试通过。
    - `rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk sh -lc 'rtk cargo tree -p codex-core --depth 1 --edges normal | rg -q "^[├└]── whoami v" && exit 1 || exit 0'`：通过，确认 core depth-1 normal graph 不再直接包含 `whoami`。
    - `rtk cargo tree -p codex-user-info --depth 1 --edges normal`：direct normal deps 只显示 `whoami`，OS/user lookup 依赖集中在小 crate。
    - `rtk cargo tree -p codex-core --invert whoami --edges normal --depth 3`：剩余路径为 `whoami -> codex-user-info -> codex-core`，证明 direct edge 已替换为明确 owner crate，而不是经其他 heavy runtime 间接拉回。
    - `rtk rg -n "whoami::|use whoami|whoami =" codex-rs/core/src codex-rs/core/tests codex-rs/core/Cargo.toml`：无结果，core 源码、测试和 manifest 不再直接引用 `whoami`。
    - `rtk just bazel-lock-check`：通过，新增 crate/manifest 调整未破坏 Bazel lock 检查。
    - 已完成 workflow API 切片：新增 `codex-workflow-api` 承载 workflow manifest、registry、summary/details、diagnostics、registry loader、init-context renderer，以及 `WorkflowRun` / `WorkflowRunStatus` / `WorkflowAgentBinding` / `WorkflowRuntimeBridge` / `WorkflowRuntimeRequest` / `WorkflowRuntimeError` 这类 run-control DTO/trait；`codex-workflow` 继续 re-export 旧路径并保留 `WorkflowRunManager`、runner bridge implementation、Node/TypeScript runner、snapshot persistence 和 abort/resume/status runtime control。core 的 `workflows` facade 已改为直接依赖 `codex-workflow-api`，core/app-server 的 workflow DTO/bridge type 使用也已下沉到 API crate；run manager/runtime tool 路径仍经 `codex-workflow`。
    - `rtk cargo fmt -p codex-workflow-api -p codex-workflow -p codex-core -p codex-app-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-workflow-api -p codex-workflow -p codex-core -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo test -p codex-workflow-api`：通过，9 个 registry/discovery/render tests 通过。
    - `rtk cargo test -p codex-workflow`：通过，8 个 run-manager/runtime bridge tests 通过，验证旧 runtime crate 消费 API crate 类型。
    - `rtk sh -lc 'rtk cargo tree -p codex-workflow-api --edges normal | rg -q "codex-workflow v|tokio v|once_cell" && exit 1 || exit 0'`：通过，确认 workflow API crate 没有间接拉回 workflow runtime、tokio runner runtime 或 run-state registry dependency。
    - `rtk cargo tree -p codex-workflow-api --depth 1 --edges normal`：direct normal deps 只显示 `serde`、`serde_json` 和 `serde_yaml`。
    - `rtk just bazel-lock-check`：通过，只有既有 rules_rs well-known crate annotation warnings。
    - 已完成 MCP tool metadata 切片：新增 `codex-mcp-tool-types` 承载 `ToolInfo`、raw `rmcp::model::Tool` metadata、`declared_openai_file_input_param_names` 和 `tool_with_model_visible_input_schema`；`codex-mcp` 继续 re-export 旧路径并保留 manager/connection/OAuth/auth elicitation/server construction runtime。core 中只需要 ToolInfo 或 OpenAI file-param helper 的 callsite 已改为直接依赖 `codex-mcp-tool-types`，不再经 `codex_mcp::ToolInfo` / `codex_mcp::declared_openai_file_input_param_names`。
    - `codex-mcp-tool-types` 不是纯无 async 依赖 DTO crate：`ToolInfo` 仍承载 `rmcp::model::Tool`，而 `rmcp 0.15` standalone 编译需要 `server` feature，因此该 crate 会拉入 `rmcp` 的 tokio/futures 相关依赖。当前切片的边界收益是移除 full `codex-mcp` runtime coupling，不应被描述为 core 已脱离 `rmcp` 或 MCP runtime；core 仍 direct 依赖 `rmcp` 的 resource/elicitation DTO，也仍 direct 依赖 `codex-mcp` 的 manager runtime。
    - `rtk cargo fmt -p codex-mcp-tool-types -p codex-mcp -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-mcp-tool-types`：通过，3 个 file-param/schema masking tests 通过。
    - `rtk cargo check -p codex-mcp-tool-types -p codex-mcp -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。
    - `rtk cargo test -p codex-mcp`：通过，验证 `codex-mcp` 兼容 re-export、manager 和 tool metadata 路径。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-mcp-tool-types --depth 1 --edges normal`：direct normal deps 显示 `codex-protocol`、`rmcp`、`serde`、`serde_json`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-mcp-tool-types --edges normal | rtk rg -q "codex-mcp v|codex-rmcp-client|codex-login|codex-api|codex-exec-server|codex-model-provider"; then exit 1; else exit 0; fi'`：通过，确认新 crate 没有间接拉回 full MCP runtime、rmcp-client、login/API/exec-server/model-provider。
    - `rtk rg -n "codex_mcp::ToolInfo|use codex_mcp::ToolInfo|codex_mcp::declared_openai|use codex_mcp::declared_openai" core/src core/tests`：无结果，core 不再通过 `codex_mcp` 兼容 re-export 获取 ToolInfo/file-param helper。
    - `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 4`：剩余路径仍为 `codex-mcp -> codex-core` direct manager runtime boundary，后续要拆 MCP manager/runtime trait，不能把本切片解释为已移除 core 的 MCP runtime dependency。
    - `rtk just bazel-lock-check`：通过，只有既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 workflow run-manager 注入切片：`WorkflowRunController`、`WorkflowRunUpdateReceiver`、boxed future aliases 和 `DisabledWorkflowRunController` 已下沉到 `codex-workflow-api`；`codex-workflow::WorkflowRunManager` 实现该 trait 并继续拥有 Node/TypeScript runner、snapshot persistence、abort/resume/status runtime。`codex-core` 的 `Session` 持有 `Arc<dyn WorkflowRunController>`，`ThreadManager::new` 默认 disabled controller，app-server/MCP server/core test support 通过 `ThreadManager::new_with_workflow_runs` 注入 concrete manager；普通 subagent spawn 复用 parent controller。
    - `codex-core` manifest 已移除 `codex-workflow` normal dependency，仅保留 dev-dependency 供 core integration test support 注入真实 workflow runtime；`codex-mcp-server` 新增 concrete `codex-workflow` dependency 作为组合根注入点。app-server 原本已有 `codex-workflow` dependency，继续同时服务 app-server v2 workflow control plane 和 core workflow tool runtime。
    - `rtk cargo fmt -p codex-workflow-api -p codex-workflow -p codex-core -p codex-app-server -p codex-mcp-server`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-workflow-api -p codex-workflow -p codex-core -p codex-app-server --bin codex-app-server -p codex-mcp-server`：通过，仅打印既有 file-subscription/core/app-server warnings。
    - `rtk cargo test -p codex-workflow-api`：通过，9 个 tests 通过；`rtk cargo test -p codex-workflow`：通过，8 个 tests 通过。
    - `rtk cargo test -p codex-core workflow_control_tools_manage_run_state`：通过，1 个 workflow tool integration test 通过，验证 start/status/resume/abort 和 snapshot persistence 仍使用真实 injected manager。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct normal deps 不再显示 `codex-workflow`，只保留 `codex-workflow-api`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-core --edges normal | rtk rg -q "codex-workflow v"; then exit 1; else exit 0; fi'`：通过，确认 core normal graph 不再间接拉回 workflow runtime。
    - `rtk cargo tree -p codex-workflow-api --depth 1 --edges normal`：direct deps 仍只有 `serde`、`serde_json`、`serde_yaml`；`rtk sh -lc 'if rtk cargo tree -p codex-workflow-api --edges normal | rtk rg -q "codex-workflow v|tokio v|once_cell"; then exit 1; else exit 0; fi'`：通过，确认 workflow API trait 未拉回 runtime/tokio/state registry。
    - `rtk just bazel-lock-check`：通过，只有既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 default client 切片：新增 `codex-default-client` 承载 originator、first-party originator 判断、User-Agent、default headers、residency header state、default reqwest client builder 和 `CodexHttpClient` constructor；`codex-login::default_client` 变为兼容 re-export，login token/storage/server runtime 继续留在 `codex-login`。
    - core 中只需要 default client helper 的生产和测试 callsite 已改为直接依赖 `codex-default-client`，包括 session originator、MCP skill dependency first-party 判断、connectors originator、otel/window sandbox originator、client/realtime default headers、arc monitor/test support reqwest builder，以及 client/client_websockets/plugins tests；core 中不再存在 `codex_login::default_client` 引用。
    - `codex-login` direct graph 现在通过 `codex-default-client` 获取 default client helper，`codex-terminal-detection`、`os_info` 和 default-client-only test deps 已从 login manifest 移除；`CodexAuth`、`AuthManager`、token refresh/storage、login server、revoke 和 agent-identity runtime 仍是 login 的真实边界。
    - `rtk cargo fmt -p codex-default-client -p codex-login -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-default-client`：通过，7 个 originator/User-Agent/default headers tests 通过。
    - `rtk cargo check -p codex-login`：通过，验证 login 兼容 re-export 和 auth runtime 编译。
    - `rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-default-client --depth 1 --edges normal`：direct deps 为 `codex-client`、`codex-config-types`、`codex-terminal-detection`、`os_info`、`reqwest` 和 `tracing`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-default-client --edges normal | rtk rg -q "codex-login v|codex-api v|codex-model-provider|codex-keyring-store|codex-agent-identity|tiny_http|webbrowser"; then exit 1; else exit 0; fi'`：通过，确认 default client crate 未间接拉回 login/token/storage/server runtime、codex-api、model-provider implementation、keyring、agent-identity、tiny_http 或 webbrowser。
    - `rtk sh -lc 'if rtk rg -n "codex_login::default_client|use codex_login::default_client" core/src core/tests; then exit 1; else exit 0; fi'`：通过，确认 core 不再通过 login re-export 获取 default client helper。
    - `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 3`：剩余路径仍包括 core direct `codex-login` 以及经 core-plugins/core-skills/feedback/MCP/model-provider-api/rollout/utils-plugins 等真实 auth/runtime 边界，说明本切片只剥离 default-client helper，不宣称移除 core 的 login dependency。
    - `rtk cargo tree -p codex-login --depth 1 --edges normal`：显示 `codex-default-client` direct dependency，且不再显示 `codex-terminal-detection` 或 `os_info` direct dependency。
    - `rtk just bazel-lock-check`：通过，只有既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 API request types 切片：新增 `codex-api-types` 承载 `Reasoning`、`TextControls`、`OpenAiVerbosity`、`ResponsesApiRequest`、`ResponseCreateWsRequest`、`ResponseProcessedWsRequest`、`ResponsesWsRequest`、websocket request metadata key、`create_text_param_for_request` 和 `response_create_client_metadata`；`codex-api` 继续 re-export 旧路径并保留 API client、auth adapter、HTTP transport、SSE/WebSocket parser、file upload 和 endpoint runtime。
    - core 中只需要 Responses request shape/text controls/ws metadata helper 的生产和测试 callsite 已改为直接依赖 `codex-api-types`，包括 `core/src/client.rs`、`core/src/client_common_tests.rs` 和 websocket suite headers；core 中不再通过 `codex_api::ResponsesApiRequest` / `codex_api::create_text_param_for_request` 等旧路径获取这些纯 request DTO/helper。
    - `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-api-types`：通过，3 个 text controls/request helper tests 通过。
    - `rtk cargo check -p codex-api`：通过，验证 re-export shim 和 API runtime 编译。
    - `rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 为 `codex-protocol`、`serde` 和 `serde_json`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-api-types --edges normal | rtk rg -q "codex-api v|codex-client v|reqwest v|tokio v|tokio-tungstenite|eventsource-stream|tungstenite"; then exit 1; else exit 0; fi'`：通过，确认 API request types crate 未间接拉回完整 `codex-api`、HTTP client stack、tokio runtime、SSE/WebSocket runtime 或 tungstenite。
    - `rtk sh -lc 'if rtk rg -n "codex_api::(Reasoning|ResponsesApiRequest|ResponseCreateWsRequest|ResponsesWsRequest|OpenAiVerbosity|TextControls|create_text_param_for_request|response_create_client_metadata|WS_REQUEST_HEADER)" core/src core/tests; then exit 1; else exit 0; fi'`：通过，确认 core 不再经 `codex-api` re-export 获取这些纯 DTO/helper。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 3`：剩余路径仍包括 core direct `codex-api` 以及经 MCP/model-provider-api/otel/response-debug-context/rmcp-client 等真实 API runtime 或 adapter 边界，说明本切片只剥离 request DTO/helper，不宣称移除 core 的 API client dependency。
    - `rtk just bazel-lock-check`：通过，只有既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 `ResponseEvent` stream event DTO 切片：`ResponseEvent` 从 `codex-api/src/common.rs` 下沉到 `codex-api-types`，`codex-api` 只 re-export 旧路径并继续拥有 `ResponseStream`、`ApiError`、SSE/WebSocket parser、HTTP transport 和 endpoint runtime。core 中 `client_common` 和相关 tests 改为直接依赖 `codex-api-types::ResponseEvent`，不再为了匹配 response stream event 从 `codex-api` 取该 DTO。
    - `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-api-types`：通过，3 个 tests 通过，覆盖 text controls/request helper；`ResponseEvent` 是纯 enum DTO，无独立行为测试。
    - `rtk cargo check -p codex-api`：通过，验证 re-export shim 和 API parser/runtime 编译；`rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings，验证组合根仍能编译。
    - `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 仍只有 `codex-protocol`、`serde` 和 `serde_json`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-api-types --edges normal | rtk rg -q "codex-api v|codex-client v|reqwest v|tokio v|tokio-tungstenite|eventsource-stream|tungstenite"; then exit 1; else exit 0; fi'`：通过，确认新增 `ResponseEvent` 没有让 API types crate 间接拉回完整 `codex-api`、HTTP client stack、tokio runtime、SSE/WebSocket runtime 或 tungstenite。
    - `rtk sh -lc 'if rtk rg -n "codex_api::ResponseEvent|use codex_api::ResponseEvent" core/src core/tests; then exit 1; else exit 0; fi'`：通过，确认 core 不再从 `codex-api` 旧路径获取 `ResponseEvent`。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 3`：剩余路径仍包括 core direct `codex-api` 以及经 MCP/model-provider-api/otel/response-debug-context/rmcp-client 等真实 API runtime 或 adapter 边界，说明本切片只剥离 stream event DTO，不宣称移除 core 的 API client dependency。
    - `rtk just bazel-lock-check`：通过；`rtk git diff --check`：通过。
    - 已完成 API provider/auth 基础边界切片：新增 `codex-client-types` 承载 HTTP client `Request`/body/compression/`PreparedRequestBody`、`Response`、`TransportError`/`StreamError` 和 retry policy 类型；`codex-client` 改为 re-export 这些类型并继续拥有 reqwest transport、SSE stream、custom CA、retry executor 和 default client runtime。
    - 新增 `codex-api-provider` 承载 `Provider`、`RetryConfig`、`AuthProvider`/`SharedAuthProvider`/`AuthProviderFuture`、`AuthError`、auth header telemetry、session header helper 和 Azure endpoint detection；`codex-api` 改为 re-export 旧路径并继续拥有 endpoint clients、`ResponseStream`、SSE/WebSocket parser、HTTP transport、file upload 和 runtime。`ApiError` 后续已下沉到 `codex-api-types`，`codex-api` 只保留兼容 re-export 和 rate-limit conversion。
    - `model-provider-api` 和 `model-provider` 中只需要 provider config/auth-header adapter 的路径已改为直接依赖 `codex-api-provider`，core 中 `Provider`/`AuthProvider`/session header helper 也改为直接依赖 `codex-api-provider`；`codex-api` direct dependency 在 core 中仍用于真实 API client/runtime。
    - `AuthProvider` 新边界使用 boxed future (`AuthProviderFuture`) 保持 trait object 可用，未新增 `async_trait`；Bedrock SigV4 auth 和 codex-api clients integration test 中的 custom auth override 已改为显式 boxed future。
    - `rtk cargo fmt -p codex-client-types -p codex-client -p codex-api-provider -p codex-api -p codex-model-provider-api -p codex-model-provider -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-client-types`：通过，2 个 request body preparation tests 通过；`rtk cargo check -p codex-client`：通过；`rtk cargo test -p codex-client`：通过，25 个 tests 通过。
    - `rtk cargo test -p codex-api-provider`：通过，2 个 auth telemetry/Azure endpoint tests 通过；`rtk cargo check -p codex-api`：通过；`rtk cargo test -p codex-api --test clients`：通过，8 个 integration tests 通过。
    - `rtk cargo check -p codex-model-provider-api`：通过；`rtk cargo test -p codex-model-provider-api`：通过，3 个 tests 通过；`rtk cargo test -p codex-model-provider`：通过，29 个 tests 通过。
    - `rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused import warning；`rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-client-types --depth 1 --edges normal`：direct deps 为 `bytes`、`http`、`serde`、`serde_json`、`thiserror`、`tracing` 和 `zstd`；`rtk sh -lc 'if rtk cargo tree -p codex-client-types --edges normal | rtk rg -q "codex-client v|codex-api v|reqwest v|tokio v|tokio-tungstenite|eventsource-stream|tungstenite"; then exit 1; else exit 0; fi'`：通过。
    - `rtk cargo tree -p codex-api-provider --depth 1 --edges normal`：direct deps 为 `codex-client-types`、`codex-protocol`、`http`、`thiserror` 和 `url`；`rtk sh -lc 'if rtk cargo tree -p codex-api-provider --edges normal | rtk rg -q "codex-api v|codex-client v|reqwest v|tokio v|tokio-tungstenite|eventsource-stream|tungstenite"; then exit 1; else exit 0; fi'`：通过。
    - `rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal`：direct deps 显示 `codex-api-provider`，不再显示 full `codex-api` 或 full `codex-client`；`rtk sh -lc 'if rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal | rtk rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite"; then exit 1; else exit 0; fi'`：通过。
    - `rtk cargo tree -p codex-model-provider-api --invert codex-api --edges normal --depth 4`：剩余 full `codex-api` 路径只有 `codex-model-provider-api -> codex-login -> codex-otel -> codex-api`，说明本切片已清掉 provider/auth adapter 直接 API runtime 边界，但 login/auth stack 仍是下一步需要拆的真实间接路径。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 3`：`codex-api -> codex-model-provider-api -> codex-core` 路径消失；剩余仍包括 core direct API runtime、MCP/rmcp-client、otel/login 和 response-debug-context 等真实后续边界。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 auth env telemetry DTO owner 修正：`AuthEnvTelemetryMetadata` 从 `codex-otel` 下沉到 `codex-auth-types`，`codex-otel` 继续 re-export 旧路径；`codex-login::AuthEnvTelemetry::to_otel_metadata()` 改为直接返回 auth-types DTO，`codex-login` manifest 移除 `codex-otel` normal dependency。
    - `rtk cargo fmt -p codex-auth-types -p codex-otel -p codex-login -p codex-model-provider-api -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-auth-types`：通过；`rtk cargo check -p codex-login`：通过；`rtk cargo check -p codex-otel`：通过。
    - `rtk sh -lc 'if rtk cargo tree -p codex-model-provider-api --edges normal | rtk rg -q "codex-api v|codex-otel v"; then exit 1; else exit 0; fi'`：通过，确认 `model-provider-api` normal graph 不再经 login/auth telemetry 拉回 full `codex-api` 或 `codex-otel`。
    - `rtk sh -lc 'if rtk cargo tree -p codex-login --depth 1 --edges normal | rtk rg -q "codex-otel"; then exit 1; else exit 0; fi'`：通过，确认 `codex-login` direct normal graph 不再依赖 `codex-otel`。
    - `rtk cargo tree -p codex-model-provider-api --invert codex-api --edges normal --depth 5`：Cargo 返回 package 不在 graph，说明上一个切片记录的 `model-provider-api -> codex-login -> codex-otel -> codex-api` 间接路径已消失。
    - `rtk cargo test -p codex-model-provider-api`：通过，3 个 tests 通过；`rtk cargo check -p codex-core`：通过，仅打印既有 `MIN_YIELD_TIME_MS` unused import warning；`rtk cargo check -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription/core warnings。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 3`：不再显示 `codex-login` 或 `codex-model-provider-api` 路径；剩余为 core direct API runtime、MCP/rmcp-client、otel direct/agent-runtime/core-plugins/core-skills/rollout/windows-sandbox 和 response-debug-context 等后续真实边界。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成旁路 `SharedAuthProvider`/MCP metrics 切片：`backend-client` 和 `app-server-transport` 不再经 `codex-model-provider` 兼容 re-export 获取 `auth_provider_from_auth` / `unauthenticated_auth_provider`，改为直接依赖 `codex-model-provider-api`；`codex-mcp` 不再为了 duration metric 依赖 full `codex-otel`，新增 `codex-metrics-api` 承载 `MetricsSink` 和全局 duration helper，`codex-otel` 在安装 global metrics client 时同步注册 facade。
    - `rtk cargo fmt -p codex-metrics-api -p codex-mcp -p codex-otel -p codex-backend-client -p codex-app-server-transport`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-metrics-api -p codex-backend-client -p codex-app-server-transport -p codex-mcp -p codex-otel`：通过。
    - `rtk sh -lc 'for p in codex-backend-client codex-cloud-tasks-client codex-rmcp-client codex-app-server-transport codex-mcp; do if rtk cargo tree -p "$p" --edges normal | rtk rg -q "codex-api v"; then printf "%s pulls codex-api\n" "$p"; exit 1; else printf "%s ok\n" "$p"; fi; done'`：通过，确认这些只需要 auth/provider handle 或 MCP runtime helpers 的 crate 不再间接拉回 full `codex-api`。
    - `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：当时 direct graph 只有 `codex-metrics-api` 自身；后续 `StatsigMetricsSettings` DTO 下沉后该 crate 还会显示 `serde`。`rtk sh -lc 'for heavy in codex-api codex-otel; do if rtk cargo tree -p codex-mcp --edges normal | rtk rg -q "$heavy v"; then printf "codex-mcp pulls %s\n" "$heavy"; exit 1; else printf "codex-mcp no %s\n" "$heavy"; fi; done'`：通过，确认 MCP runtime 不再为了 metrics 拉 OTEL/API runtime。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 3`：`codex-mcp`/`codex-rmcp-client` 路径已从 full API 反向图消失；剩余为 core direct API runtime、`codex-otel` direct/agent-runtime/core-plugins/core-skills/rollout/windows-sandbox 路径和 response-debug-context，属于后续真实边界。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已扩展 metrics facade 切片：`MetricsSink` 增加 counter/histogram，`SessionTelemetry` 实现该 trait 以保留 per-session metadata tags；`core-skills` 的 skill render/injection metrics 改为接收 `&dyn MetricsSink`，不再依赖 full `codex-otel`。`agent-runtime` 昵称池 reset metric 和 `core-plugins` curated plugins startup sync metric 也改为 `codex-metrics-api` global counter。
    - rollout DB telemetry 已改为 metrics facade：`sqlite_telemetry_recorder` 接收 `Arc<dyn MetricsSink>`，core 仍在 `otel_init` 组合根用 concrete OTEL metrics client 注入；rollout backfill duration 用 `GlobalDurationTimer`，DB parse/backfill counters 用 global metrics helper。`codex-rollout` normal graph 不再依赖 `codex-otel`。
    - `rtk cargo fmt -p codex-metrics-api -p codex-agent-runtime -p codex-core-plugins -p codex-core-skills -p codex-otel -p codex-core` 和 `rtk cargo fmt -p codex-metrics-api -p codex-otel -p codex-rollout -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-metrics-api -p codex-agent-runtime -p codex-core-plugins -p codex-core-skills -p codex-otel -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；`rtk cargo check -p codex-metrics-api -p codex-otel -p codex-rollout -p codex-core`：通过，同样仅打印该既有 warning。
    - `rtk sh -lc 'for p in codex-agent-runtime codex-core-skills codex-core-plugins codex-rollout; do for heavy in codex-otel codex-api; do if rtk cargo tree -p "$p" --edges normal | rtk rg -q "$heavy v"; then printf "%s pulls %s\n" "$p" "$heavy"; exit 1; else printf "%s no %s\n" "$p" "$heavy"; fi; done; done'`：通过，确认这些 metrics-only consumers 不再间接拉回 OTEL 或 full API runtime。
    - `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：当时 direct deps 只有 `codex-utils-string`；后续 `StatsigMetricsSettings` DTO 下沉后还会显示 `serde`。`rtk sh -lc 'if rtk cargo tree -p codex-metrics-api --edges normal | rtk rg -q "codex-otel v|codex-api v|codex-client v|reqwest v|tokio v|tokio-tungstenite|eventsource-stream|codex-app-server-protocol"; then exit 1; else exit 0; fi'`：通过。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 3`：agent-runtime/core-plugins/core-skills/rollout 路径已从 full API 反向图消失；剩余为 core direct API runtime、core direct OTEL、windows-sandbox 经 OTEL 和 response-debug-context 四类真实后续边界。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 response debug context 切片：`codex-response-debug-context` 不再依赖 full `codex-api`，只依赖 `codex-client-types::TransportError` 解析 HTTP debug headers 和 transport error telemetry message；`ApiError` 到 debug context / telemetry message 的 adapter 移到 `codex-api::error` 并由 `codex-api` re-export，core 改为从 `codex-api` 引用 API-specific adapter。
    - `rtk cargo fmt -p codex-response-debug-context -p codex-api -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-response-debug-context`：通过，3 个 tests 通过；`rtk cargo test -p codex-api error::tests`：通过，3 个 tests 通过、123 个 filtered out。
    - `rtk cargo check -p codex-api -p codex-core`：通过；仅打印既有 core unused/dead_code warnings。
    - `rtk cargo tree -p codex-response-debug-context --depth 1 --edges normal`：direct deps 为 `base64`、`codex-client-types`、`http` 和 `serde_json`；`rtk sh -lc 'if rtk cargo tree -p codex-response-debug-context --edges normal | rtk rg -q "codex-api v|codex-client v|reqwest v|tokio v|tokio-tungstenite|eventsource-stream|tungstenite"; then exit 1; else exit 0; fi'`：通过。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 4`：response-debug-context 路径已从 full API 反向图消失；剩余只有 core direct API runtime、core direct OTEL 和 windows-sandbox 经 OTEL 三类路径。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 `codex-otel -> codex-api` 切片：`codex-otel` 的 Responses event telemetry 改为直接依赖 `codex-api-types::ResponseEvent`，websocket event telemetry 外层错误参数改为泛型 `Display`，不再绑定 `codex_api::ApiError`；`otel/tests/suite/runtime_summary.rs` 改用 `String` 作为 outer websocket error type 证明测试也不经 API runtime。
    - `rtk cargo fmt -p codex-otel -p codex-core -p codex-api`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo test -p codex-otel`：通过，42 个 tests 通过；`rtk cargo check -p codex-otel -p codex-windows-sandbox -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo tree -p codex-otel --depth 1 --edges normal`：direct deps 显示 `codex-api-types`，不再显示 `codex-api` 或 `codex-client`。
    - `rtk sh -lc 'for p in codex-otel codex-windows-sandbox; do if rtk cargo tree -p "$p" --edges normal | rtk rg -q "codex-api v|codex-client v"; then printf "%s pulls API/client runtime\n" "$p"; exit 1; else printf "%s ok\n" "$p"; fi; done'`：通过，确认 OTEL 和 windows-sandbox normal graph 不再经 OTEL 拉回 full API/client runtime。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 4`：full API 反向图现在只剩 `codex-api -> codex-core` direct API runtime；此前 core direct OTEL、windows-sandbox 经 OTEL、response-debug-context 等间接回流都已消失。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 transport telemetry summary 切片：`codex-api-types` 新增 `SseEventTelemetry` / `WebsocketEventTelemetry` 作为 transport-neutral summary DTO；`codex-api` 的 SSE/WebSocket runtime 在 poll 边界把 `eventsource_stream::Event`、`EventStreamError` 和 tungstenite `Message/Error` 归纳为 summary；core 的 `ApiTelemetry` 与 `codex-otel::SessionTelemetry` 只消费 summary，不再直接匹配 raw transport parser/runtime 类型。
    - `codex-core` production manifest 已移除 direct `eventsource-stream`、`reqwest` 和 `tokio-tungstenite`；`reqwest` / `tokio-tungstenite` 仅保留为 core dev-dependency 供 integration test helpers 使用。`codex-otel` manifest 已移除 `eventsource-stream` 和 `tokio-tungstenite`，只保留自身 OTLP exporter 需要的 `reqwest`。
    - `rtk cargo fmt -p codex-api-types -p codex-api -p codex-otel -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-api-types -p codex-api -p codex-otel -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。
    - `rtk cargo test -p codex-api telemetry::tests`：通过，4 个 summary tests 通过；`rtk cargo test -p codex-otel runtime_metrics_summary_collects_tool_api_and_streaming_metrics`：通过，1 个 filtered test 通过。
    - `rtk cargo test -p codex-core client --lib`：通过；`rtk cargo test -p codex-core --test all --no-run`：通过，确认 core dev-dependency 移动后测试目标仍能编译。
    - `rtk sh -lc 'set -e; if rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg -q "eventsource-stream|reqwest v|tokio-tungstenite"; then exit 1; fi; if rtk cargo tree -p codex-otel --edges normal | rtk rg -q "eventsource-stream|tokio-tungstenite"; then exit 1; fi; if rtk cargo tree -p codex-api-types --edges normal | rtk rg -q "eventsource-stream|reqwest v|tokio-tungstenite|eventsource_stream"; then exit 1; fi'`：通过，确认 `codex-core --depth 1` 不再显示 `eventsource-stream`、`reqwest` 或 `tokio-tungstenite` direct normal deps，`codex-otel` normal graph 不再拉 `eventsource-stream` / `tokio-tungstenite`，`codex-api-types` 不拉 transport runtime。
    - `rtk cargo tree -p codex-otel --depth 1 --edges normal`：显示 `codex-api-types` 和 OTLP exporter 需要的 `reqwest`，不再显示 `eventsource-stream` 或 `tokio-tungstenite`；`rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 仍只有 `codex-protocol`、`serde`、`serde_json`。
    - `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 4`：仍只剩 `codex-api -> codex-core` direct API runtime，没有新增 API indirect 回流。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 OpenAI file upload owner 切片：新增 `codex-openai-files`，承载 `upload_local_file`、`UploadedOpenAiFile`、`OpenAiFileError`、`openai_file_uri`、上传限制常量和文件上传 HTTP/custom-CA runtime；`core/src/mcp_openai_file.rs` 改为直接依赖该 crate，不再经 `codex_api::upload_local_file` 旁路获取文件上传 helper。
    - 为避免把 direct edge 伪装成 indirect edge，`codex-api` 已移除 `files` module 和 `upload_local_file` re-export，manifest 也不依赖 `codex-openai-files`；否则 core 依赖 full API client 时会经 `codex-api -> codex-openai-files` 间接拉回文件上传 runtime。
    - `rtk cargo fmt -p codex-openai-files -p codex-api -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-openai-files -p codex-api -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo test -p codex-openai-files`：通过，1 个 upload/local file runtime test 通过。`rtk cargo test -p codex-core openai_file --lib`：通过，覆盖 core MCP OpenAI file argument rewrite 路径。
    - `rtk sh -lc 'set -e; if rtk cargo tree -p codex-openai-files --edges normal | rtk rg -q "codex-api v|codex-core v|codex-otel v|codex-mcp v|codex-model-provider v|codex-app-server-protocol"; then exit 1; fi; if rtk cargo tree -p codex-api --edges normal | rtk rg -q "codex-openai-files v"; then exit 1; fi; if rtk rg -n "codex_api::upload_local_file|use codex_api::upload_local_file|pub use .*upload_local_file" core/src core/tests codex-api/src; then exit 1; fi'`：通过，确认新文件上传 crate 不拉回 full API/core/OTEL/MCP/app-server protocol，`codex-api` 也不再拉文件上传 runtime，core 中无旧 API 文件上传路径。
    - `rtk cargo tree -p codex-core --invert codex-openai-files --edges normal --depth 3`：只显示 `codex-openai-files -> codex-core` direct path；`rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 4`：仍只剩 `codex-api -> codex-core` direct API runtime，没有新增 API indirect 回流。
    - `rtk cargo tree -p codex-openai-files --depth 1 --edges normal`：direct deps 为 `codex-api-provider`、`codex-client`、`http`、`reqwest`、`serde`、`serde_json`、`thiserror`、`tokio`、`tokio-util`、`tracing`，这是文件上传 runtime 自身需要的 HTTP/custom CA 边界，不包含 full `codex-api`。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 Windows sandbox OTEL feature gate 切片：`StatsigMetricsSettings` 从 `codex-otel` 下沉到 `codex-metrics-api`，`codex-otel` 继续 re-export；`codex-windows-sandbox` 默认 feature `wfp-otel` 保留 standalone setup helper 的 WFP Statsig metric emission，`codex-core` 依赖 `codex-windows-sandbox` 时关闭默认 feature，避免 core 经 Windows setup helper 间接拉回 full `codex-otel`。
    - `rtk cargo fmt -p codex-metrics-api -p codex-otel -p codex-windows-sandbox -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - `rtk cargo check -p codex-windows-sandbox --no-default-features`：通过；`rtk cargo check -p codex-windows-sandbox`：通过，验证默认 setup helper feature 仍能编译；`rtk cargo check -p codex-metrics-api -p codex-otel -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：现在只显示 `codex-otel -> codex-core` direct SessionTelemetry path，旧的 `codex-otel -> codex-windows-sandbox -> codex-core` 间接路径已消失。
    - `rtk cargo tree -p codex-windows-sandbox --no-default-features --invert codex-otel --edges normal --depth 3`：Cargo 返回 package 不在 graph；`rtk cargo tree -p codex-windows-sandbox --no-default-features --depth 1 --edges normal`：direct deps 显示 `codex-metrics-api`，不显示 `codex-otel`；默认 feature 下 `rtk cargo tree -p codex-windows-sandbox --depth 1 --edges normal` 仍显示 `codex-otel`，说明 standalone setup helper 默认 metrics 行为保留。
    - `rtk cargo tree -p codex-core --depth 1 --edges normal`：确认 core 仍直接依赖 `codex-otel`，这是 `SessionTelemetry` 主路径，下一步应通过 session telemetry facade/trait 继续拆，而不是经 Windows sandbox 间接拉回。
    - 已完成 metrics API tag source 切片：`ToolDecisionSource` 从 `codex-otel` 下沉到 `codex-metrics-api`，并手写 `Display` 避免给 API crate 增加 proc-macro dependency；`codex-otel` 继续 re-export 旧路径，`core/src/tools/orchestrator.rs` 改为直接依赖 `codex_metrics_api::ToolDecisionSource`。
    - metrics API tag source 下沉后执行 `rtk cargo fmt -p codex-metrics-api -p codex-otel -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - metrics API tag source 下沉后执行 `rtk cargo check -p codex-metrics-api -p codex-otel -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - metrics API tag source 下沉后执行 `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：direct deps 只有 `codex-utils-string` 和 `serde`；`rtk cargo tree -p codex-metrics-api --invert codex-otel --edges normal` 返回 package not in graph，确认没有间接拉回 OTEL。
    - metrics API tag source 下沉后执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct SessionTelemetry path，没有新增 indirect path。
    - metrics API tag source 下沉后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已清理 core 中仅为 metric tag sanitization 使用 `codex_otel` re-export 的路径：`core/src/mcp_tool_call.rs` 和 `core/src/windows_sandbox.rs` 改为直接依赖 `codex_utils_string::sanitize_metric_tag_value`。
    - metric tag sanitization cleanup 后执行 `rtk cargo fmt -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。`rtk cargo check -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。`rtk rg -n "use codex_otel::sanitize_metric_tag_value|codex_otel::sanitize_metric_tag_value" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果。`rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct SessionTelemetry path。
    - 已完成 metrics name constants 下沉切片：`codex-otel/src/metrics/names.rs` 中纯 metric name 常量全部迁到 `codex-metrics-api`，`codex-otel` 继续 re-export 旧路径；core 中仅消费 metric constants 的 import 已改为直接依赖 `codex_metrics_api`，不再为了常量语义依赖 OTEL。
    - metrics name constants 下沉后执行 `rtk cargo fmt -p codex-metrics-api -p codex-otel -p codex-core`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - metrics name constants 下沉后执行 `rtk cargo check -p codex-metrics-api -p codex-otel -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。
    - metrics name constants 下沉后执行 `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：direct deps 仍只有 `codex-utils-string` 和 `serde`；`rtk cargo tree -p codex-metrics-api --invert codex-otel --edges normal` 返回 package not in graph，确认没有间接拉回 OTEL。
    - metrics name constants 下沉后执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct SessionTelemetry path；`rtk rg -n "use codex_otel::.*_METRIC|codex_otel::[A-Z_]+_METRIC" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果。
    - metrics name constants 下沉后执行 `rtk just bazel-lock-check`：通过；`rtk git diff --check`：通过。
    - 已完成 telemetry auth mode 下沉切片：`TelemetryAuthMode` 从 `codex-otel` 下沉到 `codex-auth-types`，`codex-otel` 继续 re-export 旧路径；core、app-server memories runtime、TUI app-server session 和 model-provider models endpoint 改为直接依赖 `codex_auth_types::TelemetryAuthMode`。`codex-otel` 因此移除不再使用的 direct `strum_macros` dependency。
    - telemetry auth mode 下沉后执行 `rtk cargo fmt -p codex-auth-types -p codex-otel -p codex-core -p codex-app-server -p codex-model-provider -p codex-tui`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - telemetry auth mode 下沉后执行 `rtk cargo check -p codex-auth-types -p codex-otel -p codex-core -p codex-model-provider -p codex-app-server --bin codex-app-server -p codex-tui`：通过。
    - telemetry auth mode 下沉后执行 `rtk cargo tree -p codex-auth-types --invert codex-otel --edges normal`：Cargo 返回 package not in graph；`rtk cargo tree -p codex-otel --depth 1 --edges normal` 不再显示 `strum_macros`；`rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4` 仍只显示 direct SessionTelemetry path。
    - telemetry auth mode 下沉后执行 `rtk rg -n "use codex_otel::TelemetryAuthMode|codex_otel::TelemetryAuthMode" codex-rs/core/src codex-rs/core/tests codex-rs/app-server/src codex-rs/tui/src codex-rs/model-provider/src -g '*.rs'`：无结果，确认这些产品/test 路径不再为了该 enum import `codex_otel`。
    - telemetry auth mode 下沉后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 realtime session DTO 下沉切片：`RealtimeEventParser`、`RealtimeSessionMode`、`RealtimeSessionConfig` 进入 `codex-api-types`，`codex-api` 继续 re-export 旧路径并保留 realtime websocket client/parser/writer runtime；core 的 realtime conversation/client 路径改为直接依赖 `codex-api-types`，`RealtimeAudioFrame` 和 `RealtimeEvent` 改为直接使用 `codex_protocol::protocol`。
    - realtime DTO 下沉后执行 `rtk rg -n "use codex_api::Realtime(AudioFrame|Event|EventParser|SessionConfig|SessionMode)|codex_api::Realtime(AudioFrame|Event|EventParser|SessionConfig|SessionMode)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，确认 core 不再通过 `codex-api` re-export 使用这些纯 realtime DTO/payload 类型。
    - realtime DTO 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - realtime DTO 下沉后执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；`rtk cargo test -p codex-api-types`：通过，3 个 tests 通过。
    - realtime DTO 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 仍只有 `codex-protocol`、`serde` 和 `serde_json`；`rtk sh -lc 'cargo tree -p codex-api-types --edges normal | rg -n "codex-api v"; test $? -eq 1'`：通过，确认 API types crate 没有间接拉回 full `codex-api`。
    - realtime DTO 下沉后执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 4`：仍只显示 `codex-api -> codex-core` direct API runtime，没有新增 API indirect 回流。
    - realtime DTO 下沉后执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：确认 core 当前仍 direct 依赖 `codex-api`、`codex-otel`、`codex-exec-server`、`codex-execpolicy`、`codex-mcp`、`codex-network-proxy`、`codex-rollout` 等 runtime/implementation 边界，继续作为 step 5 后续拆分对象。
    - 已完成 core global metrics facade 切片：`core/src/exec.rs` 和 `core/src/windows_sandbox.rs` 中仅用于 best-effort counter/duration 的 `codex_otel::global()` 调用已改为 `codex_metrics_api::record_global_counter` / `record_global_duration`，保留原 metric name 和 tag 语义；concrete OTEL global client 不再从 core 的这些 metrics-only callsite 暴露。
    - core global metrics facade 切片后执行 `rtk rg -n "codex_otel::global\\(|let _ = metrics\\.counter|metrics\\.record_duration" codex-rs/core/src/exec.rs codex-rs/core/src/windows_sandbox.rs`：无结果；`rtk rg -n "codex_otel::global\\(" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，确认 core 不再直接获取 concrete global OTEL metrics client。
    - core global metrics facade 切片后执行 `rtk cargo fmt -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - core global metrics facade 切片后执行 `rtk cargo check -p codex-metrics-api -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。
    - core global metrics facade 切片后执行 `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：direct deps 仍只有 `codex-utils-string` 和 `serde`；`rtk sh -lc 'cargo tree -p codex-metrics-api --edges normal | rg -n "codex-otel v|codex-api v|codex-client v|reqwest v|tokio v"; test $? -eq 1'`：通过，确认 metrics facade 没有间接拉回 OTEL/API/client/runtime。
    - core global metrics facade 切片后执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct path；该 direct edge 当前由 `SessionTelemetry`、OTEL init 和 trace context helper 维持，后续应拆 session telemetry/trace context facade。
    - 已完成 core turn timer concrete type cleanup：`state::RunningTask` 不再公开持有 `codex_otel::Timer`，改为 boxed `Send + Sync` drop guard；创建点仍使用 `SessionTelemetry::start_timer(TURN_E2E_DURATION_METRIC, ...)`，保留 session metadata tags 和 drop-record 行为，只隐藏 concrete OTEL timer 类型。
    - core turn timer cleanup 后执行 `rtk rg -n "codex_otel::Timer|codex_otel::global\\(" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，确认 core 不再直接引用 concrete global metrics client 或 concrete OTEL timer 类型。
    - core turn timer cleanup 后执行 `rtk cargo fmt -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - core turn timer cleanup 后执行 `rtk cargo check -p codex-metrics-api -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。
    - core turn timer cleanup 后执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct path；没有新增间接 OTEL 回流。
    - 已完成 metrics-only helper `MetricsSink` 收口切片：`emit_unified_exec_tty_metric`、`emit_turn_network_proxy_metric`、`emit_turn_memory_metric` 和 `emit_feature_metrics` 的 helper 签名改为接收 `&dyn codex_metrics_api::MetricsSink`；调用点继续传入 `SessionTelemetry` 以保留 per-session metadata tags，但 helper 本身不再绑定 concrete `codex_otel::SessionTelemetry`。
    - metrics-only helper 收口后执行 `rtk cargo fmt -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - metrics-only helper 收口后执行 `rtk cargo check -p codex-metrics-api -p codex-core`：通过，仅打印既有 core unused/dead_code warnings，之前 `core/src/tasks/mod.rs` 的 `SessionTelemetry` unused import 已清理。
    - metrics-only helper 收口后执行 `rtk rg -n "fn emit_(turn_network_proxy|turn_memory|feature|unified_exec_tty)_metric|MetricsSink|SessionTelemetry" codex-rs/core/src/tasks/mod.rs codex-rs/core/src/session/session.rs codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs`：确认四个 helper 均使用 `MetricsSink`，`SessionTelemetry` 只剩 session 构造和调用传参。
    - metrics-only helper 收口后执行 `rtk rg -n "codex_otel::(global|Timer)|use codex_otel::SessionTelemetry" codex-rs/core/src/exec.rs codex-rs/core/src/windows_sandbox.rs codex-rs/core/src/tasks/mod.rs codex-rs/core/src/session/session.rs codex-rs/core/src/tools/handlers/unified_exec/exec_command.rs codex-rs/core/src/state -g '*.rs'`：只剩 `core/src/state/service.rs` 的真实 telemetry boundary import。
    - metrics-only helper 收口后执行 `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：direct deps 仍只有 `codex-utils-string` 和 `serde`；`rtk sh -lc 'cargo tree -p codex-metrics-api --edges normal | rg -n "codex-otel v|codex-api v|codex-client v|reqwest v|tokio v"; test $? -eq 1'`：通过，确认 metrics facade 没有间接拉回 OTEL/API/client/runtime。
    - metrics-only helper 收口后执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct path；该 direct edge 当前由 `SessionTelemetry`、OTEL init 和 trace context helper 维持。
    - 继续复核 core 剩余 OTEL 使用面：`SessionTelemetry` 仍承载 request telemetry、tool result log/trace event、tool decision event、startup phase event、runtime metrics snapshot 和 W3C trace context helper；这些不是单纯 metric DTO。下一步不能把它们草率塞进 `codex-metrics-api`，需要单独设计 session telemetry / trace context facade，否则只是把 `codex-otel` direct edge 伪装成不完整 indirect boundary。
    - 继续复核 core 剩余 `codex-exec-server` 使用面：已清理的 FS re-export 之外，core 仍直接使用 environment/process runtime（`EnvironmentManager`、`Environment`、`ExecProcess`、`StartedExecProcess`）以及 exec-server JSON-RPC protocol DTO（`ExecParams`、`ExecEnvPolicy`、`ReadResponse`、`WriteResponse`、`WriteStatus`）。可作为下一小切片的是拆 `codex-exec-server` protocol DTO / `ProcessId` 到轻量 protocol crate，exec-server 继续 re-export，core 对 remote process DTO 改用轻量 crate；但这不会删除 core 的真实 environment/process runtime direct edge，后续仍需 RuntimeServices/trait 边界处理。
    - 已完成 exec-server protocol DTO 下沉切片：新增 `codex-exec-server-protocol` 承载 `ProcessId`、exec/read/write/terminate JSON-RPC DTO、output notification、filesystem protocol DTO、executor-side HTTP request DTO、method 常量和 `ByteChunk` base64 wrapper；`codex-exec-server` 保留旧路径 re-export，但 `codex-core` 和 `codex-rmcp-client` 中纯 protocol DTO callsite 改为直接依赖新 crate。`codex-core` 对 `codex-exec-server` 的剩余生产依赖仍是真实 `EnvironmentManager`/`Environment`/`ExecProcess`/`StartedExecProcess` runtime boundary。
    - exec-server protocol DTO 下沉后执行 `rtk cargo fmt -p codex-exec-server-protocol -p codex-exec-server -p codex-rmcp-client -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - exec-server protocol DTO 下沉后执行 `rtk cargo check -p codex-exec-server-protocol -p codex-exec-server -p codex-rmcp-client -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import 等 warning。
    - exec-server protocol DTO 下沉后执行 `rtk cargo test -p codex-exec-server-protocol`：通过，1 个 protocol DTO 反序列化测试通过。
    - exec-server protocol DTO 下沉后执行 `rtk cargo tree -p codex-exec-server-protocol --depth 1 --edges normal`：direct deps 为 `base64`、`codex-file-system`、`codex-protocol`、`codex-utils-absolute-path`、`serde` 和 `serde_json`；不依赖 exec-server runtime。
    - exec-server protocol DTO 下沉后执行 `rtk sh -lc 'cargo tree -p codex-exec-server-protocol --edges normal | rg -n "codex-exec-server v|reqwest v|tokio v|axum v|tokio-tungstenite v|codex-mcp v|codex-model-provider v|codex-login v"; test $? -eq 1'`：通过，确认新 protocol crate 没有间接拉回 exec-server、HTTP async runtime、MCP、model-provider 或 login implementation。
    - exec-server protocol DTO 下沉后执行 `rtk rg -n "codex_exec_server::(ExecParams|ExecEnvPolicy|ReadResponse|WriteResponse|WriteStatus|ProcessOutputChunk|ExecOutputStream|ByteChunk|HttpRequest|InitializeParams|ProcessId|ReadParams|WriteParams|TerminateParams|HttpHeader)" codex-rs/core/src codex-rs/core/tests codex-rs/rmcp-client/src -g '*.rs'`：无结果，确认 core/rmcp-client 不再通过 exec-server re-export 消费 protocol DTO。
    - exec-server protocol DTO 下沉后执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 4`：剩余路径仍为 `codex-core` direct runtime boundary，以及 `codex-apply-patch`、`codex-mcp`、`codex-rmcp-client` runtime 路径；不再包含 core 纯 protocol DTO 通过 exec-server re-export 的理由。
    - exec-server protocol DTO 下沉后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 `codex-apply-patch` FS re-export 清理切片：`codex-apply-patch` 从 `codex-exec-server` 改为直接依赖 `codex-file-system`，所有 `ExecutorFileSystem`、`LOCAL_FS`、`FileSystemSandboxContext`、`CreateDirectoryOptions` 和 `RemoveOptions` callsite 不再通过 exec-server re-export。apply-patch 只需要 unsandboxed local FS/trait/options，不需要 environment/process/remote executor runtime。
    - apply-patch FS 清理后执行 `rtk cargo fmt -p codex-apply-patch --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - apply-patch FS 清理后执行 `rtk cargo check -p codex-apply-patch -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import 等 warning。
    - apply-patch FS 清理后执行 `rtk cargo tree -p codex-apply-patch --depth 1 --edges normal`：direct deps 显示 `codex-file-system`，不再显示 `codex-exec-server`。
    - apply-patch FS 清理后执行 `rtk sh -lc 'cargo tree -p codex-apply-patch --edges normal | rg -n "codex-exec-server v|reqwest v|tokio-tungstenite v|axum v|codex-mcp v|codex-model-provider v|codex-login v"; test $? -eq 1'`：通过，确认 apply-patch 没有间接拉回 exec-server/HTTP runtime/MCP/model-provider/login implementation。
    - apply-patch FS 清理后执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 4`：`codex-apply-patch` 分支已消失，剩余为 `codex-core` direct environment/process runtime boundary 和 MCP/RMCP runtime 路径。
    - apply-patch FS 清理后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 OTEL config validator 下沉切片：`validate_otel_span_attributes`、`validate_otel_tracestate_entries`、`validate_otel_tracestate_member` 进入 `codex-config-types`，使用无 OpenTelemetry runtime 依赖的 ASCII/W3C grammar 校验；`codex-otel` 继续 re-export 旧 `validate_span_attributes` / `validate_tracestate_*` 路径，provider/init 内部也复用同一套 helper。core config 清洗路径改为直接依赖 `codex_config_types` validator，不再为了过滤 span attributes/tracestate 配置调用 `codex_otel`。
    - OTEL config validator 下沉后执行 `rtk cargo fmt -p codex-config-types -p codex-otel -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - OTEL config validator 下沉后执行 `rtk cargo test -p codex-config-types otel`：通过，4 个 validator tests 通过；`rtk cargo test -p codex-core load_config_drops_invalid_otel_trace_metadata`：通过，确认现有 core config warning/filter 行为仍覆盖。
    - OTEL config validator 下沉后执行 `rtk cargo check -p codex-config-types -p codex-otel -p codex-core`：通过，仅打印既有 core unused/dead_code warnings。
    - OTEL config validator 下沉后执行 `rtk rg -n "codex_otel::validate_span_attributes|codex_otel::validate_tracestate|use codex_otel::validate" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，确认 core 不再通过 OTEL runtime crate 调用 config validator。
    - OTEL config validator 下沉后执行 `rtk cargo tree -p codex-config-types --depth 1 --edges normal`：direct deps 仍只有 `codex-utils-absolute-path`、`schemars`、`serde`、`serde_json`、`thiserror`、`toml` 和 `ts-rs`；`rtk sh -lc 'cargo tree -p codex-config-types --edges normal | rg -n "codex-otel v|opentelemetry|tracing-opentelemetry|reqwest v|tokio v"; test $? -eq 1'`：通过，确认 config-types 没有为 validator 间接拉回 OTEL/OpenTelemetry/runtime。
    - OTEL config validator 下沉后执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct path；没有新增间接 OTEL 回流。
    - `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 config schema CLI 拆分切片：新增 `codex-config-schema` package 承载 `codex-write-config-schema` bin，默认写入路径仍指向 `core/config.schema.json`；`just write-config-schema` 改为运行新 package；`codex-core` 删除 `[[bin]]` 和 normal `clap` dependency，不再为 schema 维护工具携带 CLI parser 依赖。
    - config schema CLI 拆分后执行 `rtk cargo fmt -p codex-config-schema -p codex-core --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - config schema CLI 拆分后执行 `rtk cargo check -p codex-config-schema -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - config schema CLI 拆分后执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct graph 已不再显示 `clap`。
    - config schema CLI 拆分后执行 `rtk cargo tree -p codex-config-schema --depth 1 --edges normal`：direct deps 只有 `anyhow`、`clap` 和 `codex-config-toml`。
    - config schema CLI 拆分后执行 `rtk cargo run -p codex-config-schema --bin codex-write-config-schema -- --out /tmp/codex-config-schema-check.json`：通过，生成 142.9K schema 文件，不修改仓内 fixture。
    - config schema CLI 拆分后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings。
    - 已完成 execpolicy CLI 拆分切片：新增 `codex-execpolicy-cli` package 承载 `ExecPolicyCheckCommand` 和 `codex-execpolicy` bin；`codex-cli execpolicy check` 改为依赖新 CLI crate；`codex-execpolicy` library 删除 CLI command re-export、`[[bin]]`、`clap`、`serde` 和 `serde_json` normal dependency，只保留 Starlark parser/error display 和 API type re-export。
    - execpolicy CLI 拆分后执行 `rtk cargo fmt -p codex-execpolicy-cli -p codex-execpolicy -p codex-cli --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - execpolicy CLI 拆分后执行 `rtk cargo check -p codex-execpolicy-cli -p codex-execpolicy -p codex-cli -p codex-core`：通过，仅打印既有 file-subscription dead_code 和 core `MIN_YIELD_TIME_MS` unused import warning。
    - execpolicy CLI 拆分后执行 `rtk cargo tree -p codex-execpolicy --depth 1 --edges normal`：direct deps 只有 `anyhow`、`codex-execpolicy-api`、`codex-utils-absolute-path`、`multimap`、`shlex`、`starlark` 和 `thiserror`，不再显示 `clap`、`serde` 或 `serde_json`。
    - execpolicy CLI 拆分后执行 `rtk cargo tree -p codex-execpolicy-cli --depth 1 --edges normal`：CLI crate direct deps 为 `anyhow`、`clap`、`codex-execpolicy`、`codex-execpolicy-api`、`serde` 和 `serde_json`。
    - execpolicy CLI 拆分后执行 `rtk cargo tree -p codex-core --invert codex-execpolicy --edges normal --depth 6`：仍只显示 `codex-execpolicy -> codex-core`，说明 core 剩余路径是 parser/runtime direct boundary；执行 `rtk sh -lc 'cargo tree -p codex-core --invert clap --edges normal --depth 6 | rg "codex-execpolicy"'` 和 `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg "codex-execpolicy-cli"'` 均无输出，确认本切片没有把 CLI crate 或 execpolicy 的 clap 依赖间接拉回 core。`clap` 仍通过 `codex-file-search`/`codex-rollout`、`codex-network-proxy`/`codex-sandboxing`/`codex-exec-server`、`codex-shell-escalation` 和 `codex-state` 间接存在，归入后续切片。
    - execpolicy CLI 拆分后执行 `rtk cargo run -p codex-execpolicy-cli --bin codex-execpolicy -- check --help`：通过，二进制入口仍暴露 `check --rules <PATH> <COMMAND>...`、`--pretty` 和 `--resolve-host-executables`。
    - execpolicy CLI 拆分后执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 state logs CLI 拆分切片：新增 `codex-state-cli` package 承载 `codex-state-logs` bin、`clap` parser、`dirs` home lookup 和 `owo-colors` terminal formatting；`codex-state` runtime crate 删除 `clap`、`dirs`、`owo-colors` normal dependency，只保留 SQLite state/runtime/model/log DB 能力。
    - state logs CLI 拆分后执行 `rtk rustfmt --check state-cli/src/main.rs`：通过；`rtk cargo fmt -p codex-state -p codex-state-cli -p codex-core --check` 被未触碰的既有 `state/src/runtime/threads.rs` formatting diff 挡住，因此未改 unrelated dirty code。
    - state logs CLI 拆分后执行 `rtk cargo check -p codex-state-cli -p codex-state -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - state logs CLI 拆分后执行 `rtk cargo tree -p codex-state --depth 1 --edges normal`：direct deps 为 `anyhow`、`chrono`、`codex-protocol`、`log`、`serde`、`serde_json`、`sqlx`、`strum`、`tokio`、`tracing`、`tracing-subscriber` 和 `uuid`，不再显示 `clap`、`dirs` 或 `owo-colors`。
    - state logs CLI 拆分后执行 `rtk cargo tree -p codex-state-cli --depth 1 --edges normal`：CLI crate direct deps 为 `anyhow`、`chrono`、`clap`、`codex-state`、`dirs`、`owo-colors` 和 `tokio`。
    - state logs CLI 拆分后执行 `rtk sh -lc 'cargo tree -p codex-core --invert clap --edges normal --depth 6 | rg "codex-state"'` 和 `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg "codex-state-cli"'`：均无输出；`rtk cargo tree -p codex-core --invert clap --edges normal --depth 6` 当前只剩 `codex-file-search -> codex-rollout`、`codex-network-proxy -> codex-core` / `codex-sandboxing` / `codex-exec-server` 和 `codex-shell-escalation -> codex-core` 路径。
    - state logs CLI 拆分后执行 `rtk cargo run -p codex-state-cli --bin codex-state-logs -- --help`：通过，二进制入口仍暴露 `--codex-home` / `--db` / `--level` / `--from` / `--to` / `--module` / `--file` / `--thread-id` / `--search` / `--threadless` / `--backfill` / `--poll-ms` / `--compact`。
    - state logs CLI 拆分后执行 `rtk cargo test -p codex-state-cli`：通过，3 个测试通过；`rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 file-search CLI 拆分切片：新增 `codex-file-search-cli` package 承载 `codex-file-search` bin、`clap` parser、stdout reporter、JSON output formatting 和 no-pattern `ls`/`cmd` fallback；`codex-file-search` 删除 `[[bin]]`、`Cli` re-export、`run_main`、`clap`、`serde_json` 和 `tokio` normal dependency，只保留 search session/runtime/options/match DTO。
    - file-search CLI 拆分后执行 `rtk rustfmt --check file-search/src/lib.rs file-search-cli/src/main.rs`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - file-search CLI 拆分后执行 `rtk cargo check -p codex-file-search-cli -p codex-file-search -p codex-rollout -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - file-search CLI 拆分后执行 `rtk cargo tree -p codex-file-search --depth 1 --edges normal`：direct deps 为 `anyhow`、`crossbeam-channel`、`ignore`、`nucleo` 和 `serde`，不再显示 `clap`、`serde_json` 或 `tokio`。
    - file-search CLI 拆分后执行 `rtk cargo tree -p codex-file-search-cli --depth 1 --edges normal`：CLI crate direct deps 为 `anyhow`、`clap`、`codex-file-search`、`serde_json` 和 `tokio`。
    - file-search CLI 拆分后执行 `rtk sh -lc 'cargo tree -p codex-core --invert clap --edges normal --depth 6 | rg "codex-file-search|codex-rollout"'` 和 `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg "codex-file-search-cli"'`：均无输出；`rtk cargo tree -p codex-core --invert clap --edges normal --depth 6` 当前只剩 `codex-network-proxy -> codex-core` / `codex-sandboxing` / `codex-exec-server` 和 `codex-shell-escalation -> codex-core` 路径。
    - file-search CLI 拆分后执行 `rtk cargo run -p codex-file-search-cli --bin codex-file-search -- --help`：通过，二进制入口仍暴露 `[PATTERN]`、`--json`、`--limit`、`--cwd`、`--compute-indices`、`--threads` 和 `--exclude`。
    - file-search CLI 拆分后执行 `rtk cargo test -p codex-file-search -p codex-file-search-cli`：通过，15 个测试通过；`rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 shell-escalation CLI 拆分切片：新增 `codex-shell-escalation-cli` package 承载 `codex-execve-wrapper` bin、`clap` parser 和 stderr tracing subscriber setup；`codex-shell-escalation` 删除 `[[bin]]`、`main_execve_wrapper` export、`execve_wrapper` module、`clap` 和 `tracing-subscriber` normal dependency，只保留 escalation protocol/server/client/runtime。
    - shell-escalation CLI 拆分后执行 `rtk rustfmt --check shell-escalation/src/lib.rs shell-escalation/src/unix/mod.rs shell-escalation-cli/src/main.rs`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - shell-escalation CLI 拆分后执行 `rtk cargo check -p codex-shell-escalation-cli -p codex-shell-escalation -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。
    - shell-escalation CLI 拆分后执行 `rtk cargo tree -p codex-shell-escalation --depth 1 --edges normal`：direct deps 为 `anyhow`、`async-trait`、`codex-protocol`、`codex-utils-absolute-path`、`libc`、`serde`、`serde_json`、`socket2`、`tokio`、`tokio-util` 和 `tracing`，不再显示 `clap` 或 `tracing-subscriber`。
    - shell-escalation CLI 拆分后执行 `rtk cargo tree -p codex-shell-escalation-cli --depth 1 --edges normal`：CLI crate direct deps 为 `anyhow`、`clap`、`codex-shell-escalation`、`tokio` 和 `tracing-subscriber`。
    - shell-escalation CLI 拆分后执行 `rtk sh -lc 'cargo tree -p codex-core --invert clap --edges normal --depth 6 | rg "codex-shell-escalation"'` 和 `rtk sh -lc 'cargo tree -p codex-core --edges normal | rg "codex-shell-escalation-cli"'`：均无输出；`rtk cargo tree -p codex-core --invert clap --edges normal --depth 6` 当前只剩 `codex-network-proxy -> codex-core` / `codex-sandboxing` / `codex-exec-server` 路径。
    - shell-escalation CLI 拆分后执行 `rtk cargo run -p codex-shell-escalation-cli --bin codex-execve-wrapper -- --help`：通过，二进制入口仍暴露 `<FILE> [ARGV]...`。
    - shell-escalation CLI 拆分后执行 `rtk cargo test -p codex-shell-escalation -p codex-shell-escalation-cli`：通过，20 个测试通过；`rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings；`rtk git diff --check`：通过。
    - 已完成 network-proxy clap 清理切片：`codex-network-proxy` 中历史兼容的空 `Args` 类型保留为普通 `Debug + Clone` struct，不再 derive `clap::Parser`；runtime crate manifest 删除 `clap` normal dependency，不新增 CLI crate，因为当前仓内没有 `codex-network-proxy` 二进制入口或 `Args::parse` 调用。
    - network-proxy clap 清理后执行 `rtk cargo tree -p codex-network-proxy --depth 1 --edges normal`：direct deps 不再显示 `clap`；执行 `rtk cargo tree -p codex-core --invert clap --edges normal --depth 6`：返回 `warning: nothing to print`，确认 `clap` 不再经 `codex-network-proxy` / `codex-sandboxing` / `codex-exec-server` 间接回流到 core。
    - network-proxy clap 清理后执行 `rtk rustfmt --check network-proxy/src/proxy.rs && rtk cargo check -p codex-network-proxy -p codex-sandboxing -p codex-exec-server -p codex-core`：通过；执行 `rtk cargo test -p codex-network-proxy`：134 个测试通过；执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings。
    - 已完成 network-proxy constraints DTO 下沉切片：`NetworkProxyConstraints`、`PartialNetworkConfig`、`PartialNetworkProxyConfig` 和 `NetworkProxyConstraintError` 进入 `codex-network-proxy-api`；`codex-network-proxy` 继续 re-export 旧路径。当时 `build_config_state`、`validate_policy_against_constraints`、`NetworkProxyState`、`ConfigReloader`、MITM/globset 编译和 `NetworkProxy` handle 仍留在 Rama-backed runtime crate；后续 step 5 已继续把 `validate_policy_against_constraints` 下沉到 API crate。
    - network-proxy constraints DTO 下沉后执行 `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：direct deps 仍只有 `serde`；执行 `rtk cargo tree -p codex-network-proxy --depth 1 --edges normal`：runtime crate 继续包含 Rama/globset/tokio 等 implementation 依赖；执行 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 6`：仍显示 core direct path 和 `sandboxing/exec-server` runtime path，后续需要 runtime facade，不应继续伪装为 DTO 下沉。
    - network-proxy constraints DTO 下沉后执行触碰文件 `rtk rustfmt --check ...`：通过（仅打印 workspace rustfmt nightly-only `imports_granularity` warning）；执行 `rtk cargo check -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；执行 `rtk cargo test -p codex-network-proxy`：134 个测试通过。
    - 已完成 sandboxing/network-proxy runtime snapshot 切片：新增 `NetworkProxyRuntimeSnapshot` 到 `codex-network-proxy-api`，承载 proxy env 写入、proxy URL env helper、端口/unix socket/local binding 这类 sandbox policy 输入；`NetworkProxy::runtime_snapshot()` 在 runtime crate 内从真实 handle 生成 snapshot。`codex-sandboxing` 的 transform/seatbelt 边界改为只接收 snapshot，真实 `NetworkProxy` 继续留在 core exec/spawn runtime 边界。
    - sandboxing snapshot 切片后执行 `rtk cargo tree -p codex-sandboxing --depth 1 --edges normal`：direct deps 显示 `codex-network-proxy-api`，不再显示 Rama-backed `codex-network-proxy`；执行 `rtk cargo tree -p codex-sandboxing --invert codex-network-proxy --edges normal --depth 6`：返回 `warning: nothing to print`；执行 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 6`：当前只剩 `codex-network-proxy -> codex-core` direct path，原 `codex-sandboxing -> codex-exec-server -> codex-core` 间接路径已消失。
    - sandboxing snapshot 切片后执行 `rtk cargo test -p codex-sandboxing -p codex-network-proxy`：197 个测试通过；执行 `rtk cargo check -p codex-sandboxing -p codex-core`：通过，仅打印既有 core dead_code/unused warnings；执行 `rtk cargo tree -p codex-core --invert clap --edges normal --depth 6`：仍无路径；执行 `rtk just bazel-lock-check` 和 `rtk git diff --check`：通过。
    - 已完成 exec-server runtime capability API 切片：新增 `codex-exec-server-api` 承载 `ExecBackend`、`ExecProcess`、`StartedExecProcess`、`ExecProcessEvent`/event log/receiver、`HttpClient`、`HttpResponseBody` 和低依赖 `ExecRuntimeError`；`codex-exec-server` 负责实现并 re-export API traits，同时保留 `Environment`/`EnvironmentManager`、transport clients、rich `ExecServerError`、local/remote process implementation 和 remote executor runtime。
    - `codex-rmcp-client` 和 `codex-mcp` 已从 concrete `codex-exec-server` 改为依赖 `codex-exec-server-api` + `codex-exec-server-protocol`；`McpRuntimeEnvironment` 不再持有 concrete exec-server `Environment`，而是持有 host-provided exec backend、local/remote HTTP client 和 fallback cwd。core/app-server 作为组合根把 concrete `Environment` 投影成 MCP runtime environment params。
    - exec-server API 切片后执行 `rtk cargo fmt -p codex-exec-server-api -p codex-exec-server -p codex-rmcp-client -p codex-mcp -p codex-core -p codex-app-server --check`：通过；仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - exec-server API 切片后执行 `rtk cargo check -p codex-exec-server-api -p codex-exec-server -p codex-rmcp-client -p codex-mcp`：通过；执行 `rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server`：通过，0 errors，仅剩既有 file-subscription dead_code 和 core `MIN_YIELD_TIME_MS` unused import warning。
    - exec-server API 切片后执行 `rtk cargo test -p codex-exec-server-api`：初次暴露 test-only tokio feature 缺少 `macros`/`rt`/`time`；已在 `codex-rs/exec-server-api/Cargo.toml` 的 dev-dependency 中补齐，复跑通过，1 个 event log 测试通过。
    - exec-server API 切片后执行 `rtk cargo tree -p codex-rmcp-client --depth 1 --edges normal` 和 `rtk cargo tree -p codex-mcp --depth 1 --edges normal`：direct deps 显示 `codex-exec-server-api`，不再显示 concrete `codex-exec-server`。`rtk cargo tree -p codex-rmcp-client --invert codex-exec-server --edges normal --depth 6` 与 `rtk cargo tree -p codex-mcp --invert codex-exec-server --edges normal --depth 6` 均返回 package 不在 graph，说明 consumer graph 没有把 concrete exec-server 间接拉回。
    - exec-server API 切片后执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：剩余路径只为 `codex-exec-server -> codex-core` direct runtime boundary，MCP/RMCP 分支已消失；`rtk cargo tree -p codex-core --depth 1 --edges normal` 仍显示 core direct `codex-exec-server`，这是后续 environment/process runtime facade 要处理的真实组合根边界。
    - exec-server API 切片后执行 `rtk git diff --check`：通过；执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 继续收紧 core 对 exec-server re-export 的使用：`core/src/unified_exec/process.rs` 和 `process_tests.rs` 中的 `ExecProcess`、`StartedExecProcess`、`ExecProcessEventReceiver` 已改为直接从 `codex-exec-server-api` 导入；测试 mock 去掉 `async_trait`，按 API crate 的 boxed-future trait 形状实现，避免下一步拆 facade 时继续依赖 concrete crate 的兼容 re-export。
    - core process capability import 收紧后执行 `rtk cargo fmt -p codex-core --check`：通过；执行 `rtk cargo test -p codex-core remote_write_unknown_process_marks_process_exited`：通过，覆盖 remote process write error path 和 mock `ExecProcess` trait 实现。执行 `rtk rg -n "codex_exec_server::(ExecProcess|StartedExecProcess|ExecProcessEvent|ExecProcessEventReceiver|ExecBackend|HttpClient|HttpResponseBody|ExecRuntimeError)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无结果，确认 core 不再经 exec-server re-export 消费这些 capability 类型。
    - core process capability import 收紧后执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：仍只剩 `codex-exec-server -> codex-core` direct runtime boundary；执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-exec-server(-api|-protocol)? v"`：确认 core direct graph 中 concrete `codex-exec-server`、`codex-exec-server-api` 和 `codex-exec-server-protocol` 分工仍清晰，下一步要处理的是 direct `Environment`/`EnvironmentManager` runtime boundary。
    - 继续下沉 exec-server 轻量环境 API：`ExecServerRuntimePaths`、`LOCAL_ENVIRONMENT_ID` 和 `REMOTE_ENVIRONMENT_ID` 已移入 `codex-exec-server-api`，`codex-exec-server` 仅 re-export 兼容旧路径；旧 `exec-server/src/runtime_paths.rs` 已删除。core/app-server 只需要 runtime paths 或标准环境 id 的 callsite 改为直接依赖 API crate，`Environment`/`EnvironmentManager` 仍保留在 concrete implementation crate。
    - 继续下沉已选 environment capability：新增 `codex_exec_server_api::ExecEnvironment` trait，暴露 `is_remote`、`exec_server_url`、`local_runtime_paths`、host-provided exec backend/HTTP client/filesystem capability；`codex_exec_server::Environment` 实现该 trait。`TurnEnvironment`、`environment_selection::primary_environment`、unified exec request/runtime、MCP runtime helper 和 AGENTS.md loader 已改为接收 `Arc<dyn ExecEnvironment>` / `&dyn ExecEnvironment`，避免在只需要已选 environment capability 的路径继续传播 concrete `Environment`。`EnvironmentManager`、environment discovery/registry、transport client 和 concrete local/remote process implementation 仍留在 `codex-exec-server`，属于后续 provider/facade 边界。
    - exec-server 环境 API 下沉后执行 `rtk cargo fmt -p codex-exec-server-api -p codex-exec-server -p codex-core -p codex-app-server --check`：通过；执行 `rtk cargo check -p codex-exec-server-api -p codex-exec-server`：通过；执行 `rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server`：通过，0 errors，仅剩既有 file-subscription dead_code 和 core `MIN_YIELD_TIME_MS` unused import warning。执行 `rtk cargo test -p codex-exec-server-api`：通过，1 个测试通过。
    - exec-server 环境 API 下沉后执行 `rtk cargo tree -p codex-exec-server-api --depth 1 --edges normal`：direct deps 为 `codex-exec-server-protocol`、`codex-file-system`、`codex-utils-absolute-path`、`futures`、`thiserror`、`tokio`，不含 concrete exec-server；执行 `rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6`：package 不在 graph；执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：仍只剩 `codex-exec-server -> codex-core` direct runtime boundary。
    - exec-server 环境 API 下沉后执行 `rtk rg -n "codex_exec_server::(ExecServerRuntimePaths|LOCAL_ENVIRONMENT_ID|REMOTE_ENVIRONMENT_ID)|use codex_exec_server::(ExecServerRuntimePaths|LOCAL_ENVIRONMENT_ID|REMOTE_ENVIRONMENT_ID)" codex-rs/core/src codex-rs/core/tests codex-rs/app-server/src -g '*.rs'`：无结果，确认 core/app-server 不再经 concrete exec-server re-export 消费这些轻量环境 API。
    - exec-server 环境 API 下沉后执行 `rtk git diff --check`：通过；执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 复核 exec-server environment capability 切片：`rtk cargo fmt -p codex-exec-server-api -p codex-exec-server -p codex-core -p codex-app-server --check` 通过；`rtk cargo check -p codex-exec-server-api -p codex-exec-server` 通过；`rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server` 通过；`rtk cargo test -p codex-exec-server-api` 通过；`rtk cargo check -p codex-core --tests` 通过，仅剩既有 dead_code/unused warnings。
    - 2026-06-20 CST 复核拓扑门禁：`rtk cargo tree -p codex-exec-server-api --depth 1 --edges normal` 显示 direct deps 为 `codex-exec-server-protocol`、`codex-file-system`、`codex-utils-absolute-path`、`futures`、`thiserror`、`tokio`，无 concrete `codex-exec-server`；`rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6` 返回 package 不在 graph；`rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8` 仍只剩 `codex-exec-server -> codex-core` direct runtime boundary。`rtk rg -n "codex_exec_server::(ExecProcess|StartedExecProcess|ExecProcessEvent|ExecProcessEventReceiver|ExecBackend|HttpClient|HttpResponseBody|ExecRuntimeError|ExecServerRuntimePaths|LOCAL_ENVIRONMENT_ID|REMOTE_ENVIRONMENT_ID)" codex-rs/core/src codex-rs/core/tests codex-rs/app-server/src -g '*.rs'` 无结果；`rtk git diff --check` 和 `rtk just bazel-lock-check` 通过。
    - 2026-06-20 CST 已完成 exec-server provider facade 第一刀：新增 `ExecEnvironmentProvider` trait，承载 default environment、ordered default ids、local fallback 和按 id 解析 environment 的 registry view；`EnvironmentManager` 实现该 trait并保留旧 concrete methods。`environment_selection`、`SessionServices`、`CodexSpawnArgs`、`Session::new` 和 core connectors accessible-tools helper 已改为接收 provider trait，`ThreadManagerState` 仍保留 concrete manager，因为 app-server 的 environment add/upsert、fs processor 和若干组合根调用还需要 concrete registry 管理能力。
    - provider facade 第一刀后执行 `rtk cargo fmt -p codex-exec-server-api -p codex-exec-server -p codex-core -p codex-app-server --check`：通过；执行 `rtk cargo check -p codex-exec-server-api -p codex-exec-server -p codex-core`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-exec-server-api`：均通过，仅剩既有 dead_code/unused warnings。
    - provider facade 第一刀后执行 `rtk cargo tree -p codex-exec-server-api --depth 1 --edges normal`：direct deps 仍为 `codex-exec-server-protocol`、`codex-file-system`、`codex-utils-absolute-path`、`futures`、`thiserror`、`tokio`；执行 `rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6`：package 不在 graph；执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：仍只剩 `codex-exec-server -> codex-core` direct runtime boundary。`rtk rg -n "Arc<EnvironmentManager>|&EnvironmentManager|environment_manager: Arc<EnvironmentManager>|use codex_exec_server::EnvironmentManager" codex-rs/core/src/session/mod.rs codex-rs/core/src/session/session.rs codex-rs/core/src/state/service.rs codex-rs/core/src/environment_selection.rs` 只剩 environment_selection 单元测试里的 fixture import；`rtk git diff --check` 通过。
    - 2026-06-20 CST 已完成 ThreadManager runtime state provider 化：`ThreadManagerState`、`ThreadManager::new(_with_workflow_runs)`、`CodexSpawnArgs` 和 test_support 的 environment registry 参数已改为 `Arc<dyn ExecEnvironmentProvider>`；app-server 在 `MessageProcessorArgs` 中继续持有 concrete `Arc<EnvironmentManager>`，并把 concrete manager 显式传给 `EnvironmentRequestProcessor`、`FsRequestProcessor`、Apps/Catalog/MCP/Plugin/Config processors。`ThreadManager` 只保留 `environment_provider()`，不再暴露 `environment_manager()` concrete getter；`SkillsWatcher` 改用 provider。core 生产路径中 concrete manager 只剩 `ThreadManager` 测试 helper、`prompt_debug`/`connectors` 自建 manager 和组合根构造边界，session/state 不再持有 concrete `EnvironmentManager`。
    - ThreadManager provider 化后执行 `rtk cargo fmt -p codex-core -p codex-app-server --check`：通过；执行 `rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-app-server --tests` 和 `rtk cargo test -p codex-exec-server-api`：均通过，仅剩既有 dead_code/unused warnings。执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：仍只剩 `codex-exec-server -> codex-core` direct runtime boundary；`rtk cargo tree -p codex-exec-server-api --depth 1 --edges normal` 仍不含 concrete exec-server；`rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6` 返回 package 不在 graph。`rtk rg -n "environment_manager\\(\\)" codex-rs/app-server/src codex-rs/core/src codex-rs/core/tests -g '*.rs'` 无结果；`rtk git diff --check` 通过。
    - 2026-06-20 CST 已完成 core connectors accessible 查询 provider 化：`core/src/connectors.rs` 不再通过 `Config.codex_home` 自建 `EnvironmentManager`，而是显式接收 `&dyn ExecEnvironmentProvider`；app-server Apps/Config/Plugin processors 和 TUI connector refresh 从组合根传入 provider，`mcp_tool_call` 的 accessible connector fallback 也复用当前 session provider。CLI debug sandbox 同步修正为把 concrete `NetworkProxy` 投影成 `NetworkProxyRuntimeSnapshot` 再传给 sandbox arg builder；TUI workflow event target 测试同步修正为从 app-server-protocol 根 re-export 导入 workflow DTO。
    - connectors provider 化后执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-tui -p codex-cli --check`：通过；执行 `rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server -p codex-tui -p codex-cli`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-app-server --tests`、`rtk cargo check -p codex-tui --tests` 和 `rtk cargo test -p codex-exec-server-api`：均通过，仅剩既有 dead_code/unused warnings。执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：仍只剩 `codex-exec-server -> codex-core` direct runtime boundary；`rtk cargo tree -p codex-exec-server-api --depth 1 --edges normal` direct deps 仍为 `codex-exec-server-protocol`、`codex-file-system`、`codex-utils-absolute-path`、`futures`、`thiserror`、`tokio`；`rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6` 返回 package 不在 graph。`rtk rg -n "list_accessible_connectors_from_mcp_tools_with_environment_manager|EnvironmentManager::from_codex_home|codex_app_server_protocol::v2" ...` 确认旧 connectors helper 和旧 protocol v2 路径无残留；仅剩的 `EnvironmentManager` 命中位于 app-server/TUI 组合根和测试 fixture。`rtk git diff --check` 通过。
    - 2026-06-20 CST 已完成 `prompt_debug` provider 化：`codex_core::build_prompt_input` 签名改为接收 `Arc<dyn ExecEnvironmentProvider>`，core 不再从 `Config.codex_home` 和 runtime paths 自建 `EnvironmentManager`；CLI `debug prompt-input` 作为组合根显式构造 `EnvironmentManager` 并传入 provider，prompt debug integration test 也显式创建 provider fixture。CLI 同时改为直接依赖 `codex-exec-server-api` 使用 `ExecServerRuntimePaths`，不再经 concrete exec-server re-export 消费该轻量 API。
    - prompt_debug provider 化后执行 `rtk cargo fmt -p codex-core -p codex-cli --check`：通过；执行 `rtk cargo check -p codex-core -p codex-cli` 和 `rtk cargo check -p codex-core --tests`：通过，仅剩既有 dead_code/unused warnings；执行 `rtk cargo test -p codex-core build_prompt_input_includes_context_and_user_message`：通过。执行 `rtk rg -n "EnvironmentManager::from_codex_home|use codex_exec_server::EnvironmentManager|ExecServerRuntimePaths" codex-rs/core/src/prompt_debug.rs codex-rs/core/src/connectors.rs codex-rs/core/src/mcp_tool_call.rs -g '*.rs'`：无结果；执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：仍只剩 `codex-exec-server -> codex-core` direct runtime boundary；执行 `rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6`：package 不在 graph；`rtk git diff --check` 通过。
    - 2026-06-20 CST 已完成 core concrete `codex-exec-server` normal edge 移除：`codex-core` 的 production MCP runtime environment 不再直接构造 `ReqwestHttpClient`，改为从 `ExecEnvironment` provider 边界传入 selected environment 与 local environment，保证 remote MCP 仍走 remote client、local streamable HTTP 仍走 local environment client。`codex-core` 的 `codex-exec-server` dependency 改为 optional `test-support` feature 和 dev-dependency，`ThreadManager::*_for_tests` helper gated 到 `cfg(test|feature = "test-support")`；普通 core normal graph 只依赖 `codex-exec-server-api` 和 `codex-exec-server-protocol`。
    - concrete exec-server edge 移除后执行 `rtk cargo fmt -p codex-core -p codex-cli --check`：通过；执行 `rtk cargo check -p codex-core`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p core_test_support` 和 `rtk cargo check -p codex-app-server --bin codex-app-server`：均通过，仅剩既有 unused/dead_code warnings。执行 `rtk sh -lc 'tree=$(cargo tree -p codex-core --depth 1 --edges normal); printf "%s\n" "$tree" | rg "codex-exec-server(-api|-protocol)? v"; if printf "%s\n" "$tree" | rg -q "codex-exec-server v"; then exit 1; fi'`：只显示 `codex-exec-server-api` 和 `codex-exec-server-protocol`。执行 `rtk cargo tree -p codex-core --invert codex-exec-server --edges normal --depth 8`：warning nothing to print；执行 `rtk cargo tree -p codex-exec-server-api --invert codex-exec-server --edges normal --depth 6`：package 不在 graph。`rtk rg -n "ReqwestHttpClient|use codex_exec_server::EnvironmentManager|codex_exec_server::ExecServerRuntimePaths" codex-rs/core/src codex-rs/core/tests -g '*.rs'` 只剩 cfg-test/test fixture concrete manager imports，无 production `ReqwestHttpClient`；`rtk git diff --check` 通过。
    - 2026-06-20 CST 已完成 OpenAI file upload provider 化：新增 `codex-openai-files-api` 承载 `UploadedOpenAiFile`、`OpenAiFileUploader`、`SharedOpenAiFileUploader` 和 disabled uploader；`codex-openai-files` 保留 reqwest/custom-CA 上传 runtime 并实现 `ReqwestOpenAiFileUploader`。`SessionServices`、`CodexSpawnArgs` 和 `ThreadManagerState` 只持有 `Arc<dyn OpenAiFileUploader>`；app-server、mcp-server 和 core test-support 组合根显式注入真实 uploader，core 普通构造和测试 fixture 使用 disabled uploader。`core/src/mcp_openai_file.rs` 不再直接调用 concrete `upload_local_file`，而是走 session service trait。
    - OpenAI file provider 化后执行 `rtk cargo check -p codex-openai-files-api -p codex-openai-files -p codex-core`：通过，仅剩既有 core unused/dead_code warnings；执行 `rtk cargo check -p codex-core --tests`：通过，仅剩既有 warnings；执行 `rtk cargo check -p codex-app-server --bin codex-app-server -p codex-mcp-server -p core_test_support`：通过，仅剩既有 file-subscription/core warnings；执行 `rtk cargo test -p codex-core openai_file`：通过；执行 `rtk cargo fmt -p codex-openai-files-api -p codex-openai-files -p codex-core -p codex-app-server -p codex-mcp-server -p core_test_support --check`：通过，仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warnings；执行 `rtk git diff --check`：通过。
    - OpenAI file provider 化后执行 `rtk cargo tree -p codex-openai-files-api --depth 1 --edges normal`：direct deps 只有 `codex-api-provider` 和 `serde`，不含 `reqwest`、`codex-client`、`codex-api`、core、MCP 或 app-server protocol；执行 `rtk cargo tree -p codex-core --invert codex-openai-files --edges normal --depth 6`：warning nothing to print；执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-openai-files"`：只显示 `codex-openai-files-api`。
    - 2026-06-20 CST 已完成 feedback request tags API 切片：新增 `codex-feedback-api` 承载 `FeedbackRequestTags`、`emit_feedback_request_tags` 和 `emit_feedback_request_tags_with_auth_env`；带 auth env helper 改为接收 `codex_auth_types::AuthEnvTelemetryMetadata`，避免 API crate 依赖 `codex-login`。`codex-feedback` 继续拥有 ring buffer、metadata layer、Sentry upload、diagnostics runtime 和 `tracing-subscriber`，并 re-export 新 API 保持旧路径兼容；`codex-core` 和 `codex-model-provider` 生产路径改为直接依赖 `codex-feedback-api`。
    - feedback API 切片后执行 `rtk cargo check -p codex-feedback-api -p codex-feedback -p codex-core -p codex-model-provider`：通过，仅剩既有 core `MIN_YIELD_TIME_MS` unused import warning；执行 `rtk cargo test -p codex-core emit_feedback_request_tags`：通过；执行 `rtk cargo fmt -p codex-feedback-api -p codex-feedback -p codex-core -p codex-model-provider --check`：通过，仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warnings；执行 `rtk git diff --check`：通过。
    - feedback API 切片后执行 `rtk cargo tree -p codex-feedback-api --depth 1 --edges normal`：direct deps 只有 `codex-auth-types` 和 `tracing`，不含 `codex-login`、`sentry`、`tracing-subscriber`、`codex-protocol` 或 core；执行 `rtk cargo tree -p codex-core --invert codex-feedback --edges normal --depth 6` 和 `rtk cargo tree -p codex-model-provider --invert codex-feedback --edges normal --depth 6`：均返回 package 不在 graph；执行 `rtk cargo tree -p codex-core --depth 1 --edges normal` 与 `rtk cargo tree -p codex-model-provider --depth 1 --edges normal`：direct graph 均显示 `codex-feedback-api`，不显示 heavy `codex-feedback`。
    - 2026-06-20 CST 已完成 W3C trace context helper 切片：新增 `codex-trace-context` 承载 current span trace id、W3C trace carrier、traceparent/tracestate validation、environment traceparent 恢复和 set-parent helper；`codex-otel` 继续 re-export 这些 helper 兼容旧路径，并在 provider init 中使用 tracestate helper。`codex-core` 的 session/client/test trace propagation callsite 改为直接依赖 `codex-trace-context`，不再为了 trace helper 绑定 full OTEL runtime。
    - trace context 切片后执行 `rtk cargo check -p codex-trace-context -p codex-otel -p codex-core`、`rtk cargo test -p codex-core trace_context` 和 `rtk cargo test -p codex-trace-context`：均通过；执行 `rtk cargo fmt -p codex-trace-context -p codex-otel -p codex-core --check`、`rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过。
    - trace context 切片后执行 `rtk cargo tree -p codex-trace-context --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-protocol`、`opentelemetry`、`opentelemetry_sdk`、`tracing` 和 `tracing-opentelemetry`；执行 `rtk cargo tree -p codex-trace-context --invert codex-otel --edges normal --depth 6`：package 不在 graph；执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 4`：仍只显示 `codex-otel -> codex-core` direct path，该 direct edge 当前主要由 `SessionTelemetry` 和 OTEL init 保留。
    - 2026-06-20 CST 已完成 `SessionTelemetry` API 面收缩小切片：删除未使用的 `SessionTelemetry::log_request` reqwest-bound helper，并移除 `session_telemetry.rs` 中仅为该 helper 存在的 `reqwest::Response/Error` 与 `std::future::Future` import。API request telemetry 继续由 core/client adapter 归纳出 status/error/duration/auth/recovery fields 后调用 `record_api_request`；本切片不声称移除 `codex-core -> codex-otel` direct edge，只减少后续 session telemetry facade 需要兼容的 concrete transport 方法。
    - SessionTelemetry API 面收缩后执行 `rtk cargo fmt -p codex-otel --check`：通过；执行 `rtk cargo check -p codex-otel`：通过，31 个 crate 编译；执行 `rtk sh -lc 'if rg -n "log_request\\(|use reqwest::(Error|Response)|std::future::Future" otel/src/events/session_telemetry.rs; then exit 1; else exit 0; fi'`：通过，确认该文件无 reqwest-bound helper 残留。执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 5`：仍只显示 `codex-otel -> codex-core` direct path；执行 `rtk cargo tree -p codex-otel --depth 1 --edges normal`：direct deps 未新增，`reqwest` 仍由 OTLP exporter runtime 保留。
    - 2026-06-20 CST 已完成 OTEL init 组合根拆分：新增 `codex-otel-init` 承载 `OtelProviderConfig`、`build_provider`、`record_process_start`、`install_sqlite_telemetry` 和 `codex_export_filter`。该 crate 依赖 `codex-otel`、`codex-rollout`、`codex-state`、`codex-config-types` 和 `codex-default-client`，但不依赖 `codex-core`。app-server、TUI、exec、mcp-server、app-server-test-client 和 app-server-client legacy re-export 已改为使用 `codex-otel-init`；`codex_core::otel_init` 模块已删除。
    - OTEL init 拆分后执行 `rtk cargo check -p codex-otel-init -p codex-core -p codex-app-server --bin codex-app-server -p codex-mcp-server -p codex-exec -p codex-tui -p codex-app-server-client -p codex-app-server-test-client`：通过，仅剩既有 file-subscription/core warnings；执行 `rtk cargo check -p codex-app-server --tests` 和 `rtk cargo check -p codex-mcp-server --tests`：均通过，仅剩既有 warnings；执行 `rtk cargo fmt -p codex-otel-init -p codex-core -p codex-app-server -p codex-mcp-server -p codex-exec -p codex-tui -p codex-app-server-client -p codex-app-server-test-client --check`、`rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过。
    - OTEL init 拆分后执行 `rtk cargo tree -p codex-otel-init --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-default-client`、`codex-otel`、`codex-rollout`、`codex-state` 和 `tracing`；执行 `rtk sh -lc 'out=$(cargo tree -p codex-otel-init --invert codex-core --edges normal --depth 8 2>&1 || true); ...'`：返回 `codex-core did not match any packages`，说明新 crate normal graph 不含 core；执行 `rtk sh -lc 'out=$(cargo tree -p codex-core --invert codex-otel-init --edges normal --depth 8 2>&1 || true); ...'`：返回 `codex-otel-init did not match any packages`，说明 core normal graph 不含新 init crate；执行 `rtk sh -lc 'tree=$(cargo tree -p codex-core --depth 1 --edges normal); ...'`：只显示 `codex-otel`，不显示 `codex-otel-init`；执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 5`：仍只显示 `codex-otel -> codex-core` direct path，该 direct edge 现在主要由 `SessionTelemetry` 保留。
    - 2026-06-20 CST 已完成 runtime metrics DTO 下沉切片：`RuntimeMetricTotals` / `RuntimeMetricsSummary` 进入 `codex-metrics-api`，`codex-otel` 继续 re-export 旧路径；OpenTelemetry snapshot 解析函数留在 `codex-otel`，避免 `metrics-api` 依赖 OpenTelemetry SDK。TUI runtime metrics 展示和测试改为直接依赖 `codex-metrics-api`，不再为了 totals/summary DTO 绑定 full OTEL runtime。本切片不声称 TUI 或 core 完全脱离 OTEL，TUI 仍因 `SessionTelemetry`、`codex-otel-init`、model-provider 等真实运行时路径依赖 `codex-otel`。
    - runtime metrics DTO 下沉后执行 `rtk cargo fmt -p codex-metrics-api -p codex-otel -p codex-tui --check`：通过，仍打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-metrics-api -p codex-otel -p codex-tui`：通过；执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs crate annotation warning。
    - runtime metrics DTO 下沉后执行 `rtk cargo tree -p codex-metrics-api --depth 1 --edges normal`：direct deps 只有 `codex-utils-string` 和 `serde`；执行 `rtk sh -lc 'if cargo tree -p codex-metrics-api --edges normal | rg -q "codex-otel v|opentelemetry|reqwest v|tokio v|codex-api v"; then exit 1; else exit 0; fi'`：通过，确认 metrics API 没有间接拉回 OTEL/OpenTelemetry/runtime/API；执行 `rtk rg -n "codex_otel::RuntimeMetric|codex_otel::RuntimeMetrics|use codex_otel::Runtime" codex-rs -g '*.rs'`：只剩 `codex-otel` 自身兼容 re-export 测试。
    - 2026-06-20 CST 已完成 execpolicy loader provider 化切片：新增 `codex-execpolicy-loader` 作为组合根 Starlark rules loader，依赖 `codex-core` 的 `ExecPolicyLoader` trait、`codex-execpolicy` parser 和 `codex-execpolicy-api` policy model；`codex-core` 的生产路径只保留 `ExecPolicyManager` runtime evaluator、amendment update 和 `ExecPolicyLoader`/`ExecPolicyLoadResult` trait DTO，`codex-execpolicy` 从 core normal dependency 降为 dev-dependency。app-server、mcp-server、exec/app-server-client legacy boundary 和 network proxy loader 均通过组合根注入 `StarlarkExecPolicyLoader`；core tests 使用 test-only loader 保留 parser 覆盖。
    - execpolicy loader 切片后执行 `rtk cargo fmt -p codex-core -p codex-execpolicy-loader -p codex-app-server -p codex-mcp-server -p codex-exec -p codex-app-server-client --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。执行 `rtk cargo check -p codex-execpolicy-loader -p codex-core`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-app-server --bin codex-app-server -p codex-mcp-server -p codex-exec -p codex-app-server-client`：均通过，仅剩既有 unused/dead_code warnings。执行 `rtk cargo test -p codex-core exec_policy --lib`：通过。
    - execpolicy loader 切片后执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct normal deps 显示 `codex-execpolicy-api`，不再显示 `codex-execpolicy`。执行 `rtk cargo tree -p codex-core --invert codex-execpolicy --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert starlark --edges normal --depth 8`：均返回 `warning: nothing to print`，确认 core normal graph 不再间接拉回 parser implementation 或 Starlark。执行 `rtk cargo tree -p codex-execpolicy-loader --depth 1 --edges normal`：direct deps 为 `codex-config-state`、`codex-config-types`、`codex-core`、`codex-execpolicy`、`codex-execpolicy-api`、`thiserror`、`tokio`、`tracing`；执行 `rtk cargo tree -p codex-app-server --invert starlark --edges normal --depth 8`：Starlark 路径变为 `starlark -> codex-execpolicy -> codex-execpolicy-loader -> codex-app-server`，不再经过 `codex-core`。
    - 2026-06-20 CST 已完成 network proxy constraints validator 下沉切片：`validate_policy_against_constraints` 和 constraint-only `DomainPattern`/pattern helper 迁移到 `codex-network-proxy-api`；`codex-network-proxy` 仅 re-export 兼容旧路径，runtime/state 继续保留 Rama backend、state builder、config reloader、proxy handle 和 host policy evaluation。core 的 `NetworkProxySpec` 与 `network_proxy_loader` 直接依赖 API validator，不再为了 validator-only 使用走 Rama-backed runtime crate。
    - network proxy validator 下沉后执行 `rtk cargo fmt -p codex-network-proxy-api -p codex-network-proxy -p codex-core --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。执行 `rtk cargo check -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过，仅剩既有 core unused/dead_code warnings。执行 `rtk cargo test -p codex-network-proxy validate_policy_against_constraints`：通过，16 个测试通过、118 个 filtered out。
    - network proxy validator 下沉后执行 `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：direct normal deps 只有 `serde`，不含 `url`、Rama、tokio 或 async-trait。执行 `rtk sh -lc 'tree=$(cargo tree -p codex-network-proxy-api --edges normal); printf "%s\n" "$tree"; if printf "%s\n" "$tree" | rg -q "codex-network-proxy v|rama-http|rama-core|rama-net|tokio v|async-trait|url v"; then exit 1; fi'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 6`：仍只显示 `codex-network-proxy -> codex-core` direct runtime boundary，未新增 indirect path。执行 `rtk rg -n "codex_network_proxy::validate_policy_against_constraints|use codex_network_proxy::validate_policy_against_constraints" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配，确认 core 生产路径没有旧 runtime validator import。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 `ApiError` 下沉到 API types 切片：新增 `codex-api-types/src/error.rs` 承载 `ApiError`、`extract_response_debug_context_from_api_error` 和 `telemetry_api_error_message`；`codex-api` 仅 re-export `ApiError` 并保留 `RateLimitError -> ApiError` conversion，继续拥有具体 endpoint clients、`ResponseStream`、HTTP/SSE/WebSocket transport 和 endpoint runtime。core 的 client/realtime conversation/tests 改为直接依赖 `codex-api-types::ApiError` 和 API error helper，不再为了 error debug context 或 telemetry-safe error message 走 full `codex-api` re-export。
    - `ApiError` 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core --check`：通过；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core`：通过，仅剩既有 core unused/dead_code warnings；执行 `rtk cargo test -p codex-api-types`：通过，覆盖 API error debug context 和 telemetry-safe message helper。
    - `ApiError` 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 为 `codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`，不含完整 `codex-api`、`codex-client`、reqwest、tokio、SSE/WebSocket runtime 或 tungstenite。执行 `rtk cargo tree -p codex-api-types --invert codex-api --edges normal --depth 6`：返回 package 不在 graph 中；执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6`：仍只显示 `codex-api -> codex-core` direct runtime boundary，说明本切片只是移除 error/helper 纯类型需求，后续仍需处理 concrete Responses/ChatCompletions/Realtime/Memories/Compact client runtime。
    - 2026-06-20 CST 已完成 `Compression` request option 下沉切片：`codex-api-types` 新增 `Compression`，`codex-api::requests` 和 crate root 保留旧路径 re-export；core 的 responses request compression selection 改为直接依赖 `codex-api-types::Compression`。本切片只迁移 request option 纯 enum，不改变 `ResponsesClient` runtime、request compression application 或 SSE transport 边界。
    - `Compression` 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo test -p codex-api-types`：通过，6 个测试通过；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core`：通过，仅剩既有 core unused/dead_code warnings。执行 `rtk rg -n "codex_api::Compression|use codex_api::Compression|pub enum Compression" codex-rs/core/src codex-rs/core/tests codex-rs/codex-api/src codex-rs/api-types/src -g '*.rs'`：只剩 `codex-rs/api-types/src/lib.rs` 中的 canonical enum 定义。
    - `Compression` 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 未新增，仍为 `codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过；执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。
    - 2026-06-20 CST 已完成 compact/memory payload DTO 下沉切片：`CompactionInput`、`MemorySummarizeInput`、`MemorySummarizeOutput`、`RawMemory` 和 `RawMemoryMetadata` 从 `codex-api::common` 迁移到 `codex-api-types`；`codex-api::common` 和 crate root 保留旧路径 re-export，endpoint compact/memories runtime 继续拥有 HTTP execution、serde_json encode/decode 和 endpoint path，core 改为直接依赖 `codex-api-types` 构造 payload。该切片不改变 `CompactClient` / `MemoriesClient` concrete runtime，只移除 core 为 compact/memory payload DTO 通过 full `codex-api` re-export 的需求。
    - compact/memory payload DTO 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo test -p codex-api-types`：通过，6 个测试通过；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core`：通过，仅剩既有 core unused/dead_code warnings。执行 `rtk rg -n "codex_api::(CompactionInput|MemorySummarizeInput|MemorySummarizeOutput|RawMemory)|use codex_api::(CompactionInput|MemorySummarizeInput|MemorySummarizeOutput|RawMemory)|struct CompactionInput|struct MemorySummarizeInput|struct MemorySummarizeOutput|struct RawMemory" codex-rs/core/src codex-rs/core/tests codex-rs/codex-api/src codex-rs/api-types/src -g '*.rs'`：只剩 `codex-rs/api-types/src/lib.rs` 中的 canonical DTO 定义。
    - compact/memory payload DTO 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 未新增，仍为 `codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过；执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。
    - 2026-06-20 CST 已完成 `ChatCompletionsPath` option enum 下沉切片：`ChatCompletionsPath` 从 `codex-api::endpoint::chat_completions` 迁移到 `codex-api-types`；`codex-api::endpoint` 和 crate root 保留旧路径 re-export，`ChatCompletionsClient` 继续用该 enum 选择 endpoint path，core 改为直接依赖 `codex-api-types::ChatCompletionsPath`。该切片不改变 chat completions concrete client、request body construction 或 response stream runtime。
    - `ChatCompletionsPath` 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo test -p codex-api-types`：通过，6 个测试通过；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core`：通过，仅剩既有 core unused/dead_code warnings。执行 `rtk rg -n "codex_api::ChatCompletionsPath|use codex_api::ChatCompletionsPath|pub enum ChatCompletionsPath|pub use .*ChatCompletionsPath" codex-rs/core/src codex-rs/core/tests codex-rs/codex-api/src codex-rs/api-types/src -g '*.rs'`：core 已无旧 full API import，只剩 `codex-api-types` canonical enum 定义和 `codex-api` 兼容 re-export。
    - `ChatCompletionsPath` 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 未新增，仍为 `codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过；执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。
    - 2026-06-20 CST 已完成 `RequestTelemetry` 下沉到 client types 切片：`RequestTelemetry` trait 从 full `codex-client` 迁移到 `codex-client-types`；`codex-client` 和 `codex-api` 继续 re-export 旧路径，`codex-api` endpoint/session telemetry path 改为直接依赖 `codex-client-types`。core 和 model-provider 中 request telemetry implementation 改为直接依赖 `codex-client-types::{RequestTelemetry, TransportError}`，不再为了 telemetry trait 或 transport error 从 `codex-api` re-export 获取这些类型。该切片不改变 reqwest transport、retry executor、SSE/WebSocket telemetry classification 或 API endpoint runtime。
    - `RequestTelemetry` 下沉后执行 `rtk cargo fmt -p codex-client-types -p codex-client -p codex-api -p codex-core -p codex-model-provider --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo test -p codex-client-types`：通过，2 个测试通过；执行 `rtk cargo check -p codex-client-types -p codex-client -p codex-api -p codex-core -p codex-model-provider`：通过，仅剩既有 core unused/dead_code warnings。
    - `RequestTelemetry` 下沉后执行 `rtk rg -n "codex_api::RequestTelemetry|use codex_api::RequestTelemetry|codex_client::RequestTelemetry|use codex_client::RequestTelemetry|pub trait RequestTelemetry|pub use .*RequestTelemetry" codex-rs -g '*.rs'`：canonical trait 只剩 `codex-rs/client-types/src/lib.rs`，`codex-client` / `codex-api` 保留兼容 re-export；执行 `rtk rg -n "codex_api::TransportError|use codex_api::TransportError" codex-rs/core codex-rs/model-provider -g '*.rs'`：无匹配，确认 core/model-provider 不再经 full `codex-api` re-export 获取 transport error。
    - `RequestTelemetry` 下沉后执行 `rtk cargo tree -p codex-client-types --depth 1 --edges normal`：direct deps 为 `bytes`、`http`、`serde`、`serde_json`、`thiserror`、`tracing` 和 `zstd`，未新增 full client/API/runtime；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-client-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-client v|codex-api v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6` 和 `rtk cargo tree -p codex-model-provider --invert codex-api --edges normal --depth 6`：均仍只显示 `codex-api -> consumer` direct runtime boundary，未新增 indirect path。
    - 2026-06-20 CST 已完成 SSE/WebSocket telemetry sink trait 下沉切片：`SseTelemetry` 和 `WebsocketTelemetry` 从 `codex-api::telemetry` 迁移到 `codex-api-types`，与 `SseEventTelemetry` / `WebsocketEventTelemetry` summary DTO 和 `ApiError` 共处轻量 API 边界；`codex-api::telemetry` 和 crate root 保留旧路径 re-export，transport event summarizer、SSE/WebSocket parser 和 runtime callback 调用继续留在 `codex-api`。core 的 `ApiTelemetry` 实现改为直接依赖 `codex-api-types` 的 sink trait，不再为了 telemetry sink trait 通过 full `codex-api` re-export。
    - SSE/WebSocket telemetry sink trait 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core -p codex-client-types -p codex-client -p codex-model-provider --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo test -p codex-api-types -p codex-client-types`：通过，8 个测试通过；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core -p codex-client-types -p codex-client -p codex-model-provider`：通过。
    - SSE/WebSocket telemetry sink trait 下沉后执行 `rtk rg -n "codex_api::SseTelemetry|use codex_api::SseTelemetry|codex_api::WebsocketTelemetry|use codex_api::WebsocketTelemetry|pub trait SseTelemetry|pub trait WebsocketTelemetry|pub use .*SseTelemetry|pub use .*WebsocketTelemetry" codex-rs/core/src codex-rs/core/tests codex-rs/codex-api/src codex-rs/api-types/src -g '*.rs'`：canonical traits 只剩 `codex-api-types`，`codex-api` 保留兼容 re-export，core 无旧 full API imports。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 未新增，仍为 `codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过。
    - 本轮 request/API telemetry trait 下沉后再次执行 `rtk cargo tree -p codex-client-types --depth 1 --edges normal` 和禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-client-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-client v|codex-api v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6` 和 `rtk cargo tree -p codex-model-provider --invert codex-api --edges normal --depth 6`：均仍只显示 `codex-api -> consumer` direct runtime boundary，未新增 indirect path。
    - 2026-06-20 CST 已完成 realtime/websocket response DTO 下沉切片：`RealtimeCallResponse`、`ResponsesWebsocketClose` 和 `ResponsesWebsocketProbe` 从 `codex-api` endpoint implementation 迁移到 `codex-api-types`；`codex-api::endpoint` 和 crate root 保留旧路径 re-export，Realtime call/WebSocket client 继续拥有 HTTP/WebSocket runtime、probe handshake、close-frame conversion 和 status parsing。该切片只迁移 response/probe shape，不改变 Realtime/WebSocket concrete runtime。
    - realtime/websocket response DTO 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo test -p codex-api-types`：通过，6 个测试通过；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core`：通过，仅剩既有 core unused/dead_code warnings。执行 `rtk rg -n "pub struct RealtimeCallResponse|pub struct ResponsesWebsocketClose|pub struct ResponsesWebsocketProbe|pub use .*RealtimeCallResponse|pub use .*ResponsesWebsocketClose|pub use .*ResponsesWebsocketProbe" codex-rs/api-types/src codex-rs/codex-api/src -g '*.rs'`：canonical DTO 定义只剩 `codex-api-types`，`codex-api` 保留兼容 re-export。
    - realtime/websocket response DTO 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 未新增，仍为 `codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 6`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。
    - 2026-06-20 CST 继续复核 step 5 剩余 heavy edge：`rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 5`、`--invert codex-api`、`--invert codex-mcp` 和 `--invert codex-thread-store` 均显示 direct-only `heavy -> codex-core` 路径；这些适合后续用 facade/provider 化逐个移除 direct edge。`rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 5` 仍显示 `codex-rollout -> codex-thread-store -> codex-core` 间接路径，因此 rollout 切片必须先处理 thread-store/rollout shared history 边界，不能只删 core direct dependency。
    - 2026-06-20 CST 已完成 `codex-utils-plugins` default-client 边界收紧切片：该 crate 只需要 originator 和 first-party chat originator 判断时改为直接依赖 `codex-default-client`，不再通过 `codex-login::default_client` 拉 full login/token/storage/server runtime。执行 `rtk cargo fmt -p codex-utils-plugins --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-utils-plugins`：通过。执行 `rtk cargo tree -p codex-utils-plugins --depth 1 --edges normal`：direct deps 显示 `codex-default-client`、`codex-file-system`、`codex-utils-absolute-path`、`serde` 和 `serde_json`，不再显示 `codex-login`；执行 `rtk cargo tree -p codex-utils-plugins --invert codex-login --edges normal --depth 6`：返回 package 不在 graph。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：`codex-utils-plugins` 路径已消失，剩余路径仍为 core direct、core-plugins/core-skills、codex-mcp、codex-model-provider-api 和 codex-rollout 等真实 auth/runtime 边界，后续不能只删 direct edge。
    - 2026-06-20 CST 已完成 `codex-mcp` Codex Apps auth provider 注入切片：`McpConnectionManager::new`、`read_mcp_resource` 和 `collect_mcp_server_status_snapshot_with_detail` 改为接收调用方预构造的 `SharedAuthProvider`；core/app-server 在组合根边界通过 `auth_provider_from_auth` 构造 host-owned Codex Apps MCP server 的 runtime auth provider。`codex-mcp` 不再为了该 auth adapter 直接依赖 `codex-model-provider-api`，但仍保留真实 `codex-login` dependency 用于 OAuth credential store / auth snapshot 相关 MCP runtime。
    - MCP auth provider 注入后执行 `rtk rg -n "codex_model_provider_api|auth_provider_from_auth" codex-rs/codex-mcp/src codex-rs/codex-mcp/Cargo.toml`：无匹配；执行 `rtk cargo fmt -p codex-mcp -p codex-core -p codex-app-server --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-mcp -p codex-core -p codex-app-server --bin codex-app-server`：通过，仅剩既有 core/file-subscription/app-server unused/dead_code warnings；执行 `rtk cargo check -p codex-mcp -p codex-core --tests`：通过，仅剩既有 warnings；执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - MCP auth provider 注入后执行 `rtk cargo tree -p codex-mcp --depth 1 --edges normal`：direct deps 显示 `codex-api-provider`、`codex-config-types`、`codex-exec-server-api`、`codex-login`、`codex-mcp-tool-types`、`codex-mcp-types`、`codex-metrics-api`、`codex-plugin-types`、`codex-protocol`、`codex-rmcp-client`、`codex-utils-plugins` 等，不再显示 `codex-model-provider-api`。执行 `rtk cargo tree -p codex-mcp --invert codex-model-provider-api --edges normal --depth 6`：返回 package ID 不在 graph，确认 `codex-mcp` 没有通过间接路径拉回 `codex-model-provider-api`。执行 `rtk cargo tree -p codex-core --invert codex-model-provider-api --edges normal --depth 8`：剩余路径为 `codex-core` direct、`codex-core-plugins` 和 `codex-core-skills`；`codex-mcp` 分支已消失。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：`codex-mcp -> codex-core` 仍是剩余真实 auth/runtime 边界之一，归入后续 auth snapshot/provider 切片。
    - 2026-06-20 CST 已完成 `codex-mcp` Codex Apps auth context snapshot 切片：新增 `CodexAppsAuthContext` 作为调用方预投影的 auth-scoped DTO，承载 `uses_codex_backend`、account id、ChatGPT user id 和 workspace 标记。`with_codex_apps_mcp`、`effective_mcp_servers`、`read_mcp_resource`、`collect_mcp_server_status_snapshot_with_detail` 和 `codex_apps_tools_cache_key` 改为接收该 context；`compute_auth_statuses` 只接收 host-owned Codex Apps runtime auth 是否可用的 boolean。core/app-server 继续从 `CodexAuth` 投影 context/provider，CLI list 无 auth context 时传 false。`codex-mcp` 因此不再为了 Codex Apps cache key 或 auth-status availability 判断依赖 `codex-login`。
    - Codex Apps auth context 切片后执行 `rtk rg -n "CodexAuth|codex_login" codex-rs/codex-mcp/src codex-rs/codex-mcp/Cargo.toml`：无匹配；执行 `rtk cargo fmt -p codex-mcp -p codex-core -p codex-cli -p codex-app-server --check`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-mcp -p codex-core -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-cli`、`rtk cargo test -p codex-mcp` 和 `rtk cargo check -p codex-core --tests`：均通过，仅剩既有 warnings。
    - Codex Apps auth context 切片后执行 `rtk cargo tree -p codex-mcp --depth 1 --edges normal`：direct deps 显示 `codex-api-provider`、`codex-config-types`、`codex-exec-server-api`、`codex-mcp-tool-types`、`codex-mcp-types`、`codex-metrics-api`、`codex-plugin-types`、`codex-protocol`、`codex-rmcp-client`、`codex-utils-plugins` 等，不再显示 `codex-login` 或 `codex-model-provider-api`。执行 `rtk cargo tree -p codex-mcp --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-mcp --invert codex-model-provider-api --edges normal --depth 8`：均返回 package ID 不在 graph，确认 `codex-mcp` normal graph 不再间接拉回 full login runtime 或 model-provider request adapter。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：`codex-mcp -> codex-core` 分支已消失，剩余路径为 core direct、core-plugins/core-skills、`codex-model-provider-api` 和 rollout/thread-store 等真实后续 auth/runtime 边界。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已记录 `codex-mcp-types`（MCP 常量/sandbox payload/elicitation DTO）、`codex-mcp-tool-types`（rmcp-backed ToolInfo、file-param meta 和 schema masking）、`codex-plugin-types`（含 `PluginId`/telemetry metadata）、`codex-jsonrpc-types`、`codex-exec-server-protocol`（exec-server JSON-RPC wire DTO / `ProcessId` / method 常量）、`codex-exec-server-api`（host-provided exec backend/process/HTTP/environment capability/provider traits 与低依赖 runtime error）、`codex-analytics-api`、`codex-metrics-api` best-effort metrics facade / metric name constants / `StatsigMetricsSettings` DTO / `RuntimeMetricTotals` / `RuntimeMetricsSummary` / `ToolDecisionSource` tag source / session metrics trait / metrics-only helper 必须接收 `MetricsSink` / DB telemetry 边界、core 全局 counter/duration 必须走 `codex_metrics_api::record_global_*` 而不是 `codex_otel::global()` 的规则、core turn state 不暴露 concrete `codex_otel::Timer` 的规则、OTEL config 无 runtime validator 属于 `codex-config-types` 的规则、`codex-trace-context` W3C trace propagation helper 边界、`codex-otel-init` OTEL provider 初始化/process-start/SQLite telemetry 组合根边界、`codex-windows-sandbox` core dependency 必须关闭默认 OTEL setup feature 的边界、`codex-default-client` default HTTP client metadata/helper 边界、`codex-api-types` Responses request DTO/helper、`ResponseEvent` stream event DTO、`Compression` request option、`ChatCompletionsPath` option enum、compact/memory endpoint payload DTO、realtime/websocket response/probe DTO、`ApiError` 和 API error debug/telemetry helper、SSE/WebSocket transport-neutral telemetry summary/sink trait 和 realtime session DTO 边界、Realtime audio/event payload 直接归属 `codex-protocol` 的边界、`codex-openai-files-api` OpenAI file upload trait/provider 边界、`codex-openai-files` OpenAI file upload runtime 边界、`codex-feedback-api` request tag API / `AuthEnvTelemetryMetadata` 边界、`codex-feedback` feedback upload/runtime 边界、`codex-otel` 不依赖 full `codex-api` 且不匹配 raw SSE/WebSocket transport 类型的 telemetry classification 边界、`codex-client-types` HTTP client request/error/retry/request telemetry type 边界、`codex-response-debug-context` TransportError debug/telemetry helper 边界、`codex-api-provider` API provider/auth 基础边界、`codex-auth-types` auth env telemetry DTO / `TelemetryAuthMode` 边界、`codex_file_system::LOCAL_FS`、`codex-apply-patch` 只依赖 `codex-file-system` 而不通过 exec-server re-export 获取 FS trait/options/sandbox context、`codex-user-info` user display-name lookup 边界、`codex-config-schema` 承载 schema writer CLI 且 core 不为其携带 `clap` 的边界、`codex-execpolicy-cli` 承载 execpolicy check CLI 且 parser crate 不为其携带 `clap`/JSON formatting 的边界、`codex-state-cli` 承载 state logs CLI 且 state runtime 不为其携带 `clap`/`dirs`/`owo-colors` 的边界、`codex-file-search-cli` 承载 fuzzy search CLI 且 file-search library 不为其携带 `clap`/JSON/tokio process 的边界、`codex-shell-escalation-cli` 承载 execve wrapper CLI 且 shell-escalation runtime 不为其携带 `clap`/`tracing-subscriber` 的边界、`codex-workflow-api` workflow registry/manifest/run-control DTO/trait 和 workflow controller trait 注入边界、network proxy API/DTO/trait/host-port display helper 边界、protocol 不依赖 runtime async crates、core 纯策略模型直接使用 `codex-execpolicy-api`、model-provider request adapter/auth helper/factory 属于 `codex-model-provider-api`、model catalog trait/config 属于 `codex-models-manager-api`、core test support 必须经非默认 test-support feature 拉 full model manager，以及 plugin/skill analytics sink/result 边界。
    - 2026-06-20 CST 已完成 `codex-model-provider-api` / `codex-models-manager` login 边界收缩切片：`RequestAuthSnapshot` 下沉到 `codex-auth-types`，`CodexAuth::request_auth_snapshot()` 由 login-aware 边界投影 full auth；`codex-model-provider-api` 的 request header helper 改为 `auth_provider_from_auth_snapshot`，并新增 `ModelProviderAuthManager` / `SharedModelProviderAuthManager` / 401 recovery trait，使 `ModelProvider` trait/factory 不再暴露 `codex_login::AuthManager` 或 `CodexAuth`。`codex-model-provider` 和 core 分别在 login-aware 边界提供 adapter；`codex-models-manager` 只接收 `SharedModelProviderAuthManager`，normal dependency 不再需要 `codex-login`，真实 login 行为留在 model-provider/core/test adapter。
    - model-provider auth manager 切片后执行 `rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal`：direct deps 显示 `codex-agent-identity`、`codex-api-provider`、`codex-auth-types`、`codex-model-provider-info`、`codex-models-manager-api`、`codex-protocol` 和 `http`，不再显示 `codex-login`。执行 `rtk cargo tree -p codex-model-provider-api --invert codex-login --edges normal --depth 8`：返回 package ID 不在 graph。执行 `rtk cargo tree -p codex-models-manager --depth 1 --edges normal`：direct deps 显示 `codex-model-provider-api`，不显示 `codex-login`；执行 `rtk cargo tree -p codex-models-manager --invert codex-login --edges normal --depth 8`：warning nothing to print。
    - model-provider auth manager 切片后执行 `rtk cargo check -p codex-model-provider-api -p codex-models-manager -p codex-model-provider`、`rtk cargo check -p codex-core -p codex-app-server --bin codex-app-server -p codex-cli -p codex-backend-client -p codex-app-server-transport -p codex-core-plugins -p codex-core-skills`、`rtk cargo test -p codex-model-provider-api`、`rtk cargo test -p codex-model-provider`、`rtk cargo test -p codex-models-manager` 和 `rtk cargo check -p codex-core --tests`：均通过；仅打印既有 file-subscription/core unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：`codex-model-provider-api` 分支已消失，剩余为 core direct、core-plugins/core-skills 和 rollout/thread-store。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已更新 model-provider request adapter / auth snapshot / auth manager trait / models-manager auth manager 边界：`codex-model-provider-api` 不得依赖 `codex-login`，需要 full login 的调用方必须先投影成 snapshot、`SharedAuthProvider`、`CodexAppsAuthContext` 或 `SharedModelProviderAuthManager`；`codex-models-manager` 不得直接依赖 `codex_login::AuthManager`。
    - 2026-06-20 CST 已完成 `codex-core-skills` remote skill login 边界收缩切片：remote skill list/export API 改为接收调用方预投影的 `RequestAuthSnapshot`，通过 `auth_provider_from_auth_snapshot` 生成请求 headers，并直接依赖 `codex-default-client` 获取默认 reqwest client；`codex-core-skills` 不再为了 `CodexAuth` 或 `codex_login::default_client` 拉入 full login/token/storage/server runtime。
    - core-skills remote skill auth 切片后执行 `rtk rg -n "codex_login|CodexAuth|default_client" codex-rs/core-skills/src codex-rs/core-skills/Cargo.toml`：只剩 `codex_default_client::build_reqwest_client`；执行 `rtk cargo fmt -p codex-core-skills`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-core-skills` 和 `rtk cargo test -p codex-core-skills`：均通过，测试 101 passed。
    - core-skills remote skill auth 切片后执行 `rtk cargo tree -p codex-core-skills --depth 1 --edges normal`：direct deps 显示 `codex-auth-types`、`codex-default-client`、`codex-model-provider-api` 等，不再显示 `codex-login`；执行 `rtk cargo tree -p codex-core-skills --invert codex-login --edges normal --depth 8`：package ID 不在 graph；执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：`codex-core-skills` 分支已消失，剩余为 core direct、core-plugins 和 rollout/thread-store。执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已更新 core-skills remote skill 边界：remote skill API 不得依赖 `codex-login` / `CodexAuth` / `codex_login::default_client`，需要 auth 时由 login-aware caller 先投影 `RequestAuthSnapshot`，默认 client 直接走 `codex-default-client`。
    - 2026-06-20 CST 已完成 `codex-rollout` default-client 边界收缩切片：rollout 只需要 session metadata originator helper，private `default_client` shim 改为 re-export `codex-default-client`，不再通过 `codex-login::default_client` 拉 full login runtime。
    - rollout default-client 切片后执行 `rtk rg -n "codex_login|codex-login|default_client" codex-rs/rollout/src codex-rs/rollout/Cargo.toml`：只剩 `codex_default_client` 和 local shim 使用；执行 `rtk cargo fmt -p codex-rollout`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-rollout`：通过；执行 `rtk cargo test -p codex-rollout`：退出码 0。
    - rollout default-client 切片后执行 `rtk cargo tree -p codex-rollout --depth 1 --edges normal`：direct deps 显示 `codex-default-client`，不再显示 `codex-login`；执行 `rtk cargo tree -p codex-rollout --invert codex-login --edges normal --depth 8`：package ID 不在 graph；执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：rollout/thread-store 分支已消失，剩余为 core direct 和 core-plugins。执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已更新 default-client 边界：rollout/session metadata 这类只需要 originator/default-header helper 的 crate 必须直接依赖 `codex-default-client`，不要通过 `codex-login::default_client` 回流 full login runtime。
    - 2026-06-20 CST 已完成 `codex-core-plugins` remote auth DTO/provider 切片：新增 `RemotePluginAuth` 承载 `RequestAuthSnapshot`、account id、ChatGPT user id 和 workspace 标记；新增 `RemotePluginAuthProvider` 作为 startup/background remote plugin sync 的 host-provided current-auth provider。remote plugin catalog/share/install/sync、legacy remote sync 和 bundle/startup client helper 均改为 `RemotePluginAuth` + `codex-default-client`，app-server 在 login-aware 边界从 `CodexAuth` / `AuthManager` 投影 DTO/provider；`codex-login` 降为 core-plugins dev-dependency，仅用于测试 dummy auth。
    - core-plugins remote auth 切片后执行 `rtk rg -n "codex_login|codex-login|CodexAuth|AuthManager" codex-rs/core-plugins/src codex-rs/core-plugins/Cargo.toml`：生产源码无 `codex_login` / `CodexAuth` / `AuthManager`，仅测试和 dev-dependency 使用；执行 `rtk cargo fmt -p codex-core-plugins -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning。
    - core-plugins remote auth 切片后执行 `rtk cargo check -p codex-core-plugins`、`rtk cargo check -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-core-plugins --tests` 和 `rtk cargo test -p codex-core-plugins`：均通过；app-server check 只打印既有 file-subscription/core unused/dead_code warnings，core-plugins test 为 191 passed。
    - core-plugins remote auth 切片后执行 `rtk cargo tree -p codex-core-plugins --depth 1 --edges normal`：direct deps 显示 `codex-auth-types`、`codex-default-client`、`codex-model-provider-api` 等，不再显示 `codex-login`；执行 `rtk cargo tree -p codex-core-plugins --invert codex-login --edges normal --depth 8`：warning nothing to print；执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：core-plugins 分支已消失，剩余只有 `codex-login -> codex-core` direct runtime edge。执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已更新 core-plugins remote auth 边界：remote plugin auth 必须用 `RemotePluginAuth` / `RemotePluginAuthProvider`，login-aware 组合根负责从 full `CodexAuth` / `AuthManager` 投影；production normal graph 不得包含 `codex-login` 或 `codex_login::default_client`。
    - 2026-06-20 CST 已完成 core MCP OpenAI file upload auth snapshot 小切片：`core/src/mcp_openai_file.rs` 在执行 file-param argument rewrite 时先从 `AuthManager` 投影 `RequestAuthSnapshot`，后续 helper 只接收 snapshot 并通过 `auth_provider_from_auth_snapshot` 生成上传请求 auth；生产路径不再把 `CodexAuth` 传入 OpenAI file upload rewrite helper，`CodexAuth` 只留在测试中构造 dummy auth。
    - OpenAI file auth snapshot 切片后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk git diff --check`：通过；执行 `rtk cargo check -p codex-core --lib`：通过；执行 `rtk cargo test -p codex-core mcp_openai_file --lib`：通过。
    - OpenAI file auth snapshot 切片后复核 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`、`--invert codex-api`、`--invert codex-network-proxy`、`--invert codex-otel` 和 `--invert codex-mcp`：这些 heavy crates 仍只通过 direct edge 指向 `codex-core`，没有新增 indirect 回流。`codex-login` remaining path 仍只有 core direct runtime edge。
    - `AGENTS.md` 已更新 OpenAI file upload auth 边界：上传执行路径只消费 `RequestAuthSnapshot`，不得把 `CodexAuth` 或 `AuthManager` 继续穿透到 file upload argument rewrite helper 内部。
    - 2026-06-20 CST 已完成 core MCP provider helper auth snapshot 小切片：`crate::mcp::codex_apps_auth_provider` 改为接收 `RequestAuthSnapshot`，`McpManager::effective_servers` 改为接收调用方预投影的 `CodexAppsAuthContext`；session init、MCP refresh、accessible connectors 临时 MCP manager 和测试 helper 都在 login-aware 边界先投影 context/snapshot，再构造 MCP manager。
    - MCP provider helper auth snapshot 切片后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-core --lib`：通过，仅打印既有 core unused/dead_code warnings；执行 `rtk cargo test -p codex-core codex_apps_auth_elicitation_feature_disabled_returns_original_result --lib`：通过。
    - `AGENTS.md` 已更新 MCP auth provider 边界：host-owned Codex Apps MCP server 的 runtime auth provider 必须由调用方从 `RequestAuthSnapshot` 构造，不得把 `CodexAuth` 或 `AuthManager` 穿透到 MCP manager 构造和刷新 helper 中。
    - 2026-06-20 CST 已完成 core connector auth context 小切片：新增 `ConnectorAuthContext`，connector cache key、tool-suggest directory lookup、accessible connectors cache refresh 和 request-plugin-install completion refresh 不再接收 full `CodexAuth` 参数；session/tool handler 调用方在 login-aware 边界先从 auth 投影 context，再传入 connector helper。
    - Connector auth context 切片后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-core --lib`：通过；执行 `rtk cargo test -p codex-core connectors --lib`：通过；执行 `rtk git diff --check`：通过。
    - Connector auth context 切片后执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：仍只剩 `codex-login -> codex-core` direct runtime edge；执行 `rtk rg -n "list_tool_suggest_discoverable_tools_with_auth\([^\\n]*auth\\.as_ref\\(\\)|refresh_accessible_connectors_cache_from_mcp_tools\([^\\n]*auth\\.as_ref\\(\\)|auth: Option<&codex_login::CodexAuth>|auth: Option<&CodexAuth>" ...`：目标生产文件无旧 full-auth connector helper API/callsite。
    - `AGENTS.md` 已更新 connector auth 边界：connector cache/tool-suggest/plugin-install refresh 只需要 auth metadata 时应传 `ConnectorAuthContext`，不要把 `CodexAuth` 作为 connector helper 参数扩散。
    - 2026-06-20 CST 已完成 API error bridge / rate-limit parser 下沉切片：`map_api_error`、`RateLimitError`、rate-limit header parser 和 rate-limit event parser 从 `codex-api` 迁移到 `codex-api-types`；`codex-api` 保留 `api_bridge` / `rate_limits` 兼容 re-export，endpoint SSE/WebSocket parser 继续使用旧模块路径，core/model-provider 改为直接依赖 `codex-api-types::map_api_error`。该切片只迁移纯错误映射和 header/event parser，不改变 concrete API client、HTTP transport 或 stream runtime。
    - API error bridge / rate-limit parser 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core -p codex-model-provider`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-model-provider -p codex-core --lib`：通过；执行 `rtk cargo test -p codex-api-types`：通过，20 个测试通过；执行 `rtk rg -n "codex_api::map_api_error|use codex_api::map_api_error|codex_api::rate_limits|use codex_api::rate_limits" codex-rs/core/src codex-rs/core/tests codex-rs/model-provider/src codex-rs/codex-api/src codex-rs/api-types/src`：无匹配，确认 core/model-provider 不再经 full API re-export 获取 helper。
    - API error bridge / rate-limit parser 下沉后执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(rtk cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rtk rg -q "codex-api v|codex-client v|reqwest v|tokio-tungstenite|eventsource-stream|tungstenite v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | rtk sed -n "1,80p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8` 和 `rtk cargo tree -p codex-model-provider --invert codex-api --edges normal --depth 8`：均只显示 `codex-api -> consumer` direct runtime boundary，未新增 indirect path。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已更新 `codex-api-types` 边界：API error bridge `map_api_error`、rate-limit header parser 和 rate-limit event parser 属于轻量 API types crate；core/model-provider 不得为了这些纯 helper 通过 full `codex-api` re-export。
    - 2026-06-20 CST 已完成 thread-store API / implementation 初步拆分切片：新增 `codex-thread-store-api` 承载 `ThreadStore` trait、`ThreadStoreError`/`ThreadStoreResult`、thread/turn/item list/read/update DTO、`ThreadMetadataPatch`、`ThreadEventPersistenceMode` 和 `ThreadPersistenceMetadata`；`codex-thread-store` 只保留 local/in-memory implementation、live writer、rollout/state-db integration，并 re-export API 以兼容旧路径。core production 中纯 trait/DTO/error import 已改为直接依赖 `codex-thread-store-api`，只在 `LocalThreadStore` / `InMemoryThreadStore` / `LiveThread` concrete 构造和 implementation escape hatch 保留 full `codex-thread-store`。
    - thread-store API 拆分后执行 `rtk cargo fmt -p codex-thread-store-api -p codex-thread-store -p codex-core`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-thread-store-api -p codex-thread-store -p codex-core --lib`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；执行 `rtk cargo test -p codex-thread-store-api`：通过，5 个测试通过；执行 `rtk cargo test -p codex-thread-store`：通过；执行 `rtk cargo check -p codex-app-server --bin codex-app-server`：通过。
    - thread-store API 拆分后执行 `rtk cargo tree -p codex-thread-store-api --depth 1 --edges normal`：direct deps 仅为 `async-trait`、`chrono`、`codex-protocol`、`serde` 和 `thiserror`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(rtk cargo tree -p codex-thread-store-api --edges normal); if printf "%s\n" "$tree" | rtk rg -q "codex-thread-store v|codex-rollout v|codex-state v|tokio v|codex-git-utils v|codex-file-search v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | rtk sed -n "1,80p"'`：通过，确认 API crate 未间接拉回 full thread-store implementation、rollout/state runtime、tokio、git helper 或 file-search。执行 `rtk cargo tree -p codex-thread-store --depth 1 --edges normal`：full implementation crate 现在 direct 依赖 `codex-thread-store-api`，并继续拥有 `codex-rollout`、`codex-state`、tokio 和 tracing。执行 `rtk cargo tree -p codex-core --invert codex-thread-store --edges normal --depth 8`：仍只显示 `codex-thread-store -> codex-core` direct implementation boundary，说明后续还需要把 concrete store construction/factory 注入到组合根；执行 `rtk just bazel-lock-check`：通过，只打印既有 rules_rs well-known crate annotation warnings。
    - `AGENTS.md` 已记录 `codex-thread-store-api` 边界：只需要 trait/DTO/error 的代码应依赖 API crate；只有构造 `LocalThreadStore` / `InMemoryThreadStore` 或访问 implementation escape hatch 的组合根才依赖 full `codex-thread-store`。
    - 2026-06-20 CST 已完成 thread-store state DB 注入切片：`CodexSpawnArgs` / `Session::new` 现在显式接收 `StateDbHandle`，`ThreadManager` 和 interactive sub-agent spawn 从 runtime state 传递同一 handle；session 初始化读取 persisted dynamic tools、`SessionServices.state_db` 和 goals reconcile 路径不再通过 `ThreadStore::as_any().downcast_ref::<LocalThreadStore>()` 反查 concrete store state。该切片不改变 `LiveThread` / local writer 行为，只先移除 session/goals 对 concrete store implementation 的状态逃逸。
    - thread-store state DB 注入后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only `imports_granularity = Item` warning；执行 `rtk cargo check -p codex-core --lib`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。执行 `rtk rg -n "downcast_ref::<LocalThreadStore>|use codex_thread_store::LocalThreadStore" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：生产路径只剩 `thread_manager.rs` 和 `personality_migration.rs` 的 concrete construction，测试路径保留 fixture construction；session/goals 已无 `LocalThreadStore` downcast。
    - 2026-06-20 CST 已完成 live-thread handle/factory API 切片：`codex-thread-store-api` 新增 `LiveThreadHandle`、`LiveThreadFactory`、`SharedLiveThread` 和 boxed `ThreadStoreFuture`，用显式 boxed future 保持 object-safe trait，没有新增 `async_trait`；`codex-thread-store::LiveThread` 实现 handle trait，`DefaultLiveThreadFactory` 在 implementation crate 中创建 concrete live writer。core session/runtime state 现在持有 `SharedLiveThread` 和 `Arc<dyn LiveThreadFactory>`，`Session::new` 通过 factory create/resume live persistence，app-server/mcp-server/prompt-debug/thread-manager sample/test support 作为组合根或 facade 注入默认 implementation。core 仍通过 `thread_store_from_config`、`DefaultLiveThreadFactory` 过渡 re-export 和 test constructors 直接依赖 full `codex-thread-store`，后续切片需要把 concrete store/factory construction 继续推到组合根。
    - live-thread handle/factory API 切片后执行 `rtk cargo fmt -p codex-thread-store-api -p codex-thread-store -p codex-core -p codex-app-server -p codex-mcp-server -p codex-thread-manager-sample -p codex-core-api`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-thread-store-api -p codex-thread-store -p codex-core --lib`：通过；执行 `rtk cargo check -p codex-core --tests -p codex-app-server --bin codex-app-server -p codex-mcp-server -p codex-thread-manager-sample`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-thread-store-api --depth 1 --edges normal`：direct deps 仍为 `async-trait`、`chrono`、`codex-protocol`、`serde` 和 `thiserror`；禁止项门禁确认 API crate 未拉回 full thread-store、rollout/state runtime、tokio、git helper 或 file-search。执行 `rtk cargo tree -p codex-core --invert codex-thread-store --edges normal --depth 8`：仍只显示 `codex-thread-store -> codex-core` direct runtime boundary，没有新增 indirect path。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 thread-store factory 下沉切片：`codex-core` 不再导出 `thread_store_from_config` 或 `DefaultLiveThreadFactory`，默认 normal dependency 中 `codex-thread-store` 改为 optional/test-support 边界；app-server、mcp-server、CLI 和 `codex-core-api` sample facade 在组合根本地按 `Config.experimental_thread_store` 构造 `LocalThreadStore` / `InMemoryThreadStore`。`prompt_debug::build_prompt_input` 改为接收 `Arc<dyn ThreadStore>` 和 `Arc<dyn LiveThreadFactory>`，不再在 core 内创建 concrete store/factory。core test support 自己提供 test helper，测试 fixture 继续通过 dev/test-support 边界使用 local/in-memory implementation。
    - thread-store factory 下沉后执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-mcp-server -p codex-core-api -p codex-cli -p codex-thread-manager-sample`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-app-server --bin codex-app-server -p codex-mcp-server -p codex-core-api -p codex-cli -p codex-thread-manager-sample`、`rtk cargo check -p codex-core --tests`：均通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。执行 `rtk cargo tree -p codex-core --invert codex-thread-store --edges normal --depth 8`：`warning: nothing to print`，确认 core 默认 normal graph 已无 full thread-store implementation。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct deps 显示 `codex-thread-store-api`，不再显示 `codex-thread-store`。执行 `rtk cargo tree -p codex-thread-store-api --depth 1 --edges normal`：direct deps 仍为 `async-trait`、`chrono`、`codex-protocol`、`serde` 和 `thiserror`；完整 tree 未出现 full `codex-thread-store`、`codex-rollout`、`codex-state`、`tokio`、git helper 或 file-search。
    - 2026-06-20 CST 已完成 auth env telemetry helper 下沉切片：`AuthEnvTelemetry`、`AuthEnvTelemetryInput` 和 `collect_auth_env_telemetry` 进入 `codex-auth-types`；`codex-login` 保留旧签名 wrapper 兼容。core/client、core/session 和 model-provider `/models` endpoint 改为直接依赖 `codex-auth-types`，调用方只传 provider env key 和 `codex_api_key_env_enabled`，不再为了该 helper 通过 `codex-login` re-export。
    - auth env telemetry helper 下沉后执行 `rtk cargo fmt -p codex-auth-types -p codex-login -p codex-core -p codex-model-provider`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-auth-types -p codex-login -p codex-model-provider -p codex-core --lib`：通过，仅有既有 core unused/dead_code warnings；执行 `rtk cargo test -p codex-auth-types`：通过，1 个测试通过。执行 `rtk rg -n "auth_env_telemetry|collect_auth_env_telemetry|AuthEnvTelemetry" codex-rs/core/src codex-rs/model-provider/src codex-rs/login/src codex-rs/auth-types/src -g '*.rs'`：core/model-provider imports 均来自 `codex-auth-types`，`codex-login` 只剩兼容 wrapper。执行 `rtk cargo tree -p codex-model-provider --invert codex-login --edges normal --depth 8`：仍显示 direct edge，剩余原因是 `AuthManager`/`CodexAuth` adapter 和兼容 `create_model_provider` API，不是 telemetry/default-client helper；后续若继续清理，需要把 login adapter 迁到组合根或测试/compat 边界。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 core rollout state DB bridge 收敛切片：`core/src/state_db_bridge.rs` 改为用 `codex_state::StateRuntime` 暴露 `StateDbHandle`，并本地承载 `get_dynamic_tools`、`mark_thread_memory_mode_polluted` 和 `reconcile_rollout` bridge。session init、MCP tool call、stream event、goals、thread manager、CodexThread 和 state service 不再直接导入 `codex_rollout::state_db`；full rollout state DB runtime 只剩 bridge 触达。该切片不宣称移除 `core -> codex-rollout` direct edge，剩余 direct usage 主要是 `core/src/rollout.rs` legacy facade/re-export 与 bridge 的 init/reconcile runtime。
    - rollout state DB bridge 收敛后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-core --lib`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；执行 `rtk cargo test -p codex-core state_db --lib`：通过。执行 `rtk rg -n "codex_rollout::state_db|use codex_rollout::state_db" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：只剩 `core/src/state_db_bridge.rs`。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：仍为 `codex-rollout -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-rollout|codex-state|codex-thread-store"`：显示 direct `codex-rollout`、`codex-rollout-trace`、`codex-state` 和 `codex-thread-store-api`，不显示 full `codex-thread-store`。
    - 2026-06-20 CST 已完成外部 rollout facade 调用收敛切片：app-server test helper import 中的 `SessionMeta` / `read_head_for_summary` 和 `codex-exec` 的 `find_thread_meta_by_name_str` 不再通过 `codex_core` legacy facade，改为直接依赖 `codex-rollout`。`codex-exec` manifest 新增 direct `codex-rollout` dependency，表达真实 rollout metadata lookup 依赖；这不会减少 core direct graph，但减少外部 crate 对 core rollout re-export 的依赖，为后续删除 core facade/re-export 做准备。
    - 外部 rollout facade 调用收敛后执行 `rtk cargo fmt -p codex-app-server -p codex-exec`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-exec` 和 `rtk cargo check -p codex-app-server --tests`：均通过，仅打印既有 file-subscription/core unused/dead_code warnings。执行 `rtk rg -n "codex_core::(read_head_for_summary|SessionMeta|find_thread_name_by_id|find_thread_path_by_id_str|find_thread_meta_by_name_str|RolloutRecorder|ARCHIVED_SESSIONS_SUBDIR|SESSIONS_SUBDIR|ThreadSortKey|SortDirection)|use codex_core::(read_head_for_summary|SessionMeta|find_thread_name_by_id|find_thread_path_by_id_str|find_thread_meta_by_name_str|RolloutRecorder|ARCHIVED_SESSIONS_SUBDIR|SESSIONS_SUBDIR|ThreadSortKey|SortDirection)" codex-rs/app-server/src codex-rs/exec/src codex-rs/cli/src codex-rs/mcp-server/src -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：仍为 direct-only；执行 `rtk cargo tree -p codex-exec --depth 1 --edges normal | rtk rg "codex-core|codex-rollout"`：显示 `codex-exec` 同时直接依赖 core 和 rollout。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成外部 state DB / rollout facade 二次收敛切片：`codex-mcp-server`、`codex-exec`、`codex-cli` 不再通过 `codex_core::StateDbHandle` / `codex_core::init_state_db` / `codex_core::read_session_meta_line` 取得 rollout state DB 或 session metadata，改为直接依赖 `codex-rollout`。TUI personality migration 调用按下沉后的 `maybe_migrate_personality(&dyn ThreadStore)` 签名，在组合根本地按 `Config.experimental_thread_store` 构造 `LocalThreadStore` / `InMemoryThreadStore`，并新增 direct `codex-thread-store` dependency；这样 TUI 不再把 `state_db` 当作 thread store 传入 core migration 边界。
    - 外部 state DB / rollout facade 二次收敛后执行 `rtk cargo fmt -p codex-tui -p codex-mcp-server -p codex-exec -p codex-cli -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-server -p codex-exec -p codex-cli`：通过，仅打印既有 core/file-subscription/app-server-client/doctor unused warnings；执行 `rtk cargo check -p codex-app-server --tests` 和 `rtk cargo check -p codex-tui`：均通过。执行 `rtk rg -n "codex_core::init_state_db|use codex_core::init_state_db|codex_core::read_session_meta_line|use codex_core::read_session_meta_line|codex_core::StateDbHandle|use codex_core::StateDbHandle" codex-rs/app-server/src codex-rs/exec/src codex-rs/mcp-server/src codex-rs/cli/src -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：仍为 `codex-rollout -> codex-core` direct-only；执行 `rtk cargo tree -p codex-mcp-server --depth 1 --edges normal`、`rtk cargo tree -p codex-cli --depth 1 --edges normal`、`rtk cargo tree -p codex-exec --depth 1 --edges normal` 和 `rtk cargo tree -p codex-tui --depth 1 --edges normal`：目标入口均显示 direct `codex-rollout`，TUI/CLI/MCP server 也显示 direct `codex-thread-store`。执行 `rtk just bazel-lock-check` 和 `rtk git diff --check`：均通过，前者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 Codex Apps auth elicitation helper 下沉切片：`CodexAppsConnectorAuthFailure`、`CodexAppsAuthElicitation`、`CodexAppsAuthElicitationPlan`、connector auth failure metadata constants、`build_auth_elicitation_plan` / `auth_elicitation_completed_result` 等 protocol-neutral payload helper 从 `codex-mcp` 下沉到 `codex-mcp-types`；`codex-mcp` 只保留旧路径兼容 re-export。core 的 `mcp_tool_call` 直接从 `codex_mcp_types` 引用 auth elicitation helper，不再为了该 helper 走 full MCP runtime crate。
    - Codex Apps auth elicitation helper 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-types`：4 个测试通过；执行 `rtk cargo check -p codex-mcp -p codex-core --lib` 和 `rtk cargo test -p codex-mcp`：均通过。执行 `rtk rg -n "codex_mcp::(CodexAppsAuthElicitation|CodexAppsAuthElicitationPlan|CodexAppsConnectorAuthFailure|auth_elicitation_completed_result|auth_elicitation_id|build_auth_elicitation|build_auth_elicitation_plan|connector_auth_failure_from_tool_result)|use codex_mcp::(CodexAppsAuthElicitation|CodexAppsAuthElicitationPlan|CodexAppsConnectorAuthFailure|auth_elicitation_completed_result|auth_elicitation_id|build_auth_elicitation|build_auth_elicitation_plan|connector_auth_failure_from_tool_result)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 仍为 `codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`，没有新增 full MCP runtime 或 `rmcp`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。
    - 2026-06-20 CST 已完成 MCP permission prompt auto-approve helper 下沉切片：`McpPermissionPromptAutoApproveContext` 和 `mcp_permission_prompt_is_auto_approved` 从 `codex-mcp` 下沉到 `codex-mcp-types`，`codex-mcp` 只保留兼容 re-export。core 的 `mcp_tool_call`、`mcp_skill_dependencies` 和 `rmcp_client` 测试中对应 helper/常量 import 改为直接依赖 `codex_mcp_types`。`codex-mcp-types` 因该 helper 新增 direct `codex-config-types` dependency，用于轻量 `AppToolApproval` DTO；没有引入 full config loader 或 MCP runtime。
    - MCP permission prompt helper 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-types`：7 个测试通过；执行 `rtk cargo check -p codex-mcp -p codex-core --lib`、`rtk cargo test -p codex-mcp`、`rtk cargo test -p codex-core codex_apps_auth_elicitation_feature_disabled_returns_original_result --lib` 和 `rtk cargo check -p codex-core --tests`：均通过。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,90p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk rg -n "codex_mcp::MCP_SANDBOX_STATE_META_CAPABILITY|use codex_mcp::MCP_SANDBOX_STATE_META_CAPABILITY|codex_mcp::mcp_permission_prompt_is_auto_approved|use codex_mcp::mcp_permission_prompt_is_auto_approved|codex_mcp::McpPermissionPromptAutoApproveContext|use codex_mcp::McpPermissionPromptAutoApproveContext" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk just bazel-lock-check` 和 `rtk git diff --check`：均通过，前者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 Codex Apps auth context/cache key helper 下沉切片：`CodexAppsAuthContext`、`CodexAppsToolsCacheKey` 和 `codex_apps_tools_cache_key` 从 `codex-mcp` 下沉到 `codex-mcp-types`，`codex-mcp` 只保留兼容 re-export 与 runtime cache 逻辑。core/session/connectors/MCP tool call、app-server MCP processor 和 `codex-mcp` connection manager 直接使用 `codex_mcp_types`，不再为了这些 auth-scoped DTO/cache key helper 走 full MCP runtime；`codex-app-server` manifest 新增 direct `codex-mcp-types` 依赖以表达真实 DTO 使用。
    - Codex Apps auth context/cache key helper 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-types`：7 个测试通过；执行 `rtk cargo test -p codex-mcp`：54 个测试通过；执行 `rtk cargo check -p codex-app-server --tests`、`rtk cargo check -p codex-core --lib` 和 `rtk cargo check -p codex-core --tests`：均通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。执行 `rtk rg -n "codex_mcp::CodexAppsAuthContext|use codex_mcp::CodexAppsAuthContext|codex_mcp::CodexAppsToolsCacheKey|use codex_mcp::CodexAppsToolsCacheKey|codex_mcp::codex_apps_tools_cache_key|use codex_mcp::codex_apps_tools_cache_key" codex-rs/core/src codex-rs/core/tests codex-rs/app-server/src -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,90p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk just bazel-lock-check` 和 `rtk git diff --check`：均通过，前者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 MCP effective server / plugin provenance DTO 下沉切片：`EffectiveMcpServer` 和 `ToolPluginProvenance` 从 `codex-mcp` 下沉到 `codex-mcp-types`，`codex-mcp` 继续兼容 re-export，并在 runtime 边界从 `EffectiveMcpServer::configured_config()` 派生 `McpServerMetadata` 和 RMCP launch config。core 的 `McpManager` wrapper、connector provenance helper 和 MCP tool-call tests 改为直接依赖 `codex_mcp_types`，不再为了这两个纯配置/来源 DTO 走 full MCP runtime re-export。
    - MCP effective server / plugin provenance DTO 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-types -p codex-mcp`、`rtk cargo test -p codex-mcp-types`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --lib` 和 `rtk cargo check -p codex-core --tests`：均通过。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 仍为 `codex-config-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`；完整 `rtk cargo tree -p codex-mcp-types --edges normal` 未出现 `codex-mcp`、`codex-rmcp-client`、`rmcp`、`codex-login`、`codex-model-provider` 或 `codex-exec-server`。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。
    - 2026-06-20 CST 已完成 MCP client elicitation support 边界收敛切片：新增 `McpClientElicitationSupport` 到 `codex-mcp-types`，用语义 enum 表达 disabled/auth-elicitation host capability；`Config::to_mcp_config`、session init/refresh、connectors refresh 和 MCP tool-call 测试不再直接构造或传递 `rmcp::model::ElicitationCapability`。`codex-mcp` 的 connection manager 继续在 runtime protocol boundary 把 `McpClientElicitationSupport` 转成 RMCP wire capability，`rmcp_client` 仍接收 wire DTO。
    - MCP client elicitation support 边界收敛后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-types -p codex-mcp`、`rtk cargo test -p codex-mcp-types`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk rg -n "use rmcp::model::(ElicitationCapability|FormElicitationCapability|UrlElicitationCapability)|rmcp::model::ElicitationCapability" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-mcp-types --edges normal`：未出现 `codex-mcp`、`codex-rmcp-client`、`rmcp`、`codex-login`、`codex-model-provider` 或 `codex-exec-server`。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。
    - 2026-06-20 CST 已完成 MCP config/server-view helper 下沉切片：`McpConfig`、`configured_mcp_servers`、`effective_mcp_servers`、`effective_mcp_servers_from_configured`、`with_codex_apps_mcp`、`host_owned_codex_apps_enabled` 和 `tool_plugin_provenance` 从 `codex-mcp` 下沉到 `codex-mcp-types`。core 的 `Config::to_mcp_config`、`McpManager` wrapper、session MCP refresh 和 connectors refresh 不再为了这些 config/server-view helper 依赖 full MCP runtime re-export；app-server MCP status processor 也改为直接使用 `codex_mcp_types::McpConfig` / `effective_mcp_servers`。`codex-mcp` 保留兼容 re-export，并继续只在 connection manager、snapshot/read-resource runtime 和 OAuth flow 边界使用这些 DTO。
    - MCP config/server-view helper 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-types -p codex-mcp`、`rtk cargo test -p codex-mcp-types`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server 仅打印既有 file-subscription dead_code warnings。定时清理触发后执行 `rtk cargo clean`，释放 23.3GiB。执行 `rtk rg -n "codex_mcp::(McpConfig|configured_mcp_servers|effective_mcp_servers|effective_mcp_servers_from_configured|host_owned_codex_apps_enabled|tool_plugin_provenance|with_codex_apps_mcp)|use codex_mcp::(McpConfig|configured_mcp_servers|effective_mcp_servers|effective_mcp_servers_from_configured|host_owned_codex_apps_enabled|tool_plugin_provenance|with_codex_apps_mcp)" codex-rs/core/src codex-rs/core/tests codex-rs/app-server/src -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-plugin-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,120p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 MCP OAuth scopes resolution 下沉切片：`McpOAuthScopesSource`、`ResolvedMcpOAuthScopes` 和 `resolve_oauth_scopes` 从 `codex-mcp` 下沉到 `codex-mcp-types`。core 的 skill MCP dependency installer、app-server MCP OAuth login/plugins flow 和 CLI `mcp` 命令不再为了纯 scope precedence 逻辑依赖 full MCP runtime re-export；`codex-mcp` 只保留兼容 re-export，并继续拥有 OAuth discovery/login、auth status computation 和 provider error retry 判断。
    - MCP OAuth scopes resolution 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core -p codex-app-server -p codex-cli`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-types -p codex-mcp`、`rtk cargo test -p codex-mcp-types`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo build -p codex-cli`：均通过，CLI/app-server 仅打印既有 dead_code/unused warnings。`codex-cli` manifest 新增 direct `codex-mcp-types` 依赖以表达真实 DTO/helper 使用。执行 `rtk rg -n "use codex_mcp::(resolve_oauth_scopes|ResolvedMcpOAuthScopes|McpOAuthScopesSource)|codex_mcp::(resolve_oauth_scopes|ResolvedMcpOAuthScopes|McpOAuthScopesSource)" codex-rs/core/src codex-rs/app-server/src codex-rs/cli/src -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 仍为 `codex-config-types`、`codex-plugin-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,120p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk git diff --check`、新文件 no-index whitespace check 和 `rtk just bazel-lock-check`：均通过，Bazel lock check 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 AuthManagerConfig 下沉切片：`AuthManagerConfig` trait 从 `codex-login` 下沉到 `codex-auth-types`，`codex-login::auth` / `codex-login` 继续兼容 re-export。core `Config` 和 ChatGPT connector `ChatGptConfig` 的 trait 实现改为直接依赖 `codex-auth-types`，ChatGPT connector 同时直接依赖 `codex-config-types::AuthCredentialsStoreMode`，不再为了实现这个轻量 config trait 从 `codex-login` 导入类型。该切片不宣称移除 core 对 `codex-login` 的 direct edge，剩余仍是 `AuthManager` / `CodexAuth` runtime 边界。
    - AuthManagerConfig 下沉后执行 `rtk cargo fmt -p codex-auth-types -p codex-login -p codex-core -p codex-chatgpt`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-auth-types -p codex-login -p codex-core -p codex-chatgpt`：通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；执行 `rtk cargo test -p codex-auth-types`：1 个测试通过。执行 `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`schemars`、`serde`、`strum_macros` 和 `ts-rs`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-auth-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-login|codex-api v|codex-model-provider|codex-keyring-store|reqwest|tokio v|async-trait"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,100p"'`：通过。执行 `rtk rg -n "codex_login::AuthManagerConfig|use codex_login::AuthManagerConfig" codex-rs/core/src codex-rs/chatgpt/src codex-rs/core/tests codex-rs/chatgpt/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：仍为 `codex-login -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 OpenAI API key env helper 下沉切片：`read_openai_api_key_from_env` 从 `codex-login` 下沉到 `codex-auth-types`，并复用 auth-types 内部 non-empty env var helper；`codex-login` 旧函数改为委托到新 helper 保持兼容。core realtime conversation 改为直接依赖 `codex_auth_types::read_openai_api_key_from_env`，core integration test 中只使用 `OPENAI_API_KEY_ENV_VAR` / `CODEX_API_KEY_ENV_VAR` 的路径也改为直接依赖 `codex-auth-types`，`core_test_support` manifest 新增 direct `codex-auth-types` 测试依赖。该切片不宣称移除 core 对 `codex-login` 的 direct edge，剩余仍是 `AuthManager` / `CodexAuth` runtime 边界。
    - OpenAI API key env helper 下沉后执行 `rtk cargo fmt -p codex-auth-types -p codex-login -p codex-core -p core_test_support`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-auth-types -p codex-login -p codex-core -p core_test_support` 和 `rtk cargo check -p codex-core --tests`：均通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning；执行 `rtk cargo test -p codex-auth-types`：2 个测试通过。执行 `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 仍为 `codex-config-types`、`schemars`、`serde`、`strum_macros` 和 `ts-rs`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-auth-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-login|codex-api v|codex-model-provider|codex-keyring-store|reqwest|tokio v|async-trait"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,100p"'`：通过。执行 `rtk rg -n "codex_login::(OPENAI_API_KEY_ENV_VAR|CODEX_API_KEY_ENV_VAR|read_openai_api_key_from_env)|use codex_login::(OPENAI_API_KEY_ENV_VAR|CODEX_API_KEY_ENV_VAR|read_openai_api_key_from_env)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：仍为 `codex-login -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 core connector auth context 复用切片：删除 core 本地 `ConnectorAuthContext`，connector cache key、tool-suggest directory lookup、accessible connectors cache refresh 和 request-plugin-install completion refresh 改为直接接收 `codex_mcp_types::CodexAppsAuthContext`。session turn、MCP tool call 和 request-plugin-install handler 继续在 login-aware 边界调用 `crate::mcp::codex_apps_auth_context` 从 `CodexAuth` 投影 context；connectors helper 不再维护重复 auth-scoped DTO，也不再直接导入 `CodexAuth`。
    - connector auth context 复用后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-core --lib`：通过；执行 `rtk cargo test -p codex-core connectors --lib`：通过。执行 `rtk rg -n "ConnectorAuthContext|connectors::ConnectorAuthContext|from_auth\\(&auth\\)|map\\(connectors::ConnectorAuthContext::from_auth\\)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：除 `model_provider_auth.rs` 中无关 `provider_account_from_auth(&auth)` 子串外无 connector context 残留。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：均仍为对应 heavy crate -> `codex-core` direct-only，没有新增 indirect 回流。
    - 2026-06-20 CST 已完成 core rollout facade 收缩切片：`codex-core` 顶层不再 re-export rollout list/metadata/thread item/recorder API，app-server v2 tests 和 core integration/unit tests 中的 `RolloutRecorder`、`ThreadSortKey`、thread list/find helper 等调用改为直接依赖 `codex-rollout`。`core/src/rollout.rs` 现在只保留内部需要的 `SESSIONS_SUBDIR`、`list::find_thread_path_by_id_str`、`RolloutConfigView for Config` 和 session init/truncation bridge；该切片不宣称移除 `core -> codex-rollout` direct edge。
    - core rollout facade 收缩后执行 `rtk cargo fmt -p codex-core -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo check -p codex-app-server --tests`：均通过，仅打印既有 core `MIN_YIELD_TIME_MS` unused import warning。执行 `rtk rg -n "crate::rollout::(recorder::)?RolloutRecorder|codex_core::(ARCHIVED_SESSIONS_SUBDIR|Cursor|EventPersistenceMode|INTERACTIVE_SESSION_SOURCES|RolloutRecorder|RolloutRecorderParams|SESSIONS_SUBDIR|SessionMeta|SortDirection|ThreadItem|ThreadSortKey|ThreadsPage|append_thread_name|find_archived_thread_path_by_id_str|find_conversation_path_by_id_str|find_thread_meta_by_name_str|find_thread_name_by_id|find_thread_names_by_ids|find_thread_path_by_id_str|parse_cursor|read_head_for_summary|read_session_meta_line|rollout_date_parts)" codex-rs -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：仍为 `codex-rollout -> codex-core` direct-only，没有 indirect 回流。执行 `rtk git diff --check`：通过。
    - 2026-06-20 CST 已完成 core state DB facade 收口切片：`codex-core` 顶层不再 public re-export `StateDbHandle` / `init_state_db`；core 内部继续通过 `state_db_bridge` 使用 `codex_state::StateRuntime` 和 rollout reconcile helper，unit tests 直接引用 test-only bridge。`codex-core-api` 作为 facade 自己从 `codex-rollout` 导出 `StateDbHandle` 并包装 `codex_rollout::state_db::init`；`core_test_support`、`prompt_debug` integration test 和 `app-server-client` tests 改为直接依赖 `codex-rollout`。`app-server-client` 的 legacy `test_support` re-export 也收紧为仅在 `feature = "test-support"` 时编译，避免普通 `--tests` 隐式要求 `codex-core/test-support`。
    - core state DB facade 收口后执行 `rtk cargo fmt -p codex-core -p codex-core-api -p codex-app-server-client -p core_test_support`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-core-api -p codex-app-server-client --tests` 和 `rtk cargo check -p codex-app-server-client --features test-support`：均通过，仅打印既有 dead_code warnings。执行 `rtk rg -n "codex_core::(StateDbHandle|init_state_db)|use codex_core::(StateDbHandle|init_state_db)|pub use codex_core::(StateDbHandle|init_state_db)|pub\\(crate\\) use state_db_bridge::init_state_db|crate::init_state_db|use crate::init_state_db" codex-rs -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：仍为 direct-only，没有 indirect 回流。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 API event/header facade 收口切片：`X_RESPONSESAPI_INCLUDE_TIMING_METRICS_HEADER` 迁入 `codex-api-types`，core client 和 websocket tests 直接依赖该 API request header 常量；core 顶层不再 public re-export `ResponseEvent` 或 timing metrics header。core integration tests 中的 stream event 类型改为直接使用 `codex_api_types::ResponseEvent`；core 内部仍通过 `client_common::ResponseEvent` 使用同一 API DTO。
    - API event/header facade 收口后执行 `rtk cargo fmt -p codex-api-types -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-types -p codex-core --tests`：通过。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 未新增 heavy runtime，仍为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`http`、`serde`、`serde_json` 和 `thiserror`。执行 `rtk rg -n "codex_core::(ResponseEvent|X_RESPONSESAPI_INCLUDE_TIMING_METRICS_HEADER)|use codex_core::(ResponseEvent|X_RESPONSESAPI_INCLUDE_TIMING_METRICS_HEADER)|pub use client_common::ResponseEvent|pub use client::X_RESPONSESAPI_INCLUDE_TIMING_METRICS_HEADER|crate::ResponseEvent" codex-rs -g '*.rs'`：无匹配。执行 `rtk git diff --check`：通过。
    - 2026-06-20 CST 已完成 core 纯 DTO re-export 收口切片：删除 `codex_core::SandboxState` 和 `codex_core::ModelProviderAuthInfo` 顶层 re-export；前者 owner 是 `codex-mcp-types`，后者 owner 是 `codex-protocol::config_types`。仓库内没有通过 core facade 使用这两个 DTO，避免继续把 core 当作跨域 DTO 汇总层。
    - core 纯 DTO re-export 收口后执行 `rtk cargo fmt -p codex-core`：通过；执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo check -p codex-core --tests`：通过。执行 `rtk rg -n "codex_core::(SandboxState|ModelProviderAuthInfo)|use codex_core::(SandboxState|ModelProviderAuthInfo)|pub use codex_mcp_types::SandboxState|pub use codex_protocol::config_types::ModelProviderAuthInfo" codex-rs -g '*.rs'`：core 无残留，唯一匹配是 `codex-mcp` 自己的兼容 re-export。
    - 2026-06-20 CST 已完成 core API header facade 降级切片：`X_CODEX_INSTALLATION_ID_HEADER` / `X_CODEX_TURN_METADATA_HEADER` 不再从 core 顶层 public re-export，`client` 模块内常量改为 `pub(crate)`；MCP tool metadata 构造和测试改为直接引用 `crate::client::X_CODEX_TURN_METADATA_HEADER`。仓库内没有外部 `codex_core::X_CODEX_*` 使用点。
    - core API header facade 降级后执行 `rtk cargo fmt -p codex-core`：通过；执行 `rtk cargo check -p codex-core --tests`：通过，仅打印既有 dead_code warnings。执行 `rtk rg -n "codex_core::(X_CODEX_INSTALLATION_ID_HEADER|X_CODEX_TURN_METADATA_HEADER)|use codex_core::(X_CODEX_INSTALLATION_ID_HEADER|X_CODEX_TURN_METADATA_HEADER)|pub use client::X_CODEX_INSTALLATION_ID_HEADER|pub use client::X_CODEX_TURN_METADATA_HEADER|crate::X_CODEX_TURN_METADATA_HEADER|crate::X_CODEX_INSTALLATION_ID_HEADER" codex-rs -g '*.rs'`：无匹配。
    - 2026-06-20 CST 已完成 MCP elicitation reviewer API 下沉切片：`ElicitationReviewRequest`、`ElicitationReviewer` 和 `ElicitationReviewerHandle` 从 `codex-mcp` runtime module 下沉到 `codex-mcp-types`，`codex-mcp` 保留兼容 re-export。新的 reviewer request 使用 `codex_protocol::approvals::ElicitationRequest` 与 `codex_protocol::mcp::RequestId`，`codex-mcp` runtime 在收到 RMCP server elicitation 时负责把 `rmcp::model::CreateElicitationRequestParams` / `rmcp::model::RequestId` 转换到 protocol-neutral request；core Guardian review 逻辑不再依赖 RMCP elicitation request/schema 类型。
    - MCP elicitation reviewer API 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-types -p codex-mcp`、`rtk cargo test -p codex-mcp-types`、`rtk cargo test -p codex-mcp`、`rtk cargo test -p codex-core guardian_elicitation --lib`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-plugin-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,120p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 MCP OAuth login support DTO 下沉切片：`McpOAuthLoginConfig`、`McpOAuthLoginSupport` 和 `McpAuthStatusEntry` 从 `codex-mcp` auth runtime module 下沉到 `codex-mcp-types`，`codex-mcp` 保留兼容 re-export。core skill MCP dependency installer、app-server plugin install OAuth flow 和 CLI `mcp` 命令直接从 `codex-mcp-types` 匹配 support result；`oauth_login_support`、`discover_supported_scopes`、`compute_auth_statuses` 和 `should_retry_without_scopes` 继续留在 `codex-mcp` / `codex-rmcp-client` runtime 边界。
    - MCP OAuth login support DTO 下沉后执行 `rtk cargo fmt -p codex-mcp-types -p codex-mcp -p codex-core -p codex-app-server -p codex-cli`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-types -p codex-mcp`、`rtk cargo test -p codex-mcp-types`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo build -p codex-cli`：均通过，app-server 仅打印既有 file-subscription dead_code warnings。执行 `rtk rg -n "pub struct McpOAuthLoginConfig|pub enum McpOAuthLoginSupport|pub struct McpAuthStatusEntry|use codex_mcp::McpOAuthLoginSupport|crate::mcp::auth::McpAuthStatusEntry" codex-rs -g '*.rs'`：canonical definitions 只剩 `codex-mcp-types`，核心调用侧无旧 full MCP type import。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 仍为 `anyhow`、`codex-config-types`、`codex-plugin-types`、`codex-protocol`、`schemars`、`serde`、`serde_json` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,120p"'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。
    - 2026-06-20 CST 已完成 MCP runtime environment API 切片：新增 `codex-mcp-runtime-api`，承载 `McpRuntimeEnvironment` 和 `McpRuntimeEnvironmentParams`。该 crate 只依赖 `codex-exec-server-api` 的 host-provided exec/http trait；`codex-mcp` 保留旧路径兼容 re-export并继续拥有 RMCP connection manager、client startup 和 metrics emission。core/app-server 已改为直接依赖 `codex-mcp-runtime-api` 构造和传递 runtime placement DTO，不再为了该 DTO 从 full `codex-mcp` 取类型。
    - MCP runtime environment API 切片后执行 `rtk cargo fmt -p codex-mcp-runtime-api -p codex-mcp -p codex-core -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-runtime-api -p codex-mcp`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server 仅打印既有 file-subscription dead_code warnings。执行 `rtk cargo tree -p codex-mcp-runtime-api --depth 1 --edges normal`：direct deps 仅为 `codex-exec-server-api`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-mcp-runtime-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,120p"'`：通过；`codex-exec-server-api` 当前 normal graph 自身包含 `tokio`，因此门禁禁止 concrete `codex-exec-server v` 而不是禁止所有 tokio。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-mcp|codex-mcp-runtime-api|codex-exec-server-api"`：core direct graph 显式包含 `codex-mcp-runtime-api`，同时 `codex-mcp` 剩余 direct edge 仍代表后续 manager/auth runtime 边界。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 MCP auth runtime capability 切片：`McpAuthRuntime` 和 `McpOAuthLoginRequest` 放入 `codex-mcp-runtime-api`，core/session 通过 trait 做 OAuth discovery、browser login 和 auth status 计算，不再直接调用 `codex-mcp::oauth_login_support`、`perform_oauth_login`、`should_retry_without_scopes` 或 `compute_auth_statuses`。`codex-mcp` 新增 `DefaultMcpAuthRuntime` concrete implementation，继续拥有 RMCP/OAuth provider error 判定、credential discovery 和浏览器 login flow；`McpConnectionManager` 的 tool/resource/RMCP 调用面暂不进入 runtime-api，避免把 `rmcp` 类型带入轻量边界。
    - MCP auth runtime capability 切片后执行 `rtk cargo fmt -p codex-mcp-runtime-api -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-runtime-api -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server 仅打印既有 file-subscription dead_code warnings。执行 `rtk cargo tree -p codex-mcp-runtime-api --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-exec-server-api` 和 `codex-mcp-types`。执行禁止项门禁 `rtk sh -lc 'rtk cargo tree -p codex-mcp-runtime-api --edges normal | rtk rg "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server v"'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk rg -n "use codex_mcp::(oauth_login_support|perform_oauth_login|should_retry_without_scopes|compute_auth_statuses)|codex_mcp::(oauth_login_support|perform_oauth_login|should_retry_without_scopes|compute_auth_statuses)" core/src core/tests -g '*.rs'`：无匹配，core 源码不再直接使用 full MCP auth helper。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 ThreadManager MCP auth runtime 显式注入切片：`ThreadManagerState` 继续持有 `Arc<dyn McpAuthRuntime>`，新增 `ThreadManager::new_with_mcp_auth_runtime`，app-server、mcp-server 和 core test support 的底层 thread manager 构造路径显式传入 `codex_mcp::DefaultMcpAuthRuntime`。旧 `ThreadManager::new` 保留为过渡/样例便捷构造器，用于 `thread-manager-sample` 这类只依赖 `codex-core-api` 的 facade；subagent spawn 继承 parent session 的 runtime handle，不重新构造 concrete auth runtime。
    - ThreadManager MCP auth runtime 注入切片后执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-mcp-server -p core_test_support`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-mcp-server` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk rg -n "DefaultMcpAuthRuntime|new_with_mcp_auth_runtime|mcp_auth_runtime:" codex-rs/core/src codex-rs/app-server/src codex-rs/mcp-server/src codex-rs/core/tests/common codex-rs/core/tests/suite/client.rs -g '*.rs'`：确认 app-server/mcp-server/test support 使用显式注入，core 剩余 concrete default 仅在 `ThreadManager::new` 过渡 wrapper、测试/prompt_debug 和 connectors fallback。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 MCP tool-call runtime trait 切片：`McpToolRuntime` 放入 `codex-mcp-runtime-api`，只覆盖 `call_tool`、server origin、memory pollution、host-owned Codex Apps 判断和 sandbox-state meta capability 这些不需要 RMCP DTO 的能力；`McpConnectionManager` 实现该 trait。core 的 `Session::call_tool`、MCP tool telemetry、Codex Apps auth elicitation判断、sandbox metadata 和 memory-mode pollution 判断改为显式调用 `McpToolRuntime`。list-tools、Codex Apps hard refresh、resource list/read 仍留在 concrete manager，因为它们当前返回 `codex-mcp-tool-types::ToolInfo` 或 `rmcp::model::*`，不能塞进 runtime-api。
    - MCP tool-call runtime trait 切片后执行 `rtk cargo fmt -p codex-mcp-runtime-api -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp-runtime-api -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server 仅打印既有 file-subscription dead_code warnings。执行 `rtk cargo tree -p codex-mcp-runtime-api --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-exec-server-api`、`codex-mcp-types`、`codex-protocol` 和 `serde_json`。执行禁止项门禁 `rtk sh -lc 'rtk cargo tree -p codex-mcp-runtime-api --edges normal | rtk rg "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server v"'`：无匹配，runtime-api 仍未拉 full MCP/RMCP/login/model-provider/concrete exec-server。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk rg -n "McpToolRuntime::(call_tool|server_origin|server_pollutes_memory|is_host_owned_codex_apps_server|server_supports_sandbox_state_meta_capability)|\\.call_tool\\(" codex-rs/core/src/session/mcp.rs codex-rs/core/src/mcp_tool_call.rs`：确认 tool-call 路径使用 trait，剩余 `.call_tool` 是 session wrapper。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-20 CST 已完成 MCP ToolInfo / tool spec RMCP DTO 解耦切片：`codex-mcp-tool-types::ToolInfo` 的 `tool` 字段从 `rmcp::model::Tool` 改为 protocol-neutral `McpTool`，并新增轻量 `ToolAnnotations`。OpenAI file-param schema masking 继续留在 `codex-mcp-tool-types`，但该 crate 不再依赖 `rmcp`；`codex-mcp` 在 RMCP list-tools 返回后、写入 manager/cache 前显式把 `rmcp::model::Tool` 转成 `McpTool`。`codex-tools` 的 MCP tool spec/Responses API 转换也改为消费 `McpTool`，移除了 `codex-tools -> rmcp` normal edge。core 的 MCP approval/app policy/tool search/spec tests 改为使用轻量 DTO；`rmcp` 在 core 中剩余的是 resource request/result、request id 和 concrete MCP runtime 直连边界，不再来自 ToolInfo/tool spec 层。
    - MCP ToolInfo / tool spec 解耦切片后执行 `rtk cargo fmt -p codex-mcp-tool-types -p codex-tools -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-tool-types -p codex-tools`：100 个测试通过；执行 `rtk cargo check -p codex-mcp-tool-types -p codex-tools -p codex-mcp -p codex-core --lib` 和 `rtk cargo check -p codex-mcp-tool-types -p codex-tools -p codex-mcp -p codex-core --tests`：均通过；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过。执行 `rtk cargo tree -p codex-mcp-tool-types --depth 1 --edges normal`：direct deps 为 `codex-protocol`、`serde` 和 `serde_json`；执行 `rtk cargo tree -p codex-tools --depth 1 --edges normal`：direct deps 包含 `codex-mcp-tool-types`，不再包含 `rmcp`。对 `codex-mcp-tool-types` 和 `codex-tools` 分别执行禁止项门禁 `codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server v`：均无匹配。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk cargo tree -p codex-core --invert rmcp --edges normal --depth 6`：仍显示 core direct resource/RMCP path 与 `codex-mcp` direct runtime path，后续 resource DTO / manager trait 切片继续处理。
    - 2026-06-20 CST 已完成 MCP resource list/read DTO 解耦切片：在 `codex_protocol::mcp` 新增 `PaginatedRequestParams`、`ReadResourceRequestParams`、`ListResourcesResult`、`ListResourceTemplatesResult` 和 `ReadResourceResult`，复用既有 `Resource`、`ResourceTemplate`、`ResourceContent`。`codex-mcp` connection manager 继续在 runtime 内部调用 RMCP client，但在 manager API 边界把 RMCP resource/template/content 转成 protocol-neutral DTO；`core` session/resource tool handlers、`codex_thread::read_mcp_resource` 和 resource payload tests 不再直接使用 `rmcp::model::*` resource/list/read DTO。app-server status snapshot 现在直接消费 manager 返回的 protocol resource maps，不再做二次 RMCP 转换。
    - MCP resource DTO 解耦切片后执行 `rtk cargo check -p codex-protocol -p codex-mcp -p codex-core --lib` 和 `rtk cargo check -p codex-protocol -p codex-mcp -p codex-core --tests`：均通过；执行 `rtk cargo fmt -p codex-protocol -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-protocol mcp::`：通过；执行 `rtk cargo test -p codex-core mcp_resource --lib`：通过；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 file-subscription dead_code warnings。执行 `rtk rg -n "rmcp::model::(ListResourceTemplatesResult|ListResourcesResult|PaginatedRequestParams|ReadResourceRequestParams|ReadResourceResult|Resource|ResourceTemplate|ResourceContents)|use rmcp::model::(ListResourceTemplatesResult|ListResourcesResult|PaginatedRequestParams|ReadResourceRequestParams|ReadResourceResult|Resource|ResourceTemplate)" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。执行 `rtk cargo tree -p codex-core --invert rmcp --edges normal --depth 6`：仍显示 core direct RMCP path，当前来源已收敛为 `RequestId`/`NumberOrString`/elicitation 相关边界，后续继续拆。
    - 2026-06-20 CST 已完成 MCP RequestId / elicitation RMCP DTO 收口切片：`core` 的 active turn pending elicitation map、session `request_mcp_server_elicitation` / `resolve_elicitation`、Codex Apps auth elicitation、MCP tool approval elicitation 和 request-plugin-install elicitation 全部改为使用 `codex_protocol::mcp::RequestId`。`codex-mcp` 的 `ElicitationRequestManager` 在 RMCP callback 入口把 `rmcp::model::RequestId` / `NumberOrString` 转成 protocol request id，pending responder map 和 `McpConnectionManager::resolve_elicitation` 对 core-facing API 只暴露 protocol-neutral id；`codex-core` 已移除 `rmcp` direct dependency。新增 `codex-mcp` 单测覆盖 RMCP callback 发出 typed event 后通过 protocol request id resolve pending response 的路径。
    - MCP RequestId / elicitation 切片后执行 `rtk cargo fmt -p codex-mcp -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-mcp -p codex-core --lib`、`rtk cargo check -p codex-mcp -p codex-core --tests`、`rtk cargo test -p codex-mcp elicitation_resolves_waiting_request_by_protocol_request_id`、`rtk cargo test -p codex-core request_mcp_server_elicitation_auto_accepts_when_auto_deny_is_enabled --lib`、`rtk cargo test -p codex-core codex_apps_auth_elicitation_feature_enabled_requests_elicitation --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；app-server 仅打印既有 file-subscription dead_code warnings，core tests 仅打印既有 dead_code warnings。执行 `rtk rg -n "use rmcp::model|rmcp::model::|NumberOrString" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct normal deps 不再包含 `rmcp`。执行 `rtk cargo tree -p codex-core --invert rmcp --edges normal --depth 6`：只剩 `rmcp -> codex-mcp -> codex-core` 和 `rmcp -> codex-rmcp-client -> codex-mcp -> codex-core`，说明 direct RMCP edge 已移除，剩余路径属于 concrete MCP runtime。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`：仍为 `codex-mcp -> codex-core` direct-only，没有新增 indirect 回流。
    - 2026-06-20 CST 已完成 MCP connection runtime trait 边界切片：`codex-mcp-runtime-api` 新增 `McpConnectionRuntime: McpToolRuntime`，覆盖已经 protocol-neutral 的 manager 调用面，包括 list tools / hard refresh、resource list/read、elicitation resolve、startup failure/status、approval policy/profile、auto-deny 和 shutdown；该 trait只使用 `codex-mcp-tool-types::ToolInfo`、`codex_protocol::mcp::*` resource/request DTO 和 `codex-mcp-types::ElicitationResponse`。`McpConnectionManager` 在 `codex-mcp` 中实现该 trait。
    - MCP connection runtime trait 切片后执行 `rtk cargo fmt -p codex-mcp-runtime-api -p codex-mcp`、`rtk cargo check -p codex-mcp-runtime-api -p codex-mcp`：均通过。执行 `rtk cargo tree -p codex-mcp-runtime-api --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-exec-server-api`、`codex-mcp-tool-types`、`codex-mcp-types`、`codex-protocol` 和 `serde_json`。执行禁止项门禁 `rtk cargo tree -p codex-mcp-runtime-api --edges normal | rtk rg "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server v"`：无匹配，新增 `ToolInfo` dependency 没有把 full MCP/RMCP/login/model-provider/concrete exec-server 间接拉回。
    - 2026-06-21 CST 已完成 MCP connection runtime factory injection 切片：`McpConnectionRuntimeFactory` 和 `McpConnectionRuntimeStartRequest` 成为 session startup/refresh、connector accessible 查询和 prompt debug 的唯一 MCP connection construction 边界；`SessionServices` 现在持有 `Arc<RwLock<Box<dyn McpConnectionRuntime>>>`，不再持有 concrete `McpConnectionManager`。`codex-mcp-runtime-api` 还提供 `DisabledMcpAuthRuntime` / `DisabledMcpConnectionRuntimeFactory` 供 `ThreadManager::new` 这类轻量样例路径使用；app-server、mcp-server、CLI/TUI 和 core tests/test-support 显式注入 `codex_mcp::DefaultMcpAuthRuntime` 与 `DefaultMcpConnectionRuntimeFactory`。`codex-core` 的 `codex-mcp` dependency 已改为 optional `test-support` + dev-dependency，默认 normal graph 不再拉 full `codex-mcp` 或 `rmcp`；TUI 作为组合根将 `codex-mcp` 提升为 normal dependency 来承担 real MCP runtime 注入。
    - MCP connection runtime factory injection 后执行 `rtk cargo fmt -p codex-mcp-runtime-api -p codex-mcp -p codex-core -p codex-app-server -p codex-mcp-server -p codex-cli -p codex-tui`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-mcp-runtime-api -p codex-mcp -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-cli -p codex-tui -p codex-mcp-server`：均通过；仅打印既有 core/file-subscription/app-server-client/cli unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct deps 显示 `codex-mcp-runtime-api`、`codex-mcp-tool-types`、`codex-mcp-types`，不再显示 `codex-mcp`。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert rmcp --edges normal --depth 6`：均为 warning nothing to print。执行 `rtk cargo tree -p codex-mcp-runtime-api --depth 1 --edges normal`：direct deps 为 `anyhow`、`async-channel`、`codex-api-provider`、`codex-config-types`、`codex-exec-server-api`、`codex-mcp-tool-types`、`codex-mcp-types`、`codex-protocol`、`serde_json`、`tokio-util`。执行禁止项门禁 `rtk cargo tree -p codex-mcp-runtime-api --edges normal | rtk rg "codex-mcp v|codex-rmcp-client|rmcp v|codex-login|codex-model-provider|codex-exec-server v"`：无匹配。
    - 2026-06-21 CST 已完成 network proxy runtime consumer trait 切片：`codex-network-proxy-api` 新增 `NetworkProxyRuntime` / `SharedNetworkProxyRuntime`，覆盖已启动 proxy 的 `runtime_snapshot`、`current_config`、HTTP/SOCKS 地址、动态 allow/deny domain 更新和 env 注入能力；concrete `codex_network_proxy::NetworkProxy` 在 runtime crate 内实现该 trait。`StartedNetworkProxy::proxy()` 现在返回 shared runtime handle，core 的 `TurnContext`、`ExecParams`、`ExecRequest`、`ShellRequest`、`UnifiedExecRequest`、`NetworkApprovalSpec`、sandbox attempt、spawn/Linux sandbox helper、guardian live config 和 turn metrics 等执行消费路径改为持有 `SharedNetworkProxyRuntime` 或 `&dyn NetworkProxyRuntime`，不再把 concrete `NetworkProxy` 扩散到 shell/unified exec/spawn 请求结构。concrete `NetworkProxy` / `NetworkProxyState` / `ConfigReloader` / `ConfigState` / `NetworkProxyHandle` 仍留在 `NetworkProxySpec`、`network_proxy_loader` 和测试构造中，下一刀需要把 proxy start/reload 做成组合根注入的 factory/runtime boundary 才能移除 core direct `codex-network-proxy` edge。
    - network proxy runtime consumer trait 切片后执行 `rtk cargo fmt -p codex-network-proxy-api -p codex-network-proxy -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-network-proxy-api -p codex-network-proxy -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；app-server 仅打印既有 file-subscription dead_code warnings，core tests 仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|network-proxy|login|mcp|rmcp)"`：剩余 direct heavy deps 仍包含 `codex-api`、`codex-login`、`codex-network-proxy`、`codex-otel`，且不包含 full `codex-mcp` / `rmcp`。执行 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 8`：仍只有 `codex-network-proxy -> codex-core` direct runtime boundary，没有新增 indirect 回流。执行 `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：direct deps 为 `anyhow` 和 `serde`。执行禁止项门禁 `rtk sh -lc 'tree=$(cargo tree -p codex-network-proxy-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-network-proxy v|rama|codex-core|codex-exec-server|codex-login|codex-api v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过，API crate 未间接拉回 Rama/full proxy/core/exec-server/login/api。
    - 2026-06-21 CST 已完成 network proxy start/reload factory 注入切片：`codex-network-proxy-api` 新增 `StartedNetworkProxyRuntime`、`SharedStartedNetworkProxyRuntime`、`NetworkProxyStartRequest`、`NetworkProxyRuntimeFactory`、`SharedNetworkProxyRuntimeFactory` 和 `DisabledNetworkProxyRuntimeFactory`；`NetworkProxySpec::start_proxy` 只构造 API request 并通过 factory 启动，`apply_to_started_proxy` 只调用 trait object `update_config`。Rama-backed `StaticNetworkProxyReloader`、`NetworkProxyState` 构造、真实 `NetworkProxy::builder()` / `run()` 和 `NetworkProxyHandle` 生命周期 owner 已移入 `codex-network-proxy::DefaultNetworkProxyRuntimeFactory` / `StartedNetworkProxy`，app-server、CLI、mcp-server、core test-support/dev fixture 和 core tests 显式注入 concrete factory。`codex-core` 的 `codex-network-proxy` dependency 已改为 optional `test-support` + dev-dependency，默认 normal graph 不再拉 full proxy/Rama；旧 `core/src/network_proxy_loader.rs` facade 已删除，audit metadata state threading 覆盖迁到 `codex-network-proxy` 实现 crate 单测。
    - network proxy factory 注入后执行 `rtk cargo fmt -p codex-network-proxy-api -p codex-network-proxy -p codex-core -p codex-app-server -p codex-cli -p codex-mcp-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-network-proxy-api -p codex-network-proxy -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-cli -p codex-mcp-server` 和 `rtk cargo test -p codex-network-proxy default_factory_threads_audit_metadata_to_state`：均通过；只出现既有 core/file-subscription/app-server-client/cli unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|network-proxy|login|mcp|rmcp)"`：剩余 direct heavy deps 显示 `codex-api`、`codex-login`、`codex-otel`，network proxy 只剩 `codex-network-proxy-api`，仍无 full `codex-mcp` / `rmcp`。执行 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 8`：`warning: nothing to print`。执行 `rtk cargo tree -p codex-network-proxy-api --depth 1 --edges normal`：direct deps 为 `anyhow` 和 `serde`。执行禁止项门禁 `rtk cargo tree -p codex-network-proxy-api --edges normal | rtk rg "codex-network-proxy v|rama|codex-core|codex-exec-server|codex-login|codex-api v"`：无匹配。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 API `ResponseStream` wrapper 下沉切片：`codex-api-types` 新增 transport-neutral `ResponseStream`，只暴露 `futures::Stream<Item = Result<ResponseEvent, ApiError>>` 和 upstream request id accessor；`codex-api` 保留内部 `tokio::sync::mpsc` receiver adapter、SSE/WebSocket parser、endpoint task spawning 和旧路径 re-export。core 的 response mapper 不再 destructure `codex_api::ResponseStream` 的 channel 字段，而是直接消费 `codex_api_types::ResponseStream`，避免把 provider stream channel shape 作为 full API runtime 的共享 ABI 扩散。
    - API `ResponseStream` wrapper 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server`、`rtk cargo test -p codex-api chat_response_stream_ --lib`、`rtk cargo test -p codex-api spawn_response_stream_emits_header_events --lib` 和 `rtk cargo test -p codex-api spawn_response_stream_ignores_model_verification_header --lib`：均通过；只出现既有 core/file-subscription unused/dead_code warnings。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`futures`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk cargo tree -p codex-api-types --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配，确认新增 stream wrapper 没有间接拉回 full API/client/HTTP/SSE/WebSocket/tokio runtime。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|login|mcp|rmcp|network-proxy)"`：remaining heavy direct edge 仍为 `codex-api`、`codex-login`、`codex-otel`，且没有 full `codex-mcp` / `rmcp` / full `codex-network-proxy` 回流。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 API `ResponsesOptions` 下沉切片：`ResponsesOptions` 从 `codex-api/src/endpoint/responses.rs` 迁移到 `codex-api-types`，和 `ResponsesApiRequest`、`Compression`、transport-neutral `ResponseStream` 同属 request/stream option DTO；`codex-api` 保留旧路径 re-export，继续拥有 concrete `ResponsesClient`、HTTP transport、SSE parser 和 endpoint runtime。core 的 `ModelClientSession::build_responses_options` 改为直接构造 `codex_api_types::ResponsesOptions`，不再为了 request options 通过 full `codex-api` re-export。
    - API `ResponsesOptions` 下沉后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-api --test clients` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 仍为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`futures`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk cargo tree -p codex-api-types --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。执行 `rtk rg -n "codex_api::ResponsesOptions|use codex_api::ResponsesOptions|responses::ResponsesOptions" core/src core/tests codex-api/src -g '*.rs'`：无匹配，确认 core 不再经 full API re-export 获取该 DTO。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|login|mcp|rmcp|network-proxy)"`：remaining heavy direct edge 仍为 `codex-api`、`codex-login`、`codex-otel`，且没有 full `codex-mcp` / `rmcp` / full `codex-network-proxy` 回流。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 API `ResponsesWebsocketConnectionRuntime` consumer trait 前置切片：`codex-api-types` 新增 `ApiRuntimeFuture` 和 transport-neutral `ResponsesWebsocketConnectionRuntime`，只暴露 established connection 的 `is_closed`、`send_response_processed` 和 `stream_request` 能力，返回 typed `ResponseStream` / `ApiError`，不泄漏 tungstenite、tokio channel、background task handle 或 concrete connection 类型。`codex-api::ResponsesWebsocketConnection` 在 implementation crate 内实现该 trait，core 的 `WebsocketSession` 改为持有 `Box<dyn ResponsesWebsocketConnectionRuntime>`，不再直接命名 concrete `ResponsesWebsocketConnection`。该切片是后续 WebSocket connect factory / concrete API client runtime 注入的前置工作，不宣称移除 core 的 direct `codex-api` edge。
    - API `ResponsesWebsocketConnectionRuntime` 前置切片后执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-api --test clients` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；仅出现既有 file-subscription dead_code、core unused import 和 `codex-api` 兼容 re-export warning。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 仍为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`futures`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk cargo tree -p codex-api-types --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配，确认新增 trait 没有间接拉回 full API/client/HTTP/SSE/WebSocket/tokio runtime。执行 `rtk rg -n "codex_api::ResponsesWebsocketConnection|use codex_api::ResponsesWebsocketConnection|ResponsesWebsocketConnection as ApiWebSocketConnection|codex_api::endpoint::ResponsesWebsocketConnection" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|login|mcp|rmcp|network-proxy)"`：remaining heavy direct edge 仍为 `codex-api`、`codex-login`、`codex-otel`，且没有 full `codex-mcp` / `rmcp` / full `codex-network-proxy` 回流。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`、`rtk cargo tree -p codex-core --invert rmcp --edges normal --depth 6` 和 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 8`：均为 `warning: nothing to print`。
    - 2026-06-21 CST 已完成 API `ResponsesWebsocketConnectorRuntime` 前置切片：`codex-api-types` 新增 `ResponsesWebsocketConnectRequest` 和 transport-neutral `ResponsesWebsocketConnectorRuntime`，把已配置 WebSocket client 的 connect 调用收敛到 typed request + boxed future + boxed `ResponsesWebsocketConnectionRuntime` 返回值。`codex-api::ResponsesWebsocketClient` 在 implementation crate 内实现该 trait，继续拥有 URL construction、auth header application、custom-CA/TLS connect、transport handshake 和 concrete connection lifecycle；core 的 `connect_websocket` 调用面改为构造 `ResponsesWebsocketConnectRequest` 并通过 trait 调用 connect，不再依赖 concrete connect method shape 或 concrete connection return mapping。该切片仍未引入 provider/auth factory，因此 core 仍直接构造 `ResponsesWebsocketClient`，不宣称移除 direct `codex-api` edge。
    - API `ResponsesWebsocketConnectorRuntime` 前置切片后执行 `rtk cargo fmt -p codex-api-types -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-types -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-api --test clients` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；仅出现既有 file-subscription dead_code、core unused/dead_code 和 `codex-api` 兼容 re-export warning。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 仍为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`codex-response-debug-context`、`futures`、`http`、`serde`、`serde_json` 和 `thiserror`。执行禁止项门禁 `rtk cargo tree -p codex-api-types --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配，确认 connector trait 没有间接拉回 full API/client/HTTP/SSE/WebSocket/tokio runtime。执行 `rtk rg -n "ApiWebSocketResponsesClient::new\\([^\\n]*\\)\\.connect|Box::new\\(connection\\) as ApiWebSocketConnection|codex_api::ResponsesWebsocketConnection|use codex_api::ResponsesWebsocketConnection" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|login|mcp|rmcp|network-proxy)"`：remaining heavy direct edge 仍为 `codex-api`、`codex-login`、`codex-otel`，且没有 full `codex-mcp` / `rmcp` / full `codex-network-proxy` 回流。
    - 2026-06-21 CST 已完成 API runtime factory 前置切片：新增 `codex-api-runtime-api` 承载 `ApiRuntimeFactory` / `SharedApiRuntimeFactory`，目前只暴露从 provider/auth 创建 `ResponsesWebsocketConnectorRuntime` 的 factory 方法，并提供 `DisabledApiRuntimeFactory` 供轻量 constructor/test path 使用。`codex-api` 新增 `DefaultApiRuntimeFactory` concrete implementation，继续拥有真实 WebSocket client construction、custom CA/TLS、auth header 和 endpoint runtime。`ModelClient`、`SessionServices`、`CodexSpawnArgs`、`ThreadManagerState` 和 subagent spawn 路径改为持有/传递 `SharedApiRuntimeFactory`；app-server、mcp-server、core test-support 和相关 tests 显式注入 `codex_api::DefaultApiRuntimeFactory`。该切片移除了 core 对 concrete WebSocket connector construction 的依赖，但 core 仍 direct 依赖 `codex-api`，因为 `ResponsesClient`、`ChatCompletionsClient`、`CompactClient`、`MemoriesClient`、`RealtimeCallClient`、`ReqwestTransport` 和 realtime websocket runtime 尚未 provider 化。
    - API runtime factory 前置切片后执行 `rtk cargo fmt -p codex-api-runtime-api -p codex-api -p codex-core -p codex-app-server -p codex-mcp-server -p codex-memories-write`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-runtime-api -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-api --test clients`、`rtk cargo build -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-mcp-server` 和 `rtk cargo check -p codex-memories-write --tests`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-api-runtime-api --depth 1 --edges normal`：direct deps 只有 `codex-api-provider` 和 `codex-api-types`；执行禁止项门禁 `rtk cargo tree -p codex-api-runtime-api --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(api|otel|login|mcp|rmcp|network-proxy)"`：remaining edge 为 `codex-api`、`codex-api-provider`、`codex-api-runtime-api`、`codex-api-types`、`codex-login`、`codex-mcp-runtime-api`、`codex-mcp-tool-types`、`codex-mcp-types`、`codex-network-proxy-api`、`codex-otel`；full `codex-mcp` / `rmcp` / full `codex-network-proxy` 仍无回流。执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 8`、`rtk cargo tree -p codex-core --invert rmcp --edges normal --depth 6` 和 `rtk cargo tree -p codex-core --invert codex-network-proxy --edges normal --depth 8`：均为 `warning: nothing to print`。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warning。
    - 2026-06-21 CST 已完成 compact/memories unary endpoint runtime provider 化切片：`codex-api-runtime-api::ApiRuntimeFactory` 新增 `compact_client` 和 `memories_client`，分别返回 `CompactClientRuntime` / `MemoriesClientRuntime` trait object；`codex-api-types` 新增 `CompactInputRuntimeRequest`、`MemorySummarizeRuntimeRequest` 和 `CompactionOutput`，用于传递 typed payload、headers 和 request telemetry。`codex-api::DefaultApiRuntimeFactory` 通过 existing `CompactClient` / `MemoriesClient` 和 `ReqwestTransport::new(build_reqwest_client())` 实现 concrete runtime，拥有 default reqwest/custom-CA setup、endpoint serialization 和 response parsing。core 的 `compact_conversation_history` 和 `summarize_memories` 不再直接构造 `ApiCompactClient` / `ApiMemoriesClient` / `ReqwestTransport`，改为通过 session-scoped `SharedApiRuntimeFactory` 执行。该切片继续减少 core 对 concrete API endpoint runtime shape 的依赖，但 core 仍 direct 依赖 `codex-api`，因为 Responses streaming、ChatCompletions、RealtimeCall、realtime websocket 和 test-support ModelsClient 尚未 provider 化。
    - compact/memories endpoint provider 化后执行 `rtk cargo fmt -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-runtime-api -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-api --test clients`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-api-runtime-api --depth 1 --edges normal`：direct deps 仍只有 `codex-api-provider` 和 `codex-api-types`；执行禁止项门禁 `rtk cargo tree -p codex-api-runtime-api --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。执行 `rtk rg -n "ApiCompactClient|ApiMemoriesClient|CompactClient::new|MemoriesClient::new|compact_input\\(&payload|summarize_input\\(&payload" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：无匹配，确认 core 不再直接构造这些 unary endpoint runtime。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warning。
    - 2026-06-21 CST 已完成 ChatCompletions endpoint runtime provider 化切片：`codex-api-runtime-api::ApiRuntimeFactory` 新增 `chat_completions_client`，返回 `ChatCompletionsClientRuntime` trait object；`codex-api-types` 新增 `ChatCompletionsRuntimeRequest`，用于传递 Responses-shaped request、extra headers、`ChatCompletionsPath` 和 request telemetry。`codex-api::DefaultApiRuntimeFactory` 通过 existing `ChatCompletionsClient` 和 `ReqwestTransport::new(build_reqwest_client())` 实现 concrete runtime，继续拥有 default reqwest/custom-CA setup、Responses-to-chat request adaptation、endpoint serialization 和 response parsing。core 的 `ModelClientSession::stream_chat_completions_api` 不再直接构造 `ApiChatCompletionsClient` / `ReqwestTransport`，改为通过 session-scoped `SharedApiRuntimeFactory` 执行。该切片继续减少 core 对 concrete API endpoint runtime shape 的依赖，但 core 仍 direct 依赖 `codex-api`，因为 Responses streaming、RealtimeCall、realtime websocket 和 test-support ModelsClient 尚未 provider 化。
    - ChatCompletions endpoint provider 化后执行 `rtk cargo fmt -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。因定时清理触发，执行 `rtk cargo clean` 释放 23.4GiB 后重新冷编译验证：`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-api --test clients` 均通过。执行 `rtk cargo tree -p codex-api-runtime-api --depth 1 --edges normal`：direct deps 仍只有 `codex-api-provider` 和 `codex-api-types`；执行禁止项门禁 `rtk cargo tree -p codex-api-runtime-api --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。执行 `rtk rg -n "ApiChatCompletionsClient|ChatCompletionsClient::new|ReqwestTransport::new\\(build_reqwest_client\\(\\)\\).*chat|stream_chat_completions_api" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：只剩 `stream_chat_completions_api` 方法自身和调用点，不再有 concrete client construction。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过。
    - 2026-06-21 CST 已完成 RealtimeCall endpoint runtime provider 化切片：`codex-api-runtime-api::ApiRuntimeFactory` 新增 `realtime_call_client`，返回 `RealtimeCallClientRuntime` trait object；`codex-api-types` 新增 `RealtimeCallRuntimeRequest`，用于传递 SDP、`RealtimeSessionConfig`、extra headers 和 request telemetry。`codex-api::DefaultApiRuntimeFactory` 通过 existing `RealtimeCallClient` 和 `ReqwestTransport::new(build_reqwest_client())` 实现 concrete runtime，继续拥有 default reqwest/custom-CA setup、backend/openai request-shape selection、multipart/request serialization 和 response parsing。core 的 `create_realtime_call_with_headers` 不再直接构造 `ApiRealtimeCallClient` / `ReqwestTransport`，改为通过 session-scoped `SharedApiRuntimeFactory` 执行。该切片继续减少 core 对 concrete API endpoint runtime shape 的依赖，但 core 仍 direct 依赖 `codex-api`，因为 Responses streaming、realtime websocket 和 test-support ModelsClient 尚未 provider 化。
    - RealtimeCall endpoint provider 化后执行 `rtk cargo fmt -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-runtime-api -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-api --test clients`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-api-runtime-api --depth 1 --edges normal`：direct deps 仍只有 `codex-api-provider` 和 `codex-api-types`；执行禁止项门禁 `rtk cargo tree -p codex-api-runtime-api --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。执行 `rtk rg -n "ApiRealtimeCallClient|RealtimeCallClient::new|create_with_session_and_headers\\(sdp|realtime_call_client" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：只剩 factory `realtime_call_client` 调用，不再有 concrete client construction。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过。
    - 2026-06-21 CST 已完成 Responses HTTP/SSE streaming runtime provider 化切片：`codex-api-runtime-api::ApiRuntimeFactory` 新增 `responses_client`，返回 `ResponsesClientRuntime` trait object；`codex-api-types` 新增 `ResponsesStreamRuntimeRequest`，用于传递 `ResponsesApiRequest`、`ResponsesOptions`、request telemetry 和 SSE telemetry。`codex-api::DefaultApiRuntimeFactory` 通过 existing `ResponsesClient` 和 `ReqwestTransport::new(build_reqwest_client())` 实现 concrete runtime，继续拥有 default reqwest/custom-CA setup、request compression、Azure item-id attachment、SSE transport parsing、background stream task setup 和 response event stream construction。core 的 `ModelClientSession::stream_responses_api` 不再直接构造 `ApiResponsesClient` / `ReqwestTransport`，改为通过 session-scoped `SharedApiRuntimeFactory` 执行。该切片继续减少 core 对 concrete API endpoint runtime shape 的依赖，但 core 仍 direct 依赖 `codex-api`，因为 realtime websocket 和 test-support ModelsClient 尚未 provider 化。
    - Responses streaming runtime provider 化后执行 `rtk cargo fmt -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-runtime-api -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-api --test clients`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-api-runtime-api --depth 1 --edges normal`：direct deps 仍只有 `codex-api-provider` 和 `codex-api-types`；执行禁止项门禁 `rtk cargo tree -p codex-api-runtime-api --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。执行 `rtk rg -n "ApiResponsesClient|ResponsesClient::new|ReqwestTransport::new\\(build_reqwest_client\\(\\)\\)|responses_client" codex-rs/core/src codex-rs/core/tests -g '*.rs'`：core runtime 只剩 factory `responses_client` 调用；`core/src/test_support.rs` 中 `ReqwestTransport` 仍属于后续 ModelsClient test-support 路径。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8`：仍只显示 `codex-api -> codex-core` direct runtime boundary，未新增 indirect path。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过。
    - 2026-06-21 CST 已完成 Realtime WebSocket runtime provider 化和 core default `codex-api` 依赖降级切片：`codex-api-types` 新增 `RealtimeWebsocketClientRuntime`、connection、writer、events trait，以及 connect / WebRTC sideband typed request DTO；trait 只暴露 `RealtimeSessionConfig`、headers、`RealtimeAudioFrame` / `RealtimeEvent`、`ApiError` 和 boxed `Send` future，不泄漏 tungstenite、tokio channel、pump task 或 concrete writer/events。`codex-api-runtime-api::ApiRuntimeFactory` 新增 `realtime_websocket_client(provider)`；`codex-api::DefaultApiRuntimeFactory` 通过 existing `RealtimeWebsocketClient` 提供 concrete runtime。core 的 realtime conversation 通过 `ModelClient::realtime_websocket_client` 获取 trait object，不再直接构造 `RealtimeWebsocketClient` 或命名 concrete writer/events；`codex-core` manifest 将 full `codex-api` 改为 optional `test-support` + dev-dependency，默认 normal graph 不再拉 full API runtime。`core/src/test_support.rs` 的 `ModelsClient` 仍保留在 test-support/dev 边界，后续如需可单独 provider 化 test-support 模型列表 runtime，但它已不影响 core 默认生产图。
    - Realtime WebSocket provider 化后执行 `rtk cargo fmt -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-runtime-api -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-api realtime_websocket --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；仅出现既有 unused warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-api"`：只显示 `codex-api-provider`、`codex-api-runtime-api` 和 `codex-api-types`，不显示 full `codex-api`。执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8`：`warning: nothing to print`，确认 full API runtime 没有 indirect 回流。执行 `rtk cargo tree -p codex-api-runtime-api --depth 1 --edges normal`：direct deps 仍只有 `codex-api-provider` 和 `codex-api-types`；执行禁止项门禁 `rtk cargo tree -p codex-api-runtime-api --edges normal | rtk rg "codex-api v|codex-client v|reqwest v|eventsource-stream|tokio-tungstenite|tungstenite v|tokio v"`：无匹配。
    - 2026-06-21 CST 已完成 Codex Apps auth context snapshot 切片：`codex-auth-types::RequestAuthSnapshot` 补齐 account id、ChatGPT user id、workspace 标记和 FedRAMP 标记访问器/字段；`codex-login::CodexAuth::request_auth_snapshot()` 为 bearer auth 和 Agent Identity 填充这些 Codex Apps metadata。core 的 `crate::mcp::codex_apps_auth_context` 和 app-server MCP processor 的同名 helper 改为从 `RequestAuthSnapshot` 投影 `CodexAppsAuthContext`；session init、MCP refresh、connector cache/tool-suggest、request-plugin-install、MCP tool-call refresh 和相关测试都在 login-aware 边界先投影 snapshot，再传给 MCP/connectors helper。该切片不宣称移除 core 的 direct `codex-login`，但进一步减少 core/app-server 中直接理解 `CodexAuth` 枚举细节的范围，为后续 `AuthManager` runtime trait 化保留单一 login-aware 边界。
    - Codex Apps auth context snapshot 切片后执行 `rtk cargo fmt -p codex-auth-types -p codex-login -p codex-core -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-auth-types -p codex-login -p codex-core --lib`、`rtk cargo test -p codex-login request_auth_snapshot --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`schemars`、`serde`、`strum_macros` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'if rtk cargo tree -p codex-auth-types --edges normal | rtk rg -q "codex-login|codex-mcp|codex-api v|reqwest v|tokio v|rmcp|tungstenite"; then exit 1; else exit 0; fi'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 6` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：均只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。
    - 2026-06-21 CST 已完成 login-backed model-provider auth adapter 归属切片：`core/src/model_provider_auth.rs` 已删除，原 `LoginModelProviderAuthManager`、401 recovery adapter、`RefreshTokenError` 映射和 `CodexAuth -> ProviderAccount` 枚举匹配移动到 `codex-login/src/model_provider_auth.rs`，并由 `codex_login::model_provider_auth_manager` 公开。core 的 `ModelClient`、`ThreadManager`、turn context、realtime conversation 和 test support 只调用 login crate 导出的 adapter，不再在 core 内拥有 login/model-provider adapter 逻辑。`codex-login` 新增 direct `codex-model-provider-api` 依赖；这是 login-aware runtime 到 API trait 的边界，不允许反向把 full `codex-model-provider` 或 core 拉回。
    - login-backed model-provider auth adapter 切片后执行 `rtk cargo fmt -p codex-login -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-login model_provider_auth_manager_adapts_api_key_auth --lib`、`rtk cargo check -p codex-login -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：仍只显示 direct `codex-login -> codex-core`，未新增 indirect 回流。执行 `rtk cargo tree -p codex-login --depth 1 --edges normal`：direct deps 新增 `codex-model-provider-api`，同时仍不显示 full `codex-model-provider`。执行禁止项门禁 `rtk sh -lc 'if rtk cargo tree -p codex-login --edges normal | rtk rg -q "codex-model-provider v|codex-api v|codex-core v|codex-mcp v|codex-app-server-protocol"; then exit 1; else exit 0; fi'`：通过。执行 `rtk just bazel-lock-check` 和 `rtk git diff --check`：均通过；Bazel 只打印既有 well-known crate annotation warning。
    - 2026-06-21 CST 已完成 AuthRuntime consumer 前置切片：`codex-auth-types` 新增 `AuthRuntime` / `SharedAuthRuntime` / `AuthTelemetrySnapshot`，保持 object-safe boxed future 边界；`codex-login::AuthManager` 实现该 trait，把 token refresh、storage 和 `CodexAuth` 枚举匹配留在 login-aware runtime。core 的 `TurnContext` 增加过渡期 `auth_runtime` 字段，`apps_enabled()`、image generation tool auth gating、Guardian review turn context 和 ARC monitor request header 构造改为消费 `SharedAuthRuntime` / `RequestAuthSnapshot`；session init 的 auth/MCP/status bootstrap、session telemetry auth metadata、MCP refresh、turn setup 的 connector tool-suggest、MCP tool-call 后 Codex Apps cache refresh 和 request-plugin-install 也改为从 `AuthRuntime` 读取 snapshot。上述路径不再直接调用 `AuthManager::auth()` / `current_auth_uses_codex_backend()` 后匹配 `CodexAuth` 或重新投影。`auth_manager` 字段仍保留供未拆出的 model-provider/realtime/connectors/test-support 过渡路径使用，因此本切片不宣称移除 core direct `codex-login`。
    - AuthRuntime turn-time consumer 切片后执行 `rtk cargo fmt -p codex-auth-types -p codex-login -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-auth-types -p codex-login -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-login auth_runtime_returns_request_and_telemetry_snapshots --lib`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 仍为 `codex-config-types`、`schemars`、`serde`、`strum_macros` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'if rtk cargo tree -p codex-auth-types --edges normal | rtk rg -q "codex-login|codex-mcp|codex-api v|reqwest v|tokio v|rmcp|tungstenite"; then exit 1; else exit 0; fi'`：通过。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：均只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 ModelClient / TurnContext provider auth injection 切片：`ModelClient::new`、`ModelClient::new_session_for_provider` 和 `TurnContext::make_turn_context` 从接收 `Option<Arc<AuthManager>>` 或内部调用 `codex_login::model_provider_auth_manager`，改为接收已投影的 `Option<SharedModelProviderAuthManager>`；`client.rs`、`turn_context.rs` 和 `turn.rs` 已无 `codex_login` / `model_provider_auth_manager` 直接调用。session init、app-server memories runtime、memories-write startup tests 和 core tests 在组合根或过渡边界先把 `AuthManager` 投影成 provider auth manager，再注入底层 model runtime。该切片继续收缩 core 底层 model runtime 对 login-backed adapter 的可见范围，但 thread-manager/session/test-support 仍保留过渡 adapter 调用，因此不宣称移除 core direct `codex-login`。
    - ModelClient / TurnContext provider auth injection 切片后执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-memories-write`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-memories-write --tests`：均通过；仅出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：均只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk rg -n "codex_login|AuthManager::|model_provider_auth_manager" codex-rs/core/src/client.rs codex-rs/core/src/session/turn_context.rs codex-rs/core/src/session/turn.rs`：无匹配。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 connector auth runtime 上提切片：`core/src/connectors.rs` 的 cached accessible connector、accessible connector refresh 和 tool-suggest directory cache 路径不再通过 `Config` 自建 `codex_login::AuthManager`；core connector API 改为接收调用方预投影的 `RequestAuthSnapshot`，再在 core 内投影 `CodexAppsAuthContext` / `SharedAuthProvider` 给 Codex Apps MCP 临时 runtime。app-server apps/config/plugins、TUI connector prefetch 和 MCP tool metadata lookup 已改为在组合根或 turn context 读取 snapshot 后传入；`mcp_tool_call` 的 connector description fallback 使用 `TurnContext.auth_runtime`，不再走 login fallback。
    - connector auth runtime 上提切片后执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-tui`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-tui`：均通过。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy)"`：默认 graph 仍只显示 API/MCP/network 的轻量 api/types crate，以及 direct `codex-login` / `codex-otel`。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk rg -n "codex_login|AuthManager::|auth_manager\\.auth\\(\\)" codex-rs/core/src/connectors.rs codex-rs/core/src/mcp_tool_call.rs`：无匹配。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 realtime / models manager provider-auth 边界收缩切片：`realtime_conversation::prepare_realtime_start` 不再 fallback 调用 `codex_login::model_provider_auth_manager`，只消费 session `ModelClient` 已注入的 `SharedModelProviderAuthManager`，缺失时返回明确 invalid request；`build_models_manager` 改为接收 `Option<SharedModelProviderAuthManager>`，CLI debug models 在组合根投影 login auth 后传入。`ThreadManagerState` 暂存同一份 provider auth manager，`list_models_for_provider` 不再重新从 `AuthManager` 投影。
    - realtime / models manager provider-auth 边界收缩切片后执行 `rtk cargo fmt -p codex-core -p codex-cli`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-cli`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy)"`：默认 graph 仍只显示 API/MCP/network 的轻量 api/types crate，以及 direct `codex-login` / `codex-otel`。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk rg -n "codex_login|model_provider_auth_manager|build_models_manager\\([^\\n]*AuthManager|auth_manager\\(\\)\\.unwrap_or_else" codex-rs/core/src/realtime_conversation.rs codex-rs/core/src/thread_manager.rs codex-rs/cli/src/main.rs`：确认 realtime 文件无 login 调用，`build_models_manager` 不再接收 `AuthManager`，剩余 login-backed projection 只在 CLI 组合根和 ThreadManager 过渡构造边界。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 TurnContext full AuthManager 移除切片：`TurnContext` 删除过渡期 `auth_manager` 字段，`Session::make_turn_context` 改为接收 `Option<SharedAuthRuntime>`，image generation gating、review turn 继承和 remote compact service-tier 判断都走 `AuthRuntime` / telemetry snapshot。`SessionServices` 暂存 `SharedAuthRuntime`，session init 从已有 `AuthManager` 投影一次后分别注入 MCP bootstrap、telemetry、new turn 和 `ModelClient` provider-auth；测试重建 provider 时新增 `create_model_provider_for_tests_with_provider_auth`，复用已投影 `SharedModelProviderAuthManager`，不再从 turn context 读取 full `AuthManager`。
    - TurnContext full AuthManager 移除切片后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy)"`：默认 graph 仍只显示 API/MCP/network 的轻量 api/types crate，以及 direct `codex-login` / `codex-otel`。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 OpenAI file upload auth runtime consumer 切片：`mcp_openai_file::rewrite_mcp_tool_arguments_for_openai_files` 不再通过 `SessionServices.auth_manager.auth()` 读取 `CodexAuth` 后重新投影 request snapshot，而是直接使用已注入的 `SessionServices.auth_runtime.auth()`。对应 upload failure 测试只设置 `auth_runtime`，不再把 full `AuthManager` 重新挂回 session services。该切片继续减少 core production 中直接消费 full login runtime 的 request-auth 点，但不宣称移除 core direct `codex-login`。
    - OpenAI file upload auth runtime consumer 切片后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy)"`：默认 graph 仍只显示 API/MCP/network 的轻量 api/types crate，以及 direct `codex-login` / `codex-otel`。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 service-tier account-plan auth snapshot 切片：`codex-auth-types::AuthTelemetrySnapshot` 新增轻量布尔 `uses_enterprise_default_service_tier`，默认实现保持 false 且未新增任何依赖；`codex-login::AuthManager` 的 `AuthRuntime::telemetry_snapshot()` 在 login-aware runtime 内根据 account plan 计算该布尔。core session 的 `get_service_tier` 不再直接调用 `AuthManager::auth_cached()` / `CodexAuth::account_plan_type()`，而是通过 `AuthRuntime::telemetry_snapshot()` 消费已投影语义；service-tier 测试保留 plan 分类 helper 覆盖，但生产路径不再需要 account plan DTO。
    - service-tier account-plan auth snapshot 切片后执行 `rtk cargo fmt -p codex-auth-types -p codex-login -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-auth-types -p codex-login -p codex-core --lib`、`rtk cargo test -p codex-login auth_runtime_returns_request_and_telemetry_snapshots --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-auth-types --depth 1 --edges normal`：direct deps 仍为 `codex-config-types`、`schemars`、`serde`、`strum_macros` 和 `ts-rs`。执行禁止项门禁 `rtk sh -lc 'if cargo tree -p codex-auth-types --edges normal | rtk rg -q "codex-login|codex-protocol|codex-mcp|codex-api v|reqwest v|tokio v|rmcp|tungstenite"; then exit 1; else exit 0; fi'`：通过。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy)"`、`rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 Session/CodexSpawn full AuthManager 移除切片：`CodexSpawnArgs` 和 `Session::new` 不再接收 full `Arc<AuthManager>`，改为接收 `SharedAuthRuntime` 与 `Option<SharedModelProviderAuthManager>`；`SessionServices` 删除 `auth_manager` 字段，session runtime、subagent delegate、review task 和 guardian review session 只传播已投影 runtime/provider-auth trait。`ThreadManager` 仍作为过渡边界持有 full `AuthManager`，在 `spawn_thread_with_source` 前一次性投影成 `SharedAuthRuntime` / `SharedModelProviderAuthManager`；`prompt_debug` 仍是后续需要处理的 full AuthManager 构造点。测试侧新增/复用 provider-auth helper 构造 models manager，不再把 full `AuthManager` 挂回 session services。
    - Session/CodexSpawn full AuthManager 移除切片后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy)"`：默认 graph 仍只显示 API/MCP/network 的轻量 api/types crate，以及 direct `codex-login` / `codex-otel`。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-login -> codex-core` / `codex-otel -> codex-core`，未新增 indirect 回流路径。执行 `rtk git diff --check`：通过。随后响应 `cargo-clean-my-codex` 定时触发，在无 Rust/Cargo 长命令运行时执行 `rtk cargo clean`，清理 25.0GiB target 缓存。
    - 2026-06-21 CST 已完成 ThreadManager / prompt-debug auth runtime boundary 切片：新增 `ThreadAuthRuntimes`（`SharedAuthRuntime` + `SharedModelProviderAuthManager`）作为 `ThreadManager` / prompt-debug 的 auth 输入边界；`ThreadManagerState` 不再持有 full `AuthManager`，start/resume/fork/subagent spawn 统一复用已注入的 auth runtime 和 provider-auth trait，不再在 core runtime 内调用 `codex_login::model_provider_auth_manager`。`prompt_debug::build_prompt_input` 不再从 `Config` 自建 `AuthManager`，CLI debug prompt-input 在组合根构造 login runtime 后投影注入。app-server、mcp-server、CLI、thread-manager sample 和 test-support 负责把 `AuthManager` 投影成 `ThreadAuthRuntimes`；`ThreadManager::auth_manager()` 已删除，account login completion 通知改为使用 AccountRequestProcessor 自己持有的 full auth manager。`codex-core` 的 `codex-login` manifest edge 改为 optional `test-support` + dev-dependency，默认 normal graph 不再拉 full login runtime。
    - ThreadManager / prompt-debug auth runtime boundary 切片后执行 `rtk cargo fmt -p codex-core -p codex-core-api -p codex-app-server -p codex-mcp-server -p codex-cli -p codex-thread-manager-sample`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-core-api -p codex-cli -p codex-mcp-server -p codex-thread-manager-sample`：均通过。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-(login|otel|api|mcp|rmcp|network-proxy|exec-server|thread-store|openai-files)"`：默认 graph 不再显示 `codex-login`，只剩轻量 API/types crate 和 direct `codex-otel`。执行 `rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8`：`warning: nothing to print`，确认没有 indirect login 回流；执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 6`：仍只显示 direct `codex-otel -> codex-core`，下一刀继续处理 `SessionTelemetry`/OTEL runtime。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 SessionTelemetry/OTEL runtime facade 切片：新增 `codex-session-telemetry-api`，承载 `SharedSessionTelemetry`、`SharedSessionTelemetryFactory`、`SessionTelemetryCreateParams`、boxed timer handle 和 `log_tool_result_with_tags`；`codex-core`、model client、tasks/tool registry、ThreadManager/Session services 改为只依赖 facade。`codex-otel` 实现 concrete `OtelSessionTelemetryFactory` 和 trait adapter，app-server/mcp-server/test-support 等组合根显式注入 factory；`codex-core` production manifest 已移除 full `codex-otel`，仅在 dev/test 边界保留 concrete telemetry 覆盖。
    - SessionTelemetry/OTEL facade 切片后执行 `rtk cargo fmt -p codex-session-telemetry-api -p codex-otel -p codex-core -p codex-app-server -p codex-mcp-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-mcp-server`：均通过，仅打印既有 warning。执行 `rtk cargo tree -p codex-core --invert codex-otel --edges normal --depth 8`：`warning: nothing to print`；`codex-core --depth 1` 中 heavy filter 只剩轻量 `*-api` / `*-types` / protocol crate，不再显示 `codex-login` 或 `codex-otel`。执行 `rtk cargo tree -p codex-session-telemetry-api --depth 1 --edges normal`：direct deps 为 `codex-api-types`、`codex-auth-types`、`codex-metrics-api`、`codex-protocol`、`serde_json`、`tracing`；负向 `codex-otel` 检查通过。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 Agent Identity signing API 切片：新增 `codex-agent-identity-api`，承载 `AgentIdentityKey`、`AgentTaskAuthorizationTarget` 和 `authorization_header_for_agent_task`，只依赖本地签名所需的 `anyhow`、`base64`、`chrono`、`ed25519-dalek`、`serde`、`serde_json`。`codex-model-provider-api` 的 `auth_provider_from_auth_snapshot` 改为依赖该轻量 crate，完整 `codex-agent-identity` 继续拥有 JWKS fetch、JWT decode/verify、task registration、key generation 和 URL helper，并 re-export signing API 保持旧路径兼容。
    - Agent Identity signing API 切片后执行 `rtk cargo check -p codex-agent-identity-api -p codex-agent-identity -p codex-model-provider-api`、`rtk cargo test -p codex-agent-identity-api`、`rtk cargo fmt -p codex-agent-identity-api -p codex-agent-identity -p codex-model-provider-api -p codex-core`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`：均通过，仅打印既有 warning。执行 `rtk cargo tree -p codex-core --invert codex-agent-identity --edges normal --depth 10`：`warning: nothing to print`；执行 `rtk sh -lc 'rtk cargo tree -p codex-model-provider-api --edges normal | rtk rg -q "reqwest|codex-agent-identity v" && exit 1 || exit 0'`：通过，确认 model-provider API 不再通过 Agent Identity header adapter 拉 `reqwest`。执行 `rtk cargo tree -p codex-agent-identity-api --depth 1 --edges normal`、`rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，Bazel lock check 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 default-client identity API / ARC monitor runtime provider 化切片：新增 `codex-default-client-api`，承载 `originator`、first-party originator 判断、User-Agent、default headers、residency header state 和 originator override 常量；full `codex-default-client` 继续拥有 `build_reqwest_client`、custom-CA reqwest setup 和 `CodexHttpClient` construction，并 re-export API 保持兼容。`codex-core`、`codex-rollout`、`codex-utils-plugins`、`codex-otel-init` 等只需要 identity/header helper 的路径改为依赖 API crate；`codex-core` 的 full default-client dependency 降为 optional `test-support` + dev-dependency。ARC monitor HTTP POST 新增 `ArcMonitorClientRuntime` / `ArcMonitorRuntimeRequest` / `ArcMonitorRuntimeResponse`，由 `ApiRuntimeFactory` 提供；core 只负责构造 JSON body、选择 env token 或 auth snapshot headers、解析 monitor result，真实 reqwest send/timeout/custom-CA 由 concrete `codex-api::DefaultApiRuntimeFactory` 执行。
    - default-client identity API / ARC monitor runtime provider 化后执行 `rtk cargo fmt -p codex-default-client-api -p codex-default-client -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core -p codex-rollout -p codex-utils-plugins -p codex-otel-init`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-default-client-api -p codex-default-client -p codex-api-types -p codex-api-runtime-api -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-default-client-api`、`rtk cargo test -p codex-core arc_monitor`、`rtk cargo check -p codex-rollout -p codex-utils-plugins -p codex-otel-init -p codex-default-client` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-default-client-api --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-terminal-detection`、`http`、`os_info`、`tracing`，不含 `codex-client` 或 `reqwest`。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct graph 显示 `codex-default-client-api`，不再显示 full `codex-default-client`；执行 `rtk cargo tree -p codex-core --invert codex-api --edges normal --depth 8`：`warning: nothing to print`，确认新增 ARC monitor runtime 没有把 full `codex-api` 间接拉回 core；执行 `rtk cargo tree -p codex-core --invert codex-default-client --edges normal --depth 10`：剩余路径只经 `codex-core-plugins` / `codex-core-skills`，不再有 `codex-default-client -> codex-core`、`rollout` 或 `utils-plugins` identity/helper 路径；执行 `rtk cargo tree -p codex-core --invert reqwest --edges normal --depth 12`：剩余 `reqwest` 路径收敛到 `codex-core-plugins` / `codex-core-skills` remote HTTP runtime。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，Bazel lock check 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 core-skills remote skill HTTP 非默认 feature 切片：`codex-core-skills::remote` 当前注释和仓内搜索都表明属于 future wiring，未被 active product surface 调用；将该模块挂到非默认 `remote-http` feature，并把 full `codex-default-client` 改为 optional dependency。core facade 删除默认 re-export，默认 core/session graph 不再为了未接线的 remote skill list/export API 拉 `codex-default-client` / `reqwest`。AGENTS.md 已补充规则：future-only remote skill HTTP client 不参与默认 graph，只有显式 feature 可依赖 full default-client。
    - core-skills remote HTTP feature 切片后执行 `rtk cargo fmt -p codex-core-skills -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core-skills -p codex-core --lib`、`rtk cargo check -p codex-core-skills --features remote-http` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warning。执行 `rtk cargo tree -p codex-core-skills --invert codex-default-client --edges normal --depth 6` 和 `rtk cargo tree -p codex-core-skills --invert reqwest --edges normal --depth 8`：均返回 package 不在 graph，确认默认 core-skills graph 不再拉 full default-client/reqwest；执行 `rtk cargo tree -p codex-core --invert codex-default-client --edges normal --depth 10` 和 `rtk cargo tree -p codex-core --invert reqwest --edges normal --depth 12`：剩余路径只经 `codex-core-plugins`，不再显示 `codex-core-skills`。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，Bazel lock check 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 core-plugins remote HTTP runtime 拆分切片：新增 `codex-core-plugins-remote`，承载 remote plugin catalog/list/featured/share/install/uninstall/sync、remote bundle 下载校验、curated repo startup sync 和 workspace share checkout。`codex-core-plugins` 收缩为本地 plugin manager/cache/marketplace/loading、configured marketplace auto-upgrade、remote installed plugin 纯状态 DTO 和本地 curated snapshot helper；旧 remote HTTP 源文件和测试从 `core-plugins` 删除，remote sync / featured / share / startup sync 测试迁到 remote crate。app-server 作为组合根显式依赖 `codex-core-plugins-remote`，并从 login/auth manager 投影 `RemotePluginAuth` / `RemotePluginAuthProvider` 后调用 remote runtime。
    - core-plugins remote HTTP runtime 拆分后执行 `rtk cargo fmt -p codex-core-plugins -p codex-core-plugins-remote -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core-plugins -p codex-core-plugins-remote -p codex-app-server --lib`、`rtk cargo check -p codex-core-plugins -p codex-core-plugins-remote --tests`、`rtk cargo test -p codex-core-plugins -p codex-core-plugins-remote`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-core --tests`：均通过；plugin tests 为 191 passed，app-server build 和 core tests check 退出码 0。执行 `rtk cargo tree -p codex-core --invert reqwest --edges normal --depth 12`、`rtk cargo tree -p codex-core --invert codex-default-client --edges normal --depth 10`、`rtk cargo tree -p codex-core-plugins --invert reqwest --edges normal --depth 8` 和 `rtk cargo tree -p codex-core-plugins --invert codex-default-client --edges normal --depth 6`：均为 `warning: nothing to print`。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$tree" | rg -q "codex-core-plugins-remote|codex-default-client v|reqwest v"; then printf "%s\n" "$tree" | rg "codex-core-plugins-remote|codex-default-client v|reqwest v"; exit 1; fi'`：通过，确认 core 默认图没有 remote crate、full default-client 或 reqwest 回流。执行 `rtk cargo tree -p codex-core-plugins --depth 1 --edges normal`：direct normal deps 不显示 `reqwest`、`codex-default-client`、`flate2`、`tar` 或 `zip`。执行 `rtk cargo tree -p codex-app-server --invert codex-core-plugins-remote --edges normal --depth 4`：只显示 `codex-core-plugins-remote -> codex-app-server`，确认 remote runtime 归属组合根。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 11:09 CST 已完成 rollout lightweight API 切片：新增 `codex-rollout-api`，承载 `SESSIONS_SUBDIR`、`ARCHIVED_SESSIONS_SUBDIR`、`RolloutConfig` 和 `RolloutConfigView`；full `codex-rollout` 改为 re-export 这些 API 并继续拥有 JSONL list/find、recorder、metadata extraction、session index 和 state-db reconcile runtime。`codex-core` / `codex-thread-store` 只需要 rollout 路径常量或 config view 的调用点改为依赖 API crate；`core/src/shell_snapshot.rs` 不再调用 `codex_rollout::find_thread_path_by_id_str`，cleanup 先查 state DB active rollout path，再在 `sessions/` 下按 thread id 文件名 fallback。
    - rollout lightweight API 切片后执行 `rtk cargo fmt -p codex-rollout-api -p codex-rollout -p codex-core -p codex-thread-store`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-rollout-api -p codex-rollout -p codex-thread-store -p codex-core --lib`：通过，仅打印既有 core unused/dead_code warning。执行 `rtk cargo test -p codex-core shell_snapshot --lib`：通过。执行 `rtk cargo check -p codex-rollout-api -p codex-rollout -p codex-thread-store --tests`：通过。执行 `rtk cargo tree -p codex-rollout-api --depth 1 --edges normal`：无依赖。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：仍为 `codex-rollout -> codex-core` direct-only，没有新增 indirect 回流；执行 `rtk cargo tree -p codex-core --invert codex-rollout-api --edges normal --depth 8`：显示 core direct API path 和 `codex-rollout -> core` re-export path，符合过渡期边界。执行 `rtk rg -n "\\bcodex_rollout::|\\buse codex_rollout\\b|find_thread_path_by_id_str|crate::rollout::list" core/src -g '*.rs' -g '!**/*tests.rs'`：只剩 `core/src/state_db_bridge.rs` 的 `codex_rollout::state_db` bridge。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 11:27 CST 已完成 core rollout state-db bridge 去 rollout runtime 切片：`core/src/state_db_bridge.rs` 不再导入 `codex_rollout::state_db`，只保留 `codex-state` 上的 dynamic tools、memory mode polluted 和 `insert_thread_metadata_if_absent` 窄 helper；test-only `init_state_db` 改为直接初始化 `codex_state::StateRuntime`。`goals.rs` 在 thread goal 需要 metadata row 且 DB 中缺失时，不再调用 full rollout reconcile/JSONL parse，而是从当前 session 的 rollout path、thread config snapshot、model/provider、cwd、sandbox/approval 和 rollout mtime 构造 `ThreadMetadata` 后插入 state DB。`codex-core` 的 `codex-rollout` manifest edge 移到 dev-dependency，仅供 core tests 直接使用 rollout recorder/list helper。
    - core rollout state-db bridge 去 rollout runtime 后执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo check -p codex-core --tests`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo test -p codex-core goal --lib`：通过。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --invert codex-rollout --edges normal --depth 8`：`warning: nothing to print`，确认 core 默认 normal graph 已无 full rollout runtime；执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：显示 `codex-rollout-api`，不显示 `codex-rollout`。执行 `rtk cargo tree -p codex-core --invert codex-rollout-api --edges normal --depth 8`：只显示 `codex-rollout-api -> codex-core` direct API path。执行 `rtk cargo tree -p codex-core --invert codex-file-search --edges normal --depth 10`：`warning: nothing to print`，确认 rollout list/file-search 未通过其他路径回流。执行 `rtk cargo tree -p codex-core --invert codex-state --edges normal --depth 6`：仍为 `codex-state -> codex-core` direct-only，留作后续 state facade 切片。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 git baseline runtime 拆分切片：新增 `codex-git-baseline`，承载 memory workspace 使用的 baseline repository reset/diff runtime、`GitBaselineDiff`/change DTO、`gix` 和 `similar` 依赖；`codex-memories-write` 改为直接依赖该 crate。`codex-git-utils` 删除 baseline 模块和 re-export，只保留轻量 git CLI/path/trust/info/apply helper，manifest 不再依赖 `gix` 或 `similar`。`core_test_support::TestCodex` 在当前 API 边界下显式保存测试 `AuthManager`，`memories-write` 的 startup tests 改为用 shared session telemetry trait object 调用 `ModelClient::stream`，修复该 crate test graph 中已有的 API 漂移。
    - git baseline runtime 拆分后执行 `rtk cargo fmt -p codex-git-baseline -p codex-git-utils -p codex-memories-write -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-git-baseline -p codex-git-utils -p codex-memories-write -p codex-core --lib`：通过，仅打印既有 core unused/dead_code warning。执行 `rtk cargo test -p codex-git-baseline`：6 passed。执行 `rtk cargo test -p codex-memories-write workspace`：6 passed / 23 filtered out。执行 `rtk cargo tree -p codex-git-utils --depth 1 --edges normal`：direct deps 不显示 `gix` 或 `similar`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-git-utils --edges normal); if printf "%s\n" "$tree" | rg -q "gix v|similar v|codex-git-baseline v"; then printf "%s\n" "$tree" | rg "gix v|similar v|codex-git-baseline v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,40p"'`：通过，确认轻量 git helper 未间接拉回 baseline runtime。执行 `rtk sh -lc 'set -e; output=$(cargo tree -p codex-core --invert gix --edges normal --depth 10 2>&1 || true); printf "%s\n" "$output"; if printf "%s\n" "$output" | rg -q "did not match any packages"; then exit 0; fi; if printf "%s\n" "$output" | rg -q "^gix v"; then exit 1; fi'`：通过，`gix` 不在 core normal graph 中。执行 `rtk sh -lc 'set -e; output=$(cargo tree -p codex-core --invert codex-git-baseline --edges normal --depth 10 2>&1 || true); printf "%s\n" "$output"; if printf "%s\n" "$output" | rg -q "did not match any packages|nothing to print"; then exit 0; fi; if printf "%s\n" "$output" | rg -q "codex-git-baseline v"; then exit 1; fi'`：通过，`codex-git-baseline` 未被 core 间接拉回。执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warnings。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 connector helper API 切片：新增 `codex-connectors-api`，承载 core/TUI/tools 需要的 connector accessible/filter/merge/metadata helper、workspace/account scoped directory cache key、本地 cache 读写、install URL 和 connector name/value normalization；full `codex-connectors` 删除这些本地 helper 源文件并 re-export API 模块/类型保持旧路径兼容，继续拥有远端 directory listing/fetch runtime。`codex-core` 生产和测试路径改为直接依赖 `codex-connectors-api`，不再为了本地 connector cache、tool-suggest directory lookup 或 plugin-install completion refresh 拉 full `codex-connectors`。
    - connector helper API 切片后执行 `rtk cargo fmt -p codex-connectors-api -p codex-connectors -p codex-core -p codex-chatgpt -p codex-tui`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-connectors-api -p codex-connectors -p codex-chatgpt -p codex-core --lib`、`rtk cargo test -p codex-connectors-api`、`rtk cargo test -p codex-connectors`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk sh -lc 'set -e; output=$(cargo tree -p codex-core --invert codex-connectors --edges normal --depth 10 2>&1 || true); printf "%s\n" "$output"; if printf "%s\n" "$output" | rg -q "did not match any packages|nothing to print"; then exit 0; fi; if printf "%s\n" "$output" | rg -q "^codex-connectors v"; then exit 1; fi'`：通过，`codex-connectors` 不在 core normal graph 中。执行 `rtk cargo tree -p codex-connectors-api --depth 1 --edges normal`：direct deps 仅为 `codex-connectors-types`、`serde`、`serde_json`、`sha1` 和 `tracing`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-connectors-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-connectors v|urlencoding v|anyhow v"; then printf "%s\n" "$tree"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过，确认 API crate 没有间接拉回 full connectors 或 directory fetch 相关 runtime。
    - 2026-06-21 CST 已完成 memories-read API/provider 切片：新增 `codex-memories-read-api`，承载 memory citation parser、thread id extraction、memory usage classifier、`memory_root` helper 和 `MemoryToolDeveloperInstructionsProvider` / disabled provider trait 边界；full `codex-memories-read` 改为 re-export API crate，并只保留 filesystem-backed `FsMemoryToolDeveloperInstructionsProvider`、template render、output truncation 和 Tokio fs runtime。`codex-core` 的 config/session/thread manager/memory usage/stream event 路径改为依赖 API crate和 provider trait，`ThreadManagerState`、`CodexSpawnArgs`、`SessionServices` 和 `Session::new` 均通过 constructor injection 持有 provider；app-server 与 mcp-server 组合根注入 full read crate 的 filesystem provider，core tests 使用 disabled provider。
    - memories-read API/provider 切片后执行 `rtk cargo fmt -p codex-memories-read-api -p codex-memories-read -p codex-core -p codex-app-server -p codex-mcp-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-memories-read-api -p codex-memories-read -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-memories-read-api -p codex-memories-read`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-mcp-server`：均通过；core tests 初次编译暴露 3 个手写 `Session::new` callsite 漏传 disabled memory provider，已补齐后复跑通过。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-memories"`：只显示 `codex-memories-read-api` direct edge；执行 `rtk cargo tree -p codex-core --invert codex-memories-read --edges normal --depth 10`：Cargo 返回 package 不在 core graph，确认 full read crate 未被间接拉回。执行 `rtk cargo tree -p codex-memories-read-api --depth 1 --edges normal`：direct deps 为 `codex-protocol`、`codex-shell-command` 和 `codex-utils-absolute-path`；禁止项门禁确认 API crate 不包含 `codex-memories-read`、`tokio`、`codex-utils-template` 或 `codex-utils-output-truncation`。
    - 2026-06-21 CST 已完成 hooks declaration API 切片：新增 `codex-hooks-api`，承载 plugin hook declaration projection、persisted hook key helper、hook event label 和 `HookEventsToml -> MatcherGroup` projection；full `codex-hooks` 改为 re-export API helper，并继续拥有 hook discovery/trust/hash、schema/output parsing、command execution 和 Tokio process runtime。`codex-core-plugins` 的 plugin detail hook summary 改为直接依赖 `codex-hooks-api::plugin_hook_declarations`，不再为了展示 plugin hook declaration 拉 full hook runtime。
    - hooks declaration API 切片后执行 `rtk cargo fmt -p codex-hooks-api -p codex-hooks -p codex-core-plugins`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-hooks-api -p codex-hooks -p codex-core-plugins`、`rtk cargo test -p codex-hooks-api -p codex-core-plugins plugin_hook`、`rtk cargo test -p codex-hooks`、`rtk cargo test -p codex-core-plugins`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-core-plugins --invert codex-hooks --edges normal --depth 8`：Cargo 返回 package 不在 core-plugins graph，确认 core-plugins 未间接拉回 full hooks runtime；执行 `rtk cargo tree -p codex-core-plugins --depth 1 --edges normal | rtk rg "codex-hooks"`：只显示 `codex-hooks-api` direct edge。执行 `rtk cargo tree -p codex-hooks-api --depth 1 --edges normal`：direct deps 为 `codex-config-types`、`codex-plugin-types` 和 `codex-protocol`；禁止项门禁确认 API crate 不包含 full `codex-hooks`、`tokio`、`regex`、`codex-utils-output-truncation` 或 `sha2` runtime deps。
    - 2026-06-21 CST 已完成 sandboxing API 前置切片：新增 `codex-sandboxing-api`，承载 `SandboxType` / `SandboxablePreference`、sandbox command/exec/transform DTO、transform error、platform sandbox selection、legacy `SandboxPolicy` compatibility projection 和 `policy_transforms` 权限 merge/intersection/effective policy helper；full `codex-sandboxing` 改为 re-export API 类型，并继续拥有 `SandboxManager`、seatbelt/landlock/bwrap command transformation、platform executable probing 和 `which`/`libc`/sandbox script runtime。`codex-core`、`codex-app-server` 和 `codex-exec-server` 的纯 sandbox DTO、sandbox tag、permission transform 和 legacy policy projection 调用改为直接依赖 API crate；full sandboxing 在 core 中暂时只剩 `SandboxManager`/landlock/system bwrap warning 等真实 platform runtime 边界，留待后续 runtime service 注入切片。
    - sandboxing API 前置切片后执行 `rtk cargo fmt -p codex-sandboxing-api -p codex-sandboxing -p codex-core -p codex-app-server -p codex-exec-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-sandboxing-api -p codex-sandboxing -p codex-core --lib`、`rtk cargo test -p codex-sandboxing-api -p codex-sandboxing`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-exec-server --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过；只出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-sandboxing-api --depth 1 --edges normal`：direct deps 为 `codex-network-proxy-api`、`codex-permissions-runtime`、`codex-protocol` 和 `codex-utils-absolute-path`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-sandboxing-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-sandboxing v|which v"; then printf "%s\n" "$tree" | rg "codex-sandboxing v|which v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过，确认 API crate 没有间接拉回 full platform sandbox runtime 或 executable lookup。执行 `rtk cargo tree -p codex-app-server --depth 1 --edges normal | rtk rg "codex-sandboxing"`：只显示 direct `codex-sandboxing-api`，不再显示 direct full `codex-sandboxing`；执行 `rtk cargo tree -p codex-app-server --invert codex-sandboxing --edges normal`：剩余 full sandboxing 路径来自 `codex-arg0`、`codex-core` 和 `codex-exec-server` 的真实 runtime/platform 边界。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-sandboxing"`：当前同时显示 API crate 和 full runtime crate，后者留给后续 `SandboxManager` runtime service 注入。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `SandboxManager` runtime service 注入切片：`codex-sandboxing-api` 新增 `SandboxRuntime` / `SharedSandboxRuntime` / `DisabledSandboxRuntime` capability 边界，full `codex-sandboxing::SandboxManager` 实现该 trait。`codex-core` 的 `ToolOrchestrator`、`SandboxAttempt`、standalone `exec::build_exec_request` / `process_exec_tool_call`、zsh-fork escalation 和 subagent/session spawn 路径改为接收注入的 sandbox runtime，不再在 core production 中 `SandboxManager::new()`。app-server、MCP server、codex-exec、core integration test harness 等组合根显式依赖 full `codex-sandboxing` 并注入 real manager；core test-only fixture 使用 disabled runtime 或 dev-dependency。`core::landlock::spawn_command_under_linux_sandbox` 和 `core::config::system_bwrap_warning` 旧 facade 已移除，调用方直接依赖 full sandbox runtime。
    - Sandbox runtime 注入切片后执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-mcp-server -p codex-exec`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-mcp-server`、`rtk cargo check -p codex-exec`、`rtk cargo test -p codex-sandboxing-api -p codex-sandboxing`、`rtk cargo check -p codex-exec --tests` 和 `rtk cargo check -p codex-linux-sandbox --tests`：均通过；只出现既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-sandboxing"`：只显示 `codex-sandboxing-api`。执行 `rtk cargo tree -p codex-core --invert codex-sandboxing --edges normal --depth 8`：`warning: nothing to print`，确认 core normal graph 已无 full sandboxing direct 或 indirect 路径。执行 `rtk cargo tree -p codex-app-server --invert codex-sandboxing --edges normal --depth 6`：剩余路径为 `codex-app-server` direct、`codex-arg0` 和 `codex-exec-server` runtime 边界，不经 core。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-sandboxing-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-sandboxing v|which v"; then printf "%s\n" "$tree" | rg "codex-sandboxing v|which v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过，API crate 没有间接拉回 full sandbox runtime 或 `which`。
    - 2026-06-21 CST 已完成 hooks runtime service 注入切片：`codex-hooks-api` 新增/承载 hook request/outcome DTO、hook list DTO、legacy notify payload 类型、`HooksConfig`、`HookRuntime` / `SharedHookRuntime` 和 `HookRuntimeFactory` / `SharedHookRuntimeFactory` capability 边界；full `codex-hooks::Hooks` 实现 `HookRuntime`，并新增 `HooksRuntimeFactory` 作为真实 command execution/runtime implementation。`codex-core` production manifest 从 full `codex-hooks` 切到 `codex-hooks-api`，session services 只保存可热替换的 injected `SharedHookRuntime` 和 factory；config refresh 通过 factory 重建 hook runtime。app-server 与 MCP server 组合根注入 `codex_hooks::HooksRuntimeFactory`，core test harness 使用真实或 disabled factory。为保持 hooks crate 测试 fixture 兼容，`codex-hooks-api` 新增 `config-test-support` feature，仅在 `codex-hooks` dev/test graph 开启旧 `ConfigLayerStack -> HookConfigLayerStack` 转换，默认 normal graph 不启用。
    - hooks runtime 注入切片验证：定时清理触发后执行 `rtk cargo clean`，释放 4.3GiB。随后执行 `rtk cargo fmt -p codex-hooks-api -p codex-hooks -p codex-core -p codex-app-server -p codex-mcp-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-hooks-api -p codex-hooks`：通过，110 passed。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-mcp-server`：均通过。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-hooks"`：只显示 `codex-hooks-api` direct edge；执行 `rtk cargo tree -p codex-core --invert codex-hooks --edges normal --depth 8`：`warning: nothing to print`，确认 core normal graph 未间接拉回 full `codex-hooks`。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-hooks-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-hooks v|tokio v|regex v|sha2 v|codex-utils-output-truncation|codex-config v"; then printf "%s\n" "$tree" | rg "codex-hooks v|tokio v|regex v|sha2 v|codex-utils-output-truncation|codex-config v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,120p"'`：通过，默认 API graph 未拉回 full hooks/config 或 process runtime；`rtk cargo tree -p codex-hooks-api --depth 1 --edges normal` 显示 direct deps 为 `chrono`、`codex-config-types`、`codex-plugin-types`、`codex-protocol`、`codex-utils-absolute-path`、`serde`、`serde_json` 和 `toml`。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者仅打印既有 rules_rs well-known crate annotation warnings。
    - 下一刀盘点：`rtk cargo tree -p codex-core --depth 1 --edges normal` 显示 `codex-core-plugins`、`codex-state`、`codex-tools` 仍是 core direct normal deps；`--invert codex-core-plugins` / `--invert codex-state` 显示均为 core direct path，`--invert codex-tools` 显示 core direct 以及 `codex-extension-api -> codex-core` 路径。`codex-tools` 调用面覆盖 tool spec/dispatch 大量类型，不适合先做小切片；`codex-state` 牵涉 goal/agent-job/sqlite runtime，应单独设计 state API/facade。`codex-core-plugins` 的 core 调用面不只是常量：core 会构造/持有 `PluginsManager`，调用 `plugins_for_config`、`clear_cache`、`list_marketplaces_for_config`，并在 mcp/connectors/discoverable 路径临时 new manager。因此下一刀应先拆 `codex-core-plugins` API/trait 边界，把 `PluginConfigLayerStack`、`PluginsConfigInput`、marketplace/discoverable DTO、`PluginRuntime`/`SharedPluginRuntime` trait 和轻量 outcome 类型放到 API 层；full `PluginsManager` 作为 implementation 由 app-server/thread manager/test harness 注入。不能只移动 allowlist 常量，否则 core 仍会通过 manager direct edge 拉回 plugin store/loader/filesystem/tokio runtime。
    - 2026-06-21 CST 已完成 `codex-core-plugins-api` 前置切片：新增 `codex-core-plugins-api`，承载 plugin 专用 `PluginConfigLayerStack` / `PluginConfigLayerEntry` view、`PluginsConfigInput`、OpenAI curated/bundled marketplace name 常量和 tool-suggest discoverable plugin allowlist；full `codex-core-plugins` re-export 这些 API 保持旧路径兼容，并继续拥有 `PluginsManager`、plugin store/loader、marketplace install/read/list/upgrade、local filesystem/cache 和 analytics sink 注入。`codex-core` 的 config projection 和 discoverable allowlist 路径、`codex-chatgpt` 的 config input 路径已改为直接依赖 API crate。`codex-core` 当前仍 direct 依赖 full `codex-core-plugins`，因为 `McpManager`、session/thread manager、connectors 和 request-plugin-install 仍持有/调用 concrete `PluginsManager`；这是下一步 runtime trait 注入边界，不能把本切片解释为 full plugin manager 已从 core graph 移除。
    - core-plugins API 前置切片验证：执行 `rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-chatgpt`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo test -p codex-core-plugins-api -p codex-core-plugins`：均通过，仅打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core-plugins-api --depth 1 --edges normal`：direct deps 仅为 `codex-config-types`、`codex-utils-absolute-path` 和 `toml`。执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core-plugins-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-core-plugins v|codex-plugin v|tokio v|codex-file-system v|codex-git-utils v|codex-config v"; then printf "%s\n" "$tree" | rg "codex-core-plugins v|codex-plugin v|tokio v|codex-file-system v|codex-git-utils v|codex-config v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过，API crate 默认 graph 没有间接拉回 full plugin runtime、plugin loader、tokio、filesystem/git runtime 或 full config。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-core-plugins"`：显示 `codex-core-plugins` 与 `codex-core-plugins-api` 两条 direct edge；执行 `rtk cargo tree -p codex-core --invert codex-core-plugins --edges normal --depth 6`：显示剩余路径为 `codex-core-plugins -> codex-core` direct，作为下一步 manager trait 注入待拆边界。
    - 2026-06-21 CST 已完成 `codex-core-plugins-api` 第一段 runtime trait 注入：API crate 新增 `PluginRuntime` / `SharedPluginRuntime` / `DisabledPluginRuntime`、`PluginLoadOutcome` alias 和 `is_configured_plugin_installed` 窄查询；full `PluginsManager` 实现该 trait。`SessionServices`、`CodexSpawnArgs`、`Session::new`、`McpManager` 和 `Config::to_mcp_config` 已从 concrete `PluginsManager` 收敛到 `SharedPluginRuntime` / `&dyn PluginRuntime`，request-plugin-install completion verification 改为调用 trait 的 installed bool 查询。`ThreadManager` 当前仍持有 concrete `PluginsManager`，因为 app-server plugin processors 仍通过 `ThreadManager::plugins_manager()` 使用 install/uninstall/list/read/upgrade 等完整管理 API；后续需要让 app-server 组合根自己持有 concrete manager，core 只保存 runtime trait。
    - 为修复 runtime trait 依赖门禁，已把纯 DTO `PluginSkillRoot` 下沉到 `codex-plugin-types`，`codex-utils-plugins` 只保留旧路径 re-export；`codex-plugin` 移除对 `codex-utils-plugins` 的依赖，只保留纯 plugin outcome/types facade。执行 `rtk cargo test -p codex-plugin -p codex-core-plugins-api -p codex-core-plugins`：145 passed。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-utils-plugins -p codex-core-skills` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warnings。执行 `rtk cargo tree -p codex-core-plugins-api --depth 1 --edges normal`：direct deps 为 `codex-config-types`、轻量 `codex-plugin`、`codex-utils-absolute-path` 和 `toml`。禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core-plugins-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-core-plugins v|tokio v|codex-file-system v|codex-git-utils v|codex-config v"; then printf "%s\n" "$tree" | rg "codex-core-plugins v|tokio v|codex-file-system v|codex-git-utils v|codex-config v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,100p"'`：通过，确认 API crate 没有经 `codex-plugin` 间接拉回 filesystem/default-client、tokio、full config、git runtime 或 full plugin manager。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-plugin --edges normal); if printf "%s\n" "$tree" | rg -q "codex-utils-plugins v|codex-file-system v"; then printf "%s\n" "$tree" | rg "codex-utils-plugins v|codex-file-system v"; exit 1; fi; printf "%s\n" "$tree" | sed -n "1,80p"'`：通过，确认 `codex-plugin` 本身不再依赖 plugin utils/filesystem。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-core-plugins"`：仍显示 full `codex-core-plugins` 与 `codex-core-plugins-api` 两条 direct edge，full edge 剩余原因是 ThreadManager/app-server 管理 API、connectors 临时 manager 和 discoverable marketplace detail 路径。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `ThreadManager::plugins_manager()` concrete 管理入口拆除：`ThreadManagerState` 只保存 `SharedPluginRuntime`，`ThreadManager::plugin_runtime()` 只返回 trait object；core `Session`、subagent spawn、MCP config、connectors/discoverable plugin 列表和 request-plugin-install 只依赖 `PluginRuntime`。app-server 组合根显式持有 full `PluginsManager`，把 clone 作为 `SharedPluginRuntime` 注入 `ThreadManager`，plugin/config/catalog/account/external-agent processors 自己接收 full manager 处理 install/list/read/marketplace management API；mcp-server 和 TUI 在组合根/客户端边界创建 full manager 后作为 runtime 注入 core 调用。`codex-core` production manifest 已把 full `codex-core-plugins` 移出 normal graph，仅保留 API crate 和测试/dev 边界。
    - `ThreadManager::plugins_manager()` 拆除验证：执行 `rtk cargo fmt -p codex-app-server -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --tests`：通过，仅打印既有 warnings；首次复跑暴露 `discoverable_tests.rs` 少导入 API allowlist/marketplace 常量，已改为从 `codex-core-plugins-api` 导入。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过；首次 app-server build 暴露 apps/plugin helper 中 free function 误用 `self`、processor 少 `PluginsManager` import 和 app-server 少 `codex-core-plugins-api` manifest dependency，已补齐后复跑通过。执行 `rtk cargo check -p codex-mcp-server` 和 `rtk cargo check -p codex-tui`：均通过。执行 `rtk cargo test -p codex-plugin -p codex-core-plugins-api -p codex-core-plugins`：退出码 0；执行 `rtk cargo test -p codex-core discoverable`：通过。执行门禁 `rtk sh -lc 'set -e; core_plugins=$(cargo tree -p codex-core --depth 1 --edges normal | rg "codex-core-plugins" || true); ...'`：输出只含 `codex-core-plugins-api` direct edge，`cargo tree -p codex-core --invert codex-core-plugins --edges normal --depth 8` 为 `warning: nothing to print`，`codex-core-plugins-api` 默认 graph 未拉 `codex-core-plugins`、`codex-core-plugins-remote`、`tokio`、`codex-file-system`、`codex-git-utils` 或 full `codex-config`，`codex-plugin` 默认 graph 未拉 `codex-utils-plugins` 或 `codex-file-system`。
    - 下一刀盘点：`rtk cargo tree -p codex-core --depth 1 --edges normal` 现在显示 `codex-core-plugins` 已消失，只剩 `codex-core-plugins-api`。`rtk cargo tree -p codex-core --invert codex-state --edges normal --depth 5` 仍是 direct `codex-state -> codex-core`；`rtk cargo tree -p codex-core --invert codex-tools --edges normal --depth 5` 显示 direct `codex-tools -> codex-core` 以及 `codex-tools -> codex-extension-api -> codex-core`。`codex-tools` 调用面覆盖 tool spec/dispatch/tool registry，大概率需要更大 tool-runtime 分层；下一刀优先处理 `codex-state`，拆 goal/agent-job/log/thread metadata SQLite runtime 和 core 只读/写 helper 的 API/facade，验收目标是 `codex-core --invert codex-state --edges normal` 无路径且不能通过新 API crate 间接拉回 SQLite/sqlx runtime。
    - 2026-06-21 CST 已完成 `codex-state-api` 第一批纯 DTO 切片：新增 `codex-rs/state-api`，迁移 `DirectionalThreadSpawnEdgeStatus`、`ThreadGoalStatus`、`ThreadGoal`、`ThreadGoalUpdate`、`ThreadGoalAccountingMode` 和 `ThreadGoalAccountingOutcome`。`codex-state` 改为依赖并 re-export API 类型，SQLite row conversion 和 `StateRuntime` 仍留在 full state runtime crate。`codex-core` 的 goal runtime、thread manager 和 agent control 中这些纯 goal/edge 类型引用已改为 `codex-state-api`；core 仍通过 `state_db_bridge` 使用 full `codex_state::StateRuntime`、`ThreadMetadata` 和 agent-job runtime，这是下一步 facade/trait 边界，不能把本切片解释为 full state runtime 已从 core graph 移除。
    - state API 首批切片验证：执行 `rtk cargo fmt -p codex-state-api -p codex-state -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-state-api -p codex-state`：通过；初次发现 `state/src/model/thread_goal.rs` 两个 unused import，已清理后复跑通过。执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo check -p codex-core --tests`：均通过，仅打印既有 warnings。定时清理触发后执行 `rtk cargo clean`，释放 24.9GiB；随后执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过。执行 `rtk sh -lc 'set -e; api_tree=$(cargo tree -p codex-state-api --edges normal); ...'`：`codex-state-api` 默认 graph 未出现 `codex-state`、`sqlx`、`tokio` 或 `tracing-subscriber`；同一门禁记录当前 core state path 仍为 `codex-state -> codex-core` direct，同时新增 `codex-state-api` direct API edge，下一步继续拆 runtime facade。
    - 2026-06-21 CST 已完成 `codex-state-api` runtime facade 切片：`codex-state-api` 新增 `AgentJob` / `AgentJobItem` / thread metadata DTO 与 `ThreadStateRuntime`、`GoalStateRuntime`、`AgentJobStateRuntime`、`MemoryStateRuntime`、`StateDbRuntime` / `SharedStateDbRuntime` object-safe traits；full `codex-state::StateRuntime` 在 `state/src/runtime/api.rs` 实现这些 traits，SQLite row conversion、migrations 和 concrete runtime 仍留在 full state crate。`codex-core` production `StateDbHandle` 改为 `Arc<dyn StateDbRuntime>`，`codex-state` manifest edge 改为 optional/test-support；core 的 dynamic tools、memory polluted、thread metadata insert、goal persistence 和 agent-job callsites 只依赖 state-api trait/DTO。app-server、mcp-server 和 thread-manager-sample 这类组合根继续持有 concrete `StateRuntime` 给 thread-store/rollout 使用，并显式投影成 trait object 注入 `ThreadManager`；`codex-core-api` 暴露 `CoreStateDbHandle` 与 `core_state_db_from_state_db`，保持 sample 单 facade dependency。
    - state runtime facade 验证：执行 `rtk cargo fmt -p codex-core-api -p codex-mcp-server -p codex-thread-manager-sample`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-state"`：只显示 `codex-state-api`。执行 `rtk cargo tree -p codex-core --invert codex-state --edges normal --depth 8`：`warning: nothing to print`，确认 core default normal graph 已无 full `codex-state` direct 或 indirect production 路径。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-state-api --edges normal); if printf "%s\n" "$tree" | rg -q "codex-state v|sqlx v|tokio v|tracing-subscriber v"; then ...'`：通过，API crate 未间接拉回 full state/sqlx/tokio/tracing-subscriber runtime。执行 `rtk cargo check -p codex-mcp-server -p codex-tui -p codex-thread-manager-sample`：首次暴露 mcp-server/sample 的 state_db trait object coercion 漏点，补齐后复跑通过。执行 `rtk cargo check -p codex-state-api -p codex-state -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warnings。
    - 2026-06-21 CST 已完成 `codex-tool-types` 基础类型切片：新增 `codex-rs/tool-types` 承载 `ToolCall`、`ToolPayload`、`ToolOutput` / `JsonToolOutput`、`FunctionCallError`、`ToolExecutor` trait、JSON schema subset、Responses API tool shape、`ToolSpec` 和 `ToolName` re-export；`codex-tools` 改为 re-export 这些基础类型并继续拥有 tool planning/discovery/MCP conversion/tool factory helper。`codex-extension-api` 改为依赖 `codex-tool-types`，不再为了 extension-owned `ToolExecutor` / `ToolSpec` 拉入完整 `codex-tools`；`codex-tools` 内原 json_schema/tool_spec 测试模块改为测试新 crate re-export，避免迁移后测试覆盖丢失。
    - tool-types 切片验证：执行 `rtk cargo fmt -p codex-tool-types -p codex-tools -p codex-extension-api`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-tool-types -p codex-tools -p codex-extension-api`：通过。执行 `rtk cargo test -p codex-tools`：97 passed，测试覆盖数恢复到迁移前水平。执行 `rtk cargo check -p codex-file-subscription -p codex-memories-extension -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warnings。执行 `rtk sh -lc 'set -e; if cargo tree -p codex-extension-api --invert codex-tools --edges normal --depth 4 ...'`：Cargo 返回 `codex-tools` 不在 extension-api graph，确认 `codex-extension-api` 已无 `codex-tools` normal dependency。执行 `rtk cargo tree -p codex-core --invert codex-tools --edges normal --depth 8`：只剩 `codex-tools -> codex-core` direct path，原 `codex-tools -> codex-extension-api -> codex-core` 间接路径已消失。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-tool-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-tools v|codex-extension-api v|codex-core v"; then ...'`：通过，基础类型 crate 没有间接拉回 tools/extension/core。
    - 2026-06-21 CST 已完成 `codex-tool-config` 配置/capability 切片：新增 `codex-rs/tool-config` 承载 `ToolsConfig` / `ToolsConfigParams`、`ShellCommandBackendConfig`、`ToolUserShellType`、`UnifiedExecShellMode`、`ZshForkConfig`、`ToolEnvironmentMode`、`request_user_input_available_modes` 和 image-detail capability/sanitization helpers；`codex-tools` 只保留 re-export wrapper 和原测试入口。`codex-core` 的 session/tools production callsites 改为直接依赖 `codex-tool-config`，不再为了 ToolsConfig 或 image-detail helper 经 `codex-tools` re-export。
    - tool-config 切片验证：执行 `rtk cargo fmt -p codex-tool-config -p codex-tools -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-tool-config -p codex-tools -p codex-core --tests`：通过，仅打印既有 warnings。执行 `rtk cargo test -p codex-tools`：97 passed，确认 wrapper 测试仍覆盖迁移后的 config/image-detail 行为。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 warnings。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-tool-config --edges normal); if printf "%s\n" "$tree" | rg -q "codex-tools v|codex-extension-api v|codex-core v"; then ...'`：通过，配置 crate 未间接拉回 tools/extension/core。执行 `rtk cargo tree -p codex-core --invert codex-tools --edges normal --depth 8`：仍只剩 `codex-tools -> codex-core` direct path，没有新增 indirect path。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-tool|codex-tools"`：显示 `codex-tool-config` 和 `codex-tools` 两条 direct edge，说明 config 已作为单独边界注入 core，剩余 direct `codex-tools` 仍是后续 factory/planning/discovery 调用面。
    - 2026-06-21 CST 已完成 `codex-tool-planning` planning/facade 切片：新增 `codex-rs/tool-planning` 承载原 `codex-tools` 中剩余的 tool discovery、request-plugin-install plan/helper、MCP/dynamic conversion、hosted model specs、namespace merge、agent pattern filter、code-mode exec prompt plan 和内建 tool spec 构造器；`codex-tools` 退成只 re-export `codex-tool-planning` 的兼容 facade。`codex-core` production/test callsites 全部改为直接依赖 `codex-tool-planning`，不再通过 `codex-tools` facade 获取 planning/factory helper。
    - tool-planning 切片验证：执行 `rtk cargo fmt -p codex-tool-planning -p codex-tools -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-tool-planning -p codex-tools -p codex-core --tests`：通过，仅打印既有 warnings。执行 `rtk cargo test -p codex-tool-planning`：97 passed，原 tools planning/spec 测试覆盖已迁到新 owner crate。执行 `rtk cargo test -p codex-tools`：0 passed，确认 facade crate 可编译且不再拥有实现测试。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-tool|codex-tools"`：只显示 `codex-tool-config` 和 `codex-tool-planning`，不显示 `codex-tools`。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$tree" | rg -q "codex-tools v"; then ...'`：通过，core default normal graph 已无 `codex-tools` direct 或 indirect path。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-tool-planning --edges normal); if printf "%s\n" "$tree" | rg -q "codex-tools v|codex-extension-api v|codex-core v"; then ...'`：通过，新 planning crate 未间接拉回 facade、extension API 或 core。执行 `rtk cargo tree -p codex-tool-planning --depth 1 --edges normal`：direct deps 为 `async-trait`、`codex-code-mode-api`、`codex-connectors-types`、`codex-mcp-tool-types`、`codex-protocol`、`codex-tool-config`、`codex-tool-types`、`codex-utils-string`、`serde`、`serde_json`、`thiserror` 和 `tracing`。
    - 2026-06-21 CST 已完成 `codex-git-info` git metadata/discovery 切片：新增 `codex-rs/git-info` 承载 repo root/worktree trust root、remote URL normalization、branch/default branch、recent commits、merge-base、thread metadata 所需 git command helper、`GitInfo` 和 `GitSha` re-export；`codex-git-utils` 退为 patch/apply helper crate，并 re-export `codex-git-info` 保持旧路径兼容。`codex-core`、`codex-config-local-loader`、app-server、TUI、thread-store、rollout/state metadata、analytics、cloud-tasks、core-plugins、exec 和 secrets 等非 apply consumer 改为直接依赖 `codex-git-info`，不再经 `codex-git-utils` facade 拉入 apply parser/runtime 依赖。
    - git-info 切片验证：执行 `rtk cargo fmt -p codex-git-info -p codex-git-utils -p codex-core -p codex-config-local-loader -p codex-app-server -p codex-tui -p codex-thread-store -p codex-rollout -p codex-state -p codex-analytics -p codex-cloud-tasks -p codex-core-plugins -p codex-exec -p codex-secrets`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-git-info -p codex-git-utils --tests`：通过。执行 `rtk cargo check -p codex-core --tests -p codex-config-local-loader`、`rtk cargo check -p codex-app-server -p codex-tui -p codex-thread-store -p codex-rollout -p codex-state` 和 `rtk cargo check -p codex-analytics -p codex-cloud-tasks -p codex-core-plugins -p codex-exec -p codex-secrets`：均通过，仅打印既有 warnings。执行 `rtk cargo test -p codex-git-info -p codex-git-utils`：16 passed。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-git"`：只显示 `codex-git-info`。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$tree" | rg -q "codex-git-utils v"; then ...'` 和同样针对 `codex-config-local-loader` 的门禁：均通过，确认 core/config-local-loader normal graph 不再直接或间接触达 `codex-git-utils`。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-git-info --edges normal); if printf "%s\n" "$tree" | rg -q "codex-git-utils v|regex v|tempfile v|walkdir v|codex-core v"; then ...'`：通过，git-info 未拉回 apply facade、regex/tempfile/walkdir 或 core。执行 `rtk cargo tree -p codex-git-info --depth 1 --edges normal`：direct deps 为 `codex-file-system`、`codex-protocol`、`codex-utils-absolute-path`、`futures`、`once_cell`、`schemars`、`serde`、`thiserror`、`tokio` 和 `ts-rs`；执行 `rtk cargo tree -p codex-git-utils --depth 1 --edges normal`：direct deps 为 `codex-git-info`、`once_cell`、`regex` 和 `tempfile`。
    - 2026-06-21 CST 已完成 `codex-plugin-manifest` manifest/namespace helper 切片：新增 `codex-rs/plugin-manifest` 承载 plugin manifest path discovery 和 skill path 到 plugin namespace 的 filesystem helper；`codex-plugin-types` 承载 `TOOL_MENTION_SIGIL` / `PLUGIN_TEXT_MENTION_SIGIL` 纯 mention 常量；`codex-utils-plugins` 改为兼容 re-export，继续保留 MCP connector/plugin utility helper。`codex-core` 和 `codex-core-skills` 的 mention/skill-root/namespace 调用改为直接依赖 `codex-plugin-types` / `codex-plugin-manifest`，不再为了纯 mention 常量或 skill namespace 查询把 `codex-utils-plugins` facade 拉入 `codex-core` default normal graph。
    - plugin-manifest 切片验证：执行 `rtk cargo fmt -p codex-plugin-manifest -p codex-plugin-types -p codex-utils-plugins -p codex-core-skills -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-plugin-manifest -p codex-plugin-types -p codex-utils-plugins -p codex-core-skills -p codex-core --lib`、`rtk cargo test -p codex-plugin-manifest -p codex-core-skills`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，测试为 103 passed；只打印既有 unused/dead_code warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct deps 不再显示 `codex-utils-plugins`；执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$tree" | rg -q "codex-utils-plugins v"; then ...'`：通过，core default normal graph 无 `codex-utils-plugins` direct 或 indirect path。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core-skills --edges normal); if printf "%s\n" "$tree" | rg -q "codex-utils-plugins v|codex-default-client v"; then ...'`：通过，core-skills 默认 graph 无旧 plugin utility facade 或 default-client。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-plugin-manifest --edges normal); if printf "%s\n" "$tree" | rg -q "codex-utils-plugins v|codex-default-client|codex-core v|codex-core-skills v"; then ...'`：通过，新 helper crate 未拉回旧 facade、default-client、core 或 core-skills。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `codex-response-debug-context` facade 反转切片：`ResponseDebugContext`、`extract_response_debug_context` 和 `telemetry_transport_error_message` 的 canonical implementation 移入 `codex-api-types`；`codex-response-debug-context` 退为只 re-export `codex-api-types` helper 的兼容 facade。`codex-core`、`codex-model-provider` 和 `codex-api` 的生产路径改为直接依赖 `codex-api-types`，不再为了 transport/API debug context 或 telemetry-safe error message 把旧 facade 拉入 core graph。
    - response-debug-context 反转切片验证：执行 `rtk cargo fmt -p codex-api-types -p codex-response-debug-context -p codex-core -p codex-api -p codex-model-provider`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-types -p codex-response-debug-context -p codex-core --lib -p codex-model-provider`、`rtk cargo test -p codex-api-types -p codex-response-debug-context`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，测试为 23 passed。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$tree" | rg -q "codex-response-debug-context v"; then ...'`：通过，core default normal graph 无旧 facade direct 或 indirect path。执行 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-api-types --edges normal); if printf "%s\n" "$tree" | rg -q "codex-response-debug-context v"; then ...'`：通过，API types crate 未间接回拉旧 facade。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 Windows sandbox deny-read resolver 下沉切片：`resolve_windows_deny_read_paths` 从 `codex-windows-sandbox` 移到 `codex-sandboxing-api`，`codex-windows-sandbox` 仅 re-export 旧路径；`codex-core` 的跨平台 override 解析改为直接调用 `codex_sandboxing_api::resolve_windows_deny_read_paths`，并把 `codex-windows-sandbox` manifest edge 移到 `target.'cfg(windows)'.dependencies`，避免 macOS/Linux 默认 core graph 为纯 policy resolver 编译 Windows sandbox runtime。
    - Windows sandbox resolver 切片验证：执行 `rtk cargo fmt -p codex-sandboxing-api -p codex-windows-sandbox -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-sandboxing-api -p codex-windows-sandbox -p codex-core --lib`：通过，仅打印既有 core unused import warning。执行 `rtk cargo test -p codex-sandboxing-api windows_deny_read`：8 passed。执行门禁 `rtk sh -lc 'set -e; core_tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$core_tree" | rg -q "codex-windows-sandbox v"; then ...; fi; api_tree=$(cargo tree -p codex-sandboxing-api --edges normal); if printf "%s\n" "$api_tree" | rg -q "codex-windows-sandbox v|codex-otel v|windows v"; then ...; fi'`：通过；`codex-core --depth 1` 只显示 `codex-sandboxing-api`，不显示 `codex-windows-sandbox`，`codex-sandboxing-api --depth 1` 未拉回 Windows runtime/OTEL/windows crate。随后执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `tempfile` production graph 清理切片：`codex-utils-path::write_atomically` 从 `tempfile::NamedTempFile` 改为基于 `std::fs::OpenOptions::create_new` 的同目录唯一临时文件 + rename 实现，并补充覆盖创建父目录与替换已有文件的单测；`codex-utils-path` 的 `tempfile` 仅保留为 dev-dependency。`codex-core` manifest 中 `tempfile` 从 normal dependency 移除，保留 dev-dependency 供测试 fixture 使用，避免 core default normal graph 为测试临时目录或 config/plugin atomic write helper 编译 `tempfile` / `rustix` / `getrandom`。
    - `tempfile` 清理切片验证：执行 `rtk cargo fmt -p codex-utils-path -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-utils-path`：6 passed。执行 `rtk cargo check -p codex-utils-path -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --invert tempfile --edges normal --depth 8`：`warning: nothing to print`；执行禁止项门禁 `rtk sh -lc 'set -e; tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$tree" | rg -q "tempfile v"; then ...; fi'` 与同样针对 `codex-utils-path` 的门禁：均通过。执行 `rtk cargo tree -p codex-utils-path --depth 1 --edges normal`：direct deps 只剩 `codex-utils-absolute-path` 和 `dunce`。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `codex-core-skills` remote zip 依赖收缩切片：`core-skills/src/remote.rs` 已经被 `remote-http` feature gate 住，但 `zip` 原本仍是 default normal dependency，导致 `codex-core` 默认 graph 为未接线的远程 skill export 解压编译 `zip`、压缩算法和加密相关依赖。现已把 `zip` 改为 optional dependency，并由 `remote-http = ["codex-default-client", "zip"]` 显式启用；默认本地 skill loading/render/injection graph 不再携带远程 zip 解压实现。
    - remote zip 依赖收缩验证：执行 `rtk cargo fmt -p codex-core-skills -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core-skills`：101 passed。执行 `rtk cargo check -p codex-core-skills --features remote-http`：通过，覆盖 remote HTTP + zip extraction feature graph。执行默认 graph 门禁 `rtk sh -lc 'set -e; core_tree=$(cargo tree -p codex-core --edges normal); if printf "%s\n" "$core_tree" | rg -q "zip v"; then ...; fi; skills_tree=$(cargo tree -p codex-core-skills --edges normal); if printf "%s\n" "$skills_tree" | rg -q "zip v"; then ...; fi'`：通过；直接执行 `rtk cargo tree -p codex-core --invert zip --edges normal --depth 8` / `rtk cargo tree -p codex-core-skills --invert zip --edges normal --depth 8` 均返回 package ID 未匹配，说明 default resolution 中已无 `zip`。执行 `rtk cargo tree -p codex-core-skills --features remote-http --invert zip --edges normal --depth 4`：显示 `zip -> codex-core-skills`，确认 zip 只在显式 feature graph 中出现。随后执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `codex-workflow-api` manifest `serde_yaml` 收缩切片：`WORKFLOW.md` frontmatter 解析从 `serde_yaml::from_str` 改为 `codex-workflow-api` 内部的窄 manifest parser，只支持 workflow schema 需要的标量、`when_to_use` / `whenToUse` block list 和 `inputs` 两层 block map；parser 单测覆盖当前 feature-dev manifest 形状、quoted scalar、camelCase alias、缺少 input `type` 和 inline collection 拒绝路径。`codex-workflow-api` normal dependency 已移除 `serde_yaml`，避免 `codex-core` 默认 graph 为 workflow discovery 的 frontmatter 解析拉完整 YAML runtime。
    - workflow manifest parser 依赖收缩验证：执行 `rtk cargo fmt -p codex-workflow-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-workflow-api`：13 passed。执行 `rtk cargo tree -p codex-workflow-api --depth 1 --edges normal`：direct normal deps 只剩 `serde` 和 `serde_json`；执行 `rtk sh -lc 'set -e; tree=$(rtk cargo tree -p codex-workflow-api --edges normal); if printf "%s\n" "$tree" | rtk rg -q "serde_yaml v|codex-core v"; then ...; fi; printf "%s\n" "$tree"'`：通过，确认 workflow API graph 不再含 `serde_yaml` 或 `codex-core`。执行 `rtk cargo tree -p codex-core --invert serde_yaml --edges normal --depth 8`：只剩 `serde_yaml -> codex-core` 和 `serde_yaml -> codex-core-skills -> codex-core`，不再显示 `codex-workflow-api -> codex-core` 路径；执行对应禁止 `codex-workflow-api` 的反向门禁也通过。随后执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `codex-core` Markdown agent role `serde_yaml` direct edge 清理切片：`core/src/config/agent_roles.rs` 的 Markdown agent role frontmatter 解析从 `serde_yaml::from_str` 改为 core 内部窄 parser，只支持当前 agent role schema 需要的 `name` / `description` / `model` / `effort` / `model_reasoning_effort` 标量，以及 `tools` / `skills` 的 `"*"`、block list 和 inline list；未知字段仍按旧行为忽略以兼容 `level`、`parallelizable`、`schema_version` 等 metadata。`Config` 测试新增 `tools: [exec_command, web]` 和 `skills: [code-review, "code-review-*"]` 的真实 agent 文件写法覆盖，`codex-core` manifest 移除 `serde_yaml` normal dependency。
    - Markdown agent role parser 依赖收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core discovered_markdown_agent`：退出码 0，覆盖 Markdown agent discovery、默认字段和新增 inline list case。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct normal deps 不再显示 `serde_yaml`；执行 `rtk sh -lc 'set -e; tree=$(rtk cargo tree -p codex-core --invert serde_yaml --edges normal --depth 8); if printf "%s\n" "$tree" | rtk rg -q "^[├└]── codex-core v"; then ...; fi; printf "%s\n" "$tree"'`：通过，反向图只剩 `serde_yaml -> codex-core-skills -> codex-core`，确认 direct core YAML path 已清掉且未变成新的 indirect core-internal path。执行 `rtk rg -n 'serde_yaml' codex-rs/core codex-rs/Cargo.lock codex-rs/core/Cargo.toml`：core 源码和 manifest 无匹配，Cargo.lock 中 `codex-core` package dependencies 也不再列出 `serde_yaml`。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `codex-core-skills` skill manifest `serde_yaml` 清理切片：`core-skills/src/loader.rs` 的 `SKILL.md` frontmatter 和 `agents/openai.yaml` metadata 解析从 `serde_yaml` 改为内部窄 parser；frontmatter 支持 `name`、`description`（含 `|-` block scalar）和 `metadata.short-description`，metadata 支持 `interface`、`dependencies.tools`、`policy.allow_implicit_invocation` 和 `policy.products` 当前 schema。为保持原 YAML parser 可吃 JSON 的兼容性，`openai.yaml` 内容以 `{` 开头时仍走 `serde_json::from_str::<SkillMetadataFile>`；YAML metadata 解析失败仍 fail-open warn 并忽略 metadata，`SKILL.md` frontmatter 解析失败仍作为 skill load error。`codex-core-skills` manifest 移除 `serde_yaml` normal dependency。
    - skill manifest parser 依赖收缩验证：执行 `rtk cargo fmt -p codex-core-skills -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core-skills`：101 passed。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-core --invert serde_yaml --edges normal --depth 8` 和 `rtk cargo tree -p codex-core-skills --invert serde_yaml --edges normal --depth 8`：均返回 package ID 未匹配，说明对应 default resolution 中已无 `serde_yaml`。执行禁止项门禁 `rtk sh -lc 'set -e; core_tree=$(rtk cargo tree -p codex-core --edges normal); if printf "%s\n" "$core_tree" | rtk rg -q "serde_yaml v"; then ...; fi; skills_tree=$(rtk cargo tree -p codex-core-skills --edges normal); if printf "%s\n" "$skills_tree" | rtk rg -q "serde_yaml v"; then ...; fi'`：通过。执行 `rtk rg -n 'serde_yaml' codex-rs/core-skills codex-rs/core codex-rs/Cargo.lock codex-rs/core-skills/Cargo.toml codex-rs/core/Cargo.toml codex-rs/Cargo.toml`：core/core-skills 源码和 manifests 无匹配；workspace 只剩根 `Cargo.toml` workspace dependency 和 `codex-external-agent-migration` lock entry。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 `codex-core` direct `regex-lite` / `shlex` 收缩切片：`codex-utils-string::find_uuids` 从 regex 改为固定 ASCII UUID scanner，并移除 `codex-utils-string` normal `regex-lite` dependency；`codex-core` 的 `regex-lite` 改为 dev-dependency，仅供测试断言使用。`core/src/exec_policy.rs` 的 shell command 渲染改用已有 `codex_shell_command::parse_command::shlex_join`，`codex-core` normal manifest 不再直接依赖 `shlex`；测试仍以 dev-dependency 使用 `shlex` 构造命令。`shlex` 在 core normal graph 中仍存在于既有 owner 边界 `codex-shell-command`、`codex-execpolicy-api` 和 `codex-core-skills`，本切片只移除 core 生产 direct edge，未声称将其完全移出 graph。
    - regex/shlex 收缩验证：执行 `rtk cargo fmt -p codex-core -p codex-utils-string`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-utils-string`：18 passed。执行 `rtk cargo test -p codex-core --lib exec_policy`：93 passed / 1688 filtered。尝试执行较宽 `rtk cargo test -p codex-core exec_policy`：失败在既有集成测试 `suite::exec_policy::execpolicy_blocks_shell_invocation`，实际 command output 为 `blocked`，未包含预期 policy reason；该失败说明当前 dirty tree 的 integration policy-loading 路径仍需单独处理，本轮未把它作为通过证据。执行依赖门禁 `rtk sh -lc 'set -e; core_tree=$(rtk cargo tree -p codex-core --edges normal); if printf "%s\n" "$core_tree" | rtk rg -q "regex-lite v"; then ...; fi; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── shlex v"; then ...; fi; printf "core normal graph has no regex-lite; core direct normal deps have no shlex\n"'`：通过。执行 `rtk cargo tree -p codex-core --invert shlex --edges normal --depth 4`：显示剩余路径仅经 `codex-core-skills`、`codex-execpolicy-api` 和 `codex-shell-command`。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-21 CST 已完成 home-dir lookup owner 边界收缩切片：`codex-utils-home-dir` 新增 `home_dir()` helper，作为 `dirs::home_dir` 的统一 owner；`core/src/realtime_context.rs` 和 `core-skills/src/loader.rs` 改为调用 `codex_utils_home_dir::home_dir`，`codex-core` / `codex-core-skills` normal manifests 不再直接依赖 `dirs`。`codex-core` 仍把 `dirs` 保留为 dev-dependency 供 config loader tests 对比系统 home 行为。`dirs` 仍通过 `codex-utils-absolute-path` 和 `codex-utils-home-dir` 进入 core normal graph，本切片只收敛 core/core-skills direct edge，不宣称完全移除 `dirs` graph。
    - home-dir lookup 收缩验证：执行 `rtk cargo fmt -p codex-utils-home-dir -p codex-core -p codex-core-skills`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-utils-home-dir`：4 passed；执行 `rtk cargo test -p codex-core-skills`：101 passed。执行 direct dependency 门禁 `rtk sh -lc 'set -e; core_direct=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$core_direct" | rtk rg -q "^[├└]── dirs v"; then ...; fi; skills_direct=$(rtk cargo tree -p codex-core-skills --depth 1 --edges normal); if printf "%s\n" "$skills_direct" | rtk rg -q "^[├└]── dirs v"; then ...; fi; printf "core/core-skills direct normal deps have no dirs\n"'`：通过。执行 `rtk cargo tree -p codex-core --invert dirs --edges normal --depth 6`：剩余路径经 `codex-utils-absolute-path` 和 `codex-utils-home-dir`，没有 core/core-skills direct edge。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。
    - 2026-06-21 CST 已完成 turn-context timezone lookup owner 边界收缩切片：`codex-user-info` 新增 `current_timezone()` helper，内部封装 `iana_time_zone::get_timezone()`；`core/src/session/turn_context.rs` 的 `local_time_context()` 改为调用该 helper，`codex-core` normal manifest 不再直接依赖 `iana-time-zone`。该 crate 仍通过 `chrono` 和 `codex-user-info` 出现在 core normal graph，本切片只收敛 core direct edge，未尝试改变 chrono 的 platform timezone 实现依赖。
    - timezone lookup 收缩验证：执行 `rtk cargo fmt -p codex-user-info -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-user-info`：3 passed。执行 direct dependency 门禁 `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── iana-time-zone v"; then ...; fi; printf "core direct normal deps have no iana-time-zone\n"; rtk cargo tree -p codex-core --invert iana-time-zone --edges normal --depth 4'`：通过；反向图显示剩余路径经 `chrono` 和 `codex-user-info`。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。
    - 2026-06-21 CST 已完成 unified exec process id `rand` direct edge 收缩切片：`core/src/unified_exec/process_manager.rs` 的生产随机 process id 分配从 `rand::rng().random_range(1_000..100_000)` 改为复用 core 已有 `uuid` v4 capability 生成同一数值范围内的 id；测试强制 deterministic process id 路径保持不变。`codex-core` normal manifest 移除 direct `rand` dependency。`rand@0.9.3` 仍通过 `codex-agent-runtime`、`codex-command-runtime`、`codex-utils-backoff` 和 `codex-trace-context` 等现有边界进入 core graph，本切片只移除 unified exec 的 core direct edge。
    - unified exec process id 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core process_manager --lib`：通过。执行 direct dependency 门禁 `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── rand v"; then ...; fi; printf "core direct normal deps have no rand\n"; rtk cargo tree -p codex-core --invert rand --edges normal --depth 5'`：direct check 通过，但 `--invert rand` 因 workspace 同时存在 `rand@0.8.5` / `rand@0.9.3` 返回歧义；随后执行 `rtk cargo tree -p codex-core --invert rand@0.9.3 --edges normal --depth 5`：剩余路径经 `codex-agent-runtime`、`codex-command-runtime`、`codex-utils-backoff`、`codex-trace-context`，不再有 core direct edge；执行 `rtk cargo tree -p codex-core --invert rand@0.8.5 --edges normal --depth 5`：warning nothing to print。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。
    - 2026-06-21 CST 已完成 `tool_search` BM25 dependency 收缩切片：`core/src/tools/handlers/tool_search.rs` 不再依赖外部 `bm25` crate，改为 handler 内部轻量 `ToolSearchIndex`，按 ASCII tokenization、下划线/标点分隔、保守复数归一化和 BM25 scoring 排序 deferred tool search entries。该切片不是把 `bm25` 包成间接 dependency，而是从 `codex-core` normal graph 中删除该 crate；如果后续要扩展 search runtime，应先设计可注入 tool-search runtime，不能把外部 search engine crate 重新拉回 core default graph。
    - tool_search BM25 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core tool_search --lib`：通过，覆盖 search index tokenization/description/schema term 和 mixed namespace coalescing；执行 `rtk cargo test -p codex-core tool_search_matches`：2 passed / 2571 filtered，覆盖 MCP/dynamic tool search 的 name/description/schema query。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── bm25 v"; then ...; fi; if rtk cargo tree -p codex-core --invert bm25 --edges normal --depth 6 ... | rtk rg -q "bm25 v"; then ...; fi; ...'`：通过，`cargo tree --invert bm25` 返回 package ID 未匹配，确认 core normal graph 已无 `bm25`。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk just bazel-lock-update` 和 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；随后执行 `rtk rg -n "bm25" MODULE.bazel.lock MODULE.bazel codex-rs/Cargo.lock codex-rs/core/Cargo.toml codex-rs/core/src/tools/handlers/tool_search.rs AGENTS.md .codex/pm-progress.md`：确认源码、manifest、Cargo lock 和 Bazel lock 均无 `bm25`，只剩文档记录；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 `agent_jobs` CSV direct dependency 收缩切片：`core/src/tools/handlers/agent_jobs.rs` 的 `parse_csv` 不再使用外部 `csv` crate，改为 handler 内部小型 parser，覆盖 header/BOM、quoted field、escaped quote、quoted newline、CRLF、flexible row length 和空行过滤；`render_job_csv` / `csv_escape` 输出路径保持本地实现。`codex-core` manifest 和 workspace dependency table 移除 direct `csv` 依赖；`csv` 仍通过 `rama-http` 出现在全 workspace/app-server graph，本切片只声明 core default normal graph 不再 direct/indirect 依赖 `csv`。
    - agent_jobs CSV 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core agent_jobs --lib`：通过，覆盖 quotes/commas、BOM+CRLF+empty row、quoted newline+escaped quotes、unterminated quote、CSV escaping 和 agent job template/header helper。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── csv v"; then ...; fi; invert=$(rtk cargo tree -p codex-core --invert csv --edges normal --depth 6 2>&1 || true); ...'`：通过，反向图 warning nothing to print，确认 core normal graph 无 `csv` 路径。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 `test_support` once_cell direct dependency 收缩切片：`core/src/test_support.rs` 的 `TEST_MODEL_PRESETS` 从 `once_cell::sync::Lazy` 改为标准库 `std::sync::LazyLock`，`codex-core` normal manifest 移除 direct `once_cell` 依赖。`once_cell` 仍通过 `codex-git-info`、`codex-shell-command` 和 `tracing-core` 等 owner 路径进入 core graph，本切片只收敛 core test-support helper 的 direct edge，不把这些既有基础 crate 路径伪装成已删除。
    - once_cell direct 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core test_support --lib`：通过，覆盖 `#[cfg(test)]` 下的 test_support 编译；执行 `rtk cargo check -p codex-core --features test-support --lib`：通过，覆盖跨 crate 启用 test-support feature 的编译路径。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── once_cell v"; then ...; fi; printf "core direct normal deps have no once_cell\n"; rtk cargo tree -p codex-core --invert once_cell --edges normal --depth 4 | rtk sed -n "1,80p"'`：通过，反向图只显示剩余 indirect owner 路径。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 core `dunce` direct dependency 收缩切片：`core/src/agents_md.rs` 改用 `AbsolutePathBuf::canonicalize()`；`core/src/exec.rs` 的 Windows override path cleanup 改用 `codex_utils_path::normalize_for_native_workdir()`，elevated sandbox path comparison fallback 改用 `AbsolutePathBuf::canonicalize()`；`core/src/config/edit.rs` 的 skill config path normalization 改用 `AbsolutePathBuf::relative_to_current_dir(...).canonicalize()`；`core/src/windows_sandbox_read_grants.rs` 改用 `AbsolutePathBuf::canonicalize()`。`codex-core` normal manifest 移除 direct `dunce`，只把 `dunce` 保留为 dev-dependency 供 tests 构造 canonical fixture。剩余 normal graph 中的 `dunce` 路径归属 `codex-config-loader` / `codex-config-local-loader` / `codex-core-skills` / `codex-sandboxing-api` / `codex-utils-absolute-path` 等路径 owner crate，本切片不把这些现有路径伪装成已删除。
    - core dunce 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core agents_md --lib`：通过；执行 `rtk cargo test -p codex-core windows_elevated --lib`：6 passed；执行 `rtk cargo test -p codex-core set_skill_config --lib`：3 passed；执行 `rtk cargo test -p codex-core windows_sandbox_read_grants --lib`：3 passed。尝试执行较宽 `rtk cargo test -p codex-core exec --lib`：失败在既有 `tools::spec::tests::{test_build_specs_gpt5_codex_unified_exec_web_search,test_build_specs_gpt51_codex_unified_exec_web_search,test_gpt_5_1_codex_max_unified_exec_web_search}` tool ordering 断言（workflow tools 与 `close_agent` 顺序差异），与本切片路径归一化改动无关，未作为通过证据。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── dunce v"; then ...; fi; printf "core direct normal deps have no dunce\n"; rtk cargo tree -p codex-core --invert dunce --edges normal --depth 5 | rtk sed -n "1,120p"'`：通过，反向图只显示剩余 indirect owner 路径。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 MCP telemetry server-origin `url` direct dependency 收缩切片：`core/src/mcp_tool_call.rs` 的 `record_server_fields` 不再为了 span host/port 记录使用 `url::Url`，改为基于已存在的 `http::Uri` 解析 authority host、显式端口和 http/https/ws/wss 默认端口，并补充 `parse_server_fields` 单测覆盖默认端口、显式端口、IPv6 host normalization 和 invalid origin。`codex-core` normal manifest 移除 direct `url` dependency。剩余 normal graph 中的 `url` 路径归属 `codex-api-provider` 和 `codex-shell-command` 等已有 owner crate，本切片只收敛 MCP telemetry 的 core direct edge，不宣称全图移除 `url`。
    - MCP telemetry `url` 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk proxy cargo test -p codex-core parse_server_fields --lib`：通过，4 个新增 parser 单测通过。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── url v"; then ...; fi; printf "core direct normal deps have no url\n"; rtk cargo tree -p codex-core --invert url --edges normal --depth 5 | rtk sed -n "1,120p"'`：通过，反向图只显示剩余 indirect owner 路径。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 turn diff Git blob object-id `sha1` direct dependency 收缩切片：新增 `codex-git-info::git_blob_oid`，由 Git owner crate 承载 `blob <len>\0` header 语义和 SHA-1 object-id 计算；`core/src/turn_diff_tracker.rs` 改为调用该 API，删除 core 内部 `sha1::Sha1` helper；`codex-core` normal manifest 移除 direct `sha1` dependency，`codex-git-info` normal manifest 增加 `sha1`。该切片不是把 `sha1` 包成无语义 wrapper，而是把 Git object-id 语义归属到 git-info；剩余 `sha1` graph 路径还包括 `codex-connectors-api` 和 `codex-utils-cache` 等已有 owner。
    - turn diff `sha1` 收缩验证：执行 `rtk cargo fmt -p codex-git-info -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-git-info object_id`：2 passed / 6 filtered，覆盖 Git blob header object-id 和 raw SHA-1 区分。执行 `rtk cargo test -p codex-core turn_diff_tracker --lib`：通过，turn diff 相关测试通过。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── sha1 v"; then ...; fi; printf "core direct normal deps have no sha1\n"; rtk cargo tree -p codex-core --invert sha1 --edges normal --depth 6 | rtk sed -n "1,160p"'`：通过，反向图显示剩余 indirect owner 路径为 `codex-connectors-api`、`codex-git-info` 和 `codex-utils-cache`。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 core production `similar` direct dependency 收缩切片：`codex-apply-patch` 新增 `unified_diff_from_texts`，复用 apply-patch 已有 `similar` diff engine；`core/src/turn_diff_tracker.rs` 改为通过该 helper 生成带 header/context 的 turn unified diff。`core/src/config_lock.rs` 的 replay mismatch 错误消息改为本地小型行差异摘要，不再为错误展示路径依赖完整 diff engine。`codex-core` normal manifest 移除 direct `similar`，仅在 dev-dependencies 保留 `similar` 供 schema unit test；`core/tests/common` 继续自己声明测试支持所需 `similar`。剩余 normal graph 中 `similar` 只通过 `codex-apply-patch -> codex-core` 既有 owner 路径进入，本切片没有新增新的 indirect heavy path。
    - core `similar` 收缩验证：执行 `rtk cargo fmt -p codex-apply-patch -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-apply-patch unified_diff_from_texts_applies_headers_and_context`：1 passed / 84 filtered，覆盖新增 helper 的 headers/context 行为。执行 `rtk cargo test -p codex-core turn_diff_tracker --lib`：通过，turn diff 相关测试通过。执行 `rtk cargo test -p codex-core config_lock --lib`：7 passed / 1783 filtered，覆盖 config lock mismatch 报错仍包含 config drift 和 `model = `。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── similar v"; then ...; fi; printf "core direct normal deps have no similar\n"; rtk cargo tree -p codex-core --invert similar --edges normal --depth 6 | rtk sed -n "1,160p"'`：通过，反向图只剩 `similar -> codex-apply-patch -> codex-core`。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warnings。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-21 CST 已完成 active-turn/network approval `indexmap` direct dependency 收缩切片：`core/src/state/turn.rs` 新增本地 `ActiveTasks` 容器，用 `HashMap<String, RunningTask> + Vec<String>` 保留 active turn first-task 顺序语义，并用 `swap_remove` 复刻原 `IndexMap::swap_remove` 的局部行为；`core/src/tools/network_approval.rs` 的 active network approval calls 改为普通 `HashMap`，因为该路径只需要“唯一活跃调用”判断，不依赖顺序。`codex-core` normal manifest 移除 direct `indexmap` dependency。剩余 `indexmap@2.13.0` normal graph 路径归属 `toml_edit` / config edit/diagnostics/state/local-loader owner 路径，`indexmap@1.9.3` 不在 core normal graph 中。
    - core `indexmap` 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`：通过。执行 `rtk cargo test -p codex-core network_approval --lib`：通过，覆盖 network approval active call/outcome 路径。执行 dependency gate `rtk sh -lc 'set -e; direct_tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$direct_tree" | rtk rg -q "^[├└]── indexmap v"; then ...; fi; printf "core direct normal deps have no indexmap\n"; ...'`：direct gate 通过；随后分别执行 `rtk cargo tree -p codex-core --invert indexmap@2.13.0 --edges normal --depth 6` 与 `rtk cargo tree -p codex-core --invert indexmap@1.9.3 --edges normal --depth 6`：`indexmap@2.13.0` 只经 `toml_edit` / config owner 路径进入，`indexmap@1.9.3` warning nothing to print。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - `codex-rollout-trace` 下一刀复核：当前 core/app-server hot path 使用 `ThreadTraceContext`、`InferenceTraceContext`、tool dispatch context、raw payload/status DTO；CLI 和若干 core tests 使用 `replay_bundle` / `RolloutTrace` reduced model。`replay_bundle` 可以作为 replay/viewer 边界拆分或 feature-gate，但 raw event 仍依赖 `ExecutionStatus`、`RolloutStatus`、tool/code-cell reduced-model status 类型；直接把 reducer feature-gate 掉会把共享 status/model 和测试覆盖一起撕开，收益与风险不匹配。后续若继续处理该边界，应先设计 `codex-rollout-trace-api`（raw event/status/writer/context）+ replay/reducer implementation，或选择更清晰的 direct-only implementation crate。
    - 2026-06-21 CST 已完成 rollout trace hot-path API / reducer implementation 拆分切片：新增 `codex-rollout-trace-api`，承载 raw trace bundle layout、raw event schema、payload refs、writer、`ThreadTraceContext`、`InferenceTraceContext`、tool dispatch/code-cell/MCP/compaction trace contexts，以及 `ExecutionStatus`、`RolloutStatus`、`CodeCellRuntimeStatus`、`ToolCallKind`、`ToolCallSummary` 等 raw schema 共享类型；`codex-rollout-trace` 改为依赖并 re-export API crate，继续拥有 `replay_bundle`、reducer、reduced `RolloutTrace` viewer/debug model 和 replay tests。`codex-core` production manifest 改为 direct 依赖 `codex-rollout-trace-api`，full `codex-rollout-trace` 只保留为 dev-dependency 供 replay-oriented unit tests 使用。
    - rollout trace API 拆分验证：执行 `rtk cargo check -p codex-rollout-trace-api -p codex-rollout-trace -p codex-core --lib`：通过，仅打印既有 core unused import warning。执行 `rtk cargo test -p codex-rollout-trace-api`：7 passed；执行 `rtk cargo test -p codex-rollout-trace`：35 passed。执行 `rtk cargo test -p codex-core tool_dispatch_trace --lib`、`rtk cargo test -p codex-core mcp_tool_call --lib`、`rtk cargo test -p codex-core dropped_response_stream --lib`、`rtk cargo test -p codex-core dropped_backpressured_response_stream --lib`：均通过。执行 dependency gate `rtk sh -lc '... cargo tree -p codex-core --depth 1 --edges normal ... cargo tree -p codex-core --invert codex-rollout-trace --edges normal ... cargo tree -p codex-rollout-trace-api --depth 1 --edges normal ... cargo tree -p codex-rollout-trace-api --invert codex-rollout-trace --edges normal ...'`：core direct graph 只显示 `codex-rollout-trace-api`，`codex-core --invert codex-rollout-trace` 为 `warning: nothing to print`，API direct deps 只有 `anyhow`、`codex-code-mode-api`、`codex-protocol`、`http`、`serde`、`serde_json`、`tracing`、`uuid`，API `--invert codex-rollout-trace` 返回 package 不在 graph，implementation direct graph 显示 `codex-rollout-trace -> codex-rollout-trace-api`。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 压缩/中断恢复后复核 rollout trace API 切片：先响应 cargo-clean schedule，在确认无 Cargo/rustc 进程后执行一次 `rtk cargo clean`。随后执行 `rtk cargo fmt -p codex-rollout-trace-api -p codex-rollout-trace -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-rollout-trace-api -p codex-rollout-trace --tests`、`rtk cargo check -p codex-core --lib`、`rtk cargo test -p codex-rollout-trace`、`rtk cargo test -p codex-rollout-trace-api`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-rollout-trace"`：只显示 `codex-rollout-trace-api`；执行 `rtk cargo tree -p codex-core --invert codex-rollout-trace --edges normal --depth 6`：warning nothing to print；执行 `rtk cargo tree -p codex-rollout-trace-api --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-code-mode-api`、`codex-protocol`、`http`、`serde`、`serde_json`、`tracing`、`uuid`；执行 `rtk cargo tree -p codex-rollout-trace-api --invert codex-rollout-trace --edges normal --depth 6`：package ID 不在 graph。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-22 CST 已完成 core 用户 `config.toml` edit engine / `toml_edit` direct dependency 收缩切片：`core/src/config/edit.rs` 变为兼容 wrapper，真实 `ConfigEdit`、`ConfigEditsBuilder`、model/personality/service-tier/project trust/feature flag/user preference 写入、MCP server edit helper 和 tests 迁到 `codex-config-edit`。`codex-core` production manifest 不再 normal 依赖 `toml_edit`，只在 dev-dependencies 保留测试需要；`core/src/config/mod.rs` 和 `core/src/mcp_tool_call.rs` 的写配置路径改为通过 `ConfigEdit::set_string_path`，不再构造 `toml_edit::value`。`codex-config-edit` 进一步移除 `codex-features` / `codex-protocol` 依赖，feature default-false 元数据由 core wrapper 通过 `with_default_false_feature_keys` 注入，避免 edit crate 为 core-only registry 拉回 protocol。
    - config edit / `toml_edit` 收缩验证：执行 `rtk cargo fmt -p codex-config-edit -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-config-edit` 与 `rtk cargo test -p codex-config-edit`：通过，58 个 tests 通过，覆盖新增 default-false feature key 注入语义。执行 `rtk cargo tree -p codex-config-edit --depth 1 --edges normal`：direct deps 仅为 `anyhow`、`codex-config-types`、`codex-utils-absolute-path`、`codex-utils-path`、`dunce`、`serde_json`、`sha2`、`tokio`、`toml`、`toml_edit`；执行 `rtk cargo tree -p codex-config-edit --invert codex-protocol --edges normal --depth 6` 返回 package 不在 graph；执行 `rtk sh -lc 'for pkg in codex-features codex-config codex-app-server-protocol starlark rama-http codex-code-mode; do ...; done'`：这些禁止项均返回 package 不在 graph。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "toml_edit|codex-config-edit"`：只显示 direct `codex-config-edit`，不显示 direct `toml_edit`；执行 `rtk cargo tree -p codex-core --invert toml_edit@0.24.0+spec-1.1.0 --edges normal --depth 6`：剩余路径归属 `codex-config-diagnostics`、`codex-config-state`、`codex-config-local-loader` 和 `codex-config-edit` owner 边界；执行 `rtk cargo tree -p codex-core --invert toml_edit@0.23.10+spec-1.0.0 --edges normal --depth 6`：warning nothing to print。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-core test_set_project_trusted --lib`、`rtk cargo test -p codex-core mcp_tool_call --lib`、`rtk cargo check -p codex-cli -p codex-tui`、`rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-22 CST 已完成 core production `libc` direct dependency 收缩切片：当前用户 login shell path 查询从 `core/src/shell.rs` 下沉到 `codex-user-info::current_user_shell_path()`，保留 getpwuid_r 的 caller-owned buffer 行为；spawn pre_exec parent pid capture 改为 `codex_utils_pty::process_group::current_pid_for_parent_death_signal()`；Linux seccomp SIGSYS exit 判定改为 core 显式 `LINUX_SIGSYS_CODE`，测试同步使用该 Linux 常量，避免在 macOS 上混用本机 `libc::SIGSYS`。`codex-core` manifest 中 `libc` 从 normal dependency 移到 dev-dependencies，仅供 shell snapshot、mkfifo、kill 等测试路径使用；`codex-user-info` manifest 新增 `libc` 作为 OS user-info owner 依赖。
    - core `libc` direct 收缩验证：执行 `rtk cargo fmt -p codex-user-info -p codex-utils-pty -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-user-info`：4 passed；执行 `rtk cargo test -p codex-core detects_bash --lib`：1 passed；执行 `rtk cargo test -p codex-core sandbox_detection --lib`：8 passed；执行 `rtk cargo test -p codex-utils-pty process_group`：1 passed。尝试执行较宽 `rtk cargo test -p codex-core shell --lib`：失败在当前 dirty tree 的 guardian sandbox runtime unavailable 路径（`guardian_allows_shell_command_additional_permissions_requests_past_policy_validation`、`strict_auto_review_turn_grant_forces_guardian_for_shell_command_policy_skip`），与本切片的 shell lookup / libc direct dependency 移动无关，未作为通过证据。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "libc|codex-user-info|codex-utils-pty"`：只显示 `codex-user-info` 和 `codex-utils-pty`，不显示 direct `libc`；执行 `rtk cargo tree -p codex-core --invert libc --edges normal --depth 5`：剩余路径归属 `codex-config-local-loader`、`codex-shell-escalation`、`codex-user-info`、`codex-utils-pty`、`tokio`/`uuid`/crypto 等 owner/third-party paths，没有 core direct edge。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 core production `base64` direct dependency 收缩切片：image generation result decode 和 context history original-detail image payload decode 改为调用 `codex-utils-image::decode_base64_image_bytes()`；realtime audio frame duration/sample 估算改为调用 `codex-api-types::decoded_realtime_audio_samples_per_channel()`。`codex-core` manifest 中 `base64` 从 normal dependency 移到 dev-dependencies，仅供 core 测试构造 base64 fixture 使用；image bytes/base64 data URL helper 归属 `codex-utils-image`，realtime audio frame transport-neutral base64/sample helper 归属 `codex-api-types`。
    - core `base64` direct 收缩验证：执行 `rtk cargo test -p codex-api-types`：23 passed；执行 `rtk cargo test -p codex-utils-image`：7 passed；执行 `rtk cargo test -p codex-core realtime_conversation --lib`：通过；执行 `rtk cargo test -p codex-core image_data_url_payload --lib`：3 passed；执行 `rtk cargo test -p codex-core original_detail --lib`：通过；执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。验证期间两个 cargo-clean schedule 在冷 target 重建前触发，导致前两次 `codex-api-types` 测试因 target fingerprint/rmeta/OUT_DIR 被清空而失败；确认 target 仅剩 8KB 且无 Cargo/rustc/clean 进程后重跑通过，未作为代码失败记录。执行 direct dependency 门禁 `rtk cargo tree -p codex-core --depth 1 --edges normal`：只显示 direct `codex-api-types` 与 `codex-utils-image`，不显示 direct `base64`；执行 `rtk cargo tree -p codex-core --invert base64 --edges normal --depth 5`：剩余路径归属 `codex-api-types`、`codex-utils-image`、`codex-agent-identity-api`、`codex-config-local-loader`、`codex-exec-server-protocol`、`codex-shell-command` 和 `serde_with`/`codex-protocol` 等 owner/third-party paths，没有 core direct edge。
    - 2026-06-22 CST 已完成 core production `arc-swap` direct dependency 收缩切片：`ExecPolicyManager` 的 policy snapshot 和 `SessionServices` 的 hooks runtime snapshot 从 `ArcSwap` 改为标准库 `std::sync::RwLock<Arc<T>>`，保留同步 `current()` / `hooks()` clone snapshot API；低频 policy/hook reload/update 路径不再为了 lock-free swap 让 core normal graph 依赖 `arc-swap`。`arc-swap` 仍保留在 `codex-exec-server` 的高频 notification state owner 路径，本切片只移除 `codex-core` direct/indirect normal graph 中的该依赖。
    - core `arc-swap` direct 收缩验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`：通过。执行 `rtk cargo test -p codex-core exec_policy --lib`：通过；执行 `rtk cargo test -p codex-core refresh_runtime_config_refreshes_hooks --lib`：通过，覆盖 hooks runtime snapshot update/read 路径。尝试执行 `rtk cargo test -p codex-core reload_user_config_layer_refreshes_hooks --lib`：失败，reload 后 hooks 仍为 0；同文件内直接调用 `refresh_runtime_config` 的 hooks 测试已通过，失败更像当前 dirty tree 的 reload 文件层/信任状态问题，未作为本切片通过证据。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 direct dependency 门禁 `rtk cargo tree -p codex-core --depth 1 --edges normal`：不显示 direct `arc-swap`；执行 `rtk cargo tree -p codex-core --invert arc-swap --edges normal --depth 5`：warning nothing to print，确认 core normal graph 已无 `arc-swap` 路径。
    - 2026-06-22 CST 已完成 core local error `thiserror` direct dependency 收缩切片：`core/src/exec_policy.rs` 的 `ExecPolicyUpdateError` / test-only `ExecPolicyError` 和 `core/src/unified_exec/errors.rs` 的 `UnifiedExecError` 不再使用 `#[derive(thiserror::Error)]`，改为手写 `Display` / `std::error::Error` / `From<ExecPolicyRuleError>`，错误文本与 source 链保持原语义。`codex-core` normal manifest 移除 direct `thiserror` dependency；`AGENTS.md` 已记录 core 本地小型错误不要为了 derive 重新引入 `thiserror`，共享错误 contract 应归属 owner API crate。
    - core `thiserror` direct 收缩验证：执行 `rtk rg -n "thiserror|derive\\(.*Error\\)|#\\[error\\(" codex-rs/core/Cargo.toml codex-rs/core/src codex-rs/core/tests -g '*.rs' -g 'Cargo.toml'`：无匹配。执行 `rtk cargo fmt -p codex-core`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-core exec_policy --lib` 和 `rtk cargo test -p codex-core unified_exec::process --lib`：均通过。一次较宽的 `rtk cargo test -p codex-core unified_exec --lib` 失败在既有 tool spec 顺序断言（workflow tools 与 `close_agent` 顺序），与本切片 error 类型无关，未作为通过证据。执行 direct dependency 门禁 `rtk sh -lc 'set -e; tree=$(rtk cargo tree -p codex-core --depth 1 --edges normal); if printf "%s\n" "$tree" | rtk rg -q "^[├└]── thiserror v"; then ...; fi; printf "core direct normal deps have no thiserror\n"; ...'`：通过。执行 `rtk cargo tree -p codex-core --invert thiserror@2.0.18 --edges normal --depth 5`：剩余路径归属 `codex-api-provider`、`codex-api-types`、config/API/DTO crates、`codex-protocol`、`ts-rs`、`opentelemetry` 等 owner/third-party paths，没有 core direct edge；执行 `rtk cargo tree -p codex-core --invert thiserror@1.0.69 --edges normal --depth 5`：剩余路径为 `filedescriptor -> portable-pty -> codex-utils-pty -> codex-core`。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`、`rtk just bazel-lock-check` 和 `rtk git diff --check`：均通过，仅有既有 unused/dead_code 与 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 default-client identity/header 进一步拆分切片：新增 `codex-client-identity` 承载 `originator`、first-party originator 判断、residency header state、`default_identity_headers` 和 originator override 常量；`codex-default-client-api` 改为依赖并 re-export 该 identity crate，继续拥有 User-Agent probing、User-Agent suffix 和 full `default_headers`。`codex-core` production 只依赖 `codex-client-identity` 并在 Responses WebSocket / Realtime 请求中传 identity headers；`codex-api` concrete WebSocket/Realtime runtime 在真实连接边界用 `codex_default_client::default_headers()` 补齐完整 User-Agent/default headers，caller-provided headers 仍按既有优先级覆盖默认值。`AGENTS.md` 已记录 identity、full header API 和 reqwest runtime 三层边界。
    - default-client identity/header 拆分验证：执行 `rtk rg -n "codex_default_client_api|codex-default-client-api|codex_default_client::(default_headers|originator|is_first_party|set_default_client_residency_requirement|ResidencyRequirement|DEFAULT_ORIGINATOR|CODEX_INTERNAL)|codex_default_client::default_headers|default_identity_headers" codex-rs/core/src codex-rs/core/tests codex-rs/core/Cargo.toml -g '*.rs' -g 'Cargo.toml'`：core production 只剩 `codex-client-identity::default_identity_headers` 路径。执行 `rtk cargo fmt -p codex-client-identity -p codex-default-client-api -p codex-api -p codex-core`、`rtk cargo check -p codex-client-identity -p codex-default-client-api`、`rtk cargo test -p codex-client-identity -p codex-default-client-api`、`rtk cargo check -p codex-api -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo test -p codex-api merge_request_headers`、`rtk cargo test -p codex-core client_websockets`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-default-client`：均通过，仅打印既有 unused/rustfmt warning。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-default-client-api|codex-client-identity|codex-terminal-detection|os_info|codex-default-client"`：只显示 `codex-client-identity`；执行 `rtk cargo tree -p codex-core --invert codex-default-client-api --edges normal --depth 8`、`--invert codex-default-client`、`--invert codex-terminal-detection` 和 `--invert os_info`：均为 `warning: nothing to print`。执行 `rtk cargo tree -p codex-client-identity --depth 1 --edges normal`：direct deps 只有 `codex-config-types`、`http`、`tracing`；对 `codex-default-client-api`、`codex-default-client`、`codex-terminal-detection`、`os_info`、`reqwest`、`codex-login`、`codex-api` 的反向图均为 package 不在 graph。执行 `rtk cargo tree -p codex-default-client-api --depth 1 --edges normal`：direct deps 为 `codex-client-identity`、`codex-terminal-detection`、`http`、`os_info`、`tracing`，确认 User-Agent probing 未回流 core。
    - 2026-06-22 CST 已完成 core session terminal detection direct dependency 收缩切片：`Session` 不再直接调用 `codex_terminal_detection::user_agent()`；`ThreadManagerState` 持有由组合根注入的 terminal token，并通过 `CodexSpawnArgs` / `SessionConfiguration` 传给 session telemetry 和 network proxy audit metadata。app-server、mcp-server 这类组合根调用 `ThreadManager::with_terminal_type(codex_terminal_detection::user_agent())` 注入真实 terminal token；interactive sub-Codex 继承 parent session 的 terminal token；core 测试路径使用稳定测试值。`codex-core` manifest 已移除 `codex-terminal-detection` normal dependency，mcp-server 作为组合根显式增加该 dependency。
    - core terminal detection 收缩验证：执行 `rtk cargo fmt -p codex-core -p codex-app-server -p codex-mcp-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-mcp-server --bin codex-mcp-server`：均通过；core tests check 只打印既有 unused import warnings。执行 `rtk rg -n "codex_terminal_detection|user_agent\\(" codex-rs/core/src codex-rs/core/Cargo.toml -g '*.rs' -g 'Cargo.toml'`：无匹配。执行 direct dependency 门禁 `rtk cargo tree -p codex-core --depth 1 --edges normal`：不显示 direct `codex-terminal-detection`；执行 `rtk cargo tree -p codex-core --invert codex-terminal-detection --edges normal --depth 8`：warning nothing to print，确认 core normal graph 没有 direct 或 indirect terminal detection 路径。执行 `rtk cargo tree -p codex-app-server --invert codex-terminal-detection --edges normal --depth 5` 和 `rtk cargo tree -p codex-mcp-server --invert codex-terminal-detection --edges normal --depth 5`：显示 terminal detection 仅留在 app-server/mcp-server/default-client-api 等组合根或 identity/header owner 路径，不经 `codex-core`。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过。
    - default-client/terminal detection 后重新盘点：执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`，确认 core direct graph 以 API/type/facade crate 为主，旧 `codex-default-client-api`、full `codex-default-client`、`codex-terminal-detection` 和 `os_info` 均不在 direct graph。复核 `toml` 和 `async-trait` 后确认它们不是当前低风险 direct-only 切片：`toml` 的生产使用仍分布在 config/session/agent role/config lock 的解析和序列化中，且还经 config owner crate 多路径进入；`async-trait` 仍横跨 tool handler、runtime adapter、file-system/API trait consumers 和 tests。后续不应为追求单个 direct edge 机械删除它们，而应先继续收缩 core-skills facade、MCP runtime adapter、config/local loader 或 workflow manager 这类功能边界。
    - 2026-06-22 CST 已完成 core-skills remote HTTP optional dependency 收缩切片：`codex-core-skills` 的 remote skill list/export HTTP client 已由 `remote-http` feature gate 住，本切片继续把只被 `remote.rs` 使用的 `codex-auth-types` 和 `codex-model-provider-api` 改为 optional，并纳入 `remote-http` feature；默认本地 skill loading/render/injection graph 不再为了未接线 remote skill API 拉 auth snapshot / model-provider auth adapter。`AGENTS.md` 已补充默认 `codex-core-skills` graph 不得拉 `codex-auth-types` / `codex-model-provider-api` / full default-client / archive runtime 的规则。
    - core-skills remote HTTP optional dependency 验证：执行 `rtk cargo fmt -p codex-core-skills`、`rtk cargo check -p codex-core-skills`、`rtk cargo check -p codex-core-skills --features remote-http`、`rtk cargo test -p codex-core-skills`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/rustfmt/dead_code warnings。执行 `rtk cargo tree -p codex-core-skills --depth 1 --edges normal`：direct deps 不再显示 `codex-auth-types`、`codex-model-provider-api`、`codex-default-client`、`reqwest` 或 `zip`；执行默认 graph 反向门禁 `rtk cargo tree -p codex-core-skills --invert codex-auth-types --edges normal --depth 5` 返回 `warning: nothing to print`，对 `codex-model-provider-api`、`codex-default-client`、`reqwest` 和 `zip` 返回 package 不在 graph。执行 `rtk cargo tree -p codex-core-skills --features remote-http --depth 1 --edges normal | rtk rg "codex-auth-types|codex-model-provider-api|codex-default-client|zip|reqwest"`：显式 feature graph 显示 remote auth/provider/default-client/zip deps；对这些 package 的 feature graph 反向门禁均显示只经 `codex-core-skills` 的 `remote-http` feature 进入。
    - 2026-06-22 CST 已完成 MCP Codex Apps auth-header provider 边界收缩切片：`codex-mcp-runtime-api` 新增 header-only `McpAuthHeaderProvider` / `StaticMcpAuthHeaderProvider`，`McpConnectionRuntimeStartRequest.codex_apps_auth_provider` 改为只接收 auth headers；`codex-mcp` 和 `mcp-runtime-api` 不再为了 streamable HTTP header 注入依赖 full `codex-api-provider`。core/app-server 这类持有 `RequestAuthSnapshot` / `CodexAuth` 的组合根负责把 auth state 投影成 header-only provider，再传入 MCP runtime API。app-server `mcp_processor` 中遗留的 full `AuthProvider` 调用已同步改为 header-only helper，避免 status/list/read-resource 路径编译时重新拉回旧类型。
    - MCP auth-header provider 验证：执行 `rtk cargo fmt -p codex-mcp-runtime-api -p codex-mcp -p codex-app-server -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-mcp-runtime-api -p codex-mcp`、`rtk cargo test -p codex-mcp`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-mcp-runtime-api --depth 1 --edges normal`：direct deps 为 `anyhow`、`async-channel`、`codex-config-types`、`codex-exec-server-api`、`codex-mcp-tool-types`、`codex-mcp-types`、`codex-protocol`、`http`、`serde_json`、`tokio-util`，不显示 `codex-api-provider`；执行 `rtk cargo tree -p codex-mcp --depth 1 --edges normal`：不显示 `codex-api-provider`。执行反向门禁脚本检查 `codex-mcp-runtime-api` 和 `codex-mcp` 的 normal graph 中 `codex-api-provider` 均 absent；直接 `cargo tree --invert codex-api-provider` 对两者均返回 package ID 未匹配，符合预期。执行 `rtk rg -n "codex-api-provider|codex_api_provider|auth_provider_from_auth|AuthProvider" codex-rs/mcp-runtime-api codex-rs/codex-mcp`：只剩 `OAuthProviderError` 命名，不是 full API auth provider。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 header-only auth 后续收缩切片：`codex-openai-files-api` 新增 `OpenAiFileUploadAuth`，OpenAI file upload trait 不再依赖完整 `codex-api-provider::AuthProvider` / request-signing contract；`codex-openai-files` runtime 只消费 header-only auth。`core/src/mcp_openai_file.rs` 在已有 `RequestAuthSnapshot -> AuthProvider` 投影后使用局部 adapter 转成 `OpenAiFileUploadAuth`，避免 uploader API crate 为文件上传 auth 拉入完整 API provider/client transport graph。`codex-rmcp-client` 的 streamable HTTP adapter 和 transport recipe 也从 `SharedAuthProvider` 改为 `SharedMcpAuthHeaderProvider`，并移除 normal `codex-api-provider` manifest dependency，补齐前一条 MCP header-provider 设计在 rmcp-client transport 边界的落地。
    - header-only auth 后续收缩验证：执行 `rtk cargo fmt -p codex-openai-files-api -p codex-openai-files -p codex-core -p codex-rmcp-client`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-openai-files-api -p codex-openai-files`、`rtk cargo test -p codex-openai-files upload_local_file_returns_canonical_uri`、`rtk cargo check -p codex-rmcp-client -p codex-mcp`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅剩既有 unused/dead_code warnings。一次 app-server build 在修 rmcp-client 参数前失败于 `McpAuthHeaderProvider`/`AuthProvider` 类型不匹配，修正后重跑通过。
    - header-only auth 后续依赖门禁：执行 `rtk cargo tree -p codex-openai-files-api --depth 1 --edges normal`：direct deps 只有 `http` 和 `serde`；执行 `rtk cargo tree -p codex-openai-files-api --invert codex-api-provider --edges normal --depth 6`、`--invert codex-client-types` 和 `--invert zstd`：均返回 package 不在 graph。执行 `rtk cargo tree -p codex-rmcp-client --depth 1 --edges normal`：direct deps 显示 `codex-mcp-runtime-api`，不显示 `codex-api-provider`；执行 `rtk cargo tree -p codex-rmcp-client --invert codex-api-provider --edges normal --depth 6`：package 不在 graph；执行 `rtk cargo tree -p codex-rmcp-client --invert codex-mcp-runtime-api --edges normal --depth 6`：只显示 `codex-mcp-runtime-api -> codex-rmcp-client`。执行 `rtk cargo tree -p codex-core --invert codex-api-provider --edges normal --depth 8`：剩余路径只经 `codex-api-runtime-api`、core direct API provider 和 `codex-model-provider-api`，不再经 `codex-openai-files-api` 或 `codex-rmcp-client`。执行 `rtk cargo tree -p codex-core --invert codex-openai-files-api --edges normal --depth 6`：只剩 `codex-openai-files-api -> codex-core` direct trait boundary。
    - 2026-06-22 CST 已完成 `codex-client-types` request body compression feature 收缩切片：`codex-client-types` 默认 normal graph 只保留 HTTP request/response/retry DTO、JSON serialization 和 error 类型；`zstd` body compression implementation 与对应 tracing debug 记录改为 optional `body-compression` feature。`Request::prepare_body_for_send` API 继续存在，默认 feature 下对 `RequestCompression::Zstd` 返回明确 build error；`codex-client` 作为 concrete HTTP runtime 显式启用 `body-compression`，Bedrock SigV4 signing 路径经该 runtime graph 仍可签名最终压缩 body。这样 core / API provider 共享类型层不再为压缩实现拉入 `zstd`。
    - client-types compression feature 验证：执行 `rtk cargo fmt -p codex-client-types -p codex-client -p codex-model-provider`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-client-types` 和 `rtk cargo test -p codex-client-types --features body-compression`：均通过，2 个 tests 通过。执行 `rtk cargo check -p codex-client -p codex-model-provider`：通过，仅打印既有 `codex_api_types::ResponsesOptions` unused warning。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。
    - client-types compression 依赖门禁：执行 `rtk cargo tree -p codex-client-types --depth 1 --edges normal`：direct deps 只剩 `bytes`、`http`、`serde`、`serde_json` 和 `thiserror`；执行反向门禁脚本确认 `zstd` 与 `tracing` 均 absent from `codex-client-types` default normal graph。执行 `rtk cargo tree -p codex-client --invert zstd --edges normal --depth 5`：显示 `zstd` 只经 `codex-client` direct 和 `codex-client-types` 的 `body-compression` feature 进入 concrete client runtime graph。执行 `rtk cargo tree -p codex-core --invert zstd --edges normal --depth 8`：`warning: nothing to print`，确认 core normal graph 已无 `zstd`。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `codex-api-provider` WebSocket URL parser 收缩切片：`Provider::websocket_url_for_path` 从 shared provider/auth DTO crate 中移除，`url::Url` parsing、http/https 到 ws/wss scheme conversion 和 WebSocket-specific request setup 下沉到 concrete `codex-api::endpoint::responses_websocket` runtime。`codex-api-provider` 继续保留 string-level `url_for_path`、request/header/retry/auth 基础 contract，不再为了 Responses WebSocket helper 拉 `url`。
    - API provider URL parser 验证：执行 `rtk cargo fmt -p codex-api-provider -p codex-api`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-api-provider`：2 passed。执行 `rtk cargo check -p codex-api`：通过，仅打印既有 `codex_api_types::ResponsesOptions` unused warning。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "url::|websocket_url_for_path|Url" codex-rs/api-provider codex-rs/codex-api/src/endpoint/responses_websocket.rs codex-rs/api-provider/Cargo.toml`：`api-provider` 无 `url` 使用，`Url` 只留在 concrete `responses_websocket` runtime。
    - API provider URL parser 依赖门禁：执行 `rtk cargo tree -p codex-api-provider --depth 1 --edges normal`：direct deps 只剩 `codex-client-types`、`http` 和 `thiserror`；执行 `rtk cargo tree -p codex-api-provider --invert url --edges normal --depth 6`：package ID 未匹配，确认 provider graph 已无 `url`。执行 `rtk cargo tree -p codex-core --invert url --edges normal --depth 8`：剩余路径只经 `codex-shell-command -> codex-core` 和 `codex-shell-command -> codex-memories-read-api -> codex-core`，不再经 `codex-api-provider`。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过。
    - 2026-06-22 CST 已完成 API provider protocol/header adapter 收缩切片：`codex-api-provider` 不再为了 `SessionSource` / `SubAgentSource` 到 `x-openai-subagent` 的 header adapter 依赖 `codex-protocol`，该映射改由 `codex-api` request header adapter 在具体 endpoint runtime 边界处理。CLI doctor 中需要展示 WebSocket endpoint 的路径继续在 CLI 本地用 `url::Url` 处理，不把 URL parser 或 protocol DTO 拉回 provider/auth 基础层。
    - API provider protocol/header adapter 验证：执行 `rtk cargo fmt -p codex-api-provider -p codex-api -p codex-cli`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-api-provider -p codex-api -p codex-api-runtime-api -p codex-core --lib`：通过，仅打印既有 unused warnings。执行 `rtk cargo test -p codex-api --test clients azure_default_store_attaches_ids_and_headers`：通过，覆盖 `x-openai-subagent` header 行为。执行 `rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-cli`：均通过。
    - API provider protocol/header adapter 依赖门禁：执行 `rtk cargo tree -p codex-api-provider --depth 1 --edges normal`：direct deps 只剩 `codex-client-types`、`http` 和 `thiserror`，不显示 `codex-protocol` 或 `url`。执行 `rtk cargo tree -p codex-api-provider --invert codex-protocol --edges normal --depth 6`：package ID 未匹配，确认 provider graph 已无 `codex-protocol`。执行 `rtk sh -lc 'if rtk cargo tree -p codex-core --invert codex-protocol --edges normal --depth 8 | rtk rg -q "codex-api-provider"; then exit 1; else exit 0; fi'`：通过，确认 core 的 protocol 反向图没有 API provider 分支。执行 `rtk cargo tree -p codex-core --invert codex-api-provider --edges normal --depth 8`：剩余路径仍是 `codex-api-runtime-api`、core direct API provider 和 `codex-model-provider-api` 真实 auth/request-signing 边界。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `codex-client-types` / `codex-api-provider` 本地错误 `thiserror` proc-macro 收缩切片：`TransportError`、`StreamError` 和 `AuthError` 不再使用 `#[derive(thiserror::Error)]`，改为手写 `Display` / `std::error::Error`，错误文本保持原语义。`codex-client-types` 和 `codex-api-provider` normal manifest 均移除 `thiserror`，避免 shared request/auth contract 层为了小型本地错误 enum 拉 proc-macro 依赖。
    - client-types/api-provider `thiserror` 收缩验证：执行 `rtk cargo fmt -p codex-client-types -p codex-api-provider`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-client-types` 和 `rtk cargo test -p codex-client-types --features body-compression`：均通过，2 个 tests 通过。执行 `rtk cargo test -p codex-api-provider`：2 passed。执行 `rtk cargo check -p codex-api`：通过，仅打印既有 `codex_api_types::ResponsesOptions` unused warning。响应 cargo-clean schedule 后执行了一次 `rtk cargo clean`；随后执行 `rtk cargo check -p codex-core --tests`：通过。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：一次 `command_wait` 返回 exit 101 但未携带失败输出，立即重跑同一命令通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "thiserror|derive\\(.*Error\\)|#\\[error\\(" codex-rs/client-types codex-rs/api-provider -g '*.rs' -g 'Cargo.toml'`：无匹配。
    - client-types/api-provider `thiserror` 依赖门禁：执行 `rtk cargo tree -p codex-client-types --depth 1 --edges normal`：direct deps 只剩 `bytes`、`http`、`serde` 和 `serde_json`；执行 `rtk cargo tree -p codex-api-provider --depth 1 --edges normal`：direct deps 只剩 `codex-client-types` 和 `http`。执行 `rtk cargo tree -p codex-client-types --invert thiserror --edges normal --depth 6` 与 `rtk cargo tree -p codex-api-provider --invert thiserror --edges normal --depth 6`：均返回 package ID 未匹配，确认两个 graph 已无 `thiserror`。执行 `rtk cargo tree -p codex-core --invert thiserror@2.0.18 --edges normal --depth 8`：剩余路径归属 `codex-api-types`、config/API/protocol/telemetry/tool 等既有 owner 或 third-party paths，不再出现 `codex-client-types` 或 `codex-api-provider` 分支。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `codex-protocol` isolated serde helper / `serde_with` 收缩切片：`ConversationStartParams.prompt` 的 missing/null/string 三态改用本地 double-option serde helper；`ExecCommandOutputDeltaEvent.chunk` 的 base64 bytes wire encoding 改用本地 helper + 直接 `base64` crate；`codex-protocol` normal manifest 移除 `serde_with` 和其 proc-macro feature，避免 shared protocol 为两个 isolated wire 字段拉通用 serde helper/macro 依赖。`TerminalInteractionEvent` 上多余的 `#[serde_as]` wrapper 同步移除。
    - protocol `serde_with` 收缩验证：执行 `rtk cargo fmt -p codex-protocol`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-protocol conversation_op_serializes_as_unnested_variants`：通过，覆盖 prompt 缺省、null 和 string wire 形状；执行 `rtk cargo test -p codex-protocol vec_u8_as_base64_serialization_and_deserialization`：1 passed / 208 filtered，覆盖 command output chunk base64 roundtrip。执行 `rtk cargo check -p codex-protocol`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 `ResponsesOptions` / `resolve_permission_profile` unused warnings。
    - protocol `serde_with` 依赖门禁：执行 `rtk cargo tree -p codex-protocol --depth 1 --edges normal`：direct deps 不再显示 `serde_with`，只新增轻量 `base64`；执行 `rtk cargo tree -p codex-protocol --invert serde_with --edges normal --depth 6` 和 `rtk cargo tree -p codex-core --invert serde_with --edges normal --depth 8`：均返回 package ID 未匹配，确认 protocol/core default graph 已无 `serde_with`。执行 `rtk rg -n "serde_with|serde_as" codex-rs/protocol/src codex-rs/protocol/Cargo.toml -g '*.rs' -g 'Cargo.toml'`：无匹配。执行 `rtk cargo tree -p codex-core --invert base64 --edges normal --depth 6`：base64 已通过 agent identity、API types、config loader、exec-server protocol、shell-command、utils-image 等既有路径存在，protocol 现在只是以直接轻量 dependency 替代原 `serde_with` base64 feature；未引入 runtime implementation。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `codex-thread-store-api` 本地错误 `thiserror` proc-macro 收缩切片：`ThreadStoreError` 不再使用 `#[derive(thiserror::Error)]`，改为手写 `Display` / `std::error::Error`，错误文本保持原语义；`codex-thread-store-api` manifest 移除 direct `thiserror` dependency。该切片只移除 thread-store API 自身的小型 shared error derive/direct edge，不声称全 graph 无 `thiserror`，因为 `codex-thread-store-api` 仍依赖 `codex-protocol`，而 protocol 当前仍有既有 `thiserror`/`ts-rs` 路径。
    - thread-store-api `thiserror` 收缩验证：执行 `rtk cargo fmt -p codex-thread-store-api`：通过。执行 `rtk cargo check -p codex-thread-store-api -p codex-thread-store`、`rtk cargo test -p codex-thread-store-api`、`rtk cargo test -p codex-thread-store`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "thiserror|derive\\(.*Error\\)|#\\[error\\(" codex-rs/thread-store-api -g '*.rs' -g 'Cargo.toml'`：无匹配。执行 `rtk cargo tree -p codex-thread-store-api --depth 1 --edges normal`：direct deps 为 `chrono`、`codex-protocol` 和 `serde`，不显示 direct `thiserror` 或 `async-trait`。执行 `rtk cargo tree -p codex-thread-store-api --invert thiserror --edges normal --depth 6`：剩余路径只经 `codex-protocol` / `ts-rs`，确认本切片没有新增 storage API 本地 proc-macro edge。执行 `rtk cargo tree -p codex-core --invert thiserror@2.0.18 --edges normal --depth 8`：仍可看到 `codex-protocol -> codex-thread-store-api -> codex-core` 分支，归属 protocol 既有 owner path，而不是 `ThreadStoreError` direct/local derive。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 API request auth adapter 收缩切片：新增 `codex-api-auth` 承载 `BearerAuthProvider`、`auth_provider_from_auth_snapshot`、`unauthenticated_auth_provider` 和 Agent Identity request signing adapter；`codex-model-provider-api` 仅 re-export 这些 auth helper 以保持旧路径兼容，并继续拥有 `ModelProvider*` trait/factory、provider config adapter 和 auth recovery trait。core 的 MCP Codex Apps headers、OpenAI file upload auth、ARC monitor auth headers和测试用 `BearerAuthProvider` 已改为直接依赖 `codex-api-auth`；`backend-client`、`app-server-transport`、`core-plugins-remote` 和 `core-skills remote-http` 的纯 request-auth helper 调用也直接依赖该 crate。
    - 同一切片继续收缩 login 回流：`codex-backend-client` 的 public constructor 从 `from_auth(&CodexAuth)` 改为 `from_auth_snapshot(&RequestAuthSnapshot)`，调用方在 login-aware 边界投影 snapshot 并设置 User-Agent；`codex-login` 的 `model_provider_auth_manager` adapter 改为显式 `model-provider-auth` feature，只有 app-server、mcp-server、CLI、core/core-api 和 memories-write 这类实际需要把 login 适配到 model-provider trait 的 crate 开启。默认 login graph 不再把 `codex-model-provider-api` 间接带给只需要 AuthManager/token/remote-control/backend-client 的 consumers。
    - API request auth adapter 收缩验证：执行 `rtk cargo fmt -p codex-api-auth -p codex-agent-identity-api -p codex-model-provider-api -p codex-backend-client -p codex-app-server-transport -p codex-core-plugins-remote -p codex-core-skills -p codex-core -p codex-memories-write -p codex-cloud-requirements -p codex-app-server -p codex-login -p codex-core-api -p codex-mcp-server -p codex-cli`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-api-auth`：3 passed；`rtk cargo test -p codex-backend-client`：12 passed；`rtk cargo check -p codex-agent-identity-api -p codex-api-auth -p codex-model-provider-api -p codex-backend-client -p codex-app-server-transport -p codex-core-plugins-remote -p codex-memories-write -p codex-cloud-requirements`、`rtk cargo check -p codex-core-skills --features remote-http`、`rtk cargo check -p codex-core --tests`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和 `rtk cargo check -p codex-mcp-server --bin codex-mcp-server -p codex-cli`：均通过，仅剩既有 unused/dead_code warnings。执行 `rtk cargo check -p codex-login`、`rtk cargo check -p codex-login --features model-provider-auth`、`rtk cargo test -p codex-login` 和 `rtk cargo test -p codex-login --features model-provider-auth`：均通过。
    - API request auth adapter 依赖门禁：`rtk cargo tree -p codex-api-auth --depth 1 --edges normal` 显示 direct deps 只有 `codex-agent-identity-api`、`codex-api-provider`、`codex-auth-types` 和 `http`；对 `codex-model-provider-api`、full `codex-agent-identity`、`codex-login`、`codex-client` 和 `reqwest` 的反向门禁均返回 package absent。`rtk cargo tree -p codex-model-provider-api --depth 1 --edges normal` 显示 request auth helper 只经 `codex-api-auth` 进入，未 direct 依赖 `codex-agent-identity-api`。`rtk cargo tree -p codex-login --invert codex-model-provider-api --edges normal --depth 6` 返回 nothing to print；`rtk cargo tree -p codex-login --features model-provider-auth --invert codex-model-provider-api --edges normal --depth 4` 显示只有显式 feature 路径。对 `codex-backend-client`、`codex-app-server-transport`、`codex-core-plugins-remote` 和 `codex-core-skills --features remote-http` 执行 `--invert codex-model-provider-api` 均为 absent/nothing to print。`rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-api-auth|codex-model-provider-api|codex-login"` 只显示 `codex-api-auth` 和真实 direct `codex-model-provider-api` trait boundary；`rtk cargo tree -p codex-core --invert codex-login --edges normal --depth 8` 和 `--invert codex-agent-identity` 均返回 nothing to print。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过。
    - 2026-06-22 CST 已完成 `codex-core-skills` direct `dunce` 收缩切片：`codex-core-skills` 生产代码当前通过 `AbsolutePathBuf::canonicalize()` 和已有 path owner helper 处理 canonical path；direct `dunce` 使用只剩 loader/manager tests 构造本机 canonical fixture，因此 manifest 将 `dunce` 从 normal dependency 移到 dev-dependency。该切片只移除 `codex-core-skills` 的生产 direct edge；剩余 default graph 中的 `dunce` 路径归属 `codex-utils-absolute-path`、`codex-file-system`、`codex-plugin-manifest`、`codex-config-types`、`codex-protocol` 等 path owner / shared DTO 路径，不宣称完全移除 graph 中所有 `dunce`。
    - core-skills `dunce` 收缩验证：执行 `rtk cargo check -p codex-core-skills`、`rtk cargo test -p codex-core-skills`、`rtk cargo check -p codex-core-skills --features remote-http` 和 `rtk cargo check -p codex-core --tests`：均通过，仅打印既有 unused/dead_code warnings；验证过程中发现 `codex-utils-image` 使用 `tracing::trace!` 但 manifest 缺 direct `tracing`，已补回 `tracing = { workspace = true }` 并执行 `rtk cargo check -p codex-utils-image` 通过。补充执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过。执行 `rtk cargo tree -p codex-core-skills --depth 1 --edges normal`：direct deps 显示 `codex-utils-absolute-path`，不显示 direct `dunce`；执行 `rtk cargo tree -p codex-core-skills --invert dunce --edges normal --depth 6`：剩余路径只经 `codex-utils-absolute-path` 及其 path owner consumers 返回 `codex-core-skills`，没有 `dunce -> codex-core-skills` direct edge。执行 `rtk cargo tree -p codex-core --invert dunce --edges normal --depth 6`：剩余路径归属 config/path/sandboxing/shared DTO owner crate，未新增 `core-skills` direct path。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 image token estimate cache owner 收缩切片：core 的 original-detail image data URL payload 识别、base64 decode、dimensions lookup、patch cap byte estimate 和 LRU/cache digest 逻辑迁入 `codex-utils-image`；core 的 `ContextManager` 只保留 `ResponseItem` 扫描、payload byte subtraction 和 fallback fixed-cost glue。`codex-utils-image` 不再依赖通用 `codex-utils-cache`，改为 owner crate 内部 `std::sync::Mutex<LruCache<...>>` 和 direct `sha1` digest；`tokio` 从 `codex-utils-image` normal dependency 降为 dev-dependency，仅用于 tests。这样 core normal graph 不再 direct 或 indirect 拉 `codex-utils-cache`，不是 direct edge 改成经 `codex-utils-image` 的间接回流。
    - image cache owner 收缩验证：执行 `rtk cargo fmt -p codex-utils-image -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-utils-image`：7 passed。执行 `rtk cargo test -p codex-core image_data_url_payload --lib`：3 passed / 1742 filtered；执行 `rtk cargo test -p codex-core original_detail --lib`：5 passed / 1740 filtered，覆盖 data URL fixed-cost、original-detail dimension scaling、patch cap 和 WebP original-detail。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-utils-image --depth 1 --edges normal`：direct deps 为 `base64`、`codex-utils-string`、`image`、`lru`、`mime_guess`、`sha1`、`thiserror` 和 `tracing`，不显示 `codex-utils-cache` 或 normal `tokio`。执行显式 graph 门禁确认 `codex-utils-image` normal graph 不含 `codex-utils-cache v|tokio v`：通过。执行 core graph 门禁确认 `codex-core` normal graph 不含 `codex-utils-cache v`：通过，`codex-core --depth 1` 只显示 `codex-utils-image`。执行 `rtk cargo tree -p codex-core --invert codex-utils-cache --edges normal --depth 6`：package ID 不在 graph。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过。
    - 2026-06-22 CST 继续收尾 image cache owner 切片：全仓搜索确认 `codex-utils-cache` 只剩 workspace member/dependency 和 crate 自身后，删除旧 `codex-rs/utils/cache` crate、workspace member 和 workspace dependency。项目规则同步记录不要为单一 owner 的小型缓存重新引入通用 cache crate，避免把 async runtime 或 heavy graph 拉回轻量 owner crate。
    - `codex-utils-cache` workspace 删除验证：执行 `rtk cargo metadata --format-version 1 --no-deps`：通过，workspace 成员解析不再包含 `codex-utils-cache`；执行 `rtk rg -n "codex-utils-cache|codex_utils_cache|\"utils/cache\"" codex-rs/Cargo.toml codex-rs --glob '!*target*' -g 'Cargo.toml' -g '*.rs' -g 'BUILD.bazel'`：无匹配。执行 `rtk cargo test -p codex-utils-image`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-utils-image --depth 1 --edges normal`：direct deps 仍为 `base64`、`codex-utils-string`、`image`、`lru`、`mime_guess`、`sha1`、`thiserror` 和 `tracing`，不显示 `codex-utils-cache` 或 normal `tokio`。执行显式门禁确认 `codex-utils-image` / `codex-core` normal graph 与 workspace refs 均不含 `codex-utils-cache`：通过。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：当前 direct graph 仍只显示 API/trait/type crate、core-owned runtime primitive 和基础库，不显示 deleted cache crate。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 config TOML relative path resolver owner 下沉切片：`resolve_relative_paths_in_config_toml` 从 `codex-config-local-loader` 下沉到 `codex-config-toml`，因为该 helper 只依赖 `ConfigToml` shape、`AbsolutePathBufGuard` 和 `toml::Value` round-trip；`codex-config-local-loader` 继续 re-export 旧路径并在 local layer IO 中调用新 owner。core 的 agent role layer loading 改为直接依赖 `codex_config_toml::resolve_relative_paths_in_config_toml`，不再为了 role config 的纯 path resolution 调用 local filesystem loader；`codex-core -> codex-config-local-loader` direct edge 仍保留，因为 `core/src/config/mod.rs` 仍是真实 full effective config layer loading 边界。
    - config TOML path resolver 下沉验证：执行 `rtk cargo fmt -p codex-config-toml -p codex-config-local-loader -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-config-toml`：4 passed，新增测试覆盖相对 `model_instructions_file` 解析和未知字段保留；执行 `rtk cargo test -p codex-config-local-loader`：13 passed，验证旧 re-export 和 local layer 内部调用；执行 `rtk cargo test -p codex-core agent_role --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-config-toml --depth 1 --edges normal`：direct deps 不显示 `codex-config-local-loader`；执行显式 absent 门禁 `rtk sh -lc '... cargo tree -p codex-config-toml --invert codex-config-local-loader ...'`：输出 package ID absent 并以 0 退出，确认新 owner graph 没有拉回 local-loader。执行 `rtk rg -n "codex_config_local_loader|resolve_relative_paths_in_config_toml" codex-rs/core/src -g "*.rs"`：core 中 `codex_config_local_loader` 只剩 `core/src/config/mod.rs` 的 `load_config_layers_state`，agent role 改为 `codex_config_toml`。执行 `rtk cargo tree -p codex-core --invert codex-config-local-loader --edges normal --depth 4`：只显示 `codex-config-local-loader -> codex-core` direct full-loader boundary。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过。
    - 2026-06-22 CST 已完成 ToolExecutor `async-trait` 收缩切片：`codex-tool-types::ToolExecutor` 从 `#[async_trait] async fn handle` 改为 object-safe boxed-future trait shape，新增/导出 `ToolExecutorFuture`；`codex-tool-planning` 删除未接入模块树的旧 `tool_executor.rs`，只 re-export owner contract；`codex-extension-api` 同步 re-export future alias。core tool handlers、file-subscription schedule/event-command tools 和 memories extension tools 的 `ToolExecutor` impl 改为显式 `fn handle<'a>(...) -> ToolExecutorFuture<'a, Self::Output>` + `Box::pin(async move { ... })`，不再为 tool handler impl 使用 `async-trait` proc macro。`codex-tool-types`、`codex-tool-planning`、`codex-file-subscription` 和 `codex-memories-extension` manifest 均移除 direct `async-trait` dependency；core 的 `async-trait` 只保留在 dev-dependency/test mock 路径。
    - ToolExecutor `async-trait` 收缩验证：执行 `rtk cargo fmt -p codex-core -p codex-tool-types -p codex-tool-planning -p codex-extension-api` 和 `rtk cargo fmt -p codex-file-subscription -p codex-memories-extension`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-tool-types -p codex-tool-planning -p codex-extension-api`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-file-subscription -p codex-memories-extension`、`rtk cargo build -p codex-app-server --bin codex-app-server`、`rtk cargo test -p codex-core registry_tests --lib` 和 `rtk cargo test -p codex-core extension_tools --lib`：均通过，仅有既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-tool-types --depth 1 --edges normal`：direct deps 为 `codex-protocol`、`codex-utils-string`、`serde`、`serde_json` 和 `thiserror`，不显示 `async-trait`；执行 `rtk cargo tree -p codex-tool-planning --invert async-trait --edges normal --depth 6`：package ID absent，确认 planning graph 不再经 tool-types 间接拉回 proc macro；`codex-file-subscription --depth 1` 和 `codex-memories-extension --depth 1` 均不显示 direct `async-trait`。执行 `rtk cargo tree -p codex-core --invert async-trait --edges normal --depth 6`：剩余路径归属 `codex-async-utils`、`codex-file-system`、`codex-thread-store-api` 等独立 trait owner，不再有 `codex-core` direct 或 tool handler / tool-types / tool-planning 分支。执行 `rtk rg -n "#\\[async_trait|async_trait::async_trait|use async_trait" codex-rs/core/src codex-rs/tool-types codex-rs/tool-planning codex-rs/file-subscription codex-rs/ext/memories -g "Cargo.toml" -g "*.rs"`：core 只剩测试 mock 宏，tool-types/planning/file-subscription/memories extension 无生产宏使用。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 shell-escalation object-safe async trait 收缩切片：`codex-shell-escalation` 的 `EscalationPolicy` 和 `ShellCommandExecutor` 从 `#[async_trait] async fn` 改为 owner crate 导出的 boxed-future trait shape（`EscalationPolicyFuture`、`ShellCommandRunFuture`、`PrepareEscalatedExecFuture`）。这两个 trait 仍可作为 `Arc<dyn ...>` 注入 core shell runtime；core 的 `CoreShellActionProvider` / `CoreShellCommandExecutor` 改为显式 `Box::pin(async move { ... })` 实现，`codex-core` normal manifest 移除 direct `async-trait`，只在 dev-dependency 保留给 cfg(test) mock；`codex-shell-escalation` manifest 完全移除 direct `async-trait`。
    - shell-escalation `async-trait` 收缩验证：执行 `rtk cargo fmt -p codex-shell-escalation -p codex-core`、`rtk cargo check -p codex-shell-escalation --tests`、`rtk cargo test -p codex-shell-escalation`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --features test-support --lib`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused warnings。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：direct deps 不再显示 `async-trait`；执行 `rtk cargo tree -p codex-shell-escalation --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-protocol`、`codex-utils-absolute-path`、`libc`、`serde`、`serde_json`、`socket2`、`tokio`、`tokio-util` 和 `tracing`，不显示 `async-trait`；执行 `rtk cargo tree -p codex-core --invert async-trait --edges normal --depth 6`：剩余路径只归属 `codex-file-system` 和 `codex-thread-store-api` 这两个独立 trait owner，不再有 `codex-core` direct、tool handler、tool-types/planning 或 shell-escalation 分支。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 filesystem/thread-store object-safe async trait 收缩切片：`codex-file-system::ExecutorFileSystem` 改为 owner crate 导出的 `FileSystemFuture` boxed future trait shape，`LocalFileSystem`、exec-server local/unsandboxed/direct/remote/sandboxed filesystem implementations 同步改为显式 `Box::pin(async move { ... })`；`codex-thread-store-api::ThreadStore` 改为复用既有 `ThreadStoreFuture` boxed future alias，`LocalThreadStore` / `InMemoryThreadStore` implementation 同步迁移。`codex-file-system`、`codex-thread-store-api` 和 `codex-thread-store` manifest 移除 direct `async-trait` dependency；`codex-exec-server` 仍保留 async-trait，因为 environment provider 仍是真实独立使用点，不属于本切片。
    - filesystem/thread-store `async-trait` 收缩验证：执行 `rtk cargo fmt -p codex-file-system -p codex-thread-store-api -p codex-thread-store -p codex-exec-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-file-system -p codex-thread-store-api -p codex-thread-store -p codex-exec-server`、`rtk cargo test -p codex-thread-store-api -p codex-thread-store`、`rtk cargo test -p codex-exec-server file_system`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-file-system --depth 1 --edges normal`：direct deps 仅为 `codex-protocol`、`codex-utils-absolute-path` 和 `serde`；执行 `rtk cargo tree -p codex-thread-store-api --depth 1 --edges normal`：direct deps 为 `chrono`、`codex-protocol` 和 `serde`；执行 `rtk cargo tree -p codex-thread-store --depth 1 --edges normal`：direct deps 不显示 `async-trait`。执行 `rtk cargo tree -p codex-core --invert async-trait --edges normal --depth 6`：`warning: nothing to print`，确认 core normal graph 已无 async-trait proc-macro 路径。执行 `rtk rg -n "async-trait|async_trait|#\\[async_trait\\]" codex-rs/file-system codex-rs/thread-store-api codex-rs/thread-store/src/in_memory.rs codex-rs/thread-store/src/local/mod.rs -g '*.rs' -g 'Cargo.toml'`：无匹配。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 post-turn scheduler outcome contract 下沉切片：`ThreadPostTurnState` 和 `ThreadIdleReason` 从 `core::goals` 私有类型移动到 `codex-agent-runtime`，作为后续 `codex-session-runtime` / thread status / parent completion delivery 共享的稳定调度结果 contract。状态判定逻辑仍留在 `core::goals`，因为它需要读取 active turn、pending input、goal state、direct child 和 command/event-command runtime；本切片只移动纯 outcome 类型，不把 session/turn loop 或 mailbox/tool dispatch 编排伪装成轻量 API。
    - post-turn scheduler outcome contract 验证：执行 `rtk cargo fmt -p codex-agent-runtime -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-agent-runtime -p codex-core --lib`：通过，仅打印既有 core unused import warning。执行 `rtk cargo test -p codex-agent-runtime`：15 passed；执行 `rtk cargo test -p codex-core post_turn_state --lib`：通过，覆盖 active goal continuation、wait child、wait command 和 thread completion 分支。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过。执行 `rtk cargo tree -p codex-agent-runtime --depth 1 --edges normal`：direct deps 仍仅为 `codex-metrics-api`、`codex-protocol`、`rand` 和 `serde`；执行 `rtk cargo tree -p codex-agent-runtime --invert codex-core --edges normal --depth 6`：Cargo 返回 package 不在 graph，确认 API owner 没有反向依赖 `codex-core`。执行 `rtk cargo tree -p codex-core --invert codex-agent-runtime --edges normal --depth 4`：只显示 `codex-agent-runtime -> codex-core` direct API path，没有新增间接 heavy runtime。执行 `rtk git diff --check`：通过。
    - 2026-06-22 CST 复核 `codex-core --depth 1 --edges normal`：当前 direct graph 已无 full `codex-api`、`codex-mcp`、`codex-workflow`、`codex-rollout`、`codex-state`、`codex-thread-store`、`codex-exec-server`、`codex-sandboxing`、`codex-network-proxy` 或 `codex-execpolicy` implementation crate，也不再显示 direct `async-trait` proc-macro；剩余主要是 API/trait/type crate、core-owned runtime primitive 和若干基础库（`async-channel`、`chrono`、`serde_json`、`http`、`uuid` 等）。`uuid`、`async-channel`、`chrono`、`serde_json`、`http` 都有多处 production 用途，不能简单降 dev-dependency。另复核 `codex-core-skills` 的 direct `shlex`：只用于 implicit skill invocation command tokenization；用 `codex-shell-command` 替代会把 `regex`、`tree-sitter`、`url`、`which` 等更重 shell parser graph 带进 standalone skills graph，因此本轮不做该替换。
    - 2026-06-22 CST 已完成 config-state fingerprint / config-local-loader config filename owner 收缩切片：`CONFIG_TOML_FILE` 从 `codex-config-edit` 移到 `codex-config-types`，config layer fingerprint helper `version_for_toml` 从 `codex-config-edit` 移到 `codex-config-state`；`codex-config-edit` 只保留用户 `config.toml` 写入/edit engine ownership，`codex-config` 继续 re-export 旧路径兼容。`codex-config-state` 和 `codex-config-local-loader` 不再为了常量或 fingerprint normal 依赖 `codex-config-edit`；`codex-core` 的 `codex-config-edit` direct edge 仍是真实 config edit wrapper 边界，本切片不声称移除。
    - config-state/local-loader edit-engine 回流收缩验证：执行 `rtk cargo fmt -p codex-config-types -p codex-config-state -p codex-config-edit -p codex-config-local-loader -p codex-config -p codex-core-plugins`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-config-types -p codex-config-state -p codex-config-edit -p codex-config-local-loader -p codex-config -p codex-core-plugins`：通过。执行 `rtk cargo test -p codex-config-state -p codex-config-edit -p codex-config-local-loader`：通过。执行 `rtk cargo tree -p codex-config-state --invert codex-config-edit --edges normal --depth 6` 和 `rtk cargo tree -p codex-config-local-loader --invert codex-config-edit --edges normal --depth 6`：均返回 package ID 未匹配，确认两个 crate 的 normal graph 不含 edit engine。执行 `rtk cargo tree -p codex-core --invert codex-config-edit --edges normal --depth 8`：只剩 `codex-config-edit -> codex-core` direct path，不再有 `codex-config-state` 或 `codex-config-local-loader` 分支。执行 `rtk cargo tree -p codex-config-state --depth 1 --edges normal`：direct deps 显示 `sha2` 和 config state/type/requirements/diagnostics，不显示 `codex-config-edit`；执行 `rtk cargo tree -p codex-config-local-loader --depth 1 --edges normal`：direct deps 不显示 `codex-config-edit`。执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，后者只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 core global MCP loader facade 收缩切片：`codex_core::config::load_global_mcp_servers` 和 core 内重复的 inline bearer-token guard 已删除；CLI `mcp add/remove`、CLI MCP/marketplace/plugin tests 和 core config tests 改为直接使用 `codex-config-edit` owner API，MCP transport type 改为 `codex-config-types`。这减少了 `core::config` 对用户 `config.toml` edit/load helper 的 public facade 面；`codex-core -> codex-config-local-loader` direct edge 仍保留，因为 `ConfigBuilder` 和 deprecated raw `ConfigToml` load helper 仍是真实 full effective config layer loading 边界。
    - core global MCP loader facade 收缩验证：执行 `rtk cargo fmt -p codex-cli -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-cli --tests`、`rtk cargo test -p codex-config-edit mcp_edit` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-config-edit --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-utils-absolute-path`、`codex-utils-path`、`dunce`、`serde_json`、`tokio`、`toml` 和 `toml_edit`，不显示 full `codex-config` 或 `codex-core`。执行 `rtk cargo tree -p codex-cli --depth 1 --edges normal`：direct deps 显示 `codex-config-edit`、`codex-config-loader` 和 `codex-config-types`，不显示 full `codex-config`。执行 `rtk cargo tree -p codex-core --invert codex-config-local-loader --edges normal --depth 4`：仍只显示 `codex-config-local-loader -> codex-core` direct full-loader boundary。执行 `rtk rg -n "codex_core::config::load_global_mcp_servers|pub async fn load_global_mcp_servers|ensure_no_inline_bearer_tokens" codex-rs/core/src codex-rs/cli/src codex-rs/cli/tests -g "*.rs"`：无匹配。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：当前 direct graph 仍以 API/trait/type crate、config-local-loader full-loader boundary 和 core-owned runtime primitives 为主。
    - 2026-06-22 CST 已完成 full config layer loader trait contract 切片：`codex-config-loader` 新增 `ConfigLayerLoader`、`ConfigLayerLoadRequest` 和 `ConfigLayerLoadFuture`，以 owner API 表达 full ordered config layer stack loading contract；`codex-config-local-loader` 新增 `LocalConfigLayerLoader` concrete implementation 并继续保留旧 `load_config_layers_state` free function。`ConfigBuilder` 新增可选 `config_layer_loader(Arc<dyn ConfigLayerLoader>)` 注入点，`ConfigBuilder::build_inner` 和 deprecated raw `ConfigToml` helper 均改为经 trait 调用 local loader。该切片只建立可注入 service 边界，`codex-core -> codex-config-local-loader` direct edge 仍保留，因为默认 local-loader wiring 还在 core 内，下一步需要由 CLI/TUI/app-server/core tests 组合根显式注入后再移除 direct edge。
    - full config layer loader trait contract 验证：执行 `rtk cargo fmt -p codex-config-loader -p codex-config-local-loader -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-config-loader -p codex-config-local-loader -p codex-core --lib`、`rtk cargo test -p codex-config-loader -p codex-config-local-loader`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-config-loader --depth 1 --edges normal`：direct deps 为 `codex-config-requirements`、`codex-config-state`、`codex-model-provider-info`、`codex-protocol`、`codex-utils-absolute-path`、`dunce`、`schemars`、`serde`、`thiserror` 和 `toml`，不显示 local-loader/core/full config。执行 absent 门禁脚本确认 `codex-config-local-loader`、`codex-core`、`codex-config` 和 `codex-app-server-protocol` 均 absent from `codex-config-loader` normal graph。执行 `rtk cargo tree -p codex-core --invert codex-config-local-loader --edges normal --depth 4`：仍只显示 `codex-config-local-loader -> codex-core` direct full-loader boundary，没有新增 indirect branch。执行 `rtk rg -n "load_config_layers_state\\(" codex-rs/core/src -g "*.rs"`：production `core/src/config/mod.rs` 不再直接调用 free function，剩余调用集中在 config loader tests / config tests fixture。执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `CONFIG_TOML_FILE` consumer owner 收敛切片：`core::config::CONFIG_TOML_FILE` 从本地重复常量改为 re-export `codex-config-types` owner 常量；core production wrapper/session、core config/exec/MCP/request-plugin/shell tests、CLI main/doctor、app-server config manager service 和 core-plugins marketplace add/upgrade/remove tests/metadata 不再从 `codex-config-edit` 的兼容 re-export 取 config 文件名。`codex-config-edit` 仍可 re-export 旧路径兼容，并继续拥有用户 `config.toml` 写入/edit engine；本切片不声称移除 `codex-core -> codex-config-edit` direct wrapper dependency。
    - `CONFIG_TOML_FILE` consumer 收敛验证：执行 `rtk cargo fmt -p codex-core -p codex-core-plugins -p codex-cli -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-core-plugins --tests`、`rtk cargo check -p codex-core-plugins -p codex-cli --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "codex_config_edit::CONFIG_TOML_FILE|use codex_config_edit::CONFIG_TOML_FILE|pub const CONFIG_TOML_FILE" codex-rs/core/src codex-rs/cli/src codex-rs/app-server/src codex-rs/core-plugins/src -g "*.rs"`：无匹配。执行 `rtk cargo tree -p codex-config-types --invert codex-config-edit --edges normal --depth 6`：返回 package ID 未匹配，确认 owner graph 不含 edit engine。执行 `rtk cargo tree -p codex-core --invert codex-config-edit --edges normal --depth 8`：仍只显示 `codex-config-edit -> codex-core` direct wrapper path，没有经 config-types/state/local-loader 间接回流。执行 `rtk git diff --check`：通过。本切片没有 manifest 变更，因此未重跑 Bazel lock check。
    - 2026-06-22 CST 已完成 `ConfigBuilder` 默认 local-loader wiring 外移切片：`codex-core` 的 normal `codex-config-local-loader` dependency 改为 optional 并挂到 `test-support` feature；`ConfigBuilder` production path 不再默认构造 `LocalConfigLayerLoader`，未注入 loader 时返回明确错误。CLI/TUI/app-server/exec 组合根新增本地 `config_builder()` helper 并显式注入 `Arc::new(LocalConfigLayerLoader::default())`；deprecated raw `ConfigToml` production helper 也新增带 loader 的入口，TUI/exec 改为显式传入 local loader。core 仅在 `#[cfg(any(test, feature = "test-support"))]` 下保留 dev/test fallback，避免大面积测试 fixture churn。
    - `ConfigBuilder` local-loader wiring 外移验证：执行 `rtk cargo fmt -p codex-core -p codex-cli -p codex-tui -p codex-app-server -p codex-exec`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-core --tests`、`rtk cargo check -p codex-cli -p codex-tui -p codex-exec -p codex-app-server --bin codex-app-server` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 warnings。执行 `rtk cargo tree -p codex-core --invert codex-config-local-loader --edges normal --depth 6`：warning nothing to print；执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：不再显示 `codex-config-local-loader`；执行 `rtk cargo tree -p codex-app-server --invert codex-config-local-loader --edges normal --depth 8`：只显示 app-server 组合根 direct local-loader path；执行 `rtk cargo tree -p codex-cli --invert codex-config-local-loader --edges normal --depth 8`：显示 CLI/TUI/exec/app-server 组合根路径，不经过 core。执行 `rtk git diff --check`：通过；执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 plugin load outcome owner 下沉切片：`LoadedPlugin`、`PluginLoadOutcome`、`PluginAgentDir`、`EffectiveSkillRoots` 和 `prompt_safe_plugin_description` 从 `codex-plugin` facade 下沉到 `codex-plugin-types`；`codex-plugin` 继续 re-export 保持旧路径兼容。`codex-core-plugins-api` 的 `PluginRuntime` contract 改为直接依赖 `codex-plugin-types::PluginLoadOutcome`，manifest 移除 normal `codex-plugin` dependency，避免 core 经 API crate 间接拉回 plugin facade。
    - plugin load outcome 下沉验证：执行 `rtk cargo fmt -p codex-plugin-types -p codex-plugin -p codex-core-plugins-api`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-plugin-types -p codex-plugin -p codex-core-plugins-api`、`rtk cargo test -p codex-plugin-types`、`rtk cargo check -p codex-core-plugins -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-core-plugins-api --depth 1 --edges normal`：direct deps 显示 `codex-plugin-types`，不显示 `codex-plugin`；执行 `rtk cargo tree -p codex-core-plugins-api --invert codex-plugin --edges normal --depth 6`：package ID 未匹配，确认 API graph 无 plugin facade；执行 `rtk cargo tree -p codex-core --invert codex-plugin --edges normal --depth 8`：warning nothing to print；执行 `rtk cargo tree -p codex-plugin-types --depth 1 --edges normal`：direct deps 为 config types、absolute path、schemars、serde、thiserror 和 ts-rs，不拉 `codex-plugin`。
    - 2026-06-22 CST 已完成 `codex-api-types` 本地 `ApiError` direct `thiserror` 边界收缩切片：`ApiError` 不再使用 `#[derive(thiserror::Error)]`，改为手写 `Display`、`std::error::Error::source` 和 `From<TransportError>`；错误文本保持原语义。`codex-api-types` normal manifest 移除 direct `thiserror` dependency，避免 API DTO/error contract 层为了一个本地 error enum 自己拉 proc-macro。注意该切片只收敛 direct edge；`codex-api-types` 仍通过既有 `codex-protocol` / `ts-rs` 路径间接看到 `thiserror@2`，后续如要全图移除需单独处理 protocol/TS export 边界。
    - API types `ApiError` `thiserror` 收缩验证：执行 `rtk cargo fmt -p codex-api-types`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-api-types`：23 passed。执行 `rtk cargo tree -p codex-api-types --depth 1 --edges normal`：direct deps 为 `base64`、`chrono`、`codex-client-types`、`codex-protocol`、`futures`、`http`、`serde` 和 `serde_json`，不显示 direct `thiserror`。执行 `rtk cargo tree -p codex-api-types --invert thiserror@2.0.18 --edges normal --depth 4`：剩余路径只经 `codex-protocol` / `ts-rs` 既有 owner path。执行 `rtk cargo tree -p codex-core --invert thiserror@2.0.18 --edges normal --depth 6`：剩余路径归属 apply-patch/config/protocol/tool 等既有 owner，不再有 `codex-api-types` direct `thiserror` edge。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "thiserror|derive\\(.*Error\\)|#\\[error\\(" codex-rs/api-types -g "*.rs" -g "Cargo.toml"`：无匹配。执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `codex-tool-types` / `codex-tool-planning` 本地 `FunctionCallError` direct `thiserror` 边界收缩切片：`codex-tool-types::FunctionCallError` 不再使用 `#[derive(thiserror::Error)]`，改为手写 `Display` 和 `std::error::Error`；错误文本保持原语义。`codex-tool-types` manifest 移除 direct `thiserror` dependency。`codex-tool-planning/src/function_call_error.rs` 是前序拆分后未再被 `lib.rs` 引用的死文件，已删除；`codex-tool-planning` manifest 同步移除 direct `thiserror` dependency。注意该切片只收敛 direct edge；两个 crate 仍会通过既有 `codex-protocol` / `ts-rs` 路径间接看到 `thiserror@2`。
    - tool-types/tool-planning `FunctionCallError` `thiserror` 收缩验证：执行 `rtk cargo fmt -p codex-tool-types -p codex-tool-planning -p codex-extension-api`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-tool-types -p codex-tool-planning -p codex-extension-api`：97 passed。执行 `rtk cargo tree -p codex-tool-types --depth 1 --edges normal`：direct deps 为 `codex-protocol`、`codex-utils-string`、`serde` 和 `serde_json`，不显示 direct `thiserror`。执行 `rtk cargo tree -p codex-tool-planning --depth 1 --edges normal`：direct deps 不显示 `thiserror`。执行 `rtk cargo tree -p codex-tool-types --invert thiserror@2.0.18 --edges normal --depth 6` 和 `rtk cargo tree -p codex-tool-planning --invert thiserror@2.0.18 --edges normal --depth 6`：剩余路径只经 `codex-protocol` / `ts-rs` 等既有 owner path。执行 `rtk cargo check -p codex-core --tests` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "thiserror|derive\\(.*Error\\)|#\\[error\\(" codex-rs/tool-types codex-rs/tool-planning -g "*.rs" -g "Cargo.toml"`：无匹配。执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 core-skills runtime API boundary 切片：新增 `codex-core-skills-api` 承载 `SkillMetadata`、`SkillLoadOutcome`、skills prompt render/injection/mention helper、`SkillsLoadInput`、`SkillsRuntime` trait、`SharedSkillsRuntime` 和 `DisabledSkillsRuntime`；`codex-core-skills` 继续拥有 concrete loader/manager/system bundled skills install/cache/discovery，并 re-export API crate 保持旧路径兼容。`codex-core` production dependency 从 concrete `codex-core-skills` 改为 `codex-core-skills-api`，`ThreadManagerState` / `SessionServices` 持有 `SharedSkillsRuntime`，默认 builder 使用 `DisabledSkillsRuntime`；app-server 和 mcp-server 作为组合根显式创建并注入 `codex_core_skills::SkillsManager`。`SkillsWatcher` 改为消费 `SharedSkillsRuntime` 和 trait 方法 `skill_root_paths_for_config`，避免向 app-server 暴露 concrete skill root loader 类型。
    - core-skills runtime API boundary 验证：执行 `rtk cargo fmt -p codex-core-skills-api -p codex-core-skills -p codex-core -p codex-app-server -p codex-mcp-server`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-core-skills-api -p codex-core-skills`、`rtk cargo check -p codex-core --lib`、`rtk cargo check -p codex-app-server --bin codex-app-server`、`rtk cargo check -p codex-mcp-server --bin codex-mcp-server` 和 `rtk cargo check -p codex-core --tests`：均通过，仅打印既有 warnings。执行 `rtk cargo test -p codex-core-skills-api -p codex-core-skills`：101 passed。执行 `rtk cargo tree -p codex-core --invert codex-core-skills --edges normal --depth 6`：warning nothing to print；执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：显示 direct `codex-core-skills-api`，不显示 concrete `codex-core-skills`。执行 `rtk cargo tree -p codex-core-skills-api --invert codex-skills --edges normal --depth 6` 和 `--invert include_dir`：package ID absent，确认 API graph 没有拉 embedded skill bundle loader。补充执行 `rtk git diff --check` 和 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 撤回 apply-patch heredoc shell parser tree-sitter 收缩方向：原尝试把 `codex-apply-patch` 的 tree-sitter Bash query 换成手写 line-based parser，虽然能减少 default graph，但这属于为依赖图收益重写成熟 parser 语义，且可能改变兼容输入、错误分类或安全拒绝边界。已按用户要求撤回该代码方向，后续不得靠大量手写替代成熟三方库来优化拓扑；只能通过 API/type crate 拆分、optional feature、组合根注入或 owner 边界下沉推进。`AGENTS.md` 已新增该通用规则。撤回后执行 `rtk rg -n "parse_heredoc_redirect|trim_single_line_ending|impl std::fmt::Display for ApplyPatchError|line-based|不得靠大量手写|成熟三方库" ...`：只剩 AGENTS/progress 规则记录；执行 `rtk rg -n "tree-sitter|tree_sitter|thiserror|FailedToLoadBashGrammar|derive\\(.*Error\\)|#\\[error" codex-rs/apply-patch ...`：确认 apply-patch 恢复使用 `tree-sitter` / `tree-sitter-bash` 和 `thiserror`。执行 `rtk cargo fmt -p codex-apply-patch`、`rtk cargo test -p codex-apply-patch` 和 `rtk git diff --check`：均通过；apply-patch 测试 85 passed。
    - 2026-06-22 CST 已完成 shell utility owner 收缩切片：新增 `codex-shell-utils` 承载轻量 `shlex_join` 和 shell executable PATH lookup，`codex-shell-command` 继续拥有 shell parser/safety/approval canonicalization 并 re-export 兼容旧路径。app-server bespoke event、utils-cli resume command、app-server-protocol display builder、core display/approval 路径和 TUI 测试 helper 的 token rendering 改为直接使用 `codex_shell_utils::shlex_join`；`shell-command` 的 PowerShell executable probing 也改为经 `codex_shell_utils::resolve_executable_in_path`，manifest 不再 direct normal 依赖 `which`。`AGENTS.md` 已同步记录 `shell-utils` / `shell-command` 的 owner 边界；本切片没有手写替代 shell parser 语义。
    - shell utility owner 收缩验证：执行 `rtk cargo fmt -p codex-shell-utils -p codex-shell-command -p codex-app-server-protocol -p codex-app-server -p codex-utils-cli -p codex-core -p codex-tui`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-shell-utils`：2 passed；执行 `rtk cargo test -p codex-shell-command`：139 passed；执行 `rtk cargo check -p codex-utils-cli -p codex-app-server-protocol`、`rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-shell-utils --depth 1 --edges normal`：direct deps 仅为 `shlex` 和 `which`。执行 `rtk cargo tree -p codex-shell-command --depth 1 --edges normal`：显示 direct `codex-shell-utils`，不显示 direct `which`；`shlex` 仍是真实 parser split 依赖，保留。执行 `rtk cargo tree -p codex-utils-cli --invert codex-shell-command --edges normal --depth 8`：package ID absent，确认纯 `shlex_join` consumer 不再直接或间接拉回 heavy parser crate；`rtk cargo tree -p codex-utils-cli --depth 1 --edges normal | rtk rg "codex-shell-command|codex-shell-utils"` 只显示 `codex-shell-utils`。执行 `rtk cargo tree -p codex-app-server --depth 1 --edges normal | rtk rg "codex-shell-command|codex-shell-utils"`：只显示 direct `codex-shell-utils`。执行 `rtk cargo tree -p codex-app-server --invert codex-shell-command --edges normal --depth 8`：剩余路径来自 `codex-app-server-protocol`、`codex-core` 和 `codex-memories-read-api` 的真实 parser/safety 用途，不是本轮 utility split 造成的 indirect 回流；其中 `codex-memories-read-api` 的 telemetry usage 分类仍直接使用 `parse_command` / `is_known_safe_command`，可作为下一步 API crate 瘦身候选单独设计。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 memories read API shell parser 回流收缩切片：`codex-memories-read-api::usage` 原本只被 `codex-core::memory_usage` 使用，但为 telemetry usage 分类直接依赖 `codex-shell-command::parse_command` / shell safety，导致 API crate 把 heavy parser/safety graph 带回 app-server/core 图。已将 `MEMORIES_USAGE_METRIC`、`MemoriesUsageKind` 和 `memories_usage_kinds_from_command` 机械移动为 `core/src/memory_usage.rs` 私有 runtime telemetry 实现，并补充单测覆盖安全 memory read 分类和非 known-safe 命令忽略；`codex-memories-read-api` 删除 `usage` / `metrics` 模块和 normal `codex-shell-command` dependency，`codex-memories-read` 删除旧 usage re-export。`AGENTS.md` 已同步记录 memory usage telemetry classifier 属于 core runtime telemetry，不应放回 read-api。
    - memories read API shell parser 回流收缩验证：执行 `rtk cargo fmt -p codex-core -p codex-memories-read-api -p codex-memories-read`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-core memory_usage --lib`：2 passed / 1745 filtered；执行 `rtk cargo test -p codex-memories-read-api -p codex-memories-read`：4 passed；执行 `rtk cargo check -p codex-core --tests` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "codex-shell-command" codex-rs/memories/read-api codex-rs/memories/read -g "*.rs" -g "Cargo.toml"`：无匹配。执行 `rtk cargo tree -p codex-memories-read-api --depth 1 --edges normal`：direct deps 只剩 `codex-protocol` 和 `codex-utils-absolute-path`；执行 `rtk cargo tree -p codex-memories-read-api --invert codex-shell-command --edges normal --depth 6`：package ID absent，确认 read-api graph 不再拉回 shell parser crate。执行 `rtk cargo tree -p codex-app-server --invert codex-shell-command --edges normal --depth 8`：剩余路径只来自 `codex-app-server-protocol` 和 `codex-core` 的真实 parser/safety 用途，已无 `codex-memories-read-api` 分支。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 app-server-protocol `serde_with` direct dependency 收缩切片：`CommandExecutionOutputDeltaNotification` 上的 `#[serde_as]` 没有字段级 serde-as 配置，已删除；`protocol/serde_helpers.rs` 的 double-option helper 改为本地标准 `serde` 实现，保持 missing/null/value 三态语义，并补充单测覆盖反序列化和序列化三种状态。`codex-app-server-protocol` manifest 移除 direct `serde_with` dependency，`AGENTS.md` 已同步记录 app-server-protocol 的特殊 wire serde 应使用本地 helper，不要为了单字段把 `serde_with` 拉回 app-server graph。
    - app-server-protocol `serde_with` 收缩验证：执行 `rtk cargo fmt -p codex-app-server-protocol`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-app-server-protocol double_option`：1 passed / 267 filtered；执行 `rtk cargo check -p codex-app-server-protocol`：通过；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行全量 `rtk cargo test -p codex-app-server-protocol`：失败在既有 thread status 期望不一致，`serialize_client_response` / `serialize_thread_status_changed_notification` 仍预期 `complete` 但当前输出为 `idle`，`thread_lifecycle_responses_default_missing_optional_fields` 因缺 `reason` 字段失败；这些失败与本切片的 serde helper 无关，未作为通过证据。执行 `rtk rg -n "serde_with|serde_as" codex-rs/app-server-protocol/src codex-rs/app-server-protocol/Cargo.toml ...`：无匹配。执行 `rtk cargo tree -p codex-app-server-protocol --depth 1 --edges normal`：direct deps 不显示 `serde_with`；执行 `rtk cargo tree -p codex-app-server-protocol --invert serde_with --edges normal --depth 6`：package ID absent。执行 `rtk cargo tree -p codex-app-server --invert serde_with --edges normal --depth 8`：剩余路径只来自 `codex-backend-openapi-models -> codex-backend-client`，不再经过 `codex-app-server-protocol`。执行 `rtk git diff --check` 与 `rtk just bazel-lock-check`：均通过，Bazel 只打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 app-server-protocol `TurnError` direct `thiserror` 边界收缩切片：`codex_app_server_protocol::protocol::v2::TurnError` 只是 wire/display message wrapper，已从 `#[derive(thiserror::Error)]` 改为手写 `Display` 和 `std::error::Error`，错误文本仍只输出 `message`。`codex-app-server-protocol` manifest 移除 direct `thiserror` dependency，`AGENTS.md` 已同步记录 simple wire/display error DTO 不要为了单字段 wrapper 重新拉 direct proc-macro。注意该切片只收敛 app-server-protocol 自身的 direct edge；`thiserror@2` 仍会经 `codex-config-types`、`codex-protocol`、`rmcp`、`ts-rs` 等既有 owner path 间接出现在 graph 中，不能记录成全图移除。
    - app-server-protocol `TurnError` `thiserror` 收缩验证：执行 `rtk cargo fmt -p codex-app-server-protocol`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-app-server-protocol turn_error`：1 passed / 267 filtered。执行 `rtk cargo check -p codex-app-server-protocol`：通过。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk rg -n "thiserror|derive\\(.*Error\\)|#\\[error\\(" codex-rs/app-server-protocol/src codex-rs/app-server-protocol/Cargo.toml -g "*.rs" -g "Cargo.toml"`：无匹配。执行 `rtk cargo tree -p codex-app-server-protocol --depth 1 --edges normal`：direct deps 不显示 `thiserror`。执行 `rtk cargo tree -p codex-app-server-protocol --invert thiserror --edges normal --depth 6`：仍显示既有 indirect paths，经 `codex-config-types`、`codex-protocol`、`rmcp`、`ts-rs` 等进入 `codex-app-server-protocol`；这是后续 owner path 收缩候选，不是本切片新引入的 direct 回流。执行 `rtk cargo tree -p codex-app-server-protocol --invert thiserror@1.0.69 --edges normal --depth 6`：package ID absent；执行 `rtk cargo tree -p codex-app-server --invert thiserror@2.0.18 --edges normal --depth 8`：剩余路径很多，主要来自 app-server 自身、api/model/network-proxy/config/protocol/rmcp/ts-rs 等真实 owner graph，不能用本切片声称 app-server graph 全面移除 `thiserror`。
    - app-server-protocol `TurnError` final hygiene：执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 app-server-protocol bin-only `clap` 收缩切片：`clap` 只在 `src/bin/export.rs` 和 `src/bin/write_schema_fixtures.rs` 的 schema/export CLI 中使用，已改为 optional `cli` feature dependency，并给两个 bin 显式设置 `required-features = ["cli"]`；`Justfile` / `justfile` 的 `write-app-server-schema` 目标同步传 `--features cli`。`AGENTS.md` 已记录 app-server-protocol 的 CLI dependency 不得回到 default normal graph。
    - app-server-protocol bin-only `clap` 收缩验证：执行 `rtk cargo fmt -p codex-app-server-protocol`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo check -p codex-app-server-protocol`：通过，默认库侧 check 不构建 CLI deps。执行 `rtk cargo check -p codex-app-server-protocol --features cli --bins`：通过，确认 schema/export bins 显式启用 feature 后仍可编译。执行 `rtk cargo tree -p codex-app-server-protocol --depth 1 --edges normal`：default direct deps 不显示 `clap`。执行 `rtk cargo tree -p codex-app-server-protocol --invert clap --edges normal --depth 4`：package ID absent；执行 `rtk cargo tree -p codex-app-server-protocol --features cli --invert clap --edges normal --depth 4`：只显示 `clap -> codex-app-server-protocol`，确认 CLI feature 可按需拉回。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-app-server --invert clap --edges normal --depth 8`：剩余路径来自 `codex-app-server`、`codex-app-server-transport` 和 `codex-utils-cli`，不再经过 `codex-app-server-protocol`。
    - app-server-protocol bin-only `clap` final hygiene：执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 app-server-protocol command display parser 边界收缩切片：新增 `codex-command-display`，机械迁移原 `codex-shell-command` 内的 `parse_command`、Bash tree-sitter parser、PowerShell command extraction 和 PowerShell AST parser wrapper/ps1；`codex-shell-command` 改为 re-export display parser，同时继续拥有 shell safety、approval canonicalization、PowerShell UTF-8 prefixing 和 executable probing。`codex-app-server-protocol` 的 guardian `Execve` display builder 改为直接依赖 `codex-command-display::parse_command::parse_command`，不再为了 command action metadata 拉入 full `codex-shell-command`。该切片保留现有 parser 语义和测试，没有把 Execve 展示降级为 Unknown，也没有用手写 parser 替代 tree-sitter/PowerShell AST parser。
    - app-server-protocol command display parser 边界收缩验证：执行 `rtk cargo fmt -p codex-command-display -p codex-shell-command -p codex-app-server-protocol`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-command-display`：112 passed；执行 `rtk cargo test -p codex-shell-command`：29 passed；执行 `rtk cargo check -p codex-app-server-protocol`：通过。执行 `rtk cargo tree -p codex-app-server-protocol --invert codex-shell-command --edges normal --depth 6`：package ID absent，确认 protocol graph 不再拉 full shell command crate。执行 `rtk cargo tree -p codex-app-server --invert codex-shell-command --edges normal --depth 8`：剩余路径只经 `codex-core` 与 `codex-execpolicy-loader -> codex-core`，不再有 `codex-app-server-protocol` 分支。执行 `rtk cargo tree -p codex-command-display --depth 1 --edges normal`：direct deps 为 parser 所需的 `base64`、`codex-protocol`、`codex-shell-utils`、`serde`、`serde_json`、`shlex`、`tree-sitter` 和 `tree-sitter-bash`；执行 `rtk cargo tree -p codex-command-display --invert codex-shell-command --edges normal --depth 4`：package ID absent，确认新 display crate 未间接拉回 shell safety/runtime。执行 `rtk cargo tree -p codex-app-server-protocol --depth 1 --edges normal`：显示 `codex-command-display`，不显示 `codex-shell-command`。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 context manager 历史管理边界迁出切片：新增 `codex-context-manager`，把 `ContextManager`、history normalization、tool call/output pair 修复、model-visible byte/token estimate glue、user-turn boundary 判断、contextual user/dev message marker classifier 和对应测试从 `codex-core` 迁出。`codex-core` 只保留 `context_manager::updates` 和 `TurnContext -> BaseInstructions` token estimate adapter，避免新 crate 反向依赖 Session/TurnContext/runtime service。原 core `context_manager` 目录从约 3269 行收缩为 251 行；当前 `codex-rs/core/src` 为 296 个 Rust 文件、147302 行，`codex-context-manager/src` 为 6 个 Rust 文件、3344 行。
    - context manager 迁出验证：执行 `rtk cargo fmt -p codex-context-manager -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-context-manager`：66 passed，覆盖迁出的 history/normalize/original image estimate/contextual classifier 行为。执行 `rtk cargo check -p codex-core --tests`：通过，仅打印既有 unused warnings。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-context-manager --depth 1 --edges normal`：direct deps 只有 `codex-protocol`、`codex-utils-image`、`codex-utils-output-truncation`、`serde_json` 和 `tracing`。执行 `rtk cargo tree -p codex-context-manager --invert codex-core --edges normal`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`：均返回 package ID absent，确认新历史管理 crate 没有把 core、app-server display protocol、code-mode runtime、Starlark 或 Rama 间接拉回。执行 `rtk cargo tree -p codex-core --invert codex-context-manager --edges normal --depth 6`：只显示 `codex-context-manager -> codex-core` direct API path。执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 context usage 分类边界迁出切片：新增 `codex-context-usage`，把扫描 `ContextManager` 历史并生成 `ThreadContextUsage` 的分类逻辑、显式 skill injection 解析、隐式 skill command/output 归因和 developer/user contextual 分类从 `codex-core` 迁出。`codex-core/src/context_usage.rs` 从约 613 行缩为 49 行，只负责把 runtime `TurnContext` 投影成 `ContextUsageSkillDetection`，并把 `compact::is_summary_message` 作为函数指针传入；新 crate 不依赖 core、compact runtime、session/turn loop、app-server display protocol 或 concrete skills loader。本切片没有新增 Rust `unsafe`，并用 `rtk rg -n "\\bunsafe\\b" codex-rs/context-usage codex-rs/core/src/context_usage.rs codex-rs/core/src/context/mod.rs codex-rs/core/src/event_mapping.rs` 验证触碰文件无匹配。当前 `codex-rs/core/src` 为 296 个 Rust 文件、146736 行，`codex-context-usage/src/lib.rs` 为 592 行。
    - context usage 迁出验证：执行 `rtk cargo fmt -p codex-context-usage -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-context-usage`：2 passed。执行 `rtk cargo test -p codex-core thread_context_usage --lib`：通过，验证 core wrapper、resume recompute 和 compact summary 分类行为。执行 `rtk cargo check -p codex-core --tests`：通过，仅打印既有 warnings。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-context-usage --depth 1 --edges normal`：direct deps 为 `codex-context-manager`、`codex-core-skills-api`、`codex-protocol`、`codex-utils-absolute-path` 和 `serde_json`。执行 `rtk cargo tree -p codex-context-usage --invert codex-core --edges normal`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert codex-core-skills`：均返回 package ID absent，确认没有把 core、app-server display protocol、code-mode runtime、Starlark、Rama 或 concrete skills loader 间接拉回。执行 `rtk cargo tree -p codex-core --invert codex-context-usage --edges normal --depth 6`：只显示 `codex-context-usage -> codex-core` direct API path。
    - 2026-06-22 CST 已完成 rollout truncation 纯规则迁出切片：把 `thread_rollout_truncation` 从 `codex-core` 迁到 `codex-rollout-api`，包括真实 user message positions、fork-turn positions、`ThreadRolledBack` marker 应用、按第 N 个 user message 截断和保留最后 N 个 fork turns 的 helper。`codex-context-manager` 新增并导出 `is_real_user_message_boundary`，复用 contextual user marker classifier，避免在 rollout API 中复制 core `event_mapping::parse_turn_item` 或拉入 `TurnItem` display projection。core 的 session/thread/agent 调用点改为直接使用 `codex-rollout-api`；旧 `core/src/thread_rollout_truncation.rs` 和测试文件已删除。本切片没有新增 Rust `unsafe`，并用 `rtk rg -n "\\bunsafe\\b" codex-rs/rollout-api/src codex-rs/context-manager/src/history.rs codex-rs/context-manager/src/lib.rs codex-rs/core/src/agent/control.rs codex-rs/core/src/session/mod.rs codex-rs/core/src/rollout.rs codex-rs/core/src/lib.rs` 验证触碰文件无匹配。当前 `codex-rs/core/src` 为 294 个 Rust 文件、146248 行，`codex-rollout-api/src` 为 590 行。
    - rollout truncation 迁出验证：执行 `rtk cargo fmt -p codex-rollout-api -p codex-context-manager -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning。执行 `rtk cargo test -p codex-rollout-api`：10 passed，覆盖 user/fork truncation、rollback marker、contextual user message 和 hook prompt 忽略。执行 `rtk cargo test -p codex-context-manager`：66 passed。执行 `rtk cargo check -p codex-core --tests`：通过，仅打印既有 unused warnings。执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-rollout-api --depth 1 --edges normal`：direct deps 只有 `codex-context-manager` 和 `codex-protocol`。执行 `rtk cargo tree -p codex-rollout-api --invert codex-core --edges normal`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert codex-rollout`：均返回 package ID absent，确认没有把 core、app-server display protocol、code-mode runtime、Starlark、Rama 或 concrete rollout implementation 间接拉回。执行 `rtk cargo tree -p codex-core --invert codex-rollout-api --edges normal --depth 6`：只显示 `codex-rollout-api -> codex-core` direct API path。执行 `rtk git diff --check`：通过。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 收到两个已存在 schedule（`cargo-clean-my-codex` 与 `cargo-clean-cache`）同时触发；为避免重复清理，只执行一次 `rtk cargo clean`，通过，退出码 0。后续按事件可见 label 调用 `schedule_unsubscribe` 均返回 `unsubscribed:false`，说明 label 不是真实 subscription id；按用户要求不再继续搜索。
    - 2026-06-22 CST 用户调整拆分粒度：后续 core 收缩优先千行级到万行级功能域、runtime boundary 或 owner API，不再为了几百行 helper 单独创建 crate；如果是小型纯规则，优先并入相邻 owner crate。“一两千行”只是单文件过大时的上限参考，不是每个文件都要接近这个规模；本身清晰的小文件不需要强拆，也不要为了“不要太细”把有自然 ownership 的小文件硬合并。同时要求同步处理大文件，避免 core 从大 crate 退化成少数几千行 orchestration 文件；继续禁止新增 Rust `unsafe`。
    - 2026-06-22 CST 已完成 `core/src/config/mod.rs` 第一轮大文件拆分：机械迁出 `runtime_config.rs`（`Config`、`ThreadStoreConfig`、`MultiAgentV2Config`、terminal resize config）、`permission_settings.rs`（`Permissions` 和 permission profile materialization）、`layer_stacks.rs`（hook/skill/plugin config layer stack projector）和 `agent_role_types.rs`（agent role public config types）。`config/mod.rs` 从 3744 行降到 2838 行，当前 `codex-rs/core/src` 约 146080 行；这是为后续整块 config assembly 迁出做边界整理，不声称已经完成 crate 级大拆分。本切片未新增 Rust `unsafe`，执行 `rtk cargo fmt -p codex-core`、`rtk cargo check -p codex-core --lib`、`rtk cargo test -p codex-core runtime_config --lib`、`rtk cargo test -p codex-core permissions_profiles --lib`、`rtk cargo build -p codex-app-server --bin codex-app-server` 和针对触碰文件的 `rtk git diff --check` 均通过。尝试执行较宽的 `rtk cargo test -p codex-core config --lib`：429 passed / 3 failed；失败项为当前 dirty tree 既有的 schema fixture diff、spawn-agent 描述文本 diff 和已在前文记录过的 `reload_user_config_layer_refreshes_hooks`，不作为本切片通过证据。
    - 2026-06-22 CST 已完成 `codex-model-client` 大功能域迁出切片：新增 `codex-rs/model-client`，把原 `core/src/client.rs`、`client/chat_completions.rs`、`client_common.rs`、`attestation.rs`、client/client_common 单测和 review prompt/template 资源迁出；`codex-core` 只保留 `client.rs`、`client_common.rs`、`attestation.rs` 兼容 facade。新 crate 拥有 `ModelClient`、`ModelClientSession`、`Prompt`、`ResponseStream`、compact/realtime/model request 逻辑和 attestation provider API；core session/realtime/compact 仍通过明确公开 API 调用，不把 session/turn loop 迁入新 crate。本切片属于千行级 model/realtime client 功能域拆分，不是几百行 helper 拆分；也不要求把本来合理的小文件硬合并。当前 `codex-rs/core/src` 约 142435 行，`codex-model-client/src` 约 3874 行。
    - `codex-model-client` 迁出验证：执行 `rtk cargo fmt -p codex-model-client -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-model-client --lib`：17 passed，覆盖迁出的 client/prompt 单测；执行 `rtk cargo check -p codex-core --lib`：通过，仅打印既有 core dead_code/unused warnings；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-model-client --depth 1 --edges normal`：direct deps 为 API/type/runtime trait crate 以及 `http`、`futures`、`tokio`、`tokio-util`、`tracing` 等模型请求运行时依赖；执行 `--invert codex-core`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http` 均返回 package ID absent，确认新 crate 没有把 core、app-server display protocol、code-mode runtime、Starlark 或 Rama 间接拉回。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。迁移过程中只开放 core 实际调用的 `ModelClient`/`ModelClientSession`/realtime/compact API，并在新 crate 下新增 test-only `model_provider_factory_for_tests`，避免测试反向依赖 core。
    - 2026-06-22 CST 已完成 `config_lock` TOML replay helper 下沉切片：新增 `codex-rs/config-toml/src/config_lock.rs`，把 `CONFIG_LOCK_VERSION`、`read_config_lock_from_path`、`config_lockfile`、`validate_config_lock_replay`、`config_without_lock_controls`、`clear_config_lock_debug_controls` 和 TOML round-trip/diff helper 从 `codex-core` 下沉到 `codex-config-toml`。`codex-core` 删除旧 `core/src/config_lock.rs`，只保留 session export/validation 编排，以及 `config/mod.rs` 内把 lockfile materialize 成 `ConfigLayerEntry` 的 adapter。迁移过程中避免把 helper 放进 `codex-config-state`，因为该方向会形成 `config-loader -> config-state -> config-toml -> config-loader` 依赖环。当前 `codex-rs/core/src` 约 142231 行，`codex-config-toml/src` 约 1561 行。
    - `config_lock` 下沉验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-config-toml`：5 passed；执行 `rtk cargo test -p codex-config-state`：6 passed；执行 `rtk cargo test -p codex-core config_lock --lib`：7 passed / 1639 filtered；执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-config-toml --depth 1 --edges normal`：direct deps 为 config/auth/features/model-provider/protocol/path/toml/serde/tokio/tracing 等配置解析所需依赖；执行 `--invert codex-core`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http` 均返回 package ID absent；`--invert codex-config-state` 只显示既有 `codex-config-toml -> codex-config-loader -> codex-config-state` 路径且测试无环。执行代码触碰文件 `rtk rg -n "\\bunsafe\\b" ...` 无匹配。修复验证过程中暴露的 `compact_remote_v2` 测试编译问题：`collect_context_compaction_output` 改为接受通用 `Stream<Item = CodexResult<ResponseEvent>>`，测试使用 `futures::stream::iter`，避免打开 `codex-model-client::ResponseStream` 内部字段；补跑 `rtk cargo test -p codex-core collect_context_compaction_output_accepts_additional_output_items --lib`：1 passed / 1645 filtered。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-22 CST 已完成 `codex-agent-roles` 功能域迁出切片：新增 `codex-rs/agent-roles`，把 `core/src/config/agent_roles.rs` 中的 agent role TOML layer 合并、`.agent.md` / `.agent.toml` discovery、Markdown frontmatter parser、description/nickname/config file validation、plugin role merge，以及原 `AgentRoleConfig` / `AgentRoleSource` / `AgentCapabilityAllowlist` 类型迁出；`codex-core` 删除旧 `agent_role_types.rs`，`core/src/config/agent_roles.rs` 缩为旧路径 facade，`config/mod.rs` 通过 `codex-agent-roles` re-export 保持旧 `crate::config::*` API。该切片约 1.5K production + parser 单测，属于自然 agent role declaration/discovery owner，不是几百行 helper crate。当前 `codex-rs/core/src` 约 140709 行，`codex-agent-roles/src` 约 1521 行。
    - `codex-agent-roles` 迁出验证：执行 `rtk cargo fmt -p codex-agent-roles -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-agent-roles`：6 passed，覆盖迁出的 Markdown/frontmatter parser 单测；执行 `rtk cargo test -p codex-core agent_role --lib`：通过，覆盖 core 侧旧路径、role loading 和 apply-role 相关测试；执行 `rtk cargo check -p codex-core --lib` 与 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过。执行 `rtk cargo tree -p codex-agent-roles --depth 1 --edges normal`：direct deps 为 `codex-config-state`、`codex-config-toml`、`codex-config-types`、`codex-file-system`、`codex-utils-absolute-path`、`serde_json`、`toml` 和 `tracing`；执行 `--invert codex-core`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http` 均返回 package ID absent。执行触碰代码 `rtk rg -n "\\bunsafe\\b" ...` 无匹配；执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 `rtk git diff --check`：通过。
    - 2026-06-22 CST 已完成 `codex-permissions-runtime` approval requirement owner 扩展切片：把 `ExecApprovalRequirement`、`proposed_execpolicy_amendment` helper 和 `default_exec_approval_requirement` 从 `core/src/tools/sandboxing.rs` 下沉到已有 `codex-rs/permissions-runtime`；core sandboxing 模块只 re-export 旧路径，`SandboxOverride`、approval cache、hook/guardian/user approval 编排和真实 tool runtime execution 仍留在 core。默认 approval 相关 tests 已迁到 owner crate，core 侧保留 sandbox override / permission payload tests。该切片扩展已有 permissions runtime owner，不新增几百行 helper crate；当前 `codex-rs/core/src` 为 298 个 Rust 文件、140546 行，`codex-permissions-runtime/src/lib.rs` 为 506 行。
    - `codex-permissions-runtime` approval 扩展验证：执行 `rtk cargo fmt -p codex-permissions-runtime -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-permissions-runtime`：11 passed；执行 `rtk cargo test -p codex-core sandboxing --lib`：通过；执行 `rtk cargo test -p codex-core exec_policy --lib`：93 passed / 1543 filtered；执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server build 只打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-permissions-runtime --depth 1 --edges normal`：direct deps 只有 `codex-protocol`、`codex-utils-absolute-path` 和 `globset`；执行 `--invert codex-core`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http` 均返回 package ID absent。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-permissions-runtime|codex-sandboxing|codex-execpolicy|codex-network-proxy"`：显示 `codex-permissions-runtime`、`codex-sandboxing-api`、`codex-execpolicy-api` 和 `codex-network-proxy-api`，未新增 full implementation edge。执行触碰 Rust/Cargo 文件 `rtk rg -n "\\bunsafe\\b" ...`：无匹配。
    - 2026-06-22 CST 已完成 MCP tool approval 纯规则迁出切片：把 approval key、prompt options、question id/label constants、requires-approval hint rule、approval question builder、elicitation meta/request builder、response parser、approval-mode normalization、tool params display builder，以及 consequential tool approval template JSON/template renderer 从 `core/src/mcp_tool_call.rs` / `core/src/mcp_tool_approval_templates.rs` 迁入已有 `codex-rs/mcp-types`。`codex-core` 保留 `maybe_request_mcp_tool_approval`、hook/guardian/ARC monitor 编排、session approval memory、config persistence、connection-manager metadata lookup 和真实 MCP tool-call execution；旧 core 常量/function 路径只保留 `pub(crate) use` 兼容内部调用。该切片没有新建几百行 helper crate，而是扩展既有 MCP typed DTO owner，并把 mcp-types 内部按自然 ownership 保持为 `tool_approval.rs`（496 行）和 `tool_approval_templates.rs`（371 行），不为了行数硬合并。当前 `codex-rs/core/src` 为 139750 行，`core/src/mcp_tool_call.rs` 为 1839 行。
    - MCP tool approval 迁出验证：执行 `rtk cargo fmt -p codex-mcp-types -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-types`：25 passed；执行 `rtk cargo test -p codex-core mcp_tool_approval --lib`：通过；执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server build 只打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-mcp-tool-types`、`codex-plugin-types`、`codex-protocol`、`schemars`、`serde`、`serde_json`、`tracing`、`ts-rs`。执行 `rtk cargo tree -p codex-mcp-types --invert codex-core --edges normal --depth 6`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert codex-mcp`：均返回 package ID absent，确认 mcp-types 没有把 core、app-server display protocol、code-mode runtime、Starlark、Rama 或 concrete MCP runtime 拉回。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal`：显示 core 仍直接依赖 `codex-mcp-types` 和 `codex-mcp-tool-types`，没有新增 concrete MCP runtime edge。执行触碰 Rust 文件 `rtk rg -n "\\bunsafe\\b" ...`：无匹配；执行 `rtk git diff --check`：通过；执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 MCP tool result shaping owner 收敛切片：把 `sanitize_mcp_tool_result_for_model` 和 `truncate_mcp_tool_result_for_event` 从 `core/src/mcp_tool_call.rs` 迁入已有 `codex-rs/mcp-tool-types`，同时把对应 image content 清洗和大结果截断测试从 core 测试迁到 owner crate。`codex-core` 只保留 `MCP_TOOL_CALL_EVENT_RESULT_MAX_BYTES` 这类 runtime/display policy，并在调用 `truncate_mcp_tool_result_for_event` 时显式传入 max bytes，避免 `codex-mcp-tool-types` 为展示策略依赖 `codex-utils-pty`、command runtime、app-server display protocol 或 concrete MCP runtime。当前 `codex-rs/core/src` 为 139553 行，`core/src/mcp_tool_call.rs` 为 1763 行，`mcp-tool-types/src/lib.rs` 为 454 行。
    - MCP tool result shaping 验证：执行 `rtk cargo fmt -p codex-mcp-tool-types -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-tool-types`：8 passed；执行 `rtk cargo test -p codex-core mcp_tool_call --lib`、`rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server build 只打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-mcp-tool-types --depth 1 --edges normal`：direct deps 只有 `codex-protocol`、`codex-utils-output-truncation`、`serde` 和 `serde_json`。执行反向门禁检查 `codex-core`、`codex-app-server-protocol`、`codex-code-mode`、`starlark`、`rama-http`、`codex-mcp`、`codex-rmcp-client`、`codex-login`、`codex-model-provider`、`codex-exec-server`、`codex-utils-pty` 均 absent from `codex-mcp-tool-types` normal graph；执行 `rtk cargo tree -p codex-core --invert codex-mcp --edges normal --depth 6` 返回 nothing to print，确认本切片没有把 concrete MCP runtime 带回 core。执行 touched Rust 文件 `rtk rg -n "\\bunsafe\\b" ...`：无匹配；执行 `rtk git diff --check`：通过；执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 agent fork history selector 迁出切片：把 `SpawnAgentForkMode`、forked parent rollout item 过滤规则、Last-N fork windowing 以及 MultiAgentV2 parent usage-hint 清理从 `core/src/agent/control.rs` 下沉到已有 `codex-rs/agent-runtime/src/fork_history.rs`。`AgentControl` 继续拥有 parent thread history flush/read、ThreadManager spawn/fork/resume 和 inherited shell/exec policy 编排；`codex-agent-runtime` 只拥有不依赖 core 的稳定 fork-history policy。当前 `codex-rs/core/src` 为 139493 行，`core/src/agent/control.rs` 为 1230 行，`agent-runtime/src/fork_history.rs` 为 189 行。
    - agent fork history selector 验证：执行 `rtk cargo fmt -p codex-agent-runtime -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-agent-runtime`：17 passed；执行 `rtk cargo test -p codex-core spawn_agent_fork --lib`：通过，覆盖 sanitized full-history fork、flush-before-fork 和 Last-N fork 集成路径；执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，app-server build 只打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-agent-runtime --depth 1 --edges normal`：direct deps 为 `codex-metrics-api`、`codex-protocol`、`codex-rollout-api`、`rand` 和 `serde`；新增 `codex-rollout-api` 是已有 lightweight rollout rules crate。执行反向门禁检查 `codex-core`、`codex-app-server-protocol`、`codex-code-mode`、`starlark`、`rama-http`、`codex-mcp`、`codex-exec-server`、`codex-shell-command`、`codex-workflow` 均 absent from `codex-agent-runtime` normal graph，确认没有把 core 或 heavy runtime 拉回 agent runtime owner。
    - 2026-06-22 CST 已完成 thread goal API / accounting primitive 收敛切片：把 thread goal 的 state -> protocol DTO 转换、protocol/status -> state status 转换、预算校验、external mutation DTO、token/wall-clock accounting snapshot、token accounting delta 和 goal-update `ResponseItem` builder 从 `codex-core` / app-server 本地重复实现收敛到已有 `codex-rs/state-api`。`codex-core` 的 `goals.rs` 继续拥有 Session/TurnContext 编排、state-db reconciliation、event emission、accounting lock、continuation scheduling 和 prompt 注入；app-server 只保留 v2 `ThreadGoal` DTO wrapper，通过 `protocol_goal_from_state(goal).into()` 映射客户端边界，并直接从 `codex-state-api` 引用 external goal DTO。该切片没有新增小 crate，也没有把 SQLite/sqlx runtime、app-server display protocol 或 code-mode runtime 拉入 state API。当前 `codex-rs/core/src` 为 139194 行，`core/src/goals.rs` 为 1640 行，`state-api/src/lib.rs` 为 527 行。
    - thread goal API / accounting primitive 收敛验证：执行 `rtk cargo fmt -p codex-state-api -p codex-core -p codex-app-server`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-state-api`：5 passed；执行 `rtk proxy cargo test -p codex-core goal --lib`：通过；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-state-api --depth 1 --edges normal`：direct deps 只有 `anyhow`、`chrono`、`codex-protocol`、`serde`、`serde_json` 和 `strum`。执行 `rtk cargo tree -p codex-state-api --invert codex-core --edges normal --depth 6`、`--invert codex-state`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert sqlx`、`--invert tokio` 均返回 package ID absent，确认没有把 core、full SQLite state runtime、app-server display protocol、code-mode runtime、Starlark、Rama、sqlx 或 tokio 间接拉回 state API。执行 `rtk cargo tree -p codex-core --depth 1 --edges normal | rtk rg "codex-state-api|codex-state v|codex-app-server-protocol|codex-code-mode"`：只显示 `codex-state-api` 和既有 `codex-code-mode-api`。执行触碰文件 `rtk rg -n "\\bunsafe\\b" ...` 和旧 external goal 路径扫描均无匹配；执行 touched-file `rtk git diff --check -- ...`：通过。
    - 2026-06-22 CST 已完成 MCP tool-call metadata shaping owner 收敛切片：把 server origin host/port span fields、result `_meta["codex/telemetry"].span` allowlist/truncation、MCP app resource URI fallback、Codex Apps OpenAI file input param exposure plan 和 request `_meta.threadId` 注入 helper 从 `core/src/mcp_tool_call.rs` 迁入已有 `codex-rs/mcp-types/src/tool_call.rs`。`codex-core` 只保留 Session/TurnContext wiring、MCP execution、event emission 和 tracing span record；不再本地维护这些 protocol-neutral metadata helper。该切片没有新增几百行 helper crate，而是继续扩展现有 MCP typed DTO owner；`codex-mcp-types` 为 server-origin parsing 新增 direct `http` 轻量 dependency，未引入 full MCP runtime。当前 `codex-rs/core/src` 为 138946 行，`core/src/mcp_tool_call.rs` 为 1648 行，`mcp-types/src/tool_call.rs` 为 375 行。
    - MCP tool-call metadata shaping 验证：执行 `rtk cargo fmt -p codex-mcp-types -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-mcp-types`：36 passed；执行 `rtk cargo test -p codex-core mcp_tool_call --lib`：通过；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-mcp-types --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-config-types`、`codex-mcp-tool-types`、`codex-plugin-types`、`codex-protocol`、`http`、`schemars`、`serde`、`serde_json`、`tracing` 和 `ts-rs`。执行 `rtk cargo tree -p codex-mcp-types --invert codex-core --edges normal --depth 6`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert codex-mcp`、`--invert codex-rmcp-client`、`--invert codex-login`、`--invert codex-model-provider`、`--invert codex-exec-server` 和 `--invert tokio` 均返回 package ID absent，确认没有把 core、app-server display protocol、code-mode runtime、Starlark、Rama、concrete MCP runtime/RMCP client、login/model-provider、concrete exec-server 或 async runtime 间接拉回 `codex-mcp-types`。
    - 2026-06-22 CST 已开始 Step 6 guardian runtime 功能域收缩：把 `GuardianApprovalRequest`、network trigger、MCP annotations、approval request JSON shaping、reviewed action analytics mapping、request target/turn id helper 和 action pretty formatting/truncation 从 `core/src/guardian/approval_request.rs` 迁入已有 `codex-rs/ext/guardian/src/approval_request.rs`；core 只保留旧路径 facade，Session/TurnContext approval routing 和 review session 编排不动。
    - 2026-06-22 CST 继续完成 guardian prompt/assessment owner 收敛：把 guardian transcript entry/rendering、prompt item builder、cursor/delta prompt mode、policy/template markdown、output schema、assessment JSON parsing 和相关 pure tests 迁入 `codex-guardian`；`core/src/guardian/prompt.rs` 缩为 151 行，只负责从 parent `Session` history 收集 `ResponseItem` transcript entries 并调用 `codex_guardian::build_guardian_prompt_items_from_entries`。`codex-core` 中 `GuardianAssessment` 变为 `codex-guardian` re-export，review session spawn/cache/reuse、circuit breaker、event emission、contextual user message filtering adapter 仍留在 core。当前 `codex-rs/core/src` 为 137683 行，`codex-rs/ext/guardian/src` 为 1520 行。
    - guardian runtime Step 6 验证：执行 `rtk cargo fmt -p codex-guardian -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-guardian`：11 passed；执行 `rtk cargo test -p codex-core guardian_approval_request_to_json --lib`、`rtk cargo test -p codex-core build_guardian_prompt --lib`、`rtk cargo test -p codex-core collect_guardian_transcript_entries --lib` 和 `rtk cargo test -p codex-core format_guardian_action_pretty --lib`：均通过；执行 `rtk cargo check -p codex-core --lib` 和 `rtk cargo build -p codex-app-server --bin codex-app-server`：均通过，仅剩既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-guardian --depth 1 --edges normal`：direct deps 为 `anyhow`、`codex-analytics-api`、`codex-extension-api`、`codex-protocol`、`codex-shell-utils`、`codex-utils-absolute-path`、`codex-utils-output-truncation`、`serde` 和 `serde_json`。执行 `rtk cargo tree -p codex-guardian --invert codex-core --edges normal`、`--invert codex-app-server-protocol`、`--invert codex-code-mode`、`--invert starlark`、`--invert rama-http`、`--invert codex-mcp`、`--invert codex-login`、`--invert codex-model-provider`、`--invert codex-exec-server` 和 `--invert tokio` 均返回 package ID absent，确认没有把 core、app-server protocol、code-mode runtime、Starlark/Rama、concrete MCP/login/model-provider/exec-server 或 tokio 拉回 guardian owner。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings。
    - 2026-06-22 CST 已完成 `codex-permissions-runtime` exec-policy approval evaluation owner 收敛切片：把 exec-policy 到 `ExecApprovalRequirement` 的纯 evaluation、shell command candidate parsing、unmatched-command heuristic、prefix amendment/reason derivation、intercepted exec policy evaluation 和 `join_program_and_argv` 从 `core/src/exec_policy.rs` / `core/src/tools/runtimes/shell/unix_escalation.rs` 迁入已有 `codex-rs/permissions-runtime/src/exec_policy.rs`。`ExecPolicyManager` 继续留在 core，负责 config layer loader、policy snapshot lock、append rules/network rules、tokio blocking update 和 Session/TurnContext 调用；`unix_escalation` 继续保留 hook/Guardian/user prompt、sandbox transform 和 shell-escalation executor adapter。该切片扩展已有 permissions runtime owner，不新增小 helper crate。当前 `codex-rs/core/src` 为 136993 行；`core/src/exec_policy.rs` 为 589 行，`core/src/tools/runtimes/shell/unix_escalation.rs` 为 927 行，`codex-permissions-runtime/src/exec_policy.rs` 为 741 行。
    - `codex-permissions-runtime` exec-policy evaluation 验证：执行 `rtk cargo fmt -p codex-permissions-runtime -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-permissions-runtime`：11 passed；执行 `rtk cargo test -p codex-core exec_policy --lib`：通过；执行 `rtk cargo test -p codex-core unix_escalation --lib`：16 passed / 1591 filtered；执行 `rtk cargo check -p codex-permissions-runtime -p codex-core --lib`：通过，仅剩既有 core unused/dead_code warnings；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 warning。执行 `rtk cargo tree -p codex-permissions-runtime --depth 1 --edges normal`：direct deps 为 `codex-execpolicy-api`、`codex-protocol`、`codex-shell-command`、`codex-shell-utils`、`codex-utils-absolute-path` 和 `globset`；在受限 graph 中检查 `--invert codex-core`、`--invert codex-app-server-protocol` 和 `--invert tokio` 均返回 package ID absent，确认没有把 core、app-server display protocol 或 async runtime 拉回 permissions runtime owner。执行 `rtk just bazel-lock-check`：通过，仅打印既有 rules_rs well-known crate annotation warnings；执行 touched-file `rtk git diff --check -- ...`：通过；执行 touched Rust files `rtk rg -n "\\bunsafe\\b" ...`：无匹配。验证过程中 command session 未把长命令失败输出注入模型，临时用 `/tmp/codex-core-exec-policy-test.out` 捕获一次错误定位，发现并修复 test-only import/re-export 边界后恢复正常验证。
    - 2026-06-22 CST 已完成 session display projection 收口切片：把 `is_structured_display_response_item`、Init Context `InjectedContext` projector 和 model item -> typed display event helper 从高触摸 `core/src/session/mod.rs` 移到 `core/src/event_mapping.rs`。该切片不改变 runtime 行为，只让 Session 调用共享投影边界，贴合“typed `EventMsg` 是 display source，`ResponseItem` 只维护模型上下文”的架构规则；`core/src/session/mod.rs` 当前为 4049 行。
    - session display projection 验证：执行 `rtk cargo fmt -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo check -p codex-core --lib`：通过，仅剩既有 unused/dead_code warnings。
    - 2026-06-22 CST 已完成后端 realtime conversation runtime 迁出切片：新增 `codex-rs/realtime`（`codex-realtime`），把 WebSocket/WebRTC sideband input loop、audio/text/handoff channel 管理、realtime event fanout input parser、默认 backend prompt、voice validation、ChatGPT backend rewrite、API key/header helper 和 handoff delegation formatting 从 `core/src/realtime_conversation.rs` / `core/src/realtime_prompt.rs` 迁出。`codex-core` 删除 realtime prompt template 和纯 helper tests，只保留约 408 行 Session adapter：读取 config/session、构造 startup context、调用 `RealtimeConversationManager`、发送 realtime `EventMsg`、把 handoff 文本路由回 turn。当前 `codex-rs/core/src` 为 135591 行；`codex-realtime/src` 约 1262 行。
    - `codex-realtime` 迁出验证：执行 `rtk cargo fmt -p codex-realtime -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-realtime`：15 passed；执行 `rtk cargo check -p codex-core --lib`：通过；执行 `rtk cargo test -p codex-core --test all suite::realtime_conversation::conversation_uses_default_realtime_backend_prompt -- --exact`：通过，覆盖 core realtime adapter 读取新 crate 默认 prompt 的集成路径；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅剩既有 warnings。执行 `rtk cargo tree -p codex-realtime --depth 1 --edges normal`：direct deps 为 `codex-api-provider`、`codex-api-types`、`codex-auth-types`、`codex-client-identity`、`codex-model-client`、`codex-model-provider-api`、`codex-model-provider-info`、`codex-protocol`、`codex-user-info`、`http`、`tokio` 等 realtime 真实边界；执行反向门禁确认 `codex-core`、`codex-app-server-protocol`、`codex-code-mode`、`starlark`、`rama-http`、`codex-mcp`、`codex-exec-server` 和 `codex-login` 不在 `codex-realtime` normal graph。执行 `rtk rg -n "crate::|Session|ConversationStart|ConversationText|ConversationAudio|EventMsg|Event\\b|CodexErrorInfo|ErrorEvent|RealtimeConversation(Started|Closed|Sdp|Realtime)" codex-rs/realtime/src/conversation.rs codex-rs/realtime/src/prompt.rs`：无 core/app-server adapter 残留，只剩 realtime protocol event/data 类型引用。
    - 2026-06-22 CST 已完成 realtime startup context owner 收敛切片：把 current-thread section、recent-work grouping、workspace/user-root tree rendering、startup-context section budget/truncation 和对应 pure tests 从 `core/src/realtime_context.rs` 迁入 `codex-rs/realtime/src/startup_context.rs`；`codex-core` 的 `realtime_context.rs` 缩为 55 行 adapter，只负责从 `Session` 读取 cwd/history/thread-store recent threads 并调用 `codex_realtime::build_realtime_startup_context`。该切片继续聚合 realtime 功能域，没有把 Session、EventMsg、app-server display 或 code-mode runtime 放进 `codex-realtime`。当前 `codex-rs/core/src` 为 134752 行，`codex-rs/realtime/src/startup_context.rs` 为 549 行。
    - realtime startup context 验证：执行 `rtk cargo fmt -p codex-realtime -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-realtime`：通过；执行 `rtk cargo test -p codex-realtime -- --list`：通过；执行 `rtk cargo check -p codex-core --lib`：通过；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 unused/dead_code warnings。执行 `rtk cargo tree -p codex-realtime --depth 1 --edges normal`：direct deps 新增 `codex-context-manager`、`codex-file-system`、`codex-git-info`、`codex-thread-store-api`、`codex-utils-absolute-path`、`codex-utils-home-dir`、`codex-utils-output-truncation` 和 `chrono`，均为 startup-context 实际使用的 API/util 层；执行 full normal graph absent gate 确认 `codex-core`、`codex-app-server-protocol`、`codex-code-mode`、`starlark`、`rama-http`、`codex-mcp`、`codex-rmcp-client`、`codex-exec-server`、`codex-login`、`codex-state` 和 `sqlx` 均不在 `codex-realtime` graph。执行 `rtk rg -n "crate::|Session|TurnContext|EventMsg|codex_core|app_server|code_mode|codex_state|sqlx" codex-rs/realtime/src/startup_context.rs codex-rs/realtime/src/startup_context_tests.rs`：生产代码无 core/app-server/runtime adapter 残留，测试只引用 protocol `SessionSource` DTO。执行 touched Rust files `rtk rg -n "\\bunsafe\\b" ...`：无匹配。
    - 2026-06-22 CST 已完成 agent-job data shaping owner 收敛切片：把 `spawn_agents_on_csv` 使用的 CSV parse、CSV output escaping/rendering、instruction template 渲染、worker prompt 构造、默认 output CSV path 和对应 pure tests 从 `core/src/tools/handlers/agent_jobs.rs` 迁入 `codex-rs/state-api/src/agent_job.rs`；`codex-core` 的 agent job handler 继续负责 tool argument validation、Session/AgentControl spawn loop、StateDb mutation、recover/finalize/timeout 和 tool output。该切片不新增 crate、不引入 `csv` crate，也不让 state-api 依赖 core 或 full SQLite runtime。当前 `codex-rs/core/src` 为 134370 行，`core/src/tools/handlers/agent_jobs.rs` 为 564 行，`codex-rs/state-api/src/agent_job.rs` 为 443 行。
    - agent-job data shaping 验证：执行 `rtk cargo fmt -p codex-state-api -p codex-core`：通过，仅打印 workspace rustfmt nightly-only warning；执行 `rtk cargo test -p codex-state-api`：13 passed；执行 `rtk cargo test -p codex-core agent_job --lib`：通过；执行 `rtk cargo check -p codex-core --lib`：通过，仅剩既有 unused/dead_code warnings；执行 `rtk cargo build -p codex-app-server --bin codex-app-server`：通过，仅打印既有 warnings。执行 `rtk cargo tree -p codex-state-api --depth 1 --edges normal`：direct deps 仍只有 `anyhow`、`chrono`、`codex-protocol`、`serde`、`serde_json` 和 `strum`；执行 full normal graph absent gate 确认 `codex-core`、`codex-state`、`codex-app-server-protocol`、`codex-code-mode`、`starlark`、`rama-http`、`codex-mcp`、`codex-rmcp-client`、`codex-exec-server`、`codex-login`、`sqlx` 和 `tokio` 均不在 `codex-state-api` graph。执行 touched Rust files `rtk rg -n "\\bunsafe\\b" ...`：无匹配。
  next_action: 继续 Step 6，优先沿 guardian review-session runtime、tools runtime 或 session/turn loop 这类千行级功能域继续收缩 core；每个切片继续先明确 owner crate 和 runtime/service 边界，再用 `cargo tree --depth 1` 与 `--invert <heavy-crate>` 证明没有 direct edge 变 indirect edge，并把稳定边界写回 `AGENTS.md`。

- id: app-server-compile-time-topology
  execution_mode: performance_refactor_exclusive
  workdir: /Users/bytedance/Projects/my-codex
  owner: root PM direct implementation
  status: completed
  started_at: 2026-06-18 14:14:52 CST
  context:
    - 测量命令：`rtk cargo build --timings -p codex-app-server --bin codex-app-server`，在主 checkout `codex-rs` 执行。
    - 本次使用主 checkout 独立 `codex-rs/target`，不使用共享 target。
    - wall time 约 106.67s；Cargo timing 中 `codex-core` 单 unit 202.58s，是最大瓶颈。
    - app-server 路径上依赖 `codex-core` 的本地 crate 包括 `codex-app-server`、`codex-app-server-transport`、`codex-chatgpt`、`codex-cloud-requirements`、`codex-file-subscription`、`codex-guardian`、`codex-memories-write`。
  plan:
    - step: 1
      status: completed
      description: 拆出低风险 `backoff` helper 到轻量 util crate，让 `codex-cloud-requirements` 和 `codex-app-server-transport` 不再为了 `codex_core::util::backoff` 依赖 `codex-core`。
    - step: 2
      status: completed
      description: 运行最小验证，确认相关 crate 编译通过，并检查 `cargo tree -p codex-app-server --invert codex-core --edges normal` 中 `codex-cloud-requirements` 是否脱离 core；`app-server-transport` 若仍因测试或其他 API 依赖 core，则记录剩余原因。
    - step: 3
      status: completed
      description: 评估下一层拆分：`Config` 边界、connector helper、unified exec/command runtime handle、thread runtime facade，按收益和风险排序。
    - step: 4
      status: completed
      description: 若第 1 阶段收益明确且验证通过，提交本阶段改动；更大拆分另开后续步骤，避免一次改动过大。
    - step: 5
      status: completed
      description: 完整盘点 `codex-file-subscription` 对 `codex-core` 的生产依赖调用面，确认哪些能力属于 thread runtime、event-command 注入、订阅 metadata 持久化，以及哪些 unified exec 参数当前只是透传或未使用。
    - step: 6
      status: completed
      description: 设计并落地 `file-subscription` 的轻量 runtime trait/handle 边界；由 core/app-server 侧 adapter 实现具体 `ThreadManager` 调用，避免把 `ThreadManager`、`CodexThread` 或 unified exec runtime 类型继续暴露给小 crate。
    - step: 7
      status: completed
      description: 将 `codex-file-subscription` 迁移到新 runtime 边界，移除 normal `codex-core` 依赖；保持事件注入、订阅恢复、metadata 更新和 final-status notify 行为不变。
    - step: 8
      status: completed
      description: 对 `file-subscription` 阶段做最小验证：`rtk cargo check -p codex-file-subscription -p codex-app-server --bin codex-app-server`，并用 `rtk cargo tree -p codex-app-server --invert codex-core --edges normal` 确认该路径已脱离 core；通过后提交小阶段。
    - step: 9
      status: completed
      description: 单独盘点 `codex-memories-write` 对 core runtime 的依赖，设计 thread/model/runtime facade；该阶段涉及 `CodexThread`、`ModelClient`、`Prompt`、`ResponseEvent`、`RolloutRecorder` 等较大边界，不能和 `file-subscription` patch 混合。
    - step: 10
      status: completed
      description: 在 `memories-write` facade 方案明确后实施拆分、验证 app-server 编译路径和 timing 变化；若风险或改动体量过大，先提交设计记录或中间 adapter，再继续实现。
    - step: 11
      status: completed
      description: 完成所有可行拆分后重新运行 `rtk cargo build --timings -p codex-app-server --bin codex-app-server`，比较 `codex-core` 是否仍在 app-server 必要路径、warm build wall time 和 unit seconds，并更新最终结论。
  findings:
    - 已新增 `codex-utils-backoff`，`codex-core::util::backoff` 保留为 re-export。
    - `codex-cloud-requirements` 已移除 `codex-core` 依赖；`cargo tree -p codex-app-server --invert codex-core --edges normal` 中该 crate 已消失。
    - `codex-app-server-transport` 的 backoff 调用已迁移，并把生产代码的 `find_codex_home` 改为 `codex-utils-home-dir`；`codex-core` 仅保留为 dev-dependency 供测试支持 API 使用。
    - `codex-guardian` 的 thread lifecycle contributor 已改为配置泛型，移除 `codex-core` 依赖。
    - `codex-chatgpt` 默认 feature 已改为不依赖 `codex-core`；CLI-only `apply_command` / `get_task` 放入 `apply-command` feature，`codex-cli` 显式启用该 feature。app-server/TUI 调用 ChatGPT connector/settings API 时先构造轻量 `ChatGptConfig`，accessible/enabled connector 状态直接走 core connector API。
    - 当前 `cargo tree -p codex-app-server --invert codex-core --edges normal` 剩余路径为 `codex-app-server`、`codex-file-subscription`、`codex-memories-write`。其中 `file-subscription`/`memories-write` 牵涉 thread/runtime handle，应作为后续较大拆分处理。
    - `codex-file-subscription` 的 core 依赖不再是单纯 config 类型：生产代码使用 `ThreadManager`、`UnifiedExecManagerHandle` 和 `UnifiedExecProcessManager` 来恢复订阅、注入事件和提供 event-command 工具；下一步需要先在 extension/runtime 边界定义稳定 trait/handle，再让 core adapter 实现。
    - `codex-file-subscription` 已新增 `FileSubscriptionThreadRuntime` 边界，订阅 active count、event-driven trigger、event-command event、订阅 metadata 持久化和历史恢复都通过 host adapter 执行；app-server 侧用 `CoreFileSubscriptionThreadRuntime` 适配 `ThreadManager`。原先透传但未使用的 unified exec manager 参数已从该扩展路径移除。
    - `codex-file-subscription` 已移除 normal `codex-core` 依赖；当前 `cargo tree -p codex-app-server --invert codex-core --edges normal` 剩余路径为 `codex-app-server` 自身和 `codex-memories-write`。
    - `codex-memories-write` 同样直接使用 `CodexThread`、`ThreadManager`、`ModelClient`、`Prompt`、`ResponseEvent`、`RolloutRecorder` 等 core runtime 类型；这应作为 thread runtime facade 的较大拆分，不能和低风险 util/config 拆分混在一个小 patch。
    - `codex-memories-write` 调用面已初步盘点：`start.rs` 负责入口 eligibility 和创建 `MemoryStartupContext`；`runtime.rs` 负责 telemetry、model info、stage-one model streaming、内部 consolidation agent spawn/submit/shutdown；`phase1.rs` 负责 rollout 读取、prompt 构造和 state-db job 结果写入；`phase2.rs` 负责 memory workspace sync、内部 agent loop/heartbeat/token usage；`guard.rs` 只需要 rate-limit 配置字段和 auth manager。
    - `memories-write` 拆 core 的关键不是单个 util，而是需要 host-provided facade：一层负责 thread/runtime 操作（state_db、config snapshot、model info、spawn internal thread、submit prompt、agent status/token usage/shutdown），另一层负责 stage-one model sampling（或把 `ModelClient`/`Prompt` 迁到更小 crate）。直接在小 crate 里复刻 `ThreadManager`/`CodexThread` 会扩大耦合，不应这样做。
    - `memories-write` 已先清理明显 core re-export：`RolloutRecorder` 改为直接使用 `codex-rollout`，`ResponseEvent` 改为直接使用 `codex-api`，简单的 `content_items_to_text` 本地化；剩余 core 引用集中在 `Config`、`Prompt`、`ModelClient`、`ThreadManager`/`CodexThread`、`resolve_installation_id` 和 `build_turn_metadata_header`。
    - 下一步 facade 形状应让 `memories-write` 暴露 `MemoryStartupRuntime` / `MemoryConsolidationAgent` 这类 host runtime trait，而不是继续接受 `ThreadManager`、`CodexThread`、`Config`。app-server/core adapter 捕获现有 `ThreadManager`、foreground `CodexThread` 和 `Config`，实现 state-db 访问、stage-one request context、stage-one prompt streaming、consolidation agent spawn/submit、status/token usage/shutdown。
    - `memories-write` 自身应只保留业务配置子集，例如 `codex_home`、`MemoriesConfig`、telemetry 所需的 model/provider/log_user_prompt 字段和 feature eligibility 的布尔结果；`phase1`/`phase2` 不应直接 clone 或 mutate core `Config`。phase2 的 consolidation locked-down config 构造可以下沉到 app-server/core adapter，`memories-write` 只传 root、prompt 和期望 model/reasoning effort。
    - stage-one prompt 应在 `memories-write` 内表示为轻量 `StageOnePromptRequest`（base instructions、user text、output schema、strict flag），由 adapter 转成 core `Prompt` 并调用 `ModelClient`；这样 `Prompt`、`ModelClient`、`resolve_installation_id`、`build_turn_metadata_header` 都留在 core adapter 侧。
    - `codex-memories-write` 已新增 `MemoryStartupRuntime` / `MemoryConsolidationAgent` facade 和 `MemoryStartupSettings` 配置子集；生产代码不再接受 `ThreadManager`、`CodexThread` 或 core `Config`，phase1 stage-one sampling 通过 `StageOnePromptRequest` 交给 host adapter。
    - app-server 侧新增 `CoreMemoryStartupRuntime` adapter，负责 state db、telemetry、model info、stage-one `ModelClient` streaming、phase2 consolidation internal thread spawn/submit/shutdown；core-specific `Prompt`、`ModelClient`、`build_turn_metadata_header`、locked-down consolidation `Config` 构造都留在 app-server/core 边界。
    - `codex-memories-write` 的 `codex-core` 已移到 dev-dependency，仅用于 startup tests；`codex-api` 和 `codex-rollout-trace` 也只保留为 dev-dependency，`codex-terminal-detection` 从 memories-write 移除。app-server 明确声明 adapter 需要的 direct dependencies。
    - startup tests 改为 test-only core-backed runtime adapter；phase2 测试不再启动完整 internal core thread，而是直接通过 `ModelClient` 向 mock Responses API 发送 consolidation prompt 并返回 fake `MemoryConsolidationAgent` 状态句柄，避免测试 runtime 递归和默认测试线程 stack overflow。
    - `rtk cargo tree -p codex-app-server --invert codex-core --edges normal` 已确认剩余路径只剩 `codex-app-server -> codex-core`，`codex-memories-write` 不再出现在 app-server 到 core 的 normal 反向路径中。
    - 最终 timing 显示 warm app-server build total time 20.1s，unit 为 `codex-app-server` 11.5s、bin 5.0s、`codex-memories-write` 2.3s、`codex-file-subscription` 1.7s；`codex-core` 未出现在本次重编 unit 中。相比初始 warm-target 约 100s/冷测约 106.67s 且 core 单 unit 202.58s 的状态，当前 app-server 小范围改动已不再因 file-subscription/memories-write normal core 依赖牵动 core。
  validation:
    - `rtk cargo check -p codex-utils-backoff -p codex-cloud-requirements -p codex-app-server-transport` 通过。
    - `rtk cargo test -p codex-utils-backoff` 通过，2 个测试通过。
    - `rtk cargo check -p codex-app-server --bin codex-app-server` 通过。
    - `rtk cargo check -p codex-guardian -p codex-app-server --bin codex-app-server` 通过。
    - `rtk cargo check -p codex-chatgpt -p codex-chatgpt --features apply-command -p codex-app-server --bin codex-app-server -p codex-tui -p codex-cli` 通过。
    - `rtk cargo build --timings -p codex-app-server --bin codex-app-server` 通过，warm-target wall time 约 100s；最新 timing 为 `target/cargo-timings/cargo-timing-20260618T062735.642709Z.html`。
    - `codex-chatgpt` 拆分后重新执行 `rtk cargo build --timings -p codex-app-server --bin codex-app-server` 通过，warm-target wall time 约 22s；最新 timing 为 `target/cargo-timings/cargo-timing-20260618T065116.613751Z.html`，本次只重编 `codex-app-server`、bin、`codex-chatgpt`、`codex-guardian`，未重编 `codex-core`。
    - `file-subscription` 拆分后执行 `rtk cargo test -p codex-file-subscription` 通过，退出码 0。
    - `file-subscription` 拆分后执行 `rtk cargo check -p codex-file-subscription -p codex-app-server --bin codex-app-server` 通过，0 errors，42 warnings（均为既有 unused/dead_code 风格警告）。
    - `file-subscription` 拆分后执行 `rtk cargo tree -p codex-app-server --invert codex-core --edges normal`，确认 `codex-file-subscription` 已不在 `codex-core` normal 反向路径中。
    - 对本阶段触碰的 Rust 文件执行 `rtk rustfmt --check ...` 通过；命令仍打印 workspace rustfmt 配置中 nightly-only `imports_granularity = Item` warning。
    - `memories-write` re-export 清理后执行 `rtk cargo check -p codex-memories-write` 通过，退出码 0。
    - `memories-write` re-export 清理后对触碰文件执行 `rtk rustfmt --check memories/write/src/runtime.rs memories/write/src/phase1.rs` 通过；命令仍打印 workspace rustfmt 配置中 nightly-only `imports_granularity = Item` warning。
    - `rtk rustfmt --check ...` 对本次触碰的 Rust 文件通过；全 workspace `cargo fmt --check` 仍被既有未格式化文件阻断。
    - `memories-write` runtime facade 实施后执行 `rtk cargo test -p codex-memories-write` 通过，29 个测试通过；命令只打印既有 core unused/dead_code warning。
    - `memories-write` runtime facade 实施后执行 `rtk cargo check -p codex-memories-write -p codex-app-server --bin codex-app-server` 通过，0 errors，42 warnings（既有 unused/dead_code 风格警告）。
    - `memories-write` runtime facade 实施后执行 `rtk cargo tree -p codex-app-server --invert codex-core --edges normal`，确认 `codex-memories-write` 已不在 `codex-core` normal 反向路径中，剩余路径只剩 `codex-app-server` 自身。
    - 最终执行 `rtk cargo build --timings -p codex-app-server --bin codex-app-server` 通过，0 errors，42 warnings；timing 文件为 `target/cargo-timings/cargo-timing-20260618T075028.067232Z.html`，total time 20.1s。
  next_action: 本轮 app-server 编译拓扑优化已完成并提交；后续已转入 `core-crate-topology-refactor-plan`，按全仓公共类型层和 runtime service 边界继续推进。

## Completed

- commit: 561a53ecb
  summary: 更新 agent/workflow 协作规则，改为当前主 checkout 与固定开发 checkout `~/Projects/my-codex-dev` 双目录协作；删除旧固定 tester agent 文档；创建并初始化固定开发 checkout。
  validation: `rtk node --check .codex/workflows/feature-dev/workflow.ts` 通过；确认开发目录只保留主 checkout 和 `~/Projects/my-codex-dev`。
  residual_risk: GPT auth settings 仍是未完成设计方向，不在本次合并范围内。
- commit: 46445c2
  summary: 将 PM 协作规则扩展为四个固定 checkout：`~/Projects/my-codex`、`~/Projects/my-codex-dev`、`~/Projects/my-codex-dev-2`、`~/Projects/my-codex-dev-3`，并明确任务依赖判断、合并顺序和空闲 checkout 同步策略。
  validation: `rtk node --check .codex/workflows/feature-dev/workflow.ts` 通过；`rtk git worktree list` 确认四个 checkout 已同步到同一基线；原生 `find -type l` 确认没有 target/node_modules 共享链接。
  residual_risk: 新增 checkout 尚未安装 JS 依赖或生成 Rust target；首次验证会各自独立构建。
- commit: current
  summary: 将 PM 派发规则改为四个 checkout 各绑定一个长期 owner thread，PM 复用固定 owner 串行处理该 checkout 的任务，不再为每个任务新建 owner；同时标明 dynamic workflow 的 owner 绑定范围是单个 run，不作为 PM 固定 owner 池入口。
  validation: `rtk node --check .codex/workflows/feature-dev/workflow.ts` 通过；文档搜索确认固定 owner 映射已写入 PM 规则，workflow 文档已标明 run-scoped owner 绑定限制。
  residual_risk: 当前 workflow SDK 仍按 workflow run 绑定 `Agent(id)`；PM 常规派发应直接复用固定 owner thread，不通过 `feature-dev` workflow 入口。
- commit: current
  summary: 将主 checkout `~/Projects/my-codex` 改为仅用于 PM 集成合并；开发任务只派发到三个固定开发 checkout，对应长期 owner 为 `owner_dev`、`owner_dev_2`、`owner_dev_3`。owner 只在开发 checkout 提交和验证，最终合并、冲突处理和同步由 PM 在主 checkout 完成。
  validation: `rtk node --check .codex/workflows/feature-dev/workflow.ts` 通过；文档搜索确认主 checkout 不再作为开发目录，dev 同步主 checkout 的时机已明确为派发前、合并后和 active dev 延后同步。
  residual_risk: active dev 延后同步期间，依赖该 commit 的新任务不能派发到该 dev，必须等待同步或选择已同步的空闲 dev。
- commit: current
  summary: 补充 PM 调度规则：refactor、代码健康和 performance 任务为全局独占，只有没有 active 开发任务且 dev checkout 同步完成后才能启动；独占任务运行时不得并行启动开发任务或第二个独占任务。
  validation: `rtk node --check .codex/workflows/feature-dev/workflow.ts` 通过；文档搜索确认 PM 工作规则、标准流程和 progress 模板都包含 execution_mode 与独占约束。
  residual_risk: 独占规则依赖 PM 按 progress file 判断 active work；后续派发前必须先更新 progress。
- commit: current
  summary: 允许 refactor/performance 全局独占任务直接在主 checkout 工作，由固定 `owner_main` 承接；普通开发仍只派发到三个 dev checkout，PM 负责最终验收和同步空闲 dev。
  validation: `rtk node --check .codex/workflows/feature-dev/workflow.ts` 通过；文档搜索确认 `owner_main` 只承接 refactor/performance 独占任务，普通开发仍只派发到 dev checkout。
  residual_risk: refactor/performance 在主 checkout 工作时会占用集成分支；PM 必须确保 Active Work 为空且 dev 已同步后再启动。

## Known Issues

- GPT/ChatGPT auth settings 客户端功能尚未完成；后续需要重新按调整后的 workflow/owner 流程开发，不应合并旧设计-only 分支。
