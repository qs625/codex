{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "CommandExecTerminalSize": {
      "description": "PTY size in character cells for `command/exec` PTY sessions.",
      "properties": {
        "cols": {
          "description": "Terminal width in character cells.",
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        },
        "rows": {
          "description": "Terminal height in character cells.",
          "format": "uint16",
          "minimum": 0.0,
          "type": "integer"
        }
      },
      "required": [
        "cols",
        "rows"
      ],
      "type": "object"
    },
    "ThreadId": {
      "type": "string"
    }
  },
  "properties": {
    "size": {
      "$ref": "#/definitions/CommandExecTerminalSize"
    },
    "threadId": {
      "$ref": "#/definitions/ThreadId"
    }
  },
  "required": [
    "size",
    "threadId"
  ],
  "title": "TerminalPreferredSizeUpdateParams",
  "type": "object"
}