{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "description": "PTY lifecycle notification emitted once a user-owned terminal has started.\n\n`generation` is allocated by the server for this concrete process lifetime; clients must use it for subsequent terminal control requests.",
  "properties": {
    "generation": {
      "type": "string"
    },
    "processId": {
      "type": "string"
    },
    "resumeToken": {
      "description": "Server-generated capability required to reattach this user PTY from a different app-server connection.",
      "type": "string"
    }
  },
  "required": [
    "generation",
    "processId",
    "resumeToken"
  ],
  "title": "CommandExecStartedNotification",
  "type": "object"
}